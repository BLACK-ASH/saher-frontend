---
phase: 7
plan: 6
title: Responsive Layout Pass — Staff-Used Screens
requirements: [AUDT-06]
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05]
wave: 3
files_modified:
  - features/attendance/attendance-table.tsx
  - features/dashboard/range-attendance-table.tsx
  - features/leave/leave-apply-form.tsx
  - features/mail/compose-mail-dialog.tsx
  - features/reimbursement/create-bill-dialog.tsx
  - features/payroll/payroll-table.tsx
  - features/program/program/all-programs.tsx
  - features/users/data-table.tsx
  - features/profile/profile.tsx
  - components/ui/table.tsx
autonomous: true
---

# 07-06-PLAN: Responsive Layout Pass

## Goal
All staff-used screens work at phone widths (≤640px) — tables scroll horizontally, forms stack, dialogs fit viewport.

## Tasks

### Task 1: Audit tables for horizontal scroll
**read_first:**
- features/attendance/attendance-table.tsx
- features/dashboard/range-attendance-table.tsx
- features/payroll/payroll-table.tsx
- features/users/data-table.tsx
- components/ui/table.tsx

**acceptance_criteria:**
- All `<Table>` components wrapped in `<div className="overflow-x-auto">` (or use `ScrollArea` from ui)
- Mobile: table scrolls horizontally, no layout break
- Column headers remain visible during scroll (sticky left column if needed)

### Task 2: Fix form dialogs for mobile
**read_first:**
- features/leave/leave-apply-form.tsx
- features/mail/compose-mail-dialog.tsx
- features/reimbursement/create-bill-dialog.tsx
- features/reimbursement/handle-bill-dialog.tsx
- features/payroll/record-payment-dialog.tsx

**acceptance_criteria:**
- Dialogs use `max-h-[90vh] max-w-[95vw]` on mobile
- Form fields stack vertically (`flex-col gap-4`)
- Date pickers use native `<input type="date">` on mobile (react-day-picker already handles)
- UserSearchPicker dropdown fits viewport
- Submit buttons full-width on mobile (`w-full sm:w-auto`)

### Task 3: Program/Workshop/Session card grids
**read_first:**
- features/program/program/all-programs.tsx
- features/program/workshop/all-workshops.tsx
- features/program/session/all-sessions.tsx

**acceptance_criteria:**
- Grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4` (already)
- Cards don't overflow on mobile
- Dropdown menus stay within viewport

### Task 4: Profile page two-column layout
**read_first:**
- features/profile/profile.tsx

**acceptance_criteria:**
- Mobile: stacks to single column (already `flex-col-reverse md:flex-row`)
- Right sidebar (sticky) becomes normal flow on mobile
- Accordion sections work on mobile

### Task 5: Dashboard attendance tables
**read_first:**
- features/dashboard/today-attendance-table.tsx
- features/dashboard/range-attendance-table.tsx
- features/dashboard/attendance-grid/attendance-dashboard.tsx

**acceptance_criteria:**
- Tables wrap in `overflow-x-auto`
- Attendance grid checkbox grid: horizontal scroll on mobile, frozen first column (name)

### Task 6: Notification box
**read_first:**
- features/notification/notification-box.tsx

**acceptance_criteria:**
- `ScrollArea h-60` works on mobile (touch scroll)
- Action buttons stack on mobile (`flex-col sm:flex-row`)

### Task 7: Test on mobile viewport
**acceptance_criteria:**
- Chrome DevTools device toolbar: iPhone SE (375px), iPad (768px)
- No horizontal page scroll (only table/internal scroll)
- All forms usable, buttons tappable (≥44px)
- Dialogs don't overflow viewport

### Task 8: Run lint/typecheck
**acceptance_criteria:**
- `pnpm lint` — 0 new errors
- `pnpm build` — compiles clean

## Verification
- Visit each staff screen at 375px width — no layout break
- Tables scroll horizontally with sticky first column
- Dialogs fit, forms stack, buttons full-width
- No content cut off

## Must-haves for goal-backward verification
- Zero horizontal page scroll on mobile
- All staff screens (attendance, leave, mail, reimbursement, payroll, program, users, profile, notifications) pass mobile test
- Consistent spacing/touch targets