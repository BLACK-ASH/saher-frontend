---
phase: 7
plan: 6
title: Responsive Layout Pass — Staff-Used Screens (Realized to Audit)
requirements: [AUDT-06]
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05]
wave: 3
files_modified:
  - features/attendance/attendance-table.tsx
  - features/dashboard/today-attendance-table.tsx
autonomous: true
---

# 07-06-PLAN: Responsive Layout Pass

> **Realization note:** The audit (subagent, all staff screens ≤640px) found the codebase
> is already mobile-safe via shared shadcn primitives — `Table` (built-in `overflow-x-auto`),
> `DialogContent` (`w-full max-w-[calc(100%-2rem)] max-h-[80vh] overflow-y-scroll`), profile
> `flex-col-reverse md:flex-row`, program grids `grid-cols-1 md:grid-cols-2...`. The plan's
> original file list named non-existent files (`leave-apply-form.tsx`, `compose-mail-dialog.tsx`);
> real mail/leave files confirmed no issues. Only two genuine defects found and fixed (below).

## Goal
All staff-used screens work at phone widths (≤640px) — tables scroll horizontally, forms stack, dialogs fit viewport.

## Tasks

### Task 1-7 (REALIZED): Audit confirmed conformance; only 2 genuine header-overflow defects
**read_first:**
- features/attendance/attendance-table.tsx (CardHeader line ~65)
- features/dashboard/today-attendance-table.tsx (CardHeader line ~101)
- features/dashboard/range-attendance-table.tsx (reference pattern — already `flex flex-wrap`)

**acceptance_criteria (fixed):**
- `features/attendance/attendance-table.tsx` CardHeader → `flex flex-wrap items-center justify-between`; CardAction → `flex flex-wrap gap-2 items-center`
- `features/dashboard/today-attendance-table.tsx` — same fix (default `[1fr_auto]` grid header + wide pagination action overflowed 375px)
- All other staff screens verified already conformant — no change

### Task 8: Run lint/typecheck
**acceptance_criteria:**
- `pnpm lint` — 0 new errors
- `pnpm build` — compiles clean

## Verification
- Subagent mobile audit (375px) of all staff screens — only the 2 header overflows needed fixing
- No horizontal page scroll; tables/dialogs use built-in shared-primitive safety

## Must-haves for goal-backward verification
- All staff screens (attendance, leave, mail, reimbursement, payroll, program, users, profile, notifications) pass mobile audit
- Only genuine defects patched — no speculative responsive refactor