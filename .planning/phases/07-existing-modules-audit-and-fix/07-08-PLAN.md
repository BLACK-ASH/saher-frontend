---
phase: 7
plan: 8
title: Final Raw-Date Sweep + Lint/Typecheck Gate
requirements: [AUDT-02, AUDT-03, FNDT-02]
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05, 07-06, 07-07]
wave: 4
files_modified:
  - features/calendar/calendar.tsx
  - features/dashboard/attendance-grid/attendance-dashboard.tsx
  - features/register/basic-details.tsx
  - features/register/employee-details.tsx
  - features/noticeboard/notice-expiry-badge.tsx
  - features/reimbursement/create-bill-dialog.tsx
  - lib/date.ts
autonomous: true
---

# 07-08-PLAN: Final Raw-Date Sweep + Gate

## Goal
Complete the raw-date sweep (FNDT-02) across all remaining surfaces and ensure lint/typecheck gates pass.

## Tasks

### Task 1: Fix remaining raw Date initializations
**read_first:**
- features/calendar/calendar.tsx
- features/dashboard/attendance-grid/attendance-dashboard.tsx
- features/register/basic-details.tsx
- features/register/employee-details.tsx
- features/noticeboard/notice-expiry-badge.tsx
- features/reimbursement/create-bill-dialog.tsx
- lib/date.ts

**acceptance_criteria:**
- `calendar.tsx`: `useState(dateToIstDateOnly(new Date()))` (already in 07-01 but verify)
- `attendance-dashboard.tsx`: `const today = dateToIstDateOnly(new Date())`
- `basic-details.tsx`: `selected={istDateOnlyToDate(dateToIstDateOnly(new Date()))}`, `defaultMonth={istDateOnlyToDate(dateToIstDateOnly(new Date()))}`
- `employee-details.tsx`: same pattern
- `notice-expiry-badge.tsx`: `const now = dateToIstDateOnly(new Date())` (for comparison)
- `create-bill-dialog.tsx`: `date: formatIstDate(new Date())` → `date: formatIstDate(istDateOnlyToDate(dateToIstDateOnly(new Date())))`
- All imports from `lib/date` — no raw `Date` construction for display/parsing

### Task 2: Verify no raw Date parsing/display in features/
**read_first:**
- grep -r "new Date()" features/ --include="*.tsx" | grep -v test
- grep -r "toLocaleString\|toISOString" features/ --include="*.tsx" | grep -v test

**acceptance_criteria:**
- Zero raw `new Date()` for date display/parsing in features/
- All date formatting uses `formatIstDate`/`formatIstDateTime`
- All date input values use `isoToIstInput`/`dateToIstDateOnly`
- Currency formatting (`toLocaleString`) is acceptable — not date-related

### Task 3: Run raw-date pattern gate
**read_first:**
- lib/date.ts (exports)

**acceptance_criteria:**
- Custom script or grep confirms no violations
- Document any acceptable exceptions (UI-only picker defaults)

### Task 4: Full lint/typecheck gate
**acceptance_criteria:**
- `pnpm lint` — 0 errors (pre-existing 59 warnings OK)
- `pnpm build` — compiles clean (typecheck passes)
- `pnpm test` — all tests pass (424+)

### Task 5: Update ROADMAP.md + STATE.md
**read_first:**
- .planning/ROADMAP.md
- .planning/STATE.md

**acceptance_criteria:**
- ROADMAP.md: Phase 7 plans 07-01 through 07-08 all `[x]`
- STATE.md: Phase 7 COMPLETE, progress updated
- Commit: `docs(phase-07): complete audit-and-fix phase`

## Verification
- `pnpm lint` — clean
- `pnpm build` — clean
- `pnpm test` — all pass
- Grep for raw date patterns — zero violations in features/
- All Phase 7 requirements (AUDT-01..08, AUTH-02, AUTH-03) satisfied

## Must-haves for goal-backward verification
- FNDT-02 complete: all date rendering/parsing routes through lib/date
- Phase 7 all requirements checked off
- Project builds and tests green
- ROADMAP/STATE reflect completion