---
phase: 7
plan: 2
title: Users & Program Final Alignment — Pattern Cleanup
requirements: [AUDT-03]
depends_on: []
wave: 1
files_modified:
  - app/(main)/users/[id]/page.tsx
  - features/users/data-table.tsx
  - features/users/user-action.tsx
  - features/program/program/all-programs.tsx
  - features/program/workshop/all-workshops.tsx
  features/program/session/all-sessions.tsx
autonomous: true
---

# 07-02-PLAN: Users & Program Final Alignment

## Goal
Clean up remaining type casts and verify Users/Program pages fully follow services/zod/query patterns.

## Tasks

### Task 1: Fix inline `any` in user detail page
**read_first:**
- app/(main)/users/[id]/page.tsx
- hooks/use-admin.ts
- services/admin.api.ts

**acceptance_criteria:**
- Remove `// eslint-disable-next-line @typescript-eslint/no-explicit-any` at line 196
- Define proper type for user detail response (extend `AdminUserResponse` or create `AdminUserDetailResponse` in admin.api.ts)
- Page uses typed data from `useAdminUsers` or new `useAdminUserDetail` hook
- No `any` casts in the file

### Task 2: Verify users data-table pattern alignment
**read_first:**
- features/users/data-table.tsx
- features/users/column.tsx
- hooks/use-admin.ts

**acceptance_criteria:**
- `UserDataTable` uses `useAdminUsers` query (already does)
- Column definitions in `column.tsx` use proper accessors with typed `UserT`
- Pagination uses `PaginationFooter` from `components/shared/`
- Trash/restore via `user-action.tsx` dropdown works with `isDeleted` filter

### Task 3: Verify program/workshop/session CRUD alignment
**read_first:**
- features/program/program/all-programs.tsx
- features/program/workshop/all-workshops.tsx
- features/program/session/all-sessions.tsx
- hooks/use-programs.ts
- services/program.api.ts, workshop.api.ts, session.api.ts

**acceptance_criteria:**
- All three levels use `TrashTabPattern` from `components/shared/` (already done)
- `usePrograms` hook returns queries for programs, workshops, sessions with hierarchical keys
- Mutations (`update`, `del`, `restore`) invalidate parent keys correctly
- No inline `toast.error` in service functions — `apiFetch` handles it (check new code vs legacy)
- Session attendance page (`app/(main)/program/sessions/attendance/[id]/page.tsx`) uses diff engine from 06-06

### Task 4: Run lint/typecheck
**acceptance_criteria:**
- `pnpm lint` — 0 new errors
- `pnpm build` — compiles clean

## Verification
- Navigate to `/users` — directory loads, search/pagination works, trash tab shows deleted users
- Click user → detail page loads with typed data, no console errors
- Navigate to `/program` — programs list, create/edit/delete/restore works
- Drill into workshop → sessions → attendance grid works

## Must-haves for goal-backward verification
- Zero `any` casts in users/program feature files
- All CRUD uses service layer + hook layer (no inline fetch)
- TrashTabPattern used consistently on all three event levels