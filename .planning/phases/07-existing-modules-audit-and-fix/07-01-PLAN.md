---
phase: 7
plan: 1
title: Calendar Alignment — IST Dates + Google Sync Verification
requirements: [AUDT-02]
depends_on: []
wave: 1
files_modified:
  - features/calendar/calendar.tsx
  - features/calendar/calendar.tsx
  - hooks/use-calendar.ts
  - services/calendar.api.ts
autonomous: true
---

# 07-01-PLAN: Calendar Alignment

## Goal
Align calendar module to contract: replace raw `new Date()` with IST utilities, verify month aggregation and Google sync backend contract.

## Tasks

### Task 1: Fix raw Date usage in calendar.tsx
**read_first:**
- features/calendar/calendar.tsx
- lib/date.ts

**acceptance_criteria:**
- `features/calendar/calendar.tsx` imports `dateToIstDateOnly` from `lib/date`
- Line ~13: `const [calendarDate, setCalendarDate] = useState(new Date())` → `useState(dateToIstDateOnly(new Date()))`
- Line ~17: `setCalendarDate(info.view.calendar.getDate())` — FullCalendar returns native Date; wrap with `dateToIstDateOnly(...)` when storing
- No other raw `new Date()` in calendar.tsx
- Typecheck passes (`pnpm build`)

### Task 2: Verify Google sync contract
**read_first:**
- services/calendar.api.ts
- hooks/use-calendar.ts

**acceptance_criteria:**
- `services/calendar.api.ts` has `syncGoogleCalendar` function (or similar) if backend exposes it
- If backend has `/api/calendar/sync/google` endpoint, hook exposes `syncGoogle` mutation with `onSuccess` toast
- If no backend endpoint, document "Google sync is backend-only; no frontend action needed" in RESEARCH.md update
- Check backend routes: `grep -r "google\|sync" ../saher-backend/src/calendar/` to verify

### Task 3: Verify month aggregation data flow
**read_first:**
- hooks/use-calendar.ts
- features/calendar/calendar.tsx

**acceptance_criteria:**
- `useCalendar` hook returns `events` query with proper queryKey `["calendar", "events", ...params]`
- FullCalendar `events` prop consumes the query data correctly
- Custom events from `AddEventDialog` appear in month view after creation
- No console errors on calendar navigation

### Task 4: Run lint/typecheck
**acceptance_criteria:**
- `pnpm lint` — 0 new errors (pre-existing warnings OK)
- `pnpm build` — compiles clean

## Verification
- Navigate to `/calendar` — month view loads, no hydration mismatch
- Create custom event via dialog — appears immediately
- Navigate months — events persist
- If Google sync endpoint exists, click sync button → toast success

## Must-haves for goal-backward verification
- Calendar renders without raw Date in component state
- All date display uses `formatIstDate`/`formatIstDateTime` from lib/date
- Google sync status documented (implemented or backend-only)