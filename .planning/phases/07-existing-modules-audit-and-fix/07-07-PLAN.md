---
phase: 7
plan: 7
title: Test Coverage — Money-Path Double-Submit + Auth Refresh Path
requirements: [AUDT-07, AUDT-08]
depends_on: [07-03]
wave: 3
files_modified:
  - tests/reimbursement-hook.test.tsx
  - tests/payroll-hook.test.tsx
  - tests/admin-bank-api.test.ts
  - tests/api-wrapper.test.ts
  - tests/session.test.ts
  - lib/session.ts
autonomous: true
---

# 07-07-PLAN: Test Coverage — Money & Auth Paths

## Goal
Cover money-path flows (reimbursement handle/settle, payroll installments, bank mutations) with double-submit gating tests, and auth refresh path with single-retry-then-session-death tests.

## Tasks

### Task 1: Money-path double-submit tests (reimbursement)
**read_first:**
- tests/reimbursement-hook.test.tsx
- hooks/use-reimbursement.ts
- services/reimbursement.api.ts

**acceptance_criteria:**
- Test: `handleBill` mutation called twice rapidly → only one `apiFetch` to `/api/finance/bills/:id/handle`
- Test: `settleBill` mutation called twice → only one `apiFetch`
- Test: `createBill` mutation called twice → only one `apiFetch`
- Use `isPending` guard in hook (already implemented) — test verifies it works
- msw handlers for `/api/finance/bills/*` endpoints

### Task 2: Money-path double-submit tests (payroll)
**read_first:**
- tests/payroll-hook.test.tsx
- hooks/use-payroll.ts
- services/payroll.api.ts

**acceptance_criteria:**
- Test: `recordInstallment` mutation double-call → single `apiFetch` to `/api/payroll/:id/installment`
- Test: `generatePayroll` (run now) double-call → single `apiFetch`
- msw handlers for payroll endpoints

### Task 3: Money-path double-submit tests (admin bank/account)
**read_first:**
- tests/admin-bank-api.test.ts
- hooks/use-admin.ts
- services/admin.api.ts

**acceptance_criteria:**
- Test: `updateAccount` double-call → single `apiFetch`
- Test: `createBank` double-call → single `apiFetch`
- Test: `restoreBank` double-call → single `apiFetch`
- msw handlers for admin endpoints

### Task 4: Auth refresh path tests (single retry + session death)
**read_first:**
- tests/api-wrapper.test.ts
- lib/api-wrapper.ts
- lib/session.ts
- tests/session.test.ts

**acceptance_criteria:**
- Test: `apiFetch` gets 401 → refresh succeeds → retry succeeds → returns data (already partially covered)
- Test: `apiFetch` gets 401 → refresh fails → `handleSessionDeath` called → cache cleared → redirect to `/login?next=...`
- Test: Concurrent 401s → single refresh flight (refreshPromise singleton)
- Test: `resetSessionGuard()` resets `died` flag → subsequent 401 can trigger death again
- Test: `performLogoutCleanup` clears cache and redirects to `/login`

### Task 5: Integration test — mutation + query chain with 401
**read_first:**
- tests/api-wrapper.test.ts
- hooks/use-reimbursement.ts (or any money hook)

**acceptance_criteria:**
- Simulate: mutation fires → 401 → refresh → retry → success → query invalidation works
- Verify `queryClient.invalidateQueries` called after successful retry

### Task 6: Run all tests
**acceptance_criteria:**
- `pnpm test` — all tests pass (including new ones)
- 424+ tests passing (was 424 pass / 2 fail pre-existing)
- No new flaky tests

## Verification
- Run `pnpm test` — all money-path double-submit tests pass
- Run `pnpm test` — auth refresh path tests pass
- Coverage: money mutations + auth refresh are tested

## Must-haves for goal-backward verification
- AUDT-07: Double-submit prevention verified for all money mutations
- AUDT-08: Auth refresh single-retry-then-death verified
- No regression in existing test suite