---
phase: 05-money-approval-reimbursement-payroll
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - components/sidebar/nav-list.tsx
  - features/reimbursement/bill-status-badge.tsx
  - features/reimbursement/balance-card.tsx
  - features/reimbursement/bill-table.tsx
  - features/reimbursement/bill-detail-dialog.tsx
  - features/reimbursement/create-bill-dialog.tsx
  - app/(main)/reimbursement/my-bills/page.tsx
  - tests/create-bill-dialog.test.tsx
autonomous: true
requirements: [REIM-01, REIM-02, REIM-03, REIM-04, REIM-11]
must_haves:
  truths:
    - "Staff open My Bills from the sidebar and see their balance card plus their bills with status badges"
    - "Staff submit a bill with amount, description, IST date and up to 10 receipt images"
    - "Staff edit or withdraw their own pending/on-hold bills; withdraw requires confirmation"
    - "Staff switch to a Deleted tab and restore their own soft-deleted bills (D-05)"
    - "All three money sidebar entries exist and are role-gated via can() (D-01/D-22)"
  artifacts:
    - path: "features/reimbursement/bill-status-badge.tsx"
      provides: "Badge mapping backend enums exactly per D-08 colors"
    - path: "features/reimbursement/balance-card.tsx"
      provides: "Net balance card reading GET /balance-enquiry; Total rendered as string (Quirk 5)"
    - path: "features/reimbursement/create-bill-dialog.tsx"
      provides: "RHF+zod form with ImageUpload capped at 10 images"
      min_lines: 80
    - path: "features/reimbursement/bill-table.tsx"
      provides: "Staff table with Edit/Withdraw row actions + Deleted-tab variant with Restore"
    - path: "features/reimbursement/bill-detail-dialog.tsx"
      provides: "Base detail dialog (fields, reason, receipt lightbox) — extended by plan 05-06"
    - path: "app/(main)/reimbursement/my-bills/page.tsx"
      provides: "Active|Deleted tabs + New Bill button + balance card (D-02/D-05)"
  key_links:
    - from: "app/(main)/reimbursement/my-bills/page.tsx"
      to: "hooks/use-reimbursement.ts"
      via: "useReimbursement queries/mutations"
      pattern: "useReimbursement\\("
    - from: "components/sidebar/nav-list.tsx"
      to: "/reimbursement/my-bills + /reimbursement/management + /payroll"
      via: "three new entries gated by can()"
      pattern: "preReimbursement|payroll"
---

<objective>
Deliver the staff-facing reimbursement surface end-to-end: My Bills page (balance card, badge-decorated bill table, create/edit/withdraw flows, Active|Deleted tabs with restore), the base bill detail dialog, and all three phase sidebar entries. After this plan a staff member can run their entire personal expense workflow.
</objective>

<purpose>
REIM-01..04 are the daily-driver staff flows of the money pipeline; shipping them as one vertical slice proves the data layer under real composition and establishes the shared components (badge, table, detail dialog) that finance plans reuse.
</purpose>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-money-approval-reimbursement-payroll/05-CONTEXT.md
@.planning/phases/05-money-approval-reimbursement-payroll/05-PATTERNS.md
@.planning/codebase/SLICE-CONTRACT.md
@hooks/use-reimbursement.ts
@services/reimbursement.api.ts
@lib/date.ts

<interfaces>
<!-- From prior plans / codebase — use directly. -->

From hooks/use-reimbursement.ts (plan 05-01):
```typescript
useReimbursement() -> {
  myBills: UseQueryResult<BillResponse[]>;        // pass isDeleted via hook options
  balance: UseQueryResult<BalanceEnquiryResponse>;
  createBill, updateBill, withdraw, restore: UseMutationResult<...>;
}
```

From services/reimbursement.api.ts (plan 05-01): BillResponse { id, user, image?, amount, advance, date, description, status: "pending"|"accept"|"reject"|"on-hold", reason?, isDeleted }; BalanceEnquiryResponse { PocketUse, AdvanceUse, SettledUse: number; Total: string; Empty?: true }.

From lib/date.ts: formatIstDate(iso), dateInputToIso("YYYY-MM-DD") -> "+05:30" ISO, dateToIstDateOnly(Date) -> "YYYY-MM-DD".

From lib/image-url.ts: imageUrl(url?: string) -> safe src ("/uploads/..." passthrough, placeholder fallback).

From components/image-upload.tsx: default export ImageUpload({ altName, url?, onUploadSuccess?(data: UploadedImage) }); UploadedImage = { id, fileName, url, size, width?, height?, mimetype }.

From features/leave/leave-status-badge.tsx: the Badge-per-enum-value pattern to replicate.

Route decision (stated per planner directive): payroll page lives at app/(main)/(admin)/payroll (CONTEXT integration point, D-22). The (admin) group layout's coarse can(write,"user") gate is NOT payroll-scoped (SLICE-CONTRACT CR-01 lesson), so plan 05-04 wraps its page in RoleGuard allow={(r)=>can(r,"read","payroll")}. Finance management page lives at app/(main)/reimbursement/management under plain (main), guarded by its own RoleGuard in plan 05-05.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar entries + badge + balance card + My Bills page shell with base detail dialog</name>
  <files>components/sidebar/nav-list.tsx, features/reimbursement/bill-status-badge.tsx, features/reimbursement/balance-card.tsx, features/reimbursement/bill-table.tsx, features/reimbursement/bill-detail-dialog.tsx, app/(main)/reimbursement/my-bills/page.tsx</files>
  <read_first>
    - components/sidebar/nav-list.tsx (current arrays + canSeeManagerGroup clause pattern)
    - features/leave/leave-status-badge.tsx (badge analog)
    - features/leave/leave-balance-card.tsx (card layout analog)
    - app/(main)/noticeboard/page.tsx (tab-shell analog)
    - lib/permissions.ts (can signature; resources preReimbursement/postReimbursement/payroll)
    - services/reimbursement.api.ts, hooks/use-reimbursement.ts (05-01 outputs)
  </read_first>
  <action>
    nav-list.tsx (per D-01/D-22): append to userRoutes { label:"My Bills", url:"/reimbursement/my-bills", icon: ReceiptText }; to managerRoutes { label:"Bill Management", url:"/reimbursement/management", icon: HandCoins }; to adminRoutes { label:"Payroll", url:"/payroll", icon: Banknote } (lucide-react names verified available). Extend canSeeManagerGroup with: url==="/reimbursement/management" returns can(role,"read","preReimbursement"). Payroll needs no extra clause — adminRoutes render only when canSeeAdminGroup (delete:account, admin-only) which exactly matches payroll's admin-only matrix. All three routes 404 harmlessly until plans 05-04/05-05 ship their pages.

    bill-status-badge.tsx: follow leave-status-badge.tsx switch style over backend enum values EXACTLY (never translate). Bill statuses: pending→yellow, accept→green label "Accepted", reject→red label "Rejected", on-hold→blue label "On Hold". Also export two tiny siblings in the same file for later reuse: SettlementBadge (pending→yellow "Settlement Pending", settle→gray/green "Settled", expired→red, on-hold→blue) and PayrollBadge (paid→green, unpaid→yellow, partially-paid→blue "Partial", approved→green).

    balance-card.tsx (D-09): reads balance query; when data.Empty is true render muted "No bills yet" card; otherwise show the net line using Total EXACTLY as received (string — never parsed, Quirk 5) as the headline, plus three small stats PocketUse / AdvanceUse / SettledUse formatted with Intl.NumberFormat("en-IN",{style:"currency",currency:"INR"}).

    bill-table.tsx (staff mode): props { bills?: BillResponse[]; isLoading?: boolean; onEdit?; onWithdraw?; onOpen?; onRestore?; variant: "active"|"deleted" }. Columns: Date (formatIstDate), Description (truncate ~48ch), Amount ₹ en-IN, Status → bill-status-badge, Actions slot. active variant renders Edit+Withdraw icon buttons (Pencil / Undo2 lucide) only when status is "pending" OR "on-hold" (backend edit rule covers both — research REIM-04 note) and callbacks provided. deleted variant renders Restore (RotateCcw) per row. Loading → DefaultLoader; empty → NoData. PaginationFooter NOT needed here (mybills returns full array); render list as-is.

    bill-detail-dialog.tsx BASE version (D-04): controlled Dialog { bill, open, onOpenChange } showing description, amount, advance, date, status badge, reason block when present, and receipt area: if bill.image truthy render thumbnail via imageUrl(bill.image) inside AspectRatio, clicking opens a nested full-size image Dialog (lightbox, D-16-lite). No timeline/settlement yet — plan 05-06 extends this file.

    my-bills/page.tsx (D-02/D-05): "use client"; wraps Tabs defaultValue="active": Active tab = BalanceCard + floating "New Bill" Button (Plus icon, fixed bottom-right) opening CreateBillDialog (Task 2 placeholder state now) + BillTable variant="active" fed by myBills(false) query; Deleted tab = BillTable variant="deleted" fed by myBills(true) with onRestore wired to restore mutation. Row click (onOpen) opens BillDetailDialog. Wire lifted state like features/leave/page.tsx does.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm exec vitest run tests/nav-list.test.tsx</automated>
  </verify>
  <done>Three role-gated sidebar entries render; My Bills page shows balance card (Total as string), badge table, Active|Deleted tabs, and opens the base detail dialog from rows.</done>
  <acceptance_criteria>
    - grep finds "My Bills", "Bill Management", "Payroll" URLs and the can(role,"read","preReimbursement") clause in nav-list.tsx
    - tests/nav-list.test.tsx still passes (existing gating tests unbroken)
    - balance-card.tsx contains no Number()/parseFloat call on Total (string passthrough verifiable by source assertion: `grep -c "parseFloat\\|Number(" features/reimbursement/balance-card.tsx` returns 0)
    - Page renders Active and Deleted TabsContent values
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Bill dialog — RHF + zod + ImageUpload ≤10</name>
  <files>features/reimbursement/create-bill-dialog.tsx, tests/create-bill-dialog.test.tsx</files>
  <read_first>
    - features/leave/apply-leave-dialog.tsx (RHF+zodResolver+Controller+ImageUpload analog, D-18 proven shape)
    - services/reimbursement.api.ts userBillCreateSchema (05-01)
    - components/image-upload.tsx (props + UploadedImage)
    - lib/date.ts (dateInputToIso / dateToIstDateOnly)
    - features/leave/review-leave-dialog.tsx lines 130-140 (disabled={isPending} double-submit pattern)
  </read_first>
  <behavior>
    - Submitting with empty description or amount ≤ 0 shows field errors and fires zero network calls
    - Attempting an 11th image is impossible: uploader hidden/disabled at 10 with counter "7/10"
    - Successful submit calls createBill once with { amount, description, date: dateInputToIso(dateInput), images: [ids] }, shows success toast, closes dialog; failed submit toasts error and KEEPS the dialog open with entered data intact (D-28)
    - Submit button disabled + label "Submitting…" while mutation pending (D-26)
  </behavior>
  <action>
    Build CreateBillDialog as a controlled Dialog ({ open, onOpenChange }) following apply-leave-dialog.tsx composition: useForm with zodResolver over the client copy of userBillCreateSchema (import from services/reimbursement.api.ts); Controller fields: Amount (Input type=number step 0.01), Description (Textarea), Date (Input type=date defaulting to dateToIstDateOnly(new Date())). Receipt images live OUTSIDE zod as useState<UploadImage[]> seeded empty; each ImageUpload onUploadSuccess appends; render chip row with remove buttons; cap 10 — hide uploader at cap and show "n/10". On submit convert images to their id strings and hand the whole payload to the createBill mutation from useReimbursement; onSuccess toast.success("Bill submitted") + close + form.reset; onError ONLY toast.error(err.message) — do not close or reset (D-28). Submit Button follows review-leave-dialog disabled/isPending label-swap (D-26). Wire the floating New Bill button on my-bills/page.tsx to open it.
    Component test in tests/create-bill-dialog.test.tsx using renderWithProviders + msw: validation-block case, cap-at-10 case (simulate 10 onUploadSuccess calls then assert uploader absent), successful-submit case asserting the msw handler captured body.images length ≤10 and dialog closed.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/create-bill-dialog.test.tsx && pnpm typecheck</automated>
  </verify>
  <done>Staff can submit a bill with ≤10 receipts; invalid submits fire zero network calls; failures keep the dialog open with data intact.</done>
  <acceptance_criteria>
    - Test asserting zero fetches on invalid submit passes
    - Test asserting onError leaves dialog rendered (D-28) passes
    - Source assertion: file contains "dateInputToIso" and max-10 guard
    - `pnpm lint` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Edit/withdraw pending bills + restore deleted bills</name>
  <files>features/reimbursement/create-bill-dialog.tsx, features/reimbursement/bill-table.tsx, app/(main)/reimbursement/my-bills/page.tsx</files>
  <read_first>
    - .planning/phases/05-money-approval-reimbursement-payroll/05-CONTEXT.md D-07 (edit fields, date lock, withdraw confirm)
    - features/reimbursement/create-bill-dialog.tsx (from Task 2 — extend, don't fork)
    - services/reimbursement.api.ts userBillUpdateSchema (no date key)
    - app/(main)/noticeboard/page.tsx + SLICE-CONTRACT §6 step 6 (destructive-confirm dialog rule)
  </read_first>
  <action>
    Extend CreateBillDialog with mode: "create" | "edit" (SLICE-CONTRACT shared-form pattern): edit mode prefills amount/description/images from the selected bill, OMITS the Date field entirely (backend userBillUpdateSchema has no date — D-07 lock), and calls updateBill({id, data}) instead. Reset form on open via useEffect keyed on open+bill (review-leave-dialog reset pattern).
    In bill-table.tsx wire Edit → onEdit(bill) opening the dialog in edit mode; Withdraw → confirmation AlertDialog ("Withdraw this bill?" + Cancel / destructive Withdraw) before calling deleteBill — soft delete per backend. Keep both actions gated to status pending|on-hold (Task 1 logic).
    On the Deleted tab, Restore buttons already call restore mutation; add optimistic-free behavior note: rely on invalidation refetch (hook handles it). Known contract caveat to record in SUMMARY: D-30 specifies restore guarded by write:preReimbursement which pure-staff roles lack — if the deployed backend keeps that guard, staff restore receives 403 and the error surfaces as a toast; UI ships per locked D-05 regardless, flag for backend guard adjustment (owner-or-finance check or postReimbursement:update).
    Update tests/create-bill-dialog.test.tsx: edit mode hides the date input and submits without a date key.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/create-bill-dialog.test.tsx && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>Pending/on-hold bills edit without a date field and withdraw only via confirm; Deleted tab restore works; D-30 staff-guard caveat recorded in SUMMARY.</done>
  <acceptance_criteria>
    - Edit-mode test asserts no `date` key in captured PATCH body (D-07 date lock)
    - Withdraw flow requires explicit confirm: test/source assertion that deleteBill fires only after the confirm button click
    - Edit/Withdraw buttons render only for pending|on-hold rows (source assertion on the status condition)
    - SUMMARY records the D-30 staff-guard caveat
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Staff bill submissions (amounts, images) cross here |
| Upload pipeline → rendering | Receipt files served from /uploads/* rendered back into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | Double-submitted bill creation | mitigate | D-26 disabled+spinner submit; server validates; no optimistic insert |
| T-05-03-02 | Tampering | Client-side amount validation bypassed | mitigate | zod is UX; backend revalidates userBillCreateSchema — frontend never trusted |
| T-05-03-03 | Information Disclosure | Other users' bills visible | mitigate | Data sourced only from /mybills (req.user scoped server-side); no cross-user queries on this surface |
| T-05-03-04 | Injection | Receipt URL/HTML smuggled into lightbox | mitigate | Render exclusively via <img src={imageUrl(...)}> — React escapes text; no dangerouslySetInnerHTML anywhere in feature |
| T-05-03-05 | Elevation | Staff restore hitting finance-guarded endpoint | mitigate | Server enforces; UI surfaces 403 toast; caveat recorded for backend guard decision (D-30 tension with D-05) |
| T-05-SC | Tampering | Package supply chain | accept | Zero installs |

No high-severity unresolved threats.
</threat_model>

<verification>
- `pnpm lint && pnpm typecheck && pnpm exec vitest run` exit 0 across the suite
- Manual smoke via dev server optional but NOT required — automated criteria above are authoritative
</verification>

<success_criteria>
- A staff user can: see balance + bills with correct badges, submit a bill with ≤10 receipts, edit/withdraw a pending bill (with confirm), inspect a bill in a dialog, restore a deleted bill from the Deleted tab
- Sidebar shows My Bills for everyone; Bill Management only where can(read, preReimbursement); Payroll in admin group
- Shared primitives (badge trio, table pattern, detail dialog base) exist for plans 05-05/05-06 to consume
</success_criteria>

<output>
Create `.planning/phases/05-money-approval-reimbursement-payroll/05-03-SUMMARY.md` when done
</output>
