---
phase: 05-money-approval-reimbursement-payroll
plan: 01
type: execute
wave: 1
depends_on: [04-staff-self-service-mail-leave]
files_modified:
  - services/reimbursement.api.ts
  - services/payroll.api.ts
  - hooks/use-reimbursement.ts
  - hooks/use-payroll.ts
  - features/reimbursement/
  - features/payroll/
autonomous: false
requirements: [REIM-01, REIM-02, REIM-03, REIM-04, REIM-05, REIM-06, REIM-07, REIM-08, REIM-09, REIM-10, REIM-11, REIM-12, PAYR-01, PAYR-02, PAYR-03, PAYR-04]
---

<objective>
Build the reimbursement and payroll service layers, data hooks, and foundational UI screens for the money pipeline. This plan establishes the API service modules, React-query data hooks, and the staff "My Bills" and finance "Bill Management" surfaces. All money mutations must structurally protect against double-submits with no optimistic updates. REIM-11 (bill restore) requires a backend restore endpoint (D-30) — flagged as blocker; UI should include restore button that calls non-existent endpoint until backend is implemented. Payroll `/:id` returns array (Quirk 7) — handle with `data[0]` pattern.
</objective>

<purpose>
Create the foundational service layer and UI surfaces for Phase 5's money pipeline. This plan wires the backend reimbursement and payroll endpoints into TanStack Query hooks, builds the staff and finance UI screens, and ensures all mutations gate pending state. Laid groundwork for later waves covering settlement, recycle bin, and payroll generation.
</purpose>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-money-approval-reimbursement-payroll/05-CONTEXT.md
@.planning/phases/05-money-approval-reimbursement-payroll/05-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/SLICE-CONTRACT.md
@services/notice.api.ts (reference service pattern)
@hooks/use-notice.ts (reference hook pattern)
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → frontend | All money mutations go through apiFetch with credentials: include; server validates idempotency |
| npm registry → dev machine | New packages (if any) enter dependency graph; see legitimacy gate |
| local git → remotes | Branch operations on dev; no push to main (D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | Reimbursement service layer | mitigate | All mutations use apiFetch with `credentials: 'include'`; server validates idempotency; no optimistic updates — server state always reflects truth |
| T-05-BR | Tampering/Elevation | Git push targets | mitigate | Task 1 forbids any push; `main` remains frozen; dev branch only |
| T-05-HY | Information Disclosure | Balance card data | accept | Zero importers confirmed (PATTERNS.md re-check); balance uses server-computed aggregates from `GET /balance-enquiry` |
| T-05-DB | Data Corruption | Double-submit on money mutations | mitigate | Frontend disables button + shows loading spinner on pending; server validates idempotency; no optimistic updates |
| T-05-AM | Authentication | Token refresh / session death | mitigate | apiFetch handles 401 → single-flight refresh → one retry; session death lands user on /login exactly once |

</threat_model>

<verification>
- `pnpm lint` exits 0 across all new files
- `pnpm typecheck` exits 0 (Zod schemas mirror backend exactly; apiFetch response shapes match)
- `pnpm exec vitest run` passes for all new hook/mutation tests
- Money mutation button stays disabled + shows loading spinner when pending; server state unchanged after retry
- Balance card displays `PocketUse`, `AdvanceUse`, `SettledUse` (numbers) and `Total` (string, as-is) per Quirk 5
- User IDs from bill/settlement responses resolved via `useUserMap()` hook (D-32)
- `GET /bills` returns settlements; handle queue uses `GET /?status=pending&isDeleted=false` per D-31
- REIM-11 restore button present in UI but calls non-existent endpoint until backend D-30 implemented
</verification>

<success_criteria>
- Service layers created: `services/reimbursement.api.ts` and `services/payroll.api.ts` with zod-inferred DTOs
- Data hooks created: `hooks/use-reimbursement.ts` and `hooks/use-payroll.ts` with TanStack Query queries/mutations
- Staff "My Bills" page renders with balance card, status badges, edit/withdraw actions
- Finance "Bill Management" page renders with Handle Queue tab and Recycle Bin tab
- All money mutations gate pending state (disabled button + spinner, no optimistic updates)
- REIM-11 restore UI present; backend endpoint tracked as D-30 blocker
- Payroll `/:id` endpoint handled with `data[0]` pattern per Quirk 7
- User names displayed via `useUserMap()` cache (D-32)
</success_criteria>

<output>
Create `.planning/phases/05-money-approval-reimbursement-payroll/05-01-SUMMARY.md` when done
</output>