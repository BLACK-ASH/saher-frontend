---
phase: 05-money-approval-reimbursement-payroll
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - app/(main)/reimbursement/management/page.tsx
  - features/reimbursement/finance-bill-table.tsx
  - features/reimbursement/handle-bill-dialog.tsx
  - features/reimbursement/bulk-action-bar.tsx
  - features/reimbursement/recycle-bin.tsx
autonomous: true
requirements: [REIM-05, REIM-06, REIM-09, REIM-11]
must_haves:
  truths:
    - "Finance sees a Handle Queue of bills filtered by status with debounced search and field filters (D-12)"
    - "Finance approves/rejects/holds any bill through a dialog requiring Notes (min length) (D-10)"
    - "Finance multi-selects bills and bulk-handles them sequentially with live progress and a summary toast (D-11/D-27)"
    - "Finance browses the Recycle Bin and restores deleted bills (REIM-11)"
    - "Management page is guarded by can(read,'preReimbursement')"
  artifacts:
    - path: "app/(main)/reimbursement/management/page.tsx"
      provides: "RoleGuard + Handle Queue | Recycle Bin tabs (D-03) + filter toolbar"
      min_lines: 60
    - path: "features/reimbursement/finance-bill-table.tsx"
      provides: "Selectable queue table with per-row Approve/Reject/Hold actions"
    - path: "features/reimbursement/handle-bill-dialog.tsx"
      provides: "reason-required handle form; accept notes auto-created settlement (Quirk 3)"
      min_lines: 70
    - path: "features/reimbursement/bulk-action-bar.tsx"
      provides: "Floating selection bar + shared-reason confirm + progress readout"
    - path: "features/reimbursement/recycle-bin.tsx"
      provides: "Deleted-bills table with Restore row action"
  key_links:
    - from: "app/(main)/reimbursement/management/page.tsx"
      to: "hooks/use-reimbursement.ts searchBills/handleMany"
      via: "queue query keyed by filters; bulk via handleMany"
      pattern: "handleMany|searchBills|\\[\"bills\", \"search\""
    - from: "features/reimbursement/bulk-action-bar.tsx"
      to: "bulkProgress state in use-reimbursement"
      via: "renders Processing done/total while active (D-27)"
      pattern: "bulkProgress"
---

<objective>
Deliver the finance working surface: Bill Management page with Handle Queue tab (status-filtered, searchable, single + bulk handling with required notes) and Recycle Bin tab with restore. After this plan finance can process the daily bill queue end-to-end.
</objective>

<purpose>
The handle queue is where money decisions happen — approve/reject/hold drive notifications and settlement creation server-side. Bulk safety (sequential calls, progress, partial-failure tolerance) is the core risk this slice makes structural.
</purpose>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-money-approval-reimbursement-payroll/05-CONTEXT.md
@.planning/phases/05-money-approval-reimbursement-payroll/05-PATTERNS.md
@app/(main)/reimbursement/my-bills/page.tsx
@features/reimbursement/bill-status-badge.tsx
@features/reimbursement/bill-detail-dialog.tsx
@hooks/use-reimbursement.ts

<interfaces>
<!-- From plans 05-01/05-03 — use directly. -->

From hooks/use-reimbursement.ts:
```typescript
useReimbursement() -> {
  searchResults: UseQueryResult<NormalizedList<BillResponse>>;  // ["bills","search",filters,page]
  recycleBills: UseQueryResult<BillResponse[]>;                  // ["bills","recycle"] raw array
  handleOne: UseMutationResult<void, Error, { billId: string; status: HandleStatus; reason: string }>;
  restore: UseMutationResult<void, Error, string>;
  handleMany(items: { billId: string; status: HandleStatus; reason: string }[]): Promise<void>;
  bulkProgress: { done: number; total: number } | null;
}
```

From services/reimbursement.api.ts: HandleStatus = "accept"|"reject"|"on-hold"; searchBills(filters {description?/amount?/date?/user?/status?/isDeleted/page/limit}) ALWAYS emits isDeleted=false, and per the 05-01 Quirk-10 Step-0 verdict it appends a benign `description=` param when all filters are cleared if the backend guard requires it — cleared-filters requests can never 400; getRecycleBills raw array.

Reuse from 05-03: BillStatusBadge / SettlementBadge exports; bill-detail-dialog base (row Detail opening); TrashTabPattern({title, description, children}) from components/shared/trash-tab-pattern.tsx for the Recycle Bin card shell.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Management page — tabs, filter toolbar, finance queue table, recycle bin</name>
  <files>app/(main)/reimbursement/management/page.tsx, features/reimbursement/finance-bill-table.tsx, features/reimbursement/recycle-bin.tsx</files>
  <read_first>
    - app/(main)/noticeboard/page.tsx (tab shell), components/shared/trash-tab-pattern.tsx
    - features/leave/admin-page.tsx (filterable admin queue analog)
    - components/user-search-picker.tsx lines 46-57 (300ms debounce pattern to copy)
    - hooks/use-user-map.ts (resolveName for the user column)
    - .planning/phases/05-money-approval-reimbursement-payroll/05-RESEARCH.md Quirk 10 + D-31 resolution
    - .planning/phases/05-money-approval-reimbursement-payroll/05-01-SUMMARY.md (Quirk-10 empty-guard verdict — governs the filter-builder param policy below)
  </read_first>
  <action>
    management/page.tsx ("use client"): wrap content in RoleGuard allow={(r)=>can(r,"read","preReimbursement")} (exact resource literal from lib/permissions.ts). Two Tabs per D-03: "Handle Queue" and "Recycle Bin".
    Queue toolbar (single row): search Input (placeholder "Search description or amount…") debounced 300ms into state via setTimeout pattern copied from user-search-picker; status Select defaulting "pending" with options pending/accept/reject/on-hold/all; user Select populated from useUserMap().userMap entries (value = userId, optional); date Input type=date. Build filters object: { description: textQuery || undefined, amount: numeric-only textQuery ? Number(textQuery) : undefined, status: status==="all" ? undefined : status, user: userId || undefined, date: date || undefined, isDeleted: "false" } — isDeleted always present; guard safety on fully-cleared filters (query empty, status "all") is guaranteed by searchBills itself per the 05-01 Quirk-10 Step-0 verdict (the service appends the benign `description=` fallback if the backend guard ignores isDeleted) — do NOT strip params or add local workaround params here. Feed useReimbursement().searchResults with page state; PaginationFooter bound to normalized totalPages. Export button space reserved at toolbar end (added in plan 05-06).
    finance-bill-table.tsx: props { data?: NormalizedList<BillResponse>; isLoading?; selectedIds: Set<string>; onToggleSelect(id, checked); onToggleAll(checked); onHandle(bill, status); onOpen?(bill) }. Columns: Checkbox (header = select-all-on-page), User (resolveName), Description, Amount ₹, Advance ₹, Date, Status badge (reuse BillStatusBadge), Actions: three ghost icon buttons CheckCircle2 (accept), XCircle (reject), PauseCircle (on-hold) each calling onHandle(bill, presetStatus). Row click opens detail dialog (reuse base BillDetailDialog from 05-03). Loading → DefaultLoader; empty → NoData.
    recycle-bin.tsx: content wrapped in TrashTabPattern title="Recycle Bin" WITH children (endpoint exists — PATTERNS note): table of recycleBills (user resolveName / description / amount / date) each with Restore button → restore mutation (invalidation refetches bin + active lists). Empty → NoData inside the pattern.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Management page builds with Handle Queue | Recycle Bin tabs, always-param-safe server-paged filtering (300ms debounce), and a working restore flow in the bin.</done>
  <acceptance_criteria>
    - Page source contains TabsTrigger values "queue" and "recycle" (labels Handle Queue / Recycle Bin)
    - Filter builder always includes isDeleted:"false" (source assertion — grep 'isDeleted' management/page.tsx returns ≥1 non-comment hit)
    - Cleared-filters state (empty search, status "all", no user/date) yields a guard-safe request per the 05-01 Quirk-10 verdict — UI calls searchBills unchanged with zero local workaround params (source assertion: no "description=" string literal in management/page.tsx filter builder)
    - Debounce uses 300ms constant (source assertion: "300" present near setTimeout)
    - Route builds; direct access as staff redirects via RoleGuard to /forbidden
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Handle bill dialog — required Notes, preset decision</name>
  <files>features/reimbursement/handle-bill-dialog.tsx</files>
  <read_first>
    - features/leave/review-leave-dialog.tsx (THE analog: status-preset review dialog with reset-on-open and disabled submit)
    - ../saher-backend/src/reimbursement/settlement/schema.ts handleBillSchema (field `reason`, Quirk 4)
    - .planning/phases/05-money-approval-reimbursement-payroll/05-CONTEXT.md D-10
    - services/reimbursement.api.ts (05-01 handleBill input schema)
  </read_first>
  <action>
    Controlled Dialog ({ bill, initialStatus: "accept"|"reject"|"on-hold", open, onOpenChange }) mirroring review-leave-dialog structure exactly: useForm over zod schema { status enum, reason: string min 5 message "Notes must be at least 5 characters" }; useEffect reset on open with initialStatus + empty reason. Header shows bill summary (user via resolveName, amount ₹, description). The API field is `reason`; the visible FieldLabel reads "Notes" (D-10 wording vs backend name — Quirk 4). Decision shown as three-way RadioGroup prefilled with initialStatus so finance can switch mind before submitting. Submit → handleOne({billId, status, reason}); onSuccess toast — when status==="accept" the toast says "Bill accepted — settlement created automatically" (Quirk 3 education) else "Bill {verb}"; close + reset. onError toast.error ONLY, dialog stays open with values intact (D-28). Submit Button disabled={handleOne.isPending} label-swap "Processing…" (D-26).
    Wire from finance-bill-table onHandle: set {bill, status} lifted state in management page rendering this dialog. After accept, invalidation refetches the queue (accepted bill leaves the pending view naturally); Record Settlement arrives in plan 05-06 via the detail dialog.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm exec vitest run tests/create-bill-dialog.test.tsx</automated>
  </verify>
  <done>Handle dialog enforces ≥5-char Notes on the reason field, prefills the decision, toasts auto-settlement education on accept, and keeps open on error.</done>
  <acceptance_criteria>
    - Source assertions: FieldLabel text "Notes"; zod min(5) on the field bound to name "reason"
    - RadioGroup value initialized from initialStatus and reset on each open (effect present)
    - onError does not close the dialog (D-28); submit disabled while pending (D-26)
    - Existing suite still green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Bulk selection + floating action bar with sequential progress</name>
  <files>app/(main)/reimbursement/management/page.tsx, features/reimbursement/bulk-action-bar.tsx, features/reimbursement/finance-bill-table.tsx</files>
  <read_first>
    - hooks/use-reimbursement.ts handleMany + bulkProgress contract (plan 05-01 Task 2)
    - .planning/phases/05-money-approval-reimbursement-payroll/05-CONTEXT.md D-11/D-27
  </read_first>
  <action>
    bulk-action-bar.tsx: rendered by the page INSIDE the queue tab when selectedIds.size > 0; fixed bottom-centered Card/Bar showing "{n} selected", buttons "Approve All" (CheckCircle2) and "Reject All" (XCircle) plus "Clear" ghost. Clicking Approve/Reject All opens a small shared-reason Dialog (status fixed to the chosen action, one Notes(reason) textarea with same min-5 validation) — one decision applies to every selected bill, honoring D-10's required-notes contract without N dialogs. Confirm invokes handleMany(selected.map(id => ({billId:id, status, reason}))). While bulkProgress !== null the bar swaps buttons for "Processing {done}/{total}…" readout (Progress component bound to done/total) and all table checkboxes disable. Completion summary toast is emitted by the hook itself (built in 05-01).
    management/page.tsx owns selectedIds Set<string> state; passes to table + bar; clears selection when the queue query refetches to an empty list or after handleMany resolves. finance-bill-table already exposes toggle callbacks (Task 1) — no signature change needed beyond disabling during bulkProgress.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm exec vitest run tests/reimbursement-hook.test.tsx</automated>
  </verify>
  <done>Bulk bar appears on selection, collects one shared reason, shows Processing x/y during the sequential run, and clears after the hook's summary toast.</done>
  <acceptance_criteria>
    - Bar renders only when selectedIds.size > 0 (conditional in page source)
    - Progress readout format "Processing {done}/{total}" bound to hook bulkProgress (source assertion)
    - Shared-reason dialog enforces the same min-5 rule as single handling
    - Hook-level bulk test (sequential order, failure tolerance, one summary toast) still green — UI consumes it unchanged
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Approval/rejection decisions and bulk loops cross here |
| Selection state → mutation payload | Client-built batch derived from user checkbox input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | Double-fired handle on same bill | mitigate | D-26 pending gating on dialog submit AND whole-bar disable during bulkProgress; server transitions are stateful (second handle of a settled bill fails server-side) |
| T-05-05-02 | Denial of Service | Parallel burst from naive bulk implementation | mitigate | handleMany is strictly sequential by construction (pinned by 05-01 test); UI disables inputs mid-run |
| T-05-05-03 | Repudiation | Approval without attributable reason | mitigate | Notes(reason) mandatory min-5 on both single and bulk paths |
| T-05-05-04 | Information Disclosure | Queue leaks cross-user salary/bill data to staff | mitigate | Server authorize('read','preReimbursement') guards the search endpoint; client RoleGuard mirrors it |
| T-05-05-05 | Tampering | Restore abused to resurrect foreign bills | mitigate | Backend guard decides (D-30); UI exposes restore only inside finance-guarded surface; caveat already recorded in 05-03 SUMMARY for staff-side |
| T-05-SC | Tampering | Package supply chain | accept | Zero installs |

No high-severity threats without mitigation coverage.
</threat_model>

<verification>
- `pnpm lint && pnpm typecheck && pnpm build` exit 0; vitest suite green including reimbursement hook tests
</verification>

<success_criteria>
- Finance can: filter/search the queue (server-paged), act on a bill with mandatory notes, bulk-approve/reject with visible sequential progress and honest partial-failure summary, restore deleted bills
- Every write path demonstrates D-26/D-27/D-28/D-29 behavior
- Toolbar reserves the export slot consumed by plan 05-06
</success_criteria>

<output>
Create `.planning/phases/05-money-approval-reimbursement-payroll/05-05-SUMMARY.md` when done
</output>
