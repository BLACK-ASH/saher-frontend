---
phase: 02-shared-infrastructure-session-reliability
plan: 05
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - lib/normalize-list.ts
  - lib/normalize-list.test.ts
  - components/pagination-footer.tsx
  - components/pagination-footer.test.tsx
  - services/attendance.api.ts
  - services/attendance-correction.api.ts
  - features/attendance/attendance-table.tsx
  - features/attendance/attendance-correction-requests.tsx
  - features/dashboard/range-attendance-table.tsx
  - features/dashboard/today-attendance-table.tsx
  - features/dashboard/attendance-grid/attendance-dashboard.tsx
autonomous: true
requirements: [FNDT-03, FNDT-05, FNDT-02]
must_haves:
  truths:
    - "List endpoints yield validated { items, page, limit, totalPages, totalRecords } whether meta says total or totalPages (success criterion #2)"
    - "Nullable data or absent meta yields an empty items list — never a crash"
    - "Every pagination footer renders disabled — never hidden, never crashing — when meta is missing/malformed or at a boundary (success criterion #4, D-20)"
    - "No screen computes page math from meta.total as record count anymore (research discovery #2 bug class eliminated)"
  artifacts:
    - path: "lib/normalize-list.ts"
      provides: "normalizeList<T>(res) → NormalizedList<T> absorbing all verified backend meta variance"
      exports: ["normalizeList", "NormalizedList"]
    - path: "lib/normalize-list.test.ts"
      provides: "one test per verified variance row from RESEARCH Pattern 2 table"
    - path: "components/pagination-footer.tsx"
      provides: "PaginationFooter(page, totalPages, onPageChange) with D-20 disabled-safe guards"
    - path: "components/pagination-footer.test.tsx"
      provides: "render proofs: missing meta rendered+disabled; boundary enable/disable; click fires callback"
    - path: "services/attendance.api.ts"
      provides: "all list endpoints return normalizeList output"
  key_links:
    - from: "services/*.api.ts"
      to: "lib/normalize-list.ts"
      via: "return normalizeList<T>(res) immediately after apiFetch (D-16 placement)"
      pattern: "normalizeList"
    - from: "features screens"
      to: "components/pagination-footer.tsx"
      via: "inline Button pagination blocks replaced by shared footer fed normalized fields"
      pattern: "PaginationFooter"
---

<objective>
Build the envelope normalization factory (D-16/FNDT-03) and the safe pagination footer (D-20/FNDT-05), adopt the normalizer in the paginated services, then retrofit the five attendance-family screens onto both — including their date-format alignment from Plan 02-02's module.

Purpose: kills the verified live bug class where `meta.total` is interpreted as record count in some screens (`attendance-dashboard.tsx:181` passes it into Math.ceil(total/limit)) and page count in others — plus the `Number(undefined) < n === false` crash vector that leaves next-page enabled on empty meta.
Output: tested normalizer + footer modules; attendance-family screens consuming them.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-infrastructure-session-reliability/02-CONTEXT.md
@.planning/phases/02-shared-infrastructure-session-reliability/02-PATTERNS.md
@.planning/phases/02-shared-infrastructure-session-reliability/02-RESEARCH.md

<interfaces>
<!-- lib/normalize-list.ts public surface — pinned; Plan 02-06 reuses for remaining services. -->
export type NormalizedList<T> = {
  items: T[];
  page: number;          // 1-based; 1 when un-paged
  limit: number;
  totalPages: number;    // 0 allowed for empty paged results; min 1 implied by consumers' guards
  totalRecords: number;  // derived from `count`, falling back to items.length
};
export function normalizeList<T>(res: { data?: T[] | null; meta?: unknown }): NormalizedList<T>;

<!-- Verified backend variance the factory MUST absorb (RESEARCH Pattern 2 table, all rows):
| behavior                                | evidence                                   |
| total carries PAGE COUNT                | attendance retrieve/correction/today ctrl  |
| totalPages carries PAGE COUNT           | payroll, reimbursement controllers         |
| count = RECORD count                    | all paging metas                           |
| empty result emits totalPages: 0        | today.controller.ts:60                     |
| meta entirely absent (bare array)       | admin/user/controller getAllUsers          |
| meta with ONLY count (non-paging)       | notification.controllers.ts:138            |
| data nullable (data ?? null)            | libs/class/api-response.ts                 |

<!-- components/pagination-footer.tsx props (analog: attendance-pagination.tsx layout): -->
type PaginationFooterProps = {
  page: number;
  totalPages: number;
  onPageChange: (page: number) => void;
};
// Internal guards: prev disabled when page<=1 || !Number.isFinite(totalPages) || totalPages<1
// next disabled when page>=totalPages || same NaN guards. Readout "Page {page} of {M}" with
// M rendered as "--" when !Number.isFinite(totalPages).

<!-- Current consumer defects being fixed (verified line refs): -->
features/dashboard/range-attendance-table.tsx:189 + today-attendance-table.tsx:120 +
features/attendance/attendance-table.tsx:90 + features/attendance/attendance-correction-requests.tsx:83:
    disabled={Number(data?.meta?.total) < page + 1}   // Number(undefined)<n === false → ENABLED on empty meta
features/dashboard/attendance-grid/attendance-dashboard.tsx:181:
    passes meta.total into AttendancePagination which does Math.ceil(total/limit) // wrong semantics
services shape today (attendance.api.ts:54,68,84 + attendance-correction.api.ts:56,81):
    return { data: res.data, meta: res.meta };        // ← becomes return normalizeList<X>(res)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build normalizeList factory + adopt in paginated services (FNDT-03/D-16)</name>
  <files>lib/normalize-list.ts, lib/normalize-list.test.ts, services/attendance.api.ts, services/attendance-correction.api.ts</files>
  <behavior>
    - meta {page:2, limit:7, count:20, total:3} → {totalPages:3, totalRecords:20, page:2, limit:7} (total-as-pages honored)
    - meta {..., totalPages:5} (no total) → totalPages:5 (both field names treated identically)
    - res.data null / undefined / non-array → items:[] (never throws)
    - meta absent entirely → {items, page:1, limit:items.length||10, totalPages: items.length?1:0, totalRecords:items.length}
    - meta {count:0, totalPages:0} → totalPages:0 preserved (empty paged result)
    - meta {count:9} only (non-paging) → totalPages:1, totalRecords:9
    - malformed numbers (string "3", negative, Infinity, NaN) fall back per-field defensively
    - page < 1 clamps to 1
  </behavior>
  <action>
    Write `lib/normalize-list.test.ts` FIRST covering every row above, run red, then implement following RESEARCH Pattern 2 sketch (defensive num() coercion helper; rawPages prefers totalPages then falls back to total — BOTH mean pages). Then adopt: read `services/attendance.api.ts` and `services/attendance-correction.api.ts`; replace each of the five `return { data: res.data, meta: res.meta }` sites (attendance.api lines ~54/~68/~84, attendance-correction.api lines ~56/~81) with `return normalizeList<TheResponseType>(res)` using each function's existing type argument. Keep endpoint functions' signatures otherwise unchanged. Do NOT touch notification.api (its count-only badge flow is not a list).
  </action>
  <verify>
    <automated>pnpm test lib/normalize-list.test.ts && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All eight behavior cases asserted and green
    - grep "return { data: res.data, meta: res.meta }" over services/ returns zero matches for attendance.api.ts and attendance-correction.api.ts
    - typecheck green (screens still compiling proves NormalizedList is shape-compatible pending Task 2 reads)
  </acceptance_criteria>
  <done>Every paginated service in the two files returns the normalized shape; variance absorbed in one tested place</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build PaginationFooter with D-20 disabled-safe guards + render tests</name>
  <files>components/pagination-footer.tsx, components/pagination-footer.test.tsx</files>
  <behavior>
    - Props {page:2, totalPages:5}: first/prev enabled, next/last enabled, readout "Page 2 of 5"
    - page:1 → first+prev DISABLED, next+last enabled
    - page===totalPages → next+last DISABLED
    - totalPages:0 or NaN (simulate via passing Number.NaN through a cast in test) or undefined-cast → footer STILL RENDERS, ALL controls disabled, readout shows "Page 1 of --", no crash
    - Clicking next when enabled fires onPageChange(page+1); clicking prev fires onPageChange(page-1); first→1; last→totalPages
  </behavior>
  <action>
    Create `components/pagination-footer.tsx` modeled on `features/dashboard/attendance-grid/attendance-pagination.tsx` layout (read it first) but taking PRE-NORMALIZED {page,totalPages} — it does NO Math.ceil itself (the analog's math is the bug being retired). Reuse shadcn Button + lucide ChevronsLeft/ChevronLeft/ChevronRight/ChevronsRight exactly as the analog does (ponytail rung 2 — no new primitives). Compute an internal `const safe = Number.isFinite(totalPages) && totalPages >= 1` guard used by all four buttons; readout prints totalPages when safe else "--". Write render tests FIRST via tests/render-with-providers.tsx (component needs no providers, but harness consistency wins), then implement. Name the component file exactly components/pagination-footer.tsx (discretion exercised: matches RESEARCH recommended structure).
  </action>
  <verify>
    <automated>pnpm test components/pagination-footer.test.tsx && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - Missing/NaN/zero totalPages renders the readout with "--" and every control disabled (asserted, not eyeballed)
    - Boundary enable/disable matrix asserted for first/middle/last pages
    - No case hides the footer (always rendered)
  </acceptance_criteria>
  <done>The crash vector (enabled next on missing meta) is structurally impossible in the new component</done>
</task>

<task type="auto">
  <name>Task 3: Retrofit five attendance-family screens onto footer + normalized shape + IST dates (D-01/D-02)</name>
  <files>features/attendance/attendance-table.tsx, features/attendance/attendance-correction-requests.tsx, features/dashboard/range-attendance-table.tsx, features/dashboard/today-attendance-table.tsx, features/dashboard/attendance-grid/attendance-dashboard.tsx</files>
  <action>
    Read each screen before editing. For each of the five:
    1. Update reads of the changed service shape: list data now arrives as NormalizedList — `.data` → `.items`, any `meta.total` page math DELETED (attendance-dashboard.tsx:181's Math.ceil(total/limit) interpretation goes away entirely).
    2. Replace the inline pagination Buttons block with `<PaginationFooter page={n.page} totalPages={n.totalPages} onPageChange={setPage-or-equivalent} />`, wiring onPageChange to whatever state/query param drives refetch (each screen keeps its own page state variable — read to find it).
    3. Delete the old disabled={Number(data?.meta?.total)...} expressions everywhere they appear.
    4. Date-format alignment (imports from @/lib/date per Plan 02-02 contract): formatDate→formatIstDate, formatTime/transformTime→formatIstDateTime, formatHours keeps name — these five screens are the remaining @/lib/utils/time importers inside features/attendance + features/dashboard.
    Scope guard D-04: if a screen reveals breakage beyond these utilities (wrong API params, broken filters), log it to STATE.md blockers — do not fix here.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep "meta?.total\|meta.total" across the five edited files returns zero matches
    - grep "@/lib/utils/time" across the five edited files returns zero matches
    - Each of the five renders PaginationFooter (grep-verifiable)
    - Full gates green: lint, typecheck, existing suite, production build
  </acceptance_criteria>
  <done>The whole attendance family consumes normalized lists through the shared safe footer with canonical IST dates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend meta payload → client page math | Malformed/hostile meta values previously produced NaN-driven UI states |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-12 | Tampering (malformed meta crashes or misleads paging) | normalizeList + footer | mitigate | Defensive per-field numeric coercion with fallbacks; footer re-checks Number.isFinite before enabling any control (input validation at trust boundary — never simplified away) |
| T-2-13 | DoS (UX) — unbounded page jump to huge totalPages | footer controls | accept | Controls step ±1/boundaries only; no free-text page input exists to abuse |
| T-2-14 | Information Disclosure via error rendering | normalizeList failures | mitigate | Factory never throws — degrades to empty list; React escapes all rendered strings |
| T-2-SC | Tampering | npm installs | mitigate | No installs (zero-dependency phase per RESEARCH audit) |
</threat_model>

<verification>
- `pnpm test lib/normalize-list.test.ts` — every verified variance row green
- `pnpm test components/pagination-footer.test.tsx` — D-20 matrix proven incl. missing-meta render
- `pnpm build` + full suite green after the five-screen retrofit
- grep audits: no meta.total consumers, no utils/time imports remain in touched files
</verification>

<success_criteria>
Lists return validated normalized shapes regardless of backend meta dialect, and pagination UI is provably crash-proof — success criteria #2 and #4 demonstrated by automated tests.
</success_criteria>

<output>
Create `.planning/phases/02-shared-infrastructure-session-reliability/02-05-SUMMARY.md` when done
</output>
