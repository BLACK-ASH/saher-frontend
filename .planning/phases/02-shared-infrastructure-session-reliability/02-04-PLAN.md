---
phase: 02-shared-infrastructure-session-reliability
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - hooks/use-login.ts
  - hooks/use-logout.ts
  - features/login/components/login-form.tsx
  - lib/api-wrapper.test.ts
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "Concurrent 401s trigger exactly ONE /api/auth/refresh-token call; each original request retries exactly once (success criterion #3)"
    - "After re-login the user lands back where the session died (?next= honored, open-redirect-proof)"
    - "Logout is best-effort: a failing POST /api/auth/logout still clears cache and redirects — the user is never trapped (D-08)"
    - "The session-death once-guard resets on successful login so the NEXT death still notifies"
  artifacts:
    - path: "hooks/use-login.ts"
      provides: "resetSessionGuard + real ['user','me'] invalidation (fixes invalidateQueries({queryKey:[]})"
    - path: "hooks/use-logout.ts"
      provides: "try/catch best-effort logout + performLogoutCleanup (fixes removeQueries({queryKey:[]}))"
    - path: "features/login/components/login-form.tsx"
      provides: "validated ?next= consumption after login (D-05 back half)"
    - path: "lib/api-wrapper.test.ts"
      provides: "D-19 money-path integration tests through the REAL wrapper + msw"
      contains: "refresh-token"
  key_links:
    - from: "hooks/use-logout.ts"
      to: "lib/session.ts performLogoutCleanup"
      via: "onSuccess cleanup shared with session death (one behavior, two triggers, D-17/D-08)"
      pattern: "performLogoutCleanup"
    - from: "features/login/components/login-form.tsx"
      to: "?next= param"
      via: "useSearchParams + startsWith('/') && !startsWith('//') guard → router.push(next)"
      pattern: "next"
---

<objective>
Complete the AUTH-01 contract at the flow level: fix both broken auth hooks (per D-21's bug list), wire best-effort logout onto the Plan 02-01 session module (D-08), add `?next=` return navigation with an anti-open-redirect guard (D-05), and prove refresh single-flight/retry-once/death-sentinel with msw integration tests through the real `apiFetch` (D-19).

Purpose: success criterion #3 verbatim — "/login exactly once, no toast storm, no refetch loop; refresh retries exactly once before giving up" becomes executable test code AUDT-08 will reuse in Phase 7.
Output: repaired use-login/use-logout, next-aware login form, extended api-wrapper integration suite.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-infrastructure-session-reliability/02-CONTEXT.md
@.planning/phases/02-shared-infrastructure-session-reliability/02-01-SUMMARY.md

<interfaces>
<!-- Consumed from Plan 02-01 (already merged): -->
From lib/session.ts:
  handleSessionDeath(queryClient)   // once-guarded: cancel+clear+1 toast+redirect /login?next=
  performLogoutCleanup(queryClient) // unguarded: cancel+clear+redirect "/" — NO toast (explicit logout)
  resetSessionGuard()               // clears once-guard — call on successful login

<!-- Current broken state (verified 2026-08-25): -->
hooks/use-login.ts lines 14-16:
    onSuccess: () => { queryClient.invalidateQueries({ queryKey: [] }); }   // matches nothing meaningful

hooks/use-logout.ts lines 15-18:
    onSuccess: () => {
      queryClient.removeQueries({ queryKey: [] });   // matches nothing; wrong API for wipe-anyway (D-07 wants clear())
      router.push("/");
    },

features/login/components/login-form.tsx lines 51-61: onLoginSubmit pushes hardcoded "/" — ignores ?next=

Test harness precedent (lib/api-wrapper.test.ts lines 19-48): server.use(http.get(...)) +
HttpResponse.json envelope → drive REAL apiFetch → assert. refreshSession() inside the wrapper
uses raw fetch("/api/auth/refresh-token") which msw intercepts under jsdom.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair use-login and use-logout onto the session module (D-07/D-08/D-21 bug fixes)</name>
  <files>hooks/use-login.ts, hooks/use-logout.ts</files>
  <action>
    Read both hook files first.
    use-login.ts onSuccess becomes: `resetSessionGuard()` (import from "@/lib/session" — the death once-guard must reset so a subsequent expiry notifies again, RESEARCH Pitfall 1) then `queryClient.invalidateQueries({ queryKey: ["user", "me"] })` — the real key used by hooks/use-me.ts line 43.
    use-logout.ts: wrap the apiFetch POST in try/catch with an EMPTY catch — best-effort per D-08 (network failure or already-dead session must not trap the user); onSuccess calls `performLogoutCleanup(queryClient)` and nothing else. DELETE the useRouter import/usage — redirect ownership moved into lib/session.ts (assign("/") there). Keep the exported hook name/signature so header logout button consumers are untouched.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - hooks/use-login.ts contains resetSessionGuard() and queryKey ["user", "me"]; zero occurrences of queryKey: []
    - hooks/use-logout.ts contains try/catch around the logout POST and performLogoutCleanup(queryClient); zero occurrences of removeQueries or queryKey: []
    - typecheck green (proves no dangling router import)
  </acceptance_criteria>
  <done>Both auth hooks hit real query keys and route cleanup through the one shared session module</done>
</task>

<task type="auto">
  <name>Task 2: Honor ?next= after login with open-redirect guard (D-05 back half)</name>
  <files>features/login/components/login-form.tsx</files>
  <action>
    Read the file first. Add `const params = useSearchParams()` (import from "next/navigation"; the page already renders LoginForm inside <Suspense> — required for useSearchParams, verify while reading app/(auth)/login/page.tsx and keep it that way). In onLoginSubmit onSuccess replace `router.push("/")` with: `const raw = params.get("next") ?? "/"; const next = raw.startsWith("/") && !raw.startsWith("//") ? raw : "/"; router.push(next);`. The double guard is mandatory (RESEARCH Pitfall 3): "//evil.example" is protocol-relative and startsWith("/") alone would allow it. Keep toast.success + router.refresh() as-is.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test features/login</automated>
  </verify>
  <acceptance_criteria>
    - login-form.tsx contains the exact guard chain startsWith("/") && !raw.startsWith("//")
    - Default fallback when param absent is "/"
    - Existing Phase 1 LoginForm render test still passes (component signature unchanged)
  </acceptance_criteria>
  <done>Post-login navigation returns users to their pre-death location and can never leave the origin</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-19 money-path tests — single-flight refresh, retry-exactly-once, death sentinel (AUTH-01)</name>
  <files>lib/api-wrapper.test.ts</files>
  <behavior>
    - Concurrent 401s: two apiFetch calls to endpoints returning 401 then, after refresh, 200 → refresh-token handler invoked EXACTLY once; BOTH original requests retried and resolved successfully
    - Retry-once accounting: original endpoint handler records hits — exactly initial + one retry per caller (2 callers → 4 hits total, never 5+)
    - Refresh failure: refresh handler returns 401 → every concurrent apiFetch rejects with Error("Unauthorized"); refresh STILL called exactly once; no second refresh attempt ever fires
    - Wrapper emits no toast on the death path (sonner mocked; toast.error call count === 0 for these scenarios — dedupe ownership lives in lib/session.ts per Plan 02-01)
  </behavior>
  <action>
    Extend `lib/api-wrapper.test.ts` (read it first — follow its inline-handler style). Use msw handlers with closure counters (`let refreshHits = 0`) registered via server.use per test. Sequence control: first N hits return HttpResponse.json({success:false,message:"..."},{status:401}), subsequent hits return the success envelope — msw handlers can branch on an internal counter. Fire concurrent calls with Promise.all(apiFetch(...), apiFetch(...)). Mock sonner at top (`vi.mock("sonner", ...)`) to assert toast silence. Reset counters in beforeEach. Do NOT stub window.location here — redirect behavior belongs to session.test.ts (Plan 02-01); this suite asserts only wrapper-level contracts. These four scenarios are the durable AUDT-08 reference (D-19) — name them clearly ("concurrent 401s trigger single-flight refresh", etc.).
  </action>
  <verify>
    <automated>pnpm test lib/api-wrapper.test.ts && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts refreshHits === 1 under two concurrent failing-then-passing requests
    - A test asserts each original endpoint was hit exactly twice (initial + single retry)
    - A test asserts rejection message "Unauthorized" with refreshHits === 1 on refresh failure
    - All new tests pass alongside the three existing envelope/meta tests
  </acceptance_criteria>
  <done>AUTH-01's refresh contract is executable regression protection through the real fetch wrapper</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ?next= query param → post-login navigation | Attacker-controlled URL parameter drives client redirect |
| failed logout network state | Cookies remain httpOnly-present; JS cannot clear them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-09 | Tampering/Elevation (open redirect via next) | login-form next consumption | mitigate | HIGH threat, MITIGATED: validate startsWith("/") AND NOT startsWith("//"), default "/" — blocks protocol-relative off-site targets; no unmitigated HIGH remains in this plan |
| T-2-10 | Availability (user trapped by failing logout) | use-logout mutationFn | mitigate | Empty-catch best-effort + unconditional performLogoutCleanup redirect (D-08); residual proxy-bounce degradation documented and accepted (RESEARCH Pitfall 2 option chosen: cleanup redirects "/" not /login) |
| T-2-11 | Information Disclosure (stale-role cache after re-login) | use-login invalidation | mitigate | Real ["user","me"] invalidation replaces the matches-nothing empty key; combined with Plan 02-01 clear() on death, no cross-session data survives |
| T-2-SC | Tampering | npm installs | mitigate | No installs in this plan |
</threat_model>

<verification>
- `pnpm test lib/api-wrapper.test.ts` proves single-flight, retry-once, sentinel, toast-silence
- Manual: die a session on /attendance?page=2 → login → land on /attendance?page=2
- Manual: DevTools offline + logout → app still lands logged-out (no trap)
- Full suite green before wave merge
</verification>

<success_criteria>
Login/logout/refresh behave exactly per contract: one refresh attempt per 401 storm, one retry per request, validated return navigation, untrapable logout — all locked in by D-19 integration tests.
</success_criteria>

<output>
Create `.planning/phases/02-shared-infrastructure-session-reliability/02-04-SUMMARY.md` when done
</output>
