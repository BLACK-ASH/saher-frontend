---
phase: 02-shared-infrastructure-session-reliability
plan: 06
type: execute
wave: 3
depends_on: ["02-05"]
files_modified:
  - components/data-table.tsx
  - features/attendance-correction/corrections/column.tsx
  - features/attendance-correction/corrections/data-table.tsx
  - features/dashboard/attendance-grid/attendance-dashboard.tsx
  - features/mail/data-table.tsx
  - features/program/participant/participant-header.tsx
  - features/program/program/program-header.tsx
  - features/program/session/session-header.tsx
  - features/program/workshop/workshop-header.tsx
  - features/users/data-table.tsx
  - hooks/resource-list-factory.ts
  - hooks/use-workshops.ts
  - hooks/use-sessions.ts
  - hooks/use-programs.ts
  - hooks/use-participant.ts
  - hooks/use-leave.ts
  - hooks/use-admin-attendance-correction.ts
  - services/workshop.api.ts
  - services/session.api.ts
  - services/program.api.ts
  - services/participant.api.ts
  - services/leave.api.ts
  - features/program/workshop/all-workshops.tsx
  - features/program/session/all-sessions.tsx
  - features/program/program/all-programs.tsx
  - features/program/participant/all-participant.tsx
  - app/(main)/program/[id]/page.tsx
  - features/leave/admin-page.tsx
  - features/leave/page.tsx
autonomous: true
requirements: [FNDT-03, FNDT-02, FNDT-05]
must_haves:
  truths:
    - "The corrections TanStack table paginates from normalized totalPages and renders the shared footer (D-02)"
    - "The users admin table swaps its raw Previous/Next buttons for the shared footer driven by table.getPageCount() (D-02 — explicitly named by the decision)"
    - "The generic components/data-table.tsx (sole consumer: holiday screen) swaps its raw Previous/Next block for the same client-side shared footer — no paginated list remains un-migrated (D-02)"
    - "The mail screen's TanStack tables (MailDataTable renders twice on the page) swap their raw Previous/Next Buttons for the identical client-side shared footer (D-02 — checker blocker fix)"
    - "The four program-family headers (workshop/program/session/participant) replace their raw URL-param prev/next Buttons with PaginationFooter fed by the normalized .page/.totalPages from their collapsed hooks, wired to the existing ?page= state via onPageChange — which also fixes the missing boundary disable on next (D-02 — checker blocker fix)"
    - "Six hand-rolled list hooks collapse onto one factory so query/mutation/invalidate wiring exists exactly once (D-21)"
    - "lib/utils/time.ts is deleted with zero remaining importers — one home for dates (D-18 complete)"
    - "Every paginated service in services/ returns NormalizedList"
  artifacts:
    - path: "hooks/resource-list-factory.ts"
      provides: "createResourceListHook({ baseKey, list, get?, mutations }) returning flat hook bag"
    - path: "features/attendance-correction/corrections/data-table.tsx"
      provides: "pageCount from normalized shape; PaginationFooter via setPageIndex"
      contains: "PaginationFooter"
    - path: "features/users/data-table.tsx"
      provides: "client-side pagination through shared PaginationFooter (getPaginationRowModel stays)"
      contains: "PaginationFooter"
    - path: "components/data-table.tsx"
      provides: "generic TanStack DataTable renders shared PaginationFooter from table state (getPaginationRowModel stays)"
      contains: "PaginationFooter"
    - path: "features/mail/data-table.tsx"
      provides: "MailDataTable renders shared PaginationFooter from table state (getPaginationRowModel stays); consumed twice by app/(main)/mail/page.tsx"
      contains: "PaginationFooter"
    - path: "features/attendance-correction/corrections/column.tsx"
      provides: "date cells rendered via lib/date formatters"
  key_links:
    - from: "hooks/use-{workshops,sessions,programs,participant,leave,admin-attendance-correction}.ts"
      to: "hooks/resource-list-factory.ts"
      via: "each hook becomes a thin createResourceListHook call preserving its public return names"
      pattern: "createResourceListHook"
    - from: "(repo-wide)"
      to: "lib/utils/time.ts"
      via: "zero imports remain; file deleted; pnpm build proves no stale references"
      pattern: "utils/time"
    - from: "features/program/{workshop,program,session,participant}-header.tsx"
      to: "hooks/use-{workshops,programs,sessions,participant}.ts"
      via: "headers call their resource hook with the same searchParams-derived params as the paired screen (shared cache entry) and render PaginationFooter from the normalized list's page/totalPages, onPageChange → existing ?page= URL updatePage"
      pattern: "PaginationFooter"
---

<objective>
Finish the retrofit and the consolidation: migrate the corrections screen, the dashboard-grid attendance screen, AND the users-admin table — D-02 names it explicitly; it paginates client-side over getPaginationRowModel with raw Previous/Next buttons that swap onto the shared footer — plus the generic `components/data-table.tsx` (holiday screen, same client-side recipe) and `features/mail/data-table.tsx` (rendered twice on the mail page, identical client-side recipe), plus the four program-family headers (workshop/program/session/participant — raw URL-param prev/next Buttons with NO next-boundary guard; swap onto PaginationFooter fed by their hooks' normalized .page/.totalPages wired to the existing ?page= state) (column dates + normalized pageCount + shared footer across all of them) — sweep the remaining `@/lib/utils/time` importers, DELETE `lib/utils/time.ts` (completing D-18), and collapse the six hand-rolled list hooks onto one tested-shape factory (D-21) — including the five remaining paginated services adopting normalizeList and their list-reader consumers flipping to `.items`.

Purpose: D-01/D-02 demand every existing list and date call site migrated within this phase — this plan owns the stragglers carrying pagination duties (checker blocker fix: the five missed surfaces above are declared here so "no raw paginated Previous/Next remains" becomes TRUE at this plan's exit for every paginated surface; non-pagination Previous/Next buttons — day-range navigation in attendance-toolbar/range-attendance-table and table-header labels — are out of D-02 scope), then retires the old date module outright. The phase's FINAL raw-pattern gate (zero browser-TZ `toLocale*String`/naive `new Date(arg)` rendering anywhere) executes in Plan 02-07, which sweeps the surfaces that never imported utils/time.
Output: fully modernized corrections + dashboard-grid + users-admin + mail + program-header screens plus the generic holiday DataTable; single-source list-hook factory; all six domains on normalized lists; time.ts gone.

Execution note (checker scope-sanity): this plan touches 28 files across 3 compile-coupled tasks. Run it in a FRESH session. If context runs low mid-task: finish the current task, stop cleanly, and record exact handoff state in the SUMMARY for a continuation session — do not compress remaining tasks into rushed edits.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-infrastructure-session-reliability/02-CONTEXT.md
@.planning/phases/02-shared-infrastructure-session-reliability/02-PATTERNS.md
@.planning/phases/02-shared-infrastructure-session-reliability/02-05-SUMMARY.md

<interfaces>
<!-- Consumed from earlier plans (already merged): -->
From lib/normalize-list.ts (Plan 02-05): normalizeList<T>, type NormalizedList<T> {items,page,limit,totalPages,totalRecords}
From lib/date.ts (Plan 02-02): formatIstDate, formatIstDateTime, formatHours, calculateWorkHours, getMonthYear, combineDateAndTimeToIso
From components/pagination-footer.tsx (Plan 02-05): <PaginationFooter page totalPages onPageChange />

<!-- Factory contract to create (signature at delegated discretion; pinned here for determinism): -->
hooks/resource-list-factory.ts:
  type ResourceListConfig<T> = {
    baseKey: readonly unknown[];                  // e.g. ["workshops"] or ["attendance","correction"]
    list: (params: QueryProps) => Promise<NormalizedList<T>>;   // already-normalized service fn
    get?: (id: string) => Promise<T>;             // optional detail query, enabled: !!id
    mutations?: {                                 // each invalidates baseKey prefix on success
      add?: ServiceFn; update?: ServiceFn; del?: ServiceFn;
    };
  };
  export function createResourceListHook<T>(config: ResourceListConfig<T>)
    : (params: { id?: string } & QueryProps) => object;   // returns flat bag per CONVENTIONS

<!-- The two shapes being collapsed (PATTERNS excerpts): -->
use-workshops: keys ["workshops", keyword, page, limit]; detail ["workshops", id] enabled !!id;
              add/update/del each invalidate ["workshops"]; returns { workshops, workshop, add, update, del }
use-admin-attendance-correction: hierarchical keys ["attendance","correction", limit, page, sort];
              handle mutation invalidates prefix ["attendance","correction"]

<!-- TanStack retrofit targets (verified): -->
corrections/data-table.tsx line ~77: pageCount: corrections?.meta?.total   ← becomes normalized.totalPages ?? 0
corrections/data-table.tsx lines ~189-206: raw Previous/Next Buttons ← replaced by PaginationFooter wired
  onPageChange={(p) => table.setPageIndex(Math.max(0, p - 1))}; keep manualPagination:true
attendance-dashboard.tsx:181: passes meta.total into AttendancePagination doing Math.ceil(total/limit)
  ← reads flip to normalized fields; AttendancePagination block replaced by PaginationFooter;
  its date renders align to lib/date (formatDate→formatIstDate, formatTime/transformTime→formatIstDateTime)

<!-- Users-admin retrofit target (verified; D-02 names this screen): -->
users/data-table.tsx:56: getPaginationRowModel() — CLIENT-SIDE pagination, no server meta involved
users/data-table.tsx lines ~162-179: raw Previous/Next Buttons ← replaced by PaginationFooter fed from
  page={table.getState().pagination.pageIndex + 1} totalPages={table.getPageCount()}
  onPageChange={(p) => table.setPageIndex(Math.max(0, p - 1))} — no service/hook change needed

<!-- Generic DataTable retrofit target (verified; same client-side recipe as users-admin): -->
components/data-table.tsx lines ~156-179: raw Previous/Next Button block (sole consumer
  features/holiday/holiday-table.tsx on app/(main)/calendar) ← replaced by the identical
  PaginationFooter swap; drop now-unused ChevronLeft/ChevronRight/Button imports if lint flags them

<!-- Mail retrofit target (checker blocker fix; same client-side recipe, verified): -->
features/mail/data-table.tsx lines ~167-184: raw Previous/Next Buttons over getPaginationRowModel()
  (~line 58) — CLIENT-SIDE pagination; MailDataTable is rendered TWICE by app/(main)/mail/page.tsx
  (lines ~117 and ~130: inbox + outbox) so one component edit covers both instances.
  Replace the button block with the identical PaginationFooter swap; no service/hook changes.

<!-- Program-family header retrofit targets (checker blocker fix; verified identical pattern ×4): -->
features/program/workshop/workshop-header.tsx lines ~55-64,
features/program/program/program-header.tsx lines ~55-63,
features/program/session/session-header.tsx lines ~55-63,
features/program/participant/participant-header.tsx lines ~54-62:
    raw ArrowLeft/ArrowRight Buttons driving `?page=` URL state via updatePage()
    (router.replace with searchParams); next Button has NO boundary disable.
    Each header additionally calls its resource hook (use-workshops / use-programs /
    use-sessions / use-participant) passing the SAME searchParams-derived params its paired
    all-*.tsx screen derives → shared react-query cache entry, no duplicate fetch — then reads
    the normalized list's .page/.totalPages and renders
    <PaginationFooter page={page} totalPages={list?.totalPages ?? 0} onPageChange={updatePage} />.
    Delete the two Buttons + unused ArrowLeft/ArrowRight imports. Boundary disabling on both
    ends comes free from PaginationFooter internals (D-20).

<!-- List-reader consumers needing the .data → .items read fix (verified 2026-08-25): -->
all-workshops.tsx / all-sessions.tsx / all-programs.tsx / all-participant.tsx:
    destructure { workshops | sessions | programs | participants } and map the array directly
    ← list field now arrives as NormalizedList: read .items (and totalPages/page for any paging UI)
app/(main)/program/[id]/page.tsx: participants list read ← same .items fix
features/leave/admin-page.tsx line ~78: applications.data?.data.map(...)   ← becomes applications.data?.items ?? []
features/leave/page.tsx: applications list read ← .items fix (balance.data is a DETAIL query — untouched)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modernize corrections + dashboard-grid + users-admin + generic DataTable + mail table screens — dates, pageCount, shared footer (D-01/D-02)</name>
  <files>features/attendance-correction/corrections/column.tsx, features/attendance-correction/corrections/data-table.tsx, features/dashboard/attendance-grid/attendance-dashboard.tsx, features/users/data-table.tsx, components/data-table.tsx, features/mail/data-table.tsx</files>
  <action>
    Read all five files plus hooks/use-admin-attendance-correction.ts first (the table's data source).
    column.tsx: swap @/lib/utils/time imports to @/lib/date equivalents (formatDate→formatIstDate, formatTime→formatIstDateTime).
    data-table.tsx: after Plan 02-05 the service returns NormalizedList — update reads (`corrections?.meta?.total` → `corrections?.totalPages ?? 0`; rows come from `.items`); set `pageCount` from that normalized number so manualPagination math is never undefined; replace the raw Previous/Next Button block (~lines 189-206) with `<PaginationFooter page={table.getState().pagination.pageIndex + 1} totalPages={corrections?.totalPages ?? 0} onPageChange={(p) => table.setPageIndex(Math.max(0, p - 1))} />`. Keep manualPagination/manualSorting/manualFiltering true. This closes Pitfall 5 (pageCount:undefined regression class).
    attendance-dashboard.tsx: same recipe — flip its `meta.total` read at line ~181 from the Math.ceil(total/limit) interpretation to the normalized fields coming through its hook, and replace the AttendancePagination block with `<PaginationFooter page={…} totalPages={…} onPageChange={setPage-or-equivalent} />` wired to whatever state drives refetch; align any date renders in it to lib/date formatters (formatDate→formatIstDate, formatTime/transformTime→formatIstDateTime). If its data path turns out NOT to flow through a normalizeList-returning service yet, adopt normalizeList in that service function here (same one-line return change as Plan 02-05) and declare the touched service file in the summary.
    users/data-table.tsx (per D-02 — decision names this screen explicitly): it paginates CLIENT-SIDE via getPaginationRowModel() (~line 56) with raw Previous/Next Buttons (~lines 162-179). Delete that button block and render `<PaginationFooter page={table.getState().pagination.pageIndex + 1} totalPages={table.getPageCount()} onPageChange={(p) => table.setPageIndex(Math.max(0, p - 1))} />` instead. No service/hook changes — TanStack derives pageCount internally from filtered row count. Do NOT skip this screen and do NOT log a STATE.md exemption: the plumbing exists.
    components/data-table.tsx (checker blocker fix — same client-side recipe as users-admin): its raw Previous/Next Button block (~lines 156-179) is the last un-migrated paginated list surface (sole consumer: features/holiday/holiday-table.tsx on app/(main)/calendar). Delete that block and render the identical `<PaginationFooter page={table.getState().pagination.pageIndex + 1} totalPages={table.getPageCount()} onPageChange={(p) => table.setPageIndex(Math.max(0, p - 1))} />`. No service/hook changes; remove ChevronLeft/ChevronRight (and Button, if then unused) imports.
    features/mail/data-table.tsx (checker blocker fix — identical client-side recipe as users-admin): its raw Previous/Next Buttons block (~lines 167-184) sits over getPaginationRowModel() (~line 58). Delete that block and render the identical `<PaginationFooter page={table.getState().pagination.pageIndex + 1} totalPages={table.getPageCount()} onPageChange={(p) => table.setPageIndex(Math.max(0, p - 1))} />`. MailDataTable is rendered TWICE by app/(main)/mail/page.tsx (~lines 117 and 130) so this one edit covers both instances. No service/hook changes. With Tasks 1+2 done, zero raw paginated Previous/Next blocks remain in the repo (the only other "Previous" buttons are day-range navigation and table headers, not pagination).
    attendance-dashboard.tsx raw-parse declaration (checker warning fix): lines ~19 and ~27-28 (addDays, toIsoDay helpers) and ~122-140 (previousRange/nextRange) contain naive `new Date(string)` date-only arithmetic on machine-format YYYY-MM-DD strings. This task does NOT convert them — the IST date-only boundary helpers they need land in Plan 02-07 Task 2, which owns these conversions (pre-declared there). Record them in the summary handoff; do not invent local date logic here (D-01: one home for dates).
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test && pnpm build && ! grep -n "Previous" features/users/data-table.tsx components/data-table.tsx features/mail/data-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - data-table.tsx and attendance-dashboard.tsx contain PaginationFooter with pageCount/totalPages sourced from normalized fields (no ?.meta?. chain, no Math.ceil(total/limit))
    - features/users/data-table.tsx, components/data-table.tsx AND features/mail/data-table.tsx render PaginationFooter driven by table.getPageCount()/pageIndex; no Previous/Next Button block remains in any of the three
    - column.tsx has zero @/lib/utils/time imports
    - Full gates green including production build
  </acceptance_criteria>
  <done>The server-paged screens (corrections TanStack table + dashboard grid) run on normalized meta; the client-paged users admin table, the generic holiday DataTable, and both mail-table instances run on table-derived counts — all through the safe shared footer</done>
</task>

<task type="auto">
  <name>Task 2: Create resource-list factory, collapse six hooks onto it, adopt normalizeList in their services, fix list-reader consumers, retrofit program-family headers (D-21/D-16/D-01/D-02)</name>
  <files>hooks/resource-list-factory.ts, hooks/use-workshops.ts, hooks/use-sessions.ts, hooks/use-programs.ts, hooks/use-participant.ts, hooks/use-leave.ts, hooks/use-admin-attendance-correction.ts, services/workshop.api.ts, services/session.api.ts, services/program.api.ts, services/participant.api.ts, services/leave.api.ts, features/program/workshop/all-workshops.tsx, features/program/session/all-sessions.tsx, features/program/program/all-programs.tsx, features/program/participant/all-participant.tsx, features/program/workshop/workshop-header.tsx, features/program/program/program-header.tsx, features/program/session/session-header.tsx, features/program/participant/participant-header.tsx, app/(main)/program/[id]/page.tsx, features/leave/admin-page.tsx, features/leave/page.tsx</files>
  <action>
    Read all six hooks and the five service files before writing anything. Build `hooks/resource-list-factory.ts` per the <interfaces> config contract: list query keyed [ ...baseKey, keyword, page, limit ] calling the (already-normalized) service; optional detail query keyed [ ...baseKey, id ] with enabled:!!id; each provided mutation wraps its service fn and invalidates the baseKey PREFIX on success; return the flat bag preserving EACH hook's current public field names (workshops/workshop/add/update/del; sessions/session/…; programs/…; participants/…; applications-or-per-current-file; corrections/…) — consumers must not change because of the collapse itself. Rewrite each of the six hook files as a thin createResourceListHook call (keep their exported hook names/signatures identical). use-admin-attendance-correction keeps its hierarchical key by passing baseKey:["attendance","correction"] and mapping its extra params into the list call.
    Service adoption (D-16). Verified return shapes DIFFER per file — match what is actually there (verified 2026-08-25; the single-line `{ data, meta }` shape exists only in attendance-correction, already done in Plan 02-05):
    - workshop.api.ts getWorkshops (~L32), session.api.ts list endpoint, program.api.ts list endpoint, participant.api.ts list endpoint: bare `return res.data` — meta currently DISCARDED. Replace with `return normalizeList<X>(res)`; `res.meta` is already on the ApiResponse envelope so no generic widening is needed.
    - leave.api.ts getLeaveApplications (~L243) and getAllLeaveApplications (~L265): MULTILINE `{ data: res.data, meta: res.meta as MetaResponse }` blocks. Replace each whole block with `return normalizeList<LeaveT>(res)`; drop the MetaResponse import if now unused.
    - attendance-correction.api.ts: already normalized by Plan 02-05 — untouched.
    Detail/mutation endpoints in all five files keep returning plain envelopes (`return res.data` / `return res`). mail.api/notification.api contain only bare detail/detail returns today (verified) — leave them unless execution reveals a list return with meta, in which case adopt normalizeList there too and declare the touched files.
    Consumer read-fixes (declared above; verified list-readers): all-workshops / all-sessions / all-programs / all-participant destructure their list field and map it directly → read `.items` (and `.totalPages`/`.page` if the screen renders paging UI — if it does, ALSO swap any inline pagination buttons for `<PaginationFooter>` per D-02); app/(main)/program/[id]/page.tsx same for participants; features/leave/admin-page.tsx line ~78 `applications.data?.data.map(...)` → `applications.data?.items ?? []`; features/leave/page.tsx same pattern for its applications list (its balance.data detail read is UNTOUCHED).
    Program-family headers (checker blocker fix, per D-02 — the four files are declared in <files>): each header (workshop-header.tsx ~55-64, program-header.tsx ~55-63, session-header.tsx ~55-63, participant-header.tsx ~54-62) currently drives `?page=` URL state via updatePage() with raw ArrowLeft/ArrowRight Buttons and NO boundary disable on next. After the hook collapse (this task), have each header call its resource hook passing the SAME searchParams-derived {keyword, page, limit} params its paired all-*.tsx screen derives (identical query key → shared cache entry, no extra fetch), delete the two raw Buttons + unused ArrowLeft/ArrowRight imports, and render `<PaginationFooter page={page} totalPages={list?.totalPages ?? 0} onPageChange={updatePage} />` where `list` is the hook's normalized list field. This also fixes the missing next-boundary guard (footer disables internally per D-20). Do NOT touch each header's refresh/SearchBox/Add* wiring; note the pre-existing ["worshops"] refresh-key typo in the summary as a STATE.md candidate only if it causes visible misbehavior (D-04 guard).
    D-04 scope guard: unrelated breakage discovered → STATE.md blockers, not fixes here.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Each of the six hook files delegates to createResourceListHook (grep-verifiable) while keeping its exported name
    - No hook file still contains an inline invalidateQueries over a non-prefix or empty key
    - POSITIVE shape gate: `grep -l "normalizeList" services/workshop.api.ts services/session.api.ts services/program.api.ts services/participant.api.ts services/leave.api.ts` lists ALL five files (attendance-correction.api.ts already carries its Plan 02-05 normalizeList return)
    - No list endpoint in those five files still returns bare `res.data` or a hand-built `{ data, meta }` pair — the NormalizedList return type makes any miss a typecheck error
    - The seven declared consumer files contain zero `.data?.data` / bare-array reads of changed list fields (typecheck proves it)
    - All four program-family headers render PaginationFooter fed by their hook's normalized totalPages and contain no ArrowLeft/ArrowRight pagination Buttons (`! grep -n "ArrowLeft\|ArrowRight" features/program/*/[a-z]*-header.tsx` exits clean)
    - typecheck + full suite green (proves consumer reads updated consistently)
  </acceptance_criteria>
  <done>Query/mutation/invalidate wiring exists once; all six domains ride the same tested shape and their screens — including the four program-family headers — read it through the shared footer</done>
</task>

<task type="auto">
  <name>Task 3: Delete lib/utils/time.ts and prove zero stale references (D-18 completion)</name>
  <files>lib/utils/time.ts (DELETE)</files>
  <action>
    First verify the importer surface is empty: grep the repo (excluding node_modules/.next/.planning) for `@/lib/utils/time` and `utils/time` — every hit must be migrated to @/lib/date BEFORE deletion (expected stragglers beyond Plans 02-02/05/06-T1 would surface here; migrate them with the same mapping). Known remaining importer to double-check: none outside already-covered sets, but trust the grep, not the plan. Then delete lib/utils/time.ts. Run the full gate chain; `pnpm build` is the real proof no stale reference survives (Turbopack resolves every import). The users-admin D-02 footer swap was implemented in Task 1 (client-side plumbing verified present) — nothing left for it here.
  </action>
  <verify>
    <automated>grep -r "utils/time" --include="*.ts" --include="*.tsx" app components features hooks services lib tests | grep -v node_modules; test $? -eq 1 && pnpm lint && pnpm typecheck && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - lib/utils/time.ts no longer exists in the repository
    - Repo-wide grep for "@/lib/utils/time" (source dirs only) returns zero matches
    - pnpm build succeeds standalone output without the module
    - Full suite green — phase retrofit complete
  </acceptance_criteria>
  <done>D-18 finished: exactly one home for dates; the deleted module cannot be imported even accidentally</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service responses → generic factory wiring | A refactor touching six domains at once can silently widen what flows into cache consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-15 | Tampering (refactor changes query-key semantics → cross-resource invalidation misses) | resource-list factory | mitigate | baseKey-prefix invalidation preserved per hook; keys stay hierarchical arrays per CONVENTIONS; typecheck+existing suite catch consumer drift |
| T-2-16 | Information Disclosure (stale list data after mutation due to wrong prefix) | factory invalidation | mitigate | Prefix rule enforced in ONE place now (the factory); review criterion written into acceptance criteria |
| T-2-17 | DoS (UX) — broken import graph after deletion | lib/utils/time removal | mitigate | Deletion gated behind repo-wide zero-grep AND pnpm build proof |
| T-2-SC | Tampering | npm installs | mitigate | No installs in this plan |
</threat_model>

<verification>
- Full gates: `pnpm lint && pnpm typecheck && pnpm test && pnpm build`
- Repo greps: zero utils/time references; `normalizeList` present in ALL six paginated services (workshop, session, program, participant, leave, attendance-correction)
- Every paginated surface renders PaginationFooter — users admin, corrections, dashboard grid, generic holiday DataTable, mail tables (both instances), AND the four program-family headers. Repo-wide grep `grep -rn "table.previousPage()\|updatePage(page - 1)\|getCanPreviousPage" --include="*.tsx" app components features` returns matches ONLY inside components/pagination-footer.tsx internals (raw paginated prev/next is extinct; day-range nav buttons and table-header "Previous" labels are non-pagination, out of D-02 scope)
- Six hooks visibly delegate to the factory with unchanged exported names; declared consumers read `.items`
- Phase-level: success criteria #1/#2/#4/#5 demonstrable from merged waves — EXCEPT success criterion #1's final raw-pattern proof, which is the explicit exit gate of Plan 02-07 (the surfaces that never imported utils/time are swept there, including the raw-parse sites pre-declared to 02-07 T2)
</verification>

<success_criteria>
The consolidation is complete — six duplicated hooks collapsed onto one factory with all their services, screens, and the four program-family headers on normalized lists through the shared footer (D-02/D-16/D-21) — every raw paginated Previous/Next block is extinct repo-wide, and lib/utils/time.ts is deleted with zero stale references, closing D-18. D-01's final repo-wide raw-date gate lands in Plan 02-07.
</success_criteria>

<output>
Create `.planning/phases/02-shared-infrastructure-session-reliability/02-06-SUMMARY.md` when done
</output>
