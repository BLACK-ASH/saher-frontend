---
phase: 02-shared-infrastructure-session-reliability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/permissions.ts
  - lib/permissions.test.ts
  - hooks/use-me.ts
  - components/role-guard.tsx
  - components/sidebar/nav-list.tsx
  - components/sidebar/nav-list.test.tsx
  - app/(main)/(admin)/layout.tsx
  - app/(main)/(manager)/layout.tsx
autonomous: false
requirements: [FNDT-06]
must_haves:
  truths:
    - "can(role, action, resource) answers permission questions exactly as the backend's authorize() guards would"
    - "Managers see the Manager sidebar group (today's operator-precedence bug hides it from them entirely)"
    - "Route guarding and nav filtering both derive from can() — no scattered role === string comparisons remain in gating code (D-14)"
    - "The four backend roles (intern/user/manager/admin) are all first-class; the matrix mirrors verified quirks, not a hierarchy"
  artifacts:
    - path: "lib/permissions.ts"
      provides: "UserRole union + ROLE_PERMISSIONS matrix mirrored verbatim + can()"
      min_lines: 40
    - path: "lib/permissions.test.ts"
      provides: "truth-table pinning verified quirks (admin no bank-write, user notice-write, intern minimal)"
      contains: "can("
    - path: "components/sidebar/nav-list.test.tsx"
      provides: "render proof manager sees Manager group; user sees neither elevated group"
  key_links:
    - from: "lib/permissions.ts"
      to: "../saher-backend/src/permission/role-permission.ts"
      via: "verbatim set mirror using ${resource}:${action} strings"
      pattern: "ROLE_PERMISSIONS"
    - from: "components/role-guard.tsx"
      to: "lib/permissions.ts"
      via: "allow predicate receives UserRole, calls can()"
      pattern: "@/lib/permissions"
---

<objective>
Build `lib/permissions.ts` — the hardcoded role→permissions matrix mirroring the backend's actual `authorize()` guards (D-13) — plus `can()`, then unify ALL frontend gating onto it (D-14): RoleGuard, sidebar filtering (fixing the known manager-nav bug), with the matrix vocabulary pinned by the D-15 live probe.

Purpose: FNDT-06. Research verified the backend has FOUR roles with NO hierarchy (admin lacks bank-write that managers have; users hold notice-write that admins lack). Any role-comparison heuristic gates wrongly; only a verbatim mirror is correct.
Output: tested permissions module, widened role union, can()-based RoleGuard and nav-list, manager-nav render test, D-15 probe checkpoint.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-infrastructure-session-reliability/02-CONTEXT.md
@.planning/phases/02-shared-infrastructure-session-reliability/02-PATTERNS.md

<interfaces>
<!-- lib/permissions.ts public surface (signature detail is delegated-discretion; this pure
     form was chosen because it is testable without React context and callers already hold
     user.role). Plans 05+ and future modules consume these names. -->

export type UserRole = "intern" | "user" | "manager" | "admin";
export type PermissionAction = "read" | "write" | "update" | "delete";
export type PermissionResource =
  | "account" | "user" | "bank" | "attendance" | "attendance-correction"
  | "holiday" | "notification" | "mail" | "notice" | "leave" | "leaveType"
  | "preReimbursement" | "postReimbursement" | "event" | "payroll";

export const ROLE_PERMISSIONS: Record<UserRole, ReadonlySet<string>>;
// keys are `${resource}:${action}` — mirror ../saher-backend/src/permission/role-permission.ts VERBATIM

export const can = (role: UserRole, action: PermissionAction, resource: PermissionResource): boolean;
// single lookup: ROLE_PERMISSIONS[role].has(`${resource}:${action}`)

<!-- Current state being modified (verified 2026-08-25): -->
From hooks/use-me.ts line 12: readonly role: "user" | "manager" | "admin";   ← gains "intern"

From components/role-guard.tsx lines 16-26: allowedRoles.includes(user.role) check → becomes predicate

From components/sidebar/nav-list.tsx lines 158-159 (THE BUG):
    {user?.role === "manager" ||
      (user?.role === "admin" && ( ...Manager group... ))   // admins get it, managers get NOTHING

Backend ground truth: ../saher-backend/src/permission/role-permission.ts (READ IT during Task 1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror the backend permission matrix into lib/permissions.ts with quirk-pinning tests (per D-13)</name>
  <files>lib/permissions.ts, lib/permissions.test.ts</files>
  <behavior>
    - can("admin","write","bank") === false AND can("manager","write","bank") === true (no-inheritance counterexample #1)
    - can("user","write","notice") === true AND can("admin","write","notice") === false (counterexample #2)
    - can("manager","write","notification") === true AND can("admin","write","notification") === false (counterexample #3)
    - can("intern","read","event") === true and intern has NO other permission (spot-check read user, write attendance → false)
    - can("admin","read","payroll") === true AND can("manager","read","payroll") === false (payroll admin-only)
    - can("admin","read","account") === true AND can("admin","delete","account") === true (positive controls)
    - Every role×action×resource combination answers without throwing (exhaustive smoke over the full vocabulary grid)
  </behavior>
  <action>
    Read `../saher-backend/src/permission/role-permission.ts` and `../saher-backend/src/permission/permission.ts` FIRST — they are ground truth (routes-over-docs directive). Build ROLE_PERMISSIONS as Record<UserRole, ReadonlySet<string>> copying each role's set EXACTLY (every createPermission(action, resource) becomes the string `${resource}:${action}`). Do NOT derive or normalize between roles — research found hierarchy assumptions fail on at least three verified counterexamples. Implement `can` as the one-line Set lookup per <interfaces>. Write tests FIRST asserting the behavior cases above (these pin the quirks so a future careless "simplification" fails CI), then implement. Keep header comment noting the mirror source file path and the D-15 probe requirement.
  </action>
  <verify>
    <automated>pnpm test lib/permissions.test.ts && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - lib/permissions.test.ts contains assertions for all three no-inheritance counterexamples plus intern-minimality
    - Matrix sets match backend source (spot-audit: count entries per role equals createPermission call count in role-permission.ts: admin 44, manager 38, user 14, intern 1 — verify exact counts when reading the file)
    - `pnpm test lib/permissions.test.ts` exits 0
  </acceptance_criteria>
  <done>can() answers every vocabulary question identically to the backend's authorize() data</done>
</task>

<task type="auto">
  <name>Task 2: Unify gating onto can() — widen role union, rewrite RoleGuard + nav-list predicates, prove the manager-nav fix (per D-14/D-15)</name>
  <files>hooks/use-me.ts, components/role-guard.tsx, components/sidebar/nav-list.tsx, components/sidebar/nav-list.test.tsx</files>
  <action>
    Read all three source files first.
    1. hooks/use-me.ts: import UserRole from "@/lib/permissions"; change UserT.role (and the two nested deleteBy/bannedBy role fields) to `readonly role: UserRole`.
    2. components/role-guard.tsx: change Props to `{ children: React.ReactNode; allow: (role: UserRole) => boolean }`; the effect redirects to /forbidden when `user && !allow(user.role)` (keep the !user → /login branch). Update EVERY RoleGuard call site — verified there are exactly TWO, both layout files: app/(main)/(admin)/layout.tsx (guards the admin shell incl. register/page.tsx) and app/(main)/(manager)/layout.tsx (guards dashboard, users, users/[id], leave-management, leave, attendance-correction). BEFORE choosing each layout's predicate, open the corresponding backend route files (../saher-backend/src — grep each guarded page's primary resource for authorize() usage; routes are ground truth). Conservative starting mapping if ambiguous — admin layout → can(r,"write","user"); manager layout → can(r,"read","user") || can(r,"read","leave") || can(r,"read","attendance-correction"). Log any mismatch found to STATE.md blockers per D-04 — do not change backend assumptions silently.
    3. components/sidebar/nav-list.tsx: annotate each entry of managerRoutes/adminRoutes with its permission pair (`perm: { action: "read", resource: "user" }` etc., same mapping as RoleGuard); REPLACE lines 158-179's broken conditional with an explicit visibility computation — show the Manager group iff `managerRoutes.some(r => r.perm && can(user.role, r.perm.action, r.perm.resource))`, same pattern for Admin group. This rewrites (not preserves) the boolean structure so the precedence bug dies. userRoutes stay unfiltered (all roles hold their permissions).
    4. Add components/sidebar/nav-list.test.tsx: mock use-me at module boundary (vi.mock("@/hooks/use-me")) returning isLoading:false plus role variants; render via tests/render-with-providers.tsx; assert: role="manager" → "Manager" group label PRESENT, "Admin" absent; role="user" → both absent; role="admin" → both present; role="intern" → neither.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test components/sidebar/nav-list.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep finds no remaining `=== "manager"` / `=== "admin"` role-string comparisons inside components/role-guard.tsx and components/sidebar/nav-list.tsx gating logic
    - nav-list.test.tsx asserts manager-sees-Manager-group (the previously broken case)
    - typecheck green across all updated RoleGuard call sites (proves Props migration complete)
  </acceptance_criteria>
  <done>All route + nav gating flows through can(); managers regain their navigation section; intern is representable end-to-end</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-15 live probe — pin manager role strings against GET /api/auth/me</name>
  <action>
    The permissions matrix is keyed on role strings. Static evidence (backend user.model + role-permission source) says roles are exactly "intern", "user", "manager", "admin", but D-15 mandates ONE live confirmation because a misspelled matrix key gates everything wrongly. This checkpoint asks the user to either observe the live response or accept the static evidence (research Assumption A1).
  </action>
  <what-built>The permissions matrix keyed on role strings, with all gating unified onto can().</what-built>
  <how-to-verify>
    Option A (preferred): start ../saher-backend locally (its dev command, port 4000), log into the frontend dev server, open DevTools → Network → find the /api/auth/me response, and confirm the role field spelling for your account; ideally repeat once per available role account.
    Option B: if starting the backend is not practical right now, approve proceeding on the static evidence — the executor will record assumption A1 (research Assumptions Log) in the summary and STATE.md.
    Expected strings if static evidence holds: "intern" | "user" | "manager" | "admin".
  </how-to-verify>
  <resume-signal>Type "approved" (static evidence accepted) or paste the observed role string(s) if they differ — the matrix keys will be corrected before wave merge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI gating → real authorization | can() gates pixels only; the server's authorize() middleware is the actual boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06 | Elevation of Privilege (client gating treated as security) | can()/RoleGuard/nav-list | mitigate | Header comment on lib/permissions.ts states UX-only contract; matrix MIRRORS server guards so UI never invites an action the server would reject; every endpoint remains server-enforced (verified in backend routes) |
| T-2-07 | Tampering (matrix drift from backend) | ROLE_PERMISSIONS | mitigate | Quirk-pinning truth-table tests fail CI on any casual divergence; D-15 probe pins wire spelling |
| T-2-08 | Repudiation (silent guard mismatches) | RoleGuard predicate selection | mitigate | Directive to log discovered route/guard mismatches to STATE.md blockers per D-04 instead of guessing |
| T-2-SC | Tampering | npm installs | mitigate | No installs in this plan |
</threat_model>

<verification>
- `pnpm test lib/permissions.test.ts` — full truth table incl. verified backend quirks
- `pnpm test components/sidebar/nav-list.test.tsx` — manager-nav regression test green
- Manual: log in as manager → Manager group visible (was invisible before this plan)
- Checkpoint records either observed role strings or explicit A1 acceptance
</verification>

<success_criteria>
UI affordances gate consistently through can(action, resource) derived from a verbatim backend mirror, with the manager-nav bug fixed and proven by a render test, and role-string confidence raised from MEDIUM to pinned (probe or documented A1).
</success_criteria>

<output>
Create `.planning/phases/02-shared-infrastructure-session-reliability/02-03-SUMMARY.md` when done
</output>
