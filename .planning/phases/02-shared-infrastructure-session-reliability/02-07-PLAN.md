---
phase: 02-shared-infrastructure-session-reliability
plan: 07
type: execute
wave: 4
depends_on: ["02-06"]
files_modified:
  - app/(main)/mail/page.tsx
  - app/(main)/program/sessions/[id]/page.tsx
  - app/(main)/program/sessions/review/[id]/page.tsx
  - features/leave/leave-table.tsx
  - features/leave/admin-page.tsx
  - features/leave/leave-details-dialog.tsx
  - features/holiday/holiday-columns.tsx
  - features/holiday/holiday-form-dialog.tsx
  - features/profile/profile.tsx
  - features/program/session/session-editor.tsx
  - features/register/basic-details.tsx
  - features/register/employee-details.tsx
  - features/dashboard/attendance-grid/attendance-hover-card.tsx
  - features/dashboard/attendance-grid/attendance-table.tsx
  - components/app-footer.tsx
  - lib/date.ts
  - lib/date.test.ts
autonomous: true
requirements: [FNDT-02]
user_setup: []
must_haves:
  truths:
    - "Every remaining screen renders dates identically regardless of browser timezone — mail, program sessions, leave, holiday, profile, dashboard grid included (D-01 complete, success criterion #1)"
    - "No display site invents a format outside the two canonical D-09 formats"
    - "Session-editor start/end times reach the API as +05:30-flavored ISO built from IST wall-clock input (D-10)"
    - "Picker-backed forms store machine-format YYYY-MM-DD and convert at boundaries only, through lib/date (D-10/D-12)"
    - "Repo-wide raw-pattern gate green: zero browser-TZ toLocale*String rendering and zero naive new Date(argument) parsing outside lib/date and tests"
  artifacts:
    - path: "lib/date.ts"
      provides: "+ two date-only boundary helpers: dateToIstDateOnly(d) → YYYY-MM-DD (IST), istDateOnlyToDate(v) → IST-midnight Date"
    - path: "lib/date.test.ts"
      provides: "boundary cases for the two new helpers appended to the FNDT-02 suite"
      contains: "+05:30"
  key_links:
    - from: "(the 15 swept surfaces)"
      to: "lib/date.ts"
      via: "imports of formatIstDate/formatIstDateTime/dateToIstDateOnly/istDateOnlyToDate replace raw toLocale*/new Date usage"
      pattern: "@/lib/date"
    - from: "(repo-wide)"
      to: "raw browser-TZ date patterns"
      via: "final phase gate greps return zero matches outside lib/date and test files"
      pattern: "toLocale(Date|Time)?String\\("
---

<objective>
Complete D-01's full retrofit: sweep the 15 surfaces that never imported `lib/utils/time` (raw `toLocale*String` / naive `new Date(...)` usage — verified 2026-08-25 at specific lines) onto `lib/date`, add the two missing date-only boundary helpers the picker-backed forms need, and run the phase's FINAL raw-pattern gate that Plans 02-02..06 could not prove on their own.

Purpose: the checker blocker — 02-06's deletion gate grepped only `utils/time`, so these 15 files would survive the phase with browser-TZ behavior intact, making success criterion #1 false app-wide. This plan closes that hole and owns the last word on the phase gate.
Output: every date render/parse app-wide flows through lib/date; broadened repo-wide gate green.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-infrastructure-session-reliability/02-CONTEXT.md
@.planning/phases/02-shared-infrastructure-session-reliability/02-PATTERNS.md
@.planning/phases/02-shared-infrastructure-session-reliability/02-02-SUMMARY.md

<interfaces>
<!-- Consumed from Plan 02-02 (merged): -->
From lib/date.ts: formatIstDate(iso?) → "25 Aug 2026"; formatIstDateTime(iso?) → "25 Aug 2026, 12:00 AM";
  combineDateAndTimeToIso(date, time) → `${date}T${time}:00+05:30`; istInputToIso / isoToIstInput;
  getMonthYear. Null/invalid inputs render "--", never throw.

<!-- Helpers THIS plan adds to lib/date.ts (additive — do not rename existing exports): -->
export const dateToIstDateOnly = (d: Date): string;
// → "YYYY-MM-DD" via en-CA formatToParts in Asia/Kolkata (IST calendar day of the instant)
export const istDateOnlyToDate = (value: string): Date;
// → new Date(`${value}T00:00:00+05:30`), NaN-rejected (throw-free callers validate upstream);
//   gives react-day-picker a Date anchored to the IST day it represents

<!-- Verified raw-date inventory this plan sweeps (line refs from 2026-08-25 read): -->
app/(main)/mail/page.tsx:470            new Date(selectedMail?.createdAt).toLocaleString()
app/(main)/program/sessions/[id]/page.tsx:115,135,152   toLocaleDateString(undefined,{…}) / toLocaleTimeString([],{…}) ×2
app/(main)/program/sessions/review/[id]/page.tsx:115    new Date(data.date).toLocaleDateString()
features/leave/leave-table.tsx:94,100   new Date(leave.startDate|endDate).toLocaleDateString()
features/leave/admin-page.tsx:113,117   same pattern
features/leave/leave-details-dialog.tsx:77-78  same pattern
features/holiday/holiday-columns.tsx:65 date-fns format(new Date(row.original.date),"dd MMM yyyy")
features/holiday/holiday-form-dialog.tsx:80,202-204  display map + Calendar selected/onChange locale-string round-trip
features/profile/profile.tsx:250        helper returning new Date(date).toLocaleDateString()
features/program/session/session-editor.tsx:74-75  new Date(`${values.date}T${values.startTime|endTime}`) — LOCAL-time parse bug sent to API
features/register/basic-details.tsx:213,223-224    new Date(field.value).toDateString() display; Calendar defaults
features/register/employee-details.tsx:217,227-228 same pattern
features/dashboard/attendance-grid/attendance-hover-card.tsx:31,38  toLocaleTimeString/toLocaleDateString("en-IN",{…})
features/dashboard/attendance-grid/attendance-table.tsx:57,63       toLocaleDateString("en-IN",{…})
components/app-footer.tsx:8             new Date().getFullYear()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep display-only surfaces onto lib/date formatters (D-01/D-09/D-11)</name>
  <files>app/(main)/mail/page.tsx, app/(main)/program/sessions/[id]/page.tsx, app/(main)/program/sessions/review/[id]/page.tsx, features/leave/leave-table.tsx, features/leave/admin-page.tsx, features/leave/leave-details-dialog.tsx, features/holiday/holiday-columns.tsx, features/profile/profile.tsx, features/dashboard/attendance-grid/attendance-hover-card.tsx, features/dashboard/attendance-grid/attendance-table.tsx</files>
  <action>
    Read each file before editing. Uniform swap, preserving each site's date-only vs datetime character:
    - `.toLocaleDateString()` / date-fns `format(new Date(x),"dd MMM yyyy")` → `formatIstDate(x)` (drop the date-fns import in holiday-columns if now unused)
    - `.toLocaleString()` / `.toLocaleTimeString(...)` → `formatIstDateTime(x)` (mail createdAt and session start/end times become absolute datetimes per D-11)
    - profile.tsx:250's private helper collapses to a thin formatIstDate call (or inline it at its call sites if the helper has one caller)
    - hover-card + grid table: match each call's existing shape (time-only cells → formatIstDateTime, date-only cells → formatIstDate); where the old code passed options objects ({weekday:, day:, month:, year:...}), fold the essential info into the canonical D-09 formats — screens stop inventing formats (D-09); do not recreate multi-part custom layouts
    - leave/admin-page.tsx was already edited by Plan 02-06 Task 2 (`.items` reads) — layer these formatter swaps on top of its current state
    D-04 scope guard: unrelated breakage discovered → STATE.md blockers, not fixes here.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm build && ! grep -rEn "toLocale(Date|Time)?String\(" --include="*.tsx" --include="*.ts" app/\(main\)/mail features/leave features/holiday features/profile features/dashboard app/\(main\)/program</automated>
  </verify>
  <acceptance_criteria>
    - Zero toLocale*String( matches remain in the ten touched files (gate above exits 0)
    - holiday-columns.tsx no longer imports date-fns format (unless another site genuinely uses it)
    - Full gates green including production build
  </acceptance_criteria>
  <done>Mail, program session pages, leave surfaces, holiday columns, profile, and the dashboard grid render canonical IST formats independent of browser timezone</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Input round-trips + picker boundaries — add date-only helpers, fix session-editor parse bug, picker forms, footer year (D-10/D-12)</name>
  <files>lib/date.ts, lib/date.test.ts, features/program/session/session-editor.tsx, features/holiday/holiday-form-dialog.tsx, features/register/basic-details.tsx, features/register/employee-details.tsx, components/app-footer.tsx</files>
  <behavior>
    - dateToIstDateOnly(new Date("2026-08-24T18:30:00Z")) === "2026-08-25" (instant falls on next IST day)
    - dateToIstDateOnly(new Date("2026-08-24T03:30:00Z")) === "2026-08-24"
    - istDateOnlyToDate("2026-08-24").getTime() === new Date("2026-08-24T00:00:00+05:30").getTime()
    - Round-trip: istDateOnlyToDate(dateToIstDateOnly(d)) represents the same IST calendar day (compare via dateToIstDateOnly equality, not instant equality)
  </behavior>
  <action>
    Write the four behavior cases into lib/date.test.ts FIRST (append to the existing suite), run red, then add the two exports to lib/date.ts per <interfaces> mechanics (en-CA parts assembly for dateToIstDateOnly; fixed-offset template parse with NaN guard for istDateOnlyToDate).
    Then migrate the input surfaces (read each first):
    - session-editor.tsx lines 74-75: replace `new Date(`${values.date}T${values.startTime}`)` with `combineDateAndTimeToIso(values.date, values.startTime)` (same for endTime). This kills a REAL live bug: the current local-time parse sends a different instant than intended on any non-IST device; the +05:30-flavored output matches D-10's wire contract.
    - holiday-form-dialog.tsx: line ~80 display map → formatIstDate(holiday.date); the Calendar round-trip (~lines 202-204) stops storing locale strings — form field holds machine "YYYY-MM-DD": onChange={(d) => field.onChange(d ? dateToIstDateOnly(d) : undefined)}, selected={field.value ? istDateOnlyToDate(field.value) : undefined}; submit converts via dateInputToIso(field.value) unless the surrounding payload shows the API expects literal date-only, in which case send the "YYYY-MM-DD" machine string (both parse to the correct IST day backend-side; log the choice in the summary).
    - basic-details.tsx / employee-details.tsx: same machine-format storage pattern for their date fields; line ~213/217 display `new Date(field.value).toDateString()` → formatIstDate(field.value); pass timeZone="Asia/Kolkata" through to the shadcn Calendar (spreads to DayPicker v9's supported prop — D-12); `defaultMonth={new Date()}` may stay (argument-less now-instant picking today's month; TZ-shift within a day cannot change the month except across month boundaries — accept, note in summary).
    - app-footer.tsx:8: `new Date().getFullYear()` → `{dateToIstDateOnly(new Date()).slice(0, 4)}` — the copyright year becomes the IST year, closing the last browser-TZ-dependent render.
  </action>
  <verify>
    <automated>pnpm test lib/date.test.ts && pnpm lint && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - New helper cases green in lib/date.test.ts; existing FNDT-02 cases unbroken
    - session-editor submit path contains combineDateAndTimeToIso and no template-literal new Date parse
    - All three picker dialogs store YYYY-MM-DD machine strings (no toLocaleDateString in any onChange)
    - Full gates green including production build
  </acceptance_criteria>
  <done>Every form input round-trips through lib/date to +05:30-flavored or machine date strings; pickers are IST-configured; even the footer year is IST</done>
</task>

<task type="auto">
  <name>Task 3: Run the phase-final raw-pattern gate — prove success criterion #1 app-wide (D-01 exit)</name>
  <files></files>
  <action>
    This is the phase's last-word verification task (checker-directed: Plans 02-02..06 gated only utils/time imports; this gate covers RAW patterns). Run BOTH greps over source dirs (app components features hooks services lib), excluding nothing else:
    1. `grep -rEn "toLocale(Date|Time)?String\(" --include="*.ts" --include="*.tsx" app components features hooks services lib` → MUST be empty (pattern cannot match toLocaleUpperCase/toLocaleLowerCase — they lack "String(").
    2. `grep -rEn "new Date\(.+" --include="*.ts" --include="*.tsx" app components features hooks services lib | grep -v "^lib/date" | grep -v "\.test\."` → MUST be empty. Argument-less `new Date()` is permitted only as picker defaults (register dialogs' defaultMonth — accepted in Task 2) — if the grep surfaces OTHER argument-less sites, evaluate: current-instant feeds INTO lib/date formatters are fine; anything rendered raw gets swept with the Task 1 mapping.
    Any hit found by either grep: migrate it onto lib/date using the Task 1/2 mappings (declare touched files in the summary), then re-run. Finish with the full chain: pnpm lint && pnpm typecheck && pnpm test && pnpm build. If a hit reveals breakage beyond date utilities (D-04 class), log to STATE.md blockers instead of fixing.
  </action>
  <verify>
    <automated>! grep -rEn "toLocale(Date|Time)?String\(" --include="*.ts" --include="*.tsx" app components features hooks services lib && ! grep -rEn "new Date\(.+" --include="*.ts" --include="*.tsx" app components features hooks services lib | grep -v "^lib/date" | grep -v "\.test\." && pnpm lint && pnpm typecheck && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Both gate greps exit clean (zero raw browser-TZ rendering/parsing outside lib/date and tests)
    - Full suite + production build green — phase retrofit demonstrably complete
    - Summary records the final grep outputs (or "clean") as evidence for verify-work
  </acceptance_criteria>
  <done>D-01 fully delivered: identical date rendering regardless of browser timezone is provable repo-wide, not just on utils/time's former importers</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user form input → API payloads | session-editor/holiday/register date inputs cross into ISO payloads; the old local-time parse silently shifted instants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-18 | Tampering (silently wrong instant sent to API) | session-editor date assembly | mitigate | combineDateAndTimeToIso builds fixed-offset +05:30 strings (spec-defined parsing); unit-tested round-trip guards the boundary |
| T-2-19 | Tampering (malformed date-only strings reaching pickers/APIs) | istDateOnlyToDate/dateInputToIso | mitigate | Fixed-offset parse rejects garbage as NaN; zod form schemas validate shape upstream |
| T-2-SC | Tampering | npm installs | mitigate | No installs in this plan (zero-dependency phase per RESEARCH audit) |
</threat_model>

<verification>
- Both raw-pattern greps clean repo-wide (source dirs, tests exempt)
- `pnpm test lib/date.test.ts` — new date-only helper boundary cases green alongside FNDT-02 suite
- Full gates: `pnpm lint && pnpm typecheck && pnpm test && pnpm build`
- Success criterion #1 now provable on ALL surfaces, not just former utils/time importers
</verification>

<success_criteria>
Every date render and parse in the app flows through lib/date — including the 15 surfaces no earlier plan touched — proven by the broadened repo-wide gate, completing D-01/FNDT-02 for the phase.
</success_criteria>

<output>
Create `.planning/phases/02-shared-infrastructure-session-reliability/02-07-SUMMARY.md` when done
</output>
