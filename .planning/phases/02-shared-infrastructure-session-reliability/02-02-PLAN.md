---
phase: 02-shared-infrastructure-session-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/date.ts
  - lib/date.test.ts
  - features/attendance/attendance-status.tsx
  - features/attendance/attendance-chart.tsx
  - features/attendance/attendance-comparision.tsx
  - features/attendance/attendance-correction.tsx
  - features/attendance-correction/attendance-correction-view.tsx
  - features/calendar/add-event-dialog.tsx
  - features/calendar/calendar.tsx
  - features/attendance/attendance-status.test.tsx
autonomous: true
requirements: [FNDT-02]
must_haves:
  truths:
    - "Dates render identically regardless of browser timezone (IST display, success criterion #1)"
    - "Form datetime inputs round-trip to +05:30-anchored UTC ISO strings before hitting the API (D-10)"
    - "The calendar page renders event times as IST digits, not browser-local digits (D-12)"
    - "No display site invents a format outside the two canonical ones (D-09); no relative times anywhere (D-11)"
  artifacts:
    - path: "lib/date.ts"
      provides: "formatIstDate / formatIstDateTime / input+ISO converters / folded duration + month helpers"
      min_lines: 60
    - path: "lib/date.test.ts"
      provides: "day-boundary pairs, null/invalid fallbacks, round-trips"
      contains: "18:30:00Z"
    - path: "features/attendance/attendance-status.test.tsx"
      provides: "render proof an attendance cell shows IST string independent of TZ (D-03 default)"
  key_links:
    - from: "features/calendar/calendar.tsx"
      to: "lib/date.ts"
      via: "FullCalendar timeZone='Asia/Kolkata' + events pre-shifted through isoToIstWallClock"
      pattern: "Asia/Kolkata"
    - from: "features/attendance/* and features/attendance-correction/*"
      to: "lib/date.ts"
      via: "imports of formatIstDate/formatIstDateTime replace @/lib/utils/time imports"
      pattern: "@/lib/date"
---

<objective>
Create the IST date module (`lib/date.ts`, per D-09/D-10/D-18) with exhaustive day-boundary tests, then migrate this plan's consumer set — all attendance display sites, both correction surfaces, and the calendar stack (add-event-dialog, FullCalendar config).

Purpose: FNDT-02. Current `lib/utils/time.ts` uses `getHours()`/browser-TZ `toLocale*` — wrong on any non-IST device, and `new Date("YYYY-MM-DD")` date-only parsing lands on the wrong day. This plan retires those behaviors for its consumers; Plan 02-06 migrates the last importers and deletes the old file.
Output: tested `lib/date.ts`; migrated attendance/correction/calendar consumers; render test proving TZ-independent output.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-infrastructure-session-reliability/02-CONTEXT.md
@.planning/phases/02-shared-infrastructure-session-reliability/02-PATTERNS.md

<interfaces>
<!-- lib/date.ts public surface — pinned here; Plans 02-05/02-06 import these names. -->

export const formatIstDate(iso?: string | null | Date): string;
// D-09 canonical date-only: "25 Aug 2026"; null/undefined/invalid → "--"
export const formatIstDateTime(iso?: string | null | Date): string;
// D-09 canonical datetime: "25 Aug 2026, 12:00 AM" (meridiem UPPERCASE per token A);
// replaces transformTime AND formatTime call sites — absolute datetimes only (D-11)
export const isoToIstInput(iso: string): string;
// → datetime-local value "2026-08-24T09:00" (replaces toLocalInput)
export const isoToIstWallClock(iso: string): string;
// → IST digits WITHOUT offset suffix "2026-08-24T14:30:00" — feeds FullCalendar
//   start/end so it renders literal IST digits (RESEARCH Pattern 4)
export const istInputToIso(value: string): string;
// datetime-local value → UTC ISO ("2026-08-24T09:00" → "2026-08-24T03:30:00.000Z")
export const dateInputToIso(value: string): string;
// date-only value → UTC ISO anchored at IST midnight ("2026-08-24" → "2026-08-23T18:30:00.000Z")
export const combineDateAndTimeToIso(date: string, time: string): string;
// replaces timeToDateString: `${date}T${time}:00+05:30` parsed → .toISOString()
export const formatHours(hours: number): string;
// semantics unchanged from current file ("8h 30m") — TZ-independent, folded in per D-18
export const calculateWorkHours(checkIn, checkOut): string;
// semantics unchanged (diff math) — folded in per D-18
export const getMonthYear(date: string | Date): string;
// "August 2026", now zoned via Intl Asia/Kolkata instead of browser TZ

Implementation mechanics: RESEARCH Pattern 1 (Intl.DateTimeFormat timeZone:"Asia/Kolkata",
formatToParts assembly). India has no DST → named zone ≡ fixed +05:30.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build lib/date.ts with day-boundary unit tests (FNDT-02 core)</name>
  <files>lib/date.ts, lib/date.test.ts</files>
  <behavior>
    - formatIstDate("2026-08-24T18:29:00Z") === "24 Aug 2026" (before IST midnight boundary)
    - formatIstDate("2026-08-24T18:30:00Z") === "25 Aug 2026" (AT IST midnight — the off-by-one killer)
    - formatIstDateTime("2026-08-24T18:30:00Z") === "25 Aug 2026, 12:00 AM" (midnight IST)
    - formatIstDateTime("2026-08-24T09:15:00Z") === "24 Aug 2026, 02:45 PM" (meridiem uppercase)
    - formatIstDate(null) / formatIstDate(undefined) / formatIstDate("garbage") / formatIstDate("") each return "--"
    - dateInputToIso("2026-08-24") === "2026-08-23T18:30:00.000Z" (IST midnight anchor)
    - istInputToIso("2026-08-24T09:00") === "2026-08-24T03:30:00.000Z"
    - isoToIstInput("2026-08-24T03:30:00.000Z") === "2026-08-24T09:00"
    - Round-trip: for sample instants across several hours, istInputToIso(isoToIstInput(x)) === x and combineDateAndTimeToIso round-trips against formatIstDateTime
    - calculateWorkHours("2026-08-24T04:00:00Z","2026-08-24T12:30:00Z") === "8h 30m"; missing side → "--"
    - formatHours(8.5) === "8h 30m"
  </behavior>
  <action>
    Write `lib/date.test.ts` FIRST with exactly the cases above (they are the VALIDATION.md FNDT-02 contract), run it red, then implement `lib/date.ts`. Follow RESEARCH Pattern 1 mechanics: one shared `Intl.DateTimeFormat("en-IN", { timeZone: "Asia/Kolkata", ... })` per output shape assembled via formatToParts so digit order/padding is exact (do NOT rely on Intl's default joined string — en-IN inserts different separators than D-09 specifies). Parse fixed-offset inputs natively (`new Date("...+05:30")` is spec-defined); reject NaN via Number.isNaN(d.getTime()). Tests must NOT set process.env.TZ — zone-explicit code is the point; a stray machine-TZ dependency is a bug, not a fixture problem. No new dependencies (date-fns fallback exists but should not be needed).
  </action>
  <verify>
    <automated>pnpm test lib/date.test.ts && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - Both boundary instants 18:29Z/18:30Z asserted with different day outputs
    - All null/invalid inputs return "--" (never throw, never "Invalid Date")
    - Round-trip assertions pass; `pnpm test lib/date.test.ts` exits 0
  </acceptance_criteria>
  <done>The canonical IST conversions exist, are pure, TZ-independent, and proven at the day boundary</done>
</task>

<task type="auto">
  <name>Task 2: Migrate display call sites in attendance + correction-view onto lib/date (D-01/D-09/D-11)</name>
  <files>features/attendance/attendance-status.tsx, features/attendance/attendance-chart.tsx, features/attendance/attendance-comparision.tsx, features/attendance-correction/attendance-correction-view.tsx, features/attendance/attendance-status.test.tsx</files>
  <action>
    Read each file before editing. Import swaps from `@/lib/utils/time` → `@/lib/date`:
    - attendance-status.tsx: formatDate→formatIstDate, transformTime→formatIstDateTime (check-in/out cells become absolute datetimes per D-11), formatHours keeps name
    - attendance-comparision.tsx: formatTime→formatIstDateTime
    - attendance-chart.tsx: getMonthYear keeps name (now IST-zoned internally)
    - attendance-correction-view.tsx: formatDate→formatIstDate, transformTime→formatIstDateTime, timeToDateString→combineDateAndTimeToIso (same two args)
    Then add `features/attendance/attendance-status.test.tsx`: msw handler serving a fixed record with inTime "2026-08-24T18:30:00Z" through the component's real query path (read the component first to match its hook/props), render via tests/render-with-providers.tsx, assert the rendered text contains "25 Aug 2026" — proving the D-03 default render test for the class of changed user-visible output. If the component needs auth-context props beyond providers, stub useMe at the hook boundary (Phase 1 LoginForm precedent: mock only next/navigation-level boundaries).
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test features/attendance/attendance-status.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - None of the four edited files still imports @/lib/utils/time
    - Render test asserts rendered output contains "25 Aug 2026" for inTime 2026-08-24T18:30:00Z
    - typecheck green (proves every renamed symbol found its new home)
  </acceptance_criteria>
  <done>Attendance-family displays show canonical IST formats derived from the tested module</done>
</task>

<task type="auto">
  <name>Task 3: Migrate input round-trip + calendar clock owners (D-10/D-12)</name>
  <files>features/attendance/attendance-correction.tsx, features/calendar/add-event-dialog.tsx, features/calendar/calendar.tsx</files>
  <action>
    Read each file before editing.
    - attendance-correction.tsx: timeToDateString→combineDateAndTimeToIso (identical arg order), formatDate→formatIstDate, transformTime→formatIstDateTime.
    - add-event-dialog.tsx: toLocalInput→isoToIstInput for seeding `<input type="datetime-local">` defaults; on submit convert the raw input value with istInputToIso before it reaches the API payload (D-10 round-trip). Keep the native input element (ponytail rung 4) — no picker swap here.
    - calendar.tsx line ~127: change FullCalendar `timeZone="local"` to `timeZone="Asia/Kolkata"`; map event start/end through isoToIstWallClock when building the events array so FullCalendar renders literal IST digits (its documented UTC-coercion behavior strips offsets — feeding Z-suffixed strings would display UTC digits). Any handler reading dates back off FullCalendar args (select/dateClick/event drop building payloads) must treat arg values as IST wall-clock and convert via istInputToIso-equivalent parsing before sending. Do not edit components/ui/* files (shadcn-generated).
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep finds zero remaining imports of @/lib/utils/time within features/attendance/, features/attendance-correction/, features/calendar/
    - calendar.tsx contains timeZone="Asia/Kolkata" and no longer contains timeZone="local"
    - add-event-dialog submit path calls istInputToIso (grep-verifiable) before apiFetch payload construction
    - Full suite + build green
  </acceptance_criteria>
  <done>All clock-owning surfaces (forms, FullCalendar) exchange data exclusively through lib/date conversions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user form input → API payloads | datetime-local strings cross into ISO payloads sent to the backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-04 | Tampering (malformed date input) | istInputToIso/dateInputToIso/combineDateAndTimeToIso | mitigate | Native Date parsing rejects garbage as NaN — callers get Invalid Date propagation rather than silently shifted values; form schemas upstream validate shape |
| T-2-05 | Information Disclosure (TZ fingerprint) | Intl usage | accept | Locale/zone constants are hardcoded (en-IN / Asia/Kolkata) — no user data in formatting decisions |
| T-2-SC | Tampering | npm installs | mitigate | No installs in this plan (zero-dependency phase per RESEARCH audit) |
</threat_model>

<verification>
- `pnpm test lib/date.test.ts` — all day-boundary and round-trip cases green
- Render test proves an attendance cell shows "25 Aug 2026" for a 18:30Z instant
- `grep -r "utils/time" features/attendance features/attendance-correction features/calendar` → empty
- `pnpm build` green (calendar + forms compile with new utils)
</verification>

<success_criteria>
Every date this plan's consumers render or emit is IST-correct by construction, proven at the 18:30Z boundary, with a render test guarding the visible-output change (per D-03 default).
</success_criteria>

<output>
Create `.planning/phases/02-shared-infrastructure-session-reliability/02-02-SUMMARY.md` when done
</output>
