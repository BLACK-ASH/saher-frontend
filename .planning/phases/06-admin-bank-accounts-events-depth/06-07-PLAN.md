---
phase: 06-admin-bank-accounts-events-depth
plan: 07
type: execute
wave: 5
depends_on: [06-04, 06-06]
files_modified:
  - services/session.api.ts
  - hooks/use-sessions.ts
  - app/(main)/program/sessions/[id]/page.tsx
  - components/notification/notification-box.tsx
  - tests/session-reminder-export.test.ts
autonomous: false
requirements: [EVNT-07, EVNT-08]

must_haves:
  truths:
    - "A manager sends an email reminder for a session with one click and sees a success toast"
    - "A manager requests an attendance report (PDF/XLSX) with one click and is told to check notifications"
    - "Completed exports surface as downloadable notifications in the notification box"
  artifacts:
    - path: "services/session.api.ts"
      provides: "sendSessionReminder + requestSessionExport (odd-GET jobId contract)"
      exports: ["sendSessionReminder", "requestSessionExport"]
    - path: "app/(main)/program/sessions/[id]/page.tsx"
      provides: "Send Reminder + Export Report actions on session detail"
      contains: "requestSessionExport"
  key_links:
    - from: "app/(main)/program/sessions/[id]/page.tsx"
      to: "hooks/use-sessions.ts"
      via: "sendReminder/requestExport mutations (invalidating [\"session\", id])"
    - from: "components/notification/notification-box.tsx"
      to: "download-action"
      via: "action.type === \"download\" renders <a href> (already present — verify)"
---

<objective>
Session notification depth: one-click email reminders and one-click attendance report exports (PDF/XLSX) delivered via the notification box.

Purpose: EVNT-07 + EVNT-08. Backend contract (RESEARCH-verified, HIGH confidence against `../saher-backend/src/events/**`):
- Reminder (EVNT-07): `GET /api/events/programs/workshops/sessions/:sessionId` — an odd GET that TRIGGERS the reminder job (authorize `read,event`) → sends a success notification to the session speaker, 201, data null. Frontend fn `sendSessionReminder(id)`.
- Export (EVNT-08): `GET /api/events/export/report?sessionId=<ObjectId>&format=pdf|xlsx` → enqueues a BullMQ job, returns `{ jobId, format }`; the worker posts a notification `{type:'download', label:'Report', url:'/api/attendance/download/<jobId>.<ext>', method:'GET'}` served by `GET /api/attendance/download/:fileName`. Frontend fn `requestSessionExport({ id, format })`.
Either "processing" or "already generated" responses are success from the UI's perspective — the artifact lands in the notification box, which already renders `action.type === "download"` as an `<a href>` (notification-box.tsx). Wave 5, after 06-06 (depends_on [06-04, 06-06] — sequential ownership of use-sessions.ts; see Blocker 3 fix). Depends on 06-04 (session.api.ts ownership: 06-04 edits it first, this plan adds the two functions on top).
Output: reminder + export actions wired into the session detail with notifications-download verified.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-RESEARCH.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-PATTERNS.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md
@services/session.api.ts
@hooks/use-sessions.ts
@components/notification/notification-box.tsx
@app/(main)/program/sessions/[id]/page.tsx

Reminder/export backend contract (RESEARCH-verified against `../saher-backend/src/events/**`, HIGH confidence — do NOT re-verify during execution):
- Reminder (EVNT-07): `GET /api/events/programs/workshops/sessions/:sessionId` — an ODD GET used to trigger the reminder job (authorize `read,event`). Response 201 `{ success, message, data: null }`; a success Notification is sent to the session speaker. From the UI's view ANY 2xx = done — toast "Reminder sent to speakers".
- Export (EVNT-08): `GET /api/events/export/report?sessionId=<ObjectId>&format=pdf|xlsx` — enqueues a BullMQ job; response `{ success, message, data: { jobId, format } }`. The worker posts a notification `{ type: 'download', label: 'Report', url: '/api/attendance/download/<jobId>.<ext>', method: 'GET' }`; the file is served by `GET /api/attendance/download/:fileName`. A secondary call while the job runs returns "request is processing" — treat as success too, keep the "check notifications" toast.
- Delivery: notification-box.tsx ALREADY renders `action.type === "download"` as an `<a href>` — verify only, no rebuild.
- Roles: reminder + export are session notifications, authorize `read,event` (admin + manager + staff with read); gate via existing patterns.

<interfaces>
<!-- Key contracts the executor needs. Verified from current source. -->

From services/session.api.ts (06-04 added restoreSession — the diff on top of which Task 1 lands):
```typescript
// existing style: legacy `if (!res.success)` + toast, or throw-on-failure — match the file's CURRENT dominate style (session.api.ts uses the legacy style; keep it)
export const addSession = ...
export const updateSession = ...
export const deleteSession = ...
export const restoreSession = ...   // from 06-04
// NEW in this plan:
export const sendSessionReminder = async (id: string)   // GET /api/events/programs/workshops/sessions/${id} — odd GET job trigger; 201, data null
export const requestSessionExport = async ({ id, format }: { id: string; format: "pdf" | "xlsx" })  // GET /api/events/export/report?sessionId=${id}&format=${format} → { jobId, format }
```

From hooks/use-sessions.ts (06-04 added `restore`):
```typescript
export const useSessions = (filters) => ({ sessions, session, loading, add, update, remove, restore, ... })
// NEW mutations: sendReminder (mutationFn: (id) => sendSessionReminder(id)), requestExport (mutationFn: ({id, format}) => requestSessionExport(...))
// invalidation: reminders/export touch the SESSION's notifications — invalidate ["session", id]; does session.getCached exports change? No — do NOT invalidate ["sessions"] for these (no list data changed). Comment the choice.
// D-26: disable while pending — onSuccess: toast.success("...").
```

From components/notification/notification-box.tsx (verify only):
```typescript
// renders per-notification action: action.type === "download" → <a href={url} download>…</a>  (assumed present — verify the exact field names; fix ONLY if the session export shape doesn't match, and report what changed)
```

From 06-UI-SPEC §Session Notifications: copy "Send Reminder", "Export Report", dropdown items "PDF / XLSX", toasts "Reminder sent to speakers" / "Report generating — check notifications".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reminder + export service functions with tests</name>
  <files>services/session.api.ts, tests/session-reminder-export.test.ts</files>
  <read_first>
    - services/session.api.ts (06-04 state: restoreSession present; the file's request style + how responses are unwrapped)
    - ../saher-backend/src/events/** — CONTRACT SOURCE for the two odd-GET routes, already RESEARCH-verified (HIGH confidence): reminder = GET /api/events/programs/workshops/sessions/:sessionId (201, data null); export = GET /api/events/export/report?sessionId&format → {jobId, format}. No re-verification needed — treat these as settled. Read only if you must confirm the response envelope.
    - tests/session-reminder-export.test.ts pattern from existing msw tests (payroll-api.test.ts / session tests)
  </read_first>
  <action>
    Per RESEARCH EVNT-07/08 (verified routes — assert them, do not re-derive):
    1. `services/session.api.ts`: add `sendSessionReminder(id)` → GET `/api/events/programs/workshops/sessions/${id}` (odd GET job trigger; match file's legacy style; treat any 2xx as success) and `requestSessionExport({ id, format })` → GET `/api/events/export/report?sessionId=${id}&format=${format}` (returns `{ jobId, format }`; treat "processing"/2xx as success for the caller). Keep the file's legacy error-style; no console.log (lint).
    2. Create `tests/session-reminder-export.test.ts` (msw): (a) `sendSessionReminder("s1")` hits `GET /api/events/programs/workshops/sessions/s1`; (b) `requestSessionExport({id:"s1", format:"pdf"})` URL contains `format=pdf`; (c) `requestSessionExport({id:"s1", format:"xlsx"})` URL contains `format=xlsx`; (d) `requestSessionExport` URL contains `sessionId=s1`.
  </action>
  <acceptance_criteria>
    - Source assertion: services/session.api.ts exports `sendSessionReminder` and `requestSessionExport`
    - Source assertion: sendSessionReminder hits GET `/api/events/programs/workshops/sessions/:id`; requestSessionExport hits GET `/api/events/export/report?sessionId&format` (documented in a comment above each fn — e.g. `// GET despite the verb — backend route is a GET job trigger`)
    - Test command: `pnpm test tests/session-reminder-export.test.ts` passes (a)-(d) at minimum
    - Gates: `pnpm lint` && `pnpm typecheck` pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/session-reminder-export.test.ts && pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>Reminder + export request functions exist with verified methods and format params, msw-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Session detail actions + notification-download verification (EVNT-07/08)</name>
  <files>app/(main)/program/sessions/[id]/page.tsx, hooks/use-sessions.ts, components/notification/notification-box.tsx</files>
  <read_first>
    - app/(main)/program/sessions/[id]/page.tsx (06-04 state: breadcrumb + edit prefill + restore affordance; where the action bar lives)
    - hooks/use-sessions.ts (06-04 added `restore`; 06-06 added attendance mutations/helpers — add the two NEW mutations `sendReminder` + `requestExport` alongside them; this plan is the LAST use-sessions.ts editor in the wave)
    - components/notification/notification-box.tsx (full read — the download action rendering path)
    - services/notification.api.ts (NotificationT action typing — action.type union includes "download"? verify field carrying the file URL)
    - .planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md §Session Notifications
  </read_first>
  <action>
    Per RESEARCH EVNT-07/08 + UI-SPEC §Session Notifications:
    1. `hooks/use-sessions.ts`: add `sendReminder` mutation (mutationFn: `(id: string) => sendSessionReminder(id)`; onSuccess: `queryClient.invalidateQueries({ queryKey: ["session", id] })` + `toast.success("Reminder sent to speakers")`; onError per existing hook patterns) and `requestExport` mutation (mutationFn: `({id, format}: {id: string; format: "pdf" | "xlsx"}) => requestSessionExport({id, format})`; onSuccess: toast.success("Report generating — check notifications"); onError per patterns). Both disabled while `isPending` (D-26). Do NOT invalidate ["sessions"] — no list data changes (comment that choice).
    2. `app/(main)/program/sessions/[id]/page.tsx`: in the action bar (Edit/Delete area, THE SAME actions cluster the 06-04 task placed restore in): "Send Reminder" button (secondary, disabled while its mutation pends; single click → toast) and "Export Report" dropdown (PDF / XLSX items → requestExport with the format; each keeps its pending state disabled). Copy per UI-SPEC. Placement must not shift the edit/restore affordances.
    3. `components/notification/notification-box.tsx` + `services/notification.api.ts` verification: confirm the download action type exists in the NotificationT union AND that the box renders an `<a href>` for it (per RESEARCH the box "renders download action type with `<a href>`"). Fix ONLY what's broken for the session-export shape (field mismatch / missing href); if it already works verbatim, leave untouched and note "verified download action render" in the summary. If the fix changes the notification api types/schema, update tests that construct notifications.
  </action>
  <acceptance_criteria>
    - Source assertion: session detail shows "Send Reminder" + "Export Report (PDF/XLSX)" wired to the new mutations; both disabled while pending
    - Source assertion: hooks return `sendReminder` + `requestExport`; invalidation is `["session", id]` ONLY (comment present)
    - Source assertion: notification-box.tsx renders a download action as an anchor when the notification's action.type is "download" (pre-existing OR fixed — stated in summary)
    - Gates: `pnpm lint` && `pnpm typecheck` pass; `pnpm test` full suite green
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>Session detail has reminder + export actions; notification download rendering verified (or fixed); hooks invalidate correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify reminder + export end-to-end (requires BullMQ + live backend)</name>
  <what-built>One-click session reminder + attendance report export delivered via notifications</what-built>
  <how-to-verify>
    1. Backend running (with BullMQ/queue worker) + `pnpm dev`; log in as a manager/admin.
    2. Open a session detail (with speakers set) → "Send Reminder".
    3. Expected: success toast; the spoke email queue (dev mail catcher, e.g. mailhog/mailpit) shows the reminder to the session's speakers within seconds.
    4. "Export Report" → "PDF". Expected: "Report generating — check notifications" toast.
    5. Open the notification box (bell): within a minute the export notification appears; its action is a downloadable link (PDF) — downloading fetches the file (HTTP 200) and opens a valid PDF.
    6. Repeat with XLSX — same flow, valid xlsx download.
    7. If the notification is absent or the href 404s, report the notification payload shape you saw (the backend job may need a gateway endpoint — flag it, do not fix silently).
  </how-to-verify>
  <resume-signal>Type "approved" to close, or report which step failed with the notification payload observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (reminder/export request) | one-click triggers cross here; jobs + notifications fire server-side |
| API → client (notification payload with file URL) | download href crosses here — treat URL as untrusted until fetched (anchor target is the backend origin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | DoS | reminder spam | mitigate | D-26 pending-disable (one click per job); server-side queue dedupes per session where implemented |
| T-06-07-02 | Spoofing | export notification URL | accept | href comes from the backend's own notification payload (same-origin /uploads or /api path); anchor rendered by the existing notification-box download action — no new origin trust added |
| T-06-07-03 | Tampering | format param | mitigate | format strictly typed `"pdf" | "xlsx"` in TS + zod-validated query at backend; no free-text input |
| T-06-07-04 | Integrity | "processing" vs failure ambiguity | mitigate | any 2xx treated as success + "check notifications" toast; real failures surface via apiFetch error toast (no silent success) |
| T-06-07-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — package legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm test tests/session-reminder-export.test.ts`, `pnpm lint`, `pnpm typecheck`, full `pnpm test`
- Route assertion (verified, not deferred): sendSessionReminder → GET /api/events/programs/workshops/sessions/:id; requestSessionExport → GET /api/events/export/report?sessionId&format (documented in CODE comments per EVNT-07/08)
- Human checkpoint: live reminder delivery + download of both formats (Task 3)
</verification>

<success_criteria>
- Send Reminder: one click → toast → email to speakers
- Export Report: PDF/XLSX one click → toast → downloadable notification in the box
- Notification download action rendering verified for the session-export shape
</success_criteria>

<output>
Create `.planning/phases/06-admin-bank-accounts-events-depth/06-07-SUMMARY.md` when done
</output>