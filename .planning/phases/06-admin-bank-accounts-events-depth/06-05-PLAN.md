---
phase: 06-admin-bank-accounts-events-depth
plan: 05
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - services/participant.api.ts
  - hooks/use-programs.ts
  - features/program/participant/all-participant.tsx
  - features/program/participant/add-participant.tsx
  - features/program/participant/update-participant.tsx
  - features/program/participant/participant-header.tsx
  - app/(main)/program/participants/page.tsx
  - app/(main)/program/participants/[id]/page.tsx
  - app/(main)/program/[id]/page.tsx
  - tests/participant-api.test.ts
autonomous: true
requirements: [EVNT-05]

must_haves:
  truths:
    - "A manager/admin adds free-entry participants (name, phone, age, image) to the participant pool"
    - "A manager/admin attaches participants to a program roster and detaches them; the roster renders populated participant cards"
    - "Participant rows can be soft-deleted and restored; participant lists always filter isDeleted=false"
  artifacts:
    - path: "services/participant.api.ts"
      provides: "restoreParticipant + getParticipants always sends isDeleted=false"
      exports: ["restoreParticipant"]
    - path: "app/(main)/program/[id]/page.tsx"
      provides: "roster section rendering populated participants from program detail + attach/detach"
      contains: "participants" (populated array usage)
  key_links:
    - from: "app/(main)/program/[id]/page.tsx"
      to: "services/program.api.ts"
      via: "getSingleProgram(id).participants (populated — NOT getParticipantFromProgram raw ids)"
    - from: "features/program/participant/all-participant.tsx"
      to: "services/participant.api.ts"
      via: "getParticipants({ isDeleted: \"false\" }) + restoreParticipant"
---

<objective>
Participant rosters reach operational depth: free-entry participant CRUD with restore, program attach/detach with the corrected `{participantIds}` body, and roster rendering from the populated program detail source.

Purpose: EVNT-05. RESEARCH resolved the input mode: participants are FREE-ENTRY records (name/phone/age — NOT employee search). Attach = `POST /api/events/programs/participants/:programId {participantIds[]}` ($addToSet), detach = `DELETE .../:participantId` ($pull). Quirk: the id-list endpoint returns raw ObjectIds — roster UI must read the populated array from `GET /api/events/programs/:id` (Pitfall 3). Participants carry NO default isDeleted filter — always send `isDeleted=false` explicitly (Pitfall 5). `addParticipantsInProgram`'s body fix ships in 06-03 (this plan depends on it).
Output: participant service contract + roster UI + program-detail roster attach/detach.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-RESEARCH.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-PATTERNS.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md
@services/participant.api.ts
@services/program.api.ts
@hooks/use-programs.ts
@features/program/participant/add-participant.tsx
@app/(main)/program/[id]/page.tsx

Contract checks performed during RESEARCH (confidence HIGH, verified against `../saher-backend/src/**`):
- Participants = free-entry: `POST /api/events/participants` (name free text, phone, age, image, affiliation; `age < 18 ⇒ parentDetails required` refine both sides).
- Attach: `POST /api/events/programs/participants/:programId` body `{participantIds: string[]}` ($addToSet). Detach: `DELETE /api/events/programs/participants/:programId/:participantId` ($pull).
- `GET /api/events/programs/participants/:programId` returns RAW ObjectId strings (no populate) — never use for roster rendering.
- `GET /api/events/programs/:id` returns participants POPULATED (`SingleParticipantT.participants: ParticipantT[]`) — the roster source.
- `getParticipants` applies isDeleted ONLY when present; `PATCH /api/events/participants/restore/:id` exists.

<interfaces>
<!-- Key contracts the executor needs. Verified from current source. -->

From services/participant.api.ts (current — Task 1 modifies):
```typescript
export type ParticipantT = z.infer<typeof participantResponseSchema>; // { id; name; age?; gender?; phoneNumber?; image?: {id,src,alt}; address?; affiliation?; parentDetails?; document?; }
export const getParticipants = async ({ keyword, page = 1, limit = 10 }: QueryProps) // → add isDeleted: string ALWAYS sent
// line 117-123 deleteParticipant — restore-pattern analog:
export const deleteParticipant = async (id: string) // DELETE /api/events/participants/:id
```

From services/program.api.ts (06-03 shipped these):
```typescript
export const addParticipantsInProgram = async ({ id, participants }: { id: string; participants: string[] }) // body NOW { participantIds } — do NOT re-fix
export type SingleParticipantT = ProgramsT & { participants: ParticipantT[] }; // getSingleProgram returns this
export const removeParticipantFromProgram = async ({ programId, participantId }) // DELETE
```

From hooks/use-programs.ts (06-03 shipped `restore`; current exports include a `participants` query that calls `getParticipantFromProgram` — the RAW-ID source):
```typescript
const participants = useQuery({ queryKey: ["programs","participants",id], queryFn: () => getParticipantFromProgram(id), enabled: !!id });
// The roster UI must NOT consume this. The attendance page (consumed via `const { participants: programParticipants } = usePrograms(...)`) still does until plan 06-06 — see Task 2 for the exact handling.
```

From features/program/participant/add-participant.tsx (existing free-entry form — schema `createParticipantSchema`, fields name/age/gender/phoneNumber/image/address/affiliation; keep):
```typescript
const form = useForm<FormValues>({ resolver: zodResolver(createParticipantSchema), ... })
// under-18 parentDetails refine lives in participantSchema (participant.api.ts:22-30) — keep both client + server parity
```

From .planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md §Participant Rosters: copy "Add Participant", "Add to Program", "Remove", roster empty state "No participants attached yet — add participants to this program."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Participant service contract fixes (restore + isDeleted) with tests</name>
  <files>services/participant.api.ts, tests/participant-api.test.ts</files>
  <read_first>
    - services/participant.api.ts (getParticipants at lines 70-83; deleteParticipant at 117-123 as restore analog; participantSchema refine at 22-30 — keep)
    - ../saher-backend/src/events/participant/{participant.controller,participant.schema}.ts — THE CONTRACT SOURCE for the isDeleted absence-default and the restore route (re-verify only if moved)
    - ../saher-backend/src/events/program/program-participant.controller.ts (the raw-ObjectId quirk source — read to confirm what the id-list endpoint returns)
    - tests/payroll-api.test.ts (msw style)
  </read_first>
  <action>
    Per RESEARCH Pitfall 5 + §Code Examples:
    1. `services/participant.api.ts`: add `restoreParticipant = async (id: string)` — PATCH `/api/events/participants/restore/${id}` (same legacy error-handling style as siblings).
    2. Change `getParticipants` to ALWAYS request `isDeleted=false` by default: signature gains `isDeleted = "false"` in the destructured props (type `string`), URL always contains `&isDeleted=${isDeleted}` (never omitted — the endpoint treats absence as "no filter" and shows ghost/deleted rows, Pitfall 5). Callers pass `isDeleted: "true"` only from a trash tab.
    3. Extend `participantResponseSchema` with an `isDeleted` boolean field ONLY if the live backend response includes it (verify against `../saher-backend/src/events/participant/participant.controller.ts` mapping — if the response omits it, do NOT add it; the trash-tab UI derives state from which list call populated the row).
    4. Create `tests/participant-api.test.ts` (msw): (a) `getParticipants({})` URL contains `isDeleted=false` ALWAYS (call with no props — assert the param is present, pinning the no-default-filter fix); (b) `getParticipants({isDeleted:"true"})` URL contains `isDeleted=true`; (c) `restoreParticipant("p1")` hits `PATCH /api/events/participants/restore/p1`.
  </action>
  <acceptance_criteria>
    - Source assertion: services/participant.api.ts exports `restoreParticipant`; `getParticipants` URL template always includes `isDeleted=`
    - Source assertion: `getParticipants({})` (no props) sends `isDeleted=false` — the test asserts it
    - Test command: `pnpm test tests/participant-api.test.ts` passes all 3 tests
    - Gates: `pnpm lint` && `pnpm typecheck` pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/participant-api.test.ts && pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>Participant restore + always-on isDeleted filter exist and are msw-tested; response schema reflects the verified backend shape.</done>
</task>

<task type="auto">
  <name>Task 2: Program roster — populated roster rendering + attach/detach (EVNT-05)</name>
  <files>app/(main)/program/[id]/page.tsx, features/program/participant/all-participant.tsx, features/program/participant/participant-header.tsx, hooks/use-programs.ts, app/(main)/program/participants/page.tsx, app/(main)/program/participants/[id]/page.tsx</files>
  <read_first>
    - app/(main)/program/[id]/page.tsx (current roster section — where the raw-id `participants` query is consumed today; this is the fix target)
    - hooks/use-programs.ts (the `participants` query + addParticipants/removeParticipant mutations)
    - features/program/participant/all-participant.tsx (participant pool list — add trash/restore tabs)
    - features/program/participant/add-participant.tsx + update-participant.tsx (existing free-entry forms — keep; wire restore into the pool)
    - components/user-search-* analogues — NONE: attach is participant-id based, not user search (A6: only session speakers are users)
    - .planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md §Participant Rosters (copy + "Add to Program"/"Remove" affordances)
  </read_first>
  <action>
    Per RESEARCH Pitfall 3 + EVNT-05 + UI-SPEC §Participant Rosters:
    1. `app/(main)/program/[id]/page.tsx` — roster section: render the ATTACHED roster from `program.data.participants` (the populated detail — `usePrograms({id}).program.data`), NOT from the hook's `participants` query. Attach: a picker listing pool participants (unattached ones; `getParticipants({isDeleted:"false"})` full list) with checkboxes/Add buttons → `addParticipantsInProgram` mutation (`{id: programId, participants: selectedIds}`, `addParticipants` from usePrograms) — copy "Add to Program". Detach: per-row "Remove" button → `removeParticipantFromProgram({programId, participantId})`. Both mutations already invalidate `["programs"]` (verify; if the hook's invalidation is missing for either, add it). Empty roster state per UI-SPEC: "No participants attached yet — add participants to this program."
    2. `hooks/use-programs.ts`: the raw-id `participants` query + `getParticipantFromProgram` import become dead once the roster page AND the attendance page stop consuming them. Attendance page still consumes `participants` until 06-06. So: (a) if this plan's roster fix removes the LAST consumer EXCEPT the attendance page, KEEP the query (grep `programParticipants`/`participants` consumers across features/ + app/ to confirm) and note it for 06-06; (b) if the attendance page is the only remaining consumer, leave the query in place and add a `ponytail:` comment: "raw-id roster query, last consumer is attendance page (06-06 removes it)". Do NOT delete `getParticipantFromProgram` from services/program.api.ts in this plan (06-06 owns the removal once consumers are gone).
    3. `features/program/participant/all-participant.tsx` — participant pool: Active|Deleted tabs with explicit `isDeleted` (same tab wiring as program/workshop plans), restore row action on deleted rows (`restoreParticipant` via the useParticipants hook — check hooks/use-participant.ts exports; add a restore mutation there if missing, invalidating `["participants"]`), "Add Participant" CTA opening the existing free-entry form, edit per row via update-participant.tsx. Delete gated `can(r,"delete","event")`(admin); create/edit/restore admin+manager.
    4. Detail page `app/(main)/program/participants/[id]/page.tsx` + header: reconcile to the same populated typing (no raw-id reads); keep existing fields.
  </action>
  <acceptance_criteria>
    - Source assertion: program/[id]/page.tsx roster renders from `program.data.participants` (populated) — no `getParticipantFromProgram`/`participants.data` raw-id consumption remains in that file
    - Source assertion: attach posts via `addParticipantsInProgram` with `{participantIds}` (already fixed in 06-03 — this plan only wires the mutation); detach calls `removeParticipantFromProgram`
    - Source assertion: all-participant.tsx has Active|Deleted tabs with explicit isDeleted + restore action on deleted rows + "Add Participant" CTA
    - Source assertion: addParticipants/removeParticipant mutations invalidate `["programs"]` on success (check and fix in this plan)
    - Behavioral assertion: unattached participants appear in the attach picker (pool list filtered isDeleted=false); attached rows render name/phone/affiliation from populated data
    - Gates: `pnpm lint` && `pnpm typecheck` pass; `pnpm test` full suite green
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>Program roster renders populated participants with working attach/detach; participant pool CRUD + restore with isDeleted filtering; raw-id query usage confined to the attendance page (06-06 migration noted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (participant CRUD) | Free-entry participant data crosses here; server refines age<18⇒parentDetails |
| client → API (roster attach/detach) | participantIds cross here; $addToSet/$pull server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | ghost/deleted participants in rosters | mitigate | explicit `isDeleted=false` on every participant list call (Pitfall 5, pinned by msw test); trash rows only under the Deleted tab |
| T-06-05-02 | Tampering | roster attach body | mitigate | `{participantIds}` schema fixed in 06-03 + its msw test; this plan only wires the mutation — no body reshaping |
| T-06-05-03 | Tampering | under-18 participants without parent consent | mitigate | keep the participantSchema `parentDetails` refine (client) + backend parity; add-participant form unchanged in this regard |
| T-06-05-04 | Information Disclosure | raw ObjectId roster leaks | mitigate | roster UI reads populated `program.data.participants` only; the raw-id endpoint remains unused for rendering (migration completed; attendance page consumer removed in 06-06) |
| T-06-05-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — package legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm lint`, `pnpm typecheck`, `pnpm test` pass (new tests/participant-api.test.ts included)
- Grep gate: `rg "getParticipantFromProgram|programParticipants" app/ features/` — participants pages + program/[id] must show zero matches; only hooks/use-programs.ts + the attendance page may still reference it (with the ponytail comment)
- Contract spot-check: `../saher-backend/src/events/participant/**` + `program-participant.controller.ts` unchanged since 2026-08-29
</verification>

<success_criteria>
- Free-entry participant pool: add/edit/delete/restore with isDeleted filtering
- Program roster: populated cards, attach via {participantIds}, detach via delete, empty state
- No roster surface renders raw ObjectIds
</success_criteria>

<output>
Create `.planning/phases/06-admin-bank-accounts-events-depth/06-05-SUMMARY.md` when done
</output>