---
phase: 06-admin-bank-accounts-events-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/admin.api.ts
  - hooks/use-profile.ts
  - features/register/register-schema.ts
  - features/register/employee-details.tsx
  - features/register/user-register.tsx
  - features/users/page.tsx
  - tests/register-schema.test.ts
  - tests/admin-api.test.ts
autonomous: true
requirements: [ADMN-01, ADMN-02]

must_haves:
  truths:
    - "An admin can create a free or intern employee through the 4-step wizard and it saves atomically (account + bank in one POST)"
    - "The employee directory loads from a typed service function and stays searchable + paginated client-side"
    - "After onboarding, the new employee appears in the directory"
  artifacts:
    - path: "services/admin.api.ts"
      provides: "registerAccount (POST /api/admin/account), getAdminUsers (GET /api/admin/users, normalizeList-wrapped) + zod schemas"
      exports: ["registerAccount", "getAdminUsers"]
    - path: "features/register/register-schema.ts"
      provides: "employeeType enum with all five backend values"
      contains: '"free"'
    - path: "hooks/use-profile.ts"
      provides: "AccountT.employeeType union includes free|intern"
      contains: '"free"'
    - path: "features/users/page.tsx"
      provides: "directory fetch via getAdminUsers + Create Employee CTA"
      contains: "getAdminUsers"
  key_links:
    - from: "features/register/user-register.tsx"
      to: "services/admin.api.ts"
      via: "registerAccount call replaces inline apiFetch POST"
    - from: "features/users/page.tsx"
      to: "services/admin.api.ts"
      via: "getAdminUsers in queryFn"
---

<objective>
Admin onboarding is completed to the verified backend contract so `POST /api/admin/account` works for every employee type, and the directory reads through a typed, tested service.

Purpose: ADMN-01/ADMN-02 are the only admin requirements that already have an existing UI (register wizard + users table). This plan reconciles them to the verified contract — fix the enum drift (backend `employeeTypeList = ['free','intern','full-time','part-time','volunteer']`, RESEARCH Pitfall 4), extract a typed service layer (`services/admin.api.ts`), and wire the directory + wizard onto it. The atomic-onboarding endpoint already exists and is a server-side transaction — the frontend's job is faithful envelope handling.
Output: `services/admin.api.ts` (registerAccount + getAdminUsers + schemas), enum reconciliation, directory CTA, and msw-tested contract coverage.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-RESEARCH.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-PATTERNS.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md
@services/register/../payroll.api.ts
@features/register/register-schema.ts
@features/register/user-register.tsx
@hooks/use-profile.ts

Contract checks performed during RESEARCH (confidence HIGH, verified against `../saher-backend/src/**`):
- `POST /api/admin/account` = server-side MongoDB transaction over user + account + bank; password auto-generated; welcome email after txn. Envelope `{user, account, bank}`.
- `GET /api/admin/users?fields=isActive` returns an UNPAGINATED full array (7-day Redis cache) — client-side search/pagination is the only option.
- Backend `employeeTypeList = ['free','intern','full-time','part-time','volunteer']`; `part-time ⇒ employeeShift required` refine stays.
- Account create permission: `account:write` = admin + manager.

<interfaces>
<!-- Key contracts the executor needs. Verified from current source. -->

From services/payroll.api.ts (the style template for NEW code — throw-on-failure, no `if (!res.success)` blocks):
```typescript
export const getPayrollList = async (filters, page = 1, limit = 10): Promise<ReturnType<typeof normalizeList<PayrollResponse>>> => {
  const res = await apiFetch<PayrollResponse[]>(`/api/payroll?...`, { method: "GET" });
  return normalizeList<PayrollResponse>(res);
};
```

From lib/normalize-list.ts:
```typescript
export type NormalizedList<T> = { items: T[]; page: number; limit: number; totalPages: number; totalRecords: number; };
export function normalizeList<T>(res: { data?: T[] | null; meta?: unknown }): NormalizedList<T>;
```

From features/register/register-schema.ts (line 70-73, the drift to fix):
```typescript
employeeType: z.enum(["full-time", "part-time", "volunteer"], "Employee Type Is Required."),  // → add "free","intern"
// part-time ⇒ employeeShift refine at lines 82-93 — KEEP
```

From hooks/use-profile.ts (line 23):
```typescript
readonly employeeType: "full-time" | "part-time" | "volunteer";  // → add "free" | "intern"
```

Current directory query in features/users/page.tsx (inline fetch that Task 3 replaces):
```typescript
useQuery({ queryKey: ["user", "list"], queryFn: async () => {
  const res = await apiFetch<UserT[]>(`/api/admin/users?fields=isActive`);
  return res.data;
}});
```

From components/user-search-picker.tsx / RoleAccess: RoleAccess props = `{ children, allow: (role: UserRole) => boolean, fallback?, loading? }` — gate the Create Employee CTA with `allow={(r) => can(r, "write", "account")}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile employeeType enum to the backend five-value list</name>
  <files>features/register/register-schema.ts, features/register/employee-details.tsx, hooks/use-profile.ts, tests/register-schema.test.ts</files>
  <read_first>
    - features/register/register-schema.ts (the enum at lines 70-73 + the part-time refine at 82-93 you must NOT touch)
    - features/register/employee-details.tsx (`const employeeType = [...]` at line 31 — the rendered Select list)
    - hooks/use-profile.ts (AccountT.employeeType union at line 23)
    - tests/payroll-api.test.ts (test style: vitest describe/it, msw server from "@/tests/test-server")
  </read_first>
  <action>
    Per RESEARCH Pitfall 4 + UI-SPEC §Onboarding:
    1. In `features/register/register-schema.ts`, change the `employeeType` z.enum at line 70-73 to exactly `z.enum(["free", "intern", "full-time", "part-time", "volunteer"], "Employee Type Is Required.")`. Do NOT modify the part-time ⇒ employeeShift `.refine` (register-schema.ts:82-93) — it stays exactly as-is. Do NOT modify `userSchema.role` (line 40 — roles are separate from employee types).
    2. In `features/register/employee-details.tsx` line 31, change `const employeeType = ["volunteer", "part-time", "full-time"]` to exactly `["free", "intern", "full-time", "part-time", "volunteer"]` (it feeds the rendered Select; keep the `.map((type) => ...toLocaleUpperCase())` rendering as-is).
    3. In `hooks/use-profile.ts` line 23, change the `AccountT.employeeType` union to `"free" | "intern" | "full-time" | "part-time" | "volunteer"`.
    4. Create `tests/register-schema.test.ts`: import `registerFormSchema` from "@/features/register/register-schema". Write three vitest tests: (a) a valid payload with `account.employeeType: "free"` passes `registerFormSchema.safeParse`; (b) same for `"intern"`; (c) a payload with `employeeType: "part-time"` and NO `employeeShift` fails with a path error on `account.employeeShift` (pins the refine survives the enum change). Use minimal valid payloads for the other required fields (user.name/email/role, account.gender/dateOfBirth/dateOfJoining/phoneNumber/employeeId/department/designation/salaryStructure/address, bank fields) — copy field shapes exactly from the existing schema, do not invent keys.
  </action>
  <acceptance_criteria>
    - Source assertion: `features/register/register-schema.ts` employeeType enum contains exactly the five strings `free, intern, full-time, part-time, volunteer` in that order
    - Source assertion: `features/register/employee-details.tsx` line 31 array contains the same five strings
    - Source assertion: `hooks/use-profile.ts` AccountT.employeeType union contains `"free"` and `"intern"`
    - Test command: `pnpm test tests/register-schema.test.ts` passes all 3 tests (the part-time refine test proves the refine survived)
    - Gates: `pnpm lint` and `pnpm typecheck` pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/register-schema.test.ts && pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>register-schema.ts + employee-details.tsx + use-profile.ts all expose the five-value employeeType; 3 new tests green; lint + typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create services/admin.api.ts (registerAccount + getAdminUsers) with msw tests and rewire the wizard</name>
  <files>services/admin.api.ts, features/register/user-register.tsx, tests/admin-api.test.ts</files>
  <read_first>
    - services/payroll.api.ts (the exact style: zod response schema → z.infer type, apiFetch only, NO legacy `if (!res.success)` blocks — apiFetch already toasts)
    - services/program.api.ts (normalizeList envelope usage on an unpaginated array endpoint, lines 25-38)
    - features/register/user-register.tsx (current onSubmit at lines 122-148: inline `apiFetch("/api/admin/account", {method:"POST", body: JSON.stringify({user, account, bank})})` + `queryClient.invalidateQueries({ queryKey: ["user","list"] })` + `router.refresh()`)
    - ../saher-backend/src/admin/_services/user.ts and ../saher-backend/src/admin/account/schema.ts — THE CONTRACT SOURCE: mirror the exact response field names the backend returns for the users list and the register endpoint (research verified these files; if they moved, check `../saher-backend/src/admin/**` for field names)
    - tests/payroll-api.test.ts (msw pattern: `server.use(http.get("/api/...", ...))`, `envelope = (data, meta) => HttpResponse.json({ success: true, message: "ok", data, meta })`)
  </read_first>
  <action>
    Per RESEARCH §Code Examples + PATTERNS `services/admin.api.ts` row — create `services/admin.api.ts` with the throw-on-failure payroll.api.ts style (no legacy success-checks, no inline toast):
    1. Export `registerAccount = async (data: RegisterFormData): Promise<ApiResponse<{ id: string }>>` — POST `/api/admin/account` with body `JSON.stringify({ user: data.user, account: data.account, bank: data.bank })` via `apiFetch`, returning the result so callers can toast `response.message` (apiFetch throws on failure, so the return is only ever success-shaped). Import `RegisterFormData` from "@/features/register/register-schema". Do NOT extract/transform fields — send the triplet exactly as the wizard currently does.
    2. Export `adminUserResponseSchema` — a zod object mirroring the ACTUAL backend list response fields (read `../saher-backend/src/admin/_services/user.ts`; at minimum `id`, `name`, `email`, `role`, `image` and the `isActive` field the directory's `?fields=isActive` param requests; add optional nullability where backend sends null). Export `type AdminUserResponse = z.infer<...>`.
    3. Export `getAdminUsers = async (): Promise<ReturnType<typeof normalizeList<AdminUserResponse>>>` — GET `/api/admin/users?fields=isActive` via apiFetch, return `normalizeList<AdminUserResponse>(res)`. (Endpoint is unpaginated — normalizeList wraps into `{items, page:1, limit:0, totalPages:1, totalRecords:N}` per its own meta handling; do not add page params.)
    4. Rewire `features/register/user-register.tsx` onSubmit (lines ~122-148): replace the inline `apiFetch("/api/admin/account", ...)` + `if (!response.success) return toast.error(...)` block with `const res = await registerAccount(data)` in the try branch; `toast.success(res.message)` afterwards. Keep the existing `queryClient.invalidateQueries({ queryKey: ["user","list"] })`, `setStep(1)`, `form.reset()`, `router.refresh()` calls exactly as they are. Remove the now-unused direct `apiFetch` import from user-register.tsx only if nothing else in the file uses it (grep first).
    5. Create `tests/admin-api.test.ts` with msw: (a) `getAdminUsers` hits `GET /api/admin/users?fields=isActive` and returns a `NormalizedList` — assert `hitUrl` contains `fields=isActive` and `list.items` has the mocked row; (b) `registerAccount` POSTs body `{user, account, bank}` — assert `JSON.parse(request.body)` has exactly those three top-level keys and the envelope `data.id` is the new user id; (c) `adminUserResponseSchema` parses a row with `isActive: true` and rejects a row missing `id`.
  </action>
  <acceptance_criteria>
    - Source assertion: `services/admin.api.ts` exports `registerAccount`, `getAdminUsers`, `adminUserResponseSchema`, `AdminUserResponse`; contains zero `if (!res.success)` or `res.success &&` patterns
    - Source assertion: `features/register/user-register.tsx` no longer contains `apiFetch("/api/admin/account"` — the onSubmit calls `registerAccount`
    - Test command: `pnpm test tests/admin-api.test.ts` passes all 3+ tests
    - Behavior assertion: send body of registerAccount is exactly `{user, account, bank}` (no extra keys — backend schema is strict)
    - Gates: `pnpm lint` && `pnpm typecheck` pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/admin-api.test.ts && pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>admin.api.ts service exists and is msw-tested against the verified backend contract; the register wizard submits through `registerAccount` with unchanged UX and invalidation behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Adopt the typed service in the directory page and add the Create Employee CTA</name>
  <files>features/users/page.tsx</files>
  <read_first>
    - features/users/page.tsx (the current inline `useQuery` with `["user","list"]` + `apiFetch<UserT[]>`)
    - features/users/data-table.tsx (client-side column filter + pagination — already in place, do not rebuild)
    - components/role-access.tsx (gating wrapper) and lib/permissions.ts (`can(role, action, resource)` — `can(r, "write", "account")` is true for admin+manager per the permissions matrix)
    - .planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md §Admin Staff Directory (CTA copy "Create Employee", search placeholder "Search by name, email, or role…", empty states)
  </read_first>
  <action>
    Per RESEARCH Open Q3 (extend existing users directory, no new route groups) + UI-SPEC §Admin Staff Directory:
    1. In `features/users/page.tsx`, replace the inline `apiFetch<UserT[]>`/`res.data` queryFn with `getAdminUsers` from "@/services/admin.api", keeping the exact queryKey `["user","list"]` (load-bearing: user-action.tsx and the wizard both invalidate it). Map the normalized result into the table's data array (`data.items`) and, if the current table reads `res.data` directly, adapt to `NormalizedList` (page computes `items` — search/pagination/sorting stay client-side via the existing `UserDataTable`).
    2. Add a "Create Employee" Button (variant default/accent per UI-SPEC) in the directory header, `onClick` → `router.push("/register")` (the admin route group already hosts the 4-step wizard). Wrap it in `<RoleAccess allow={(r) => can(r, "write", "account")} fallback={null}>` — admins and managers both satisfy `account:write`, matching the backend gate. Do NOT create a new wizard — the existing register flow is the guided form.
    3. Keep the existing client-side search (name column filter), sorting, and `PaginationFooter` untouched — ADMN-02 is satisfied by what the table already does; only the data source changes.
  </action>
  <acceptance_criteria>
    - Source assertion: `features/users/page.tsx` imports and calls `getAdminUsers` from "@/services/admin.api" inside the `["user","list"]` queryFn; no bare `apiFetch("/api/admin/users` remains in the file
    - Source assertion: a "Create Employee" Button exists in the page header, wrapped in RoleAccess gated by `can(r, "write", "account")`, navigating to `/register`
    - Source assertion: queryKey `["user","list"]` unchanged
    - Gates: `pnpm lint` && `pnpm typecheck` pass; `pnpm test` full suite still green
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>Directory loads through the typed service; Create Employee CTA visible to admins/managers and opens the existing wizard; full gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (register) | Untrusted onboarding payload crosses here; server holds the MongoDB transaction + password gen |
| client → API (directory) | Unpaginated user array crosses here; Redis-cached 7 days on the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | register wizard payload/`registerAccount` | mitigate | Send ONLY the `{user, account, bank}` triplet (backend schema strict) — no extra PII fields invented; zod schemas validate before send; `apiFetch` is the only HTTP path (no bare fetch) |
| T-06-01-02 | Elevation of Privilege | Create Employee CTA | mitigate | Gate with `can(r, "write", "account")` via RoleAccess (mirrors backend `account:write` admin+manager); server `authorize()` remains the enforcement boundary |
| T-06-01-03 | Information Disclosure | directory list response | accept | Directory rows render no KYC/account fields (backend list has none); the 7-day Redis cache is backend-owned; browser cache is TanStack default (no `cacheTime` extension). Full masking of account numbers is enforced in plan 06-02 |
| T-06-01-04 | Tampering | employeeType enum | mitigate | Enum pinned to the backend five-value list + tested (register-schema.test.ts); prevents invalid `employeeType` values reaching the strict backend schema |
| T-06-01-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — package legitimacy gate not triggered (RESEARCH: zero new packages this phase) |
</threat_model>

<verification>
- `pnpm lint` (ESLint, no-console rule) passes
- `pnpm typecheck` passes
- `pnpm test` — new `tests/register-schema.test.ts` + `tests/admin-api.test.ts` pass; full suite still green
- Contract spot-check: `../saher-backend/src/admin/**` route/field names still match what the schemas mirror (rerun only if backend changed since 2026-08-29)
</verification>

<success_criteria>
- Admin creates an employee with `employeeType` free or intern via the existing wizard → single `POST /api/admin/account` succeeds and the directory (["user","list"]) refreshes showing the new row
- Directory is searchable (name filter) and paginated client-side from a typed, tested service function
- No registration path bypasses `registerAccount`/apiFetch
</success_criteria>

<output>
Create `.planning/phases/06-admin-bank-accounts-events-depth/06-01-SUMMARY.md` when done
</output>