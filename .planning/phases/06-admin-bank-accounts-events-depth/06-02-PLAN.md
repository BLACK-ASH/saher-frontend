---
phase: 06-admin-bank-accounts-events-depth
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - services/admin.api.ts
  - hooks/use-admin.ts
  - features/admin/account-edit.tsx
  - features/admin/bank-details.tsx
  - app/(main)/(manager)/users/[id]/page.tsx
  - features/users/column.tsx
  - tests/admin-bank-api.test.ts
autonomous: true
requirements: [ADMN-03, ADMN-04, ADMN-05]

must_haves:
  truths:
    - "An admin/manager opens an employee and views account details; account edit saves via strict-partial PUT (account fields only)"
    - "A manager (not admin) can create/edit/restore bank details; account numbers are masked (•••• last4) in every non-editing rendering"
    - "No bank delete control exists anywhere (no role holds delete,bank — backend 403s; ADMN-04 accepted as restore-only per user ruling)"
    - "Users can still soft-delete and restore employees from the directory without data loss"
  artifacts:
    - path: "features/admin/bank-details.tsx"
      provides: "bank create/edit/restore form + maskAccount export"
      contains: "maskAccount"
    - path: "features/admin/account-edit.tsx"
      provides: "account edit drawer/form, strict-partial submit"
      contains: "updateAccount"
    - path: "hooks/use-admin.ts"
      provides: "account detail query + updateAccount/createBank/updateBank/restoreBank mutations (invalidation-only, D-29)"
      exports: ["useAdminAccount", "useBankMutations"]
  key_links:
    - from: "features/admin/bank-details.tsx"
      to: "services/admin.api.ts"
      via: "createBank/updateBank/restoreBank calls"
    - from: "features/users/column.tsx"
      to: "features/admin/bank-details.tsx"
      via: "maskAccount import for masked cell rendering"
    - from: "app/(main)/(manager)/users/[id]/page.tsx"
      to: "hooks/use-admin.ts"
      via: "account detail query + edit drawer wiring"
---

<objective>
Account and bank details become manageable per employee with the verified strict-partial contract, masking applied to every list rendering, and manager-only bank write gating. Employee soft-delete/restore is verified in place.

Purpose: ADMN-03 (view+edit account), ADMN-04 (bank CRUD, masked) and ADMN-05 (soft-delete/restore users) complete the admin people-management surface. RESEARCH verified: `PUT /api/admin/account/:id` accepts ONLY `accountBaseSchema.partial().strict()` (sending `user`/`bank` 400s); bank create/update/restore are manager-gated (`bank:write`/`bank:update`); bank DELETE requires `delete,bank` which NO role holds — so no delete control is built. **USER RULING (maker note):** ADMN-04's "delete bank details" sub-element is ACCEPTED as satisfied by create/view/update/restore + masking — no role holds `delete,bank` (every role 403), so NO delete control is added. This restore-only scope is the accepted interpretation and is recorded here. Bank/account are money/KYC mutations: D-29 (no optimistic writes, invalidate-only) and D-26 (disable submit while pending) apply.
Output: `services/admin.api.ts` account/bank endpoints, `hooks/use-admin.ts`, account-edit + bank-details forms, masked directory cell, verified delete/restore flows.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-RESEARCH.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-PATTERNS.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md
@services/admin.api.ts
@hooks/use-payroll.ts
@features/register/bank-details.tsx
@features/register/register-schema.ts
@app/(main)/(manager)/users/[id]/page.tsx
@features/users/user-action.tsx
@features/users/column.tsx

Contract checks performed during RESEARCH (confidence HIGH, verified against `../saher-backend/src/**`):
- `GET /api/admin/user/:id` returns populated `{user, account, bank}` — the existing detail view types it `AccountT`.
- `PUT /api/admin/account/:id` = `accountBaseSchema.partial().strict()` — account fields ONLY; `user`/`bank` keys 400.
- Bank: `POST /api/admin/bank`, `PUT /api/admin/bank/:id`, `PATCH /api/admin/bank/restore/:id` — gated `bank:write`/`bank:update` = **manager only** (admin has `bank:read` only).
- `DELETE /api/admin/bank/:id` requires `delete,bank` — **no role holds it** (verified `role-permission.ts`). Do NOT build a delete control. **User ruling:** ADMN-04's delete sub-element is accepted as restore-only (create/view/update/restore + masking); the absence of a delete control is the accepted interpretation, not a gap to surface.
- User soft-delete `DELETE /api/admin/user/:id` (self-delete blocked) + restore `PATCH /api/admin/user/:id/restore` — already implemented in `features/users/user-action.tsx`.

<interfaces>
<!-- Key contracts the executor needs. Verified from current source. -->

From hooks/use-payroll.ts (hook style for money/KYC — the D-29 invalidation-only pattern):
```typescript
export const usePayroll = (filters, page = 1) => {
  const queryClient = useQueryClient();
  const invalidate = () => { queryClient.invalidateQueries({ queryKey: ["payroll"] }); };
  const list = useQuery({ queryKey: ["payroll", "list", filters, page], queryFn: () => getPayrollList(filters, page) });
  const payInstallment = useMutation({ mutationFn: ..., onSuccess: invalidate });
  return { list, payInstallment, runCron };
};
```

From hooks/use-profile.ts (types to reuse — note `employeeType` gained "free"|"intern" in plan 06-01):
```typescript
export type BankT = { readonly accountHolderName: string; readonly bankName: string; readonly accountNumber: string; readonly ifcs: string; readonly branch: string; readonly mobileNumber: string; readonly id: string; };
export type AccountT = { readonly gender: "male"|"female"|"other"; readonly dateOfBirth: Date; readonly dateOfJoining: Date; readonly phoneNumber: string; readonly employeeId: string; readonly department: string; readonly designation: string; readonly employeeType: "free"|"intern"|"full-time"|"part-time"|"volunteer"; readonly salaryStructure: string; readonly address: string; readonly id: string; readonly user: UserT; readonly bank: BankT; readonly employeeShift?: "shift-1"|"shift-2"; readonly aadhar/pan/resume: {id,alt,src}; };
```

From features/register/register-schema.ts (REUSE the bank form schema for the bank edit — do not redefine it):
```typescript
export const bankDetailSchema = z.object({ accountHolderName, bankName, accountNumber, ifcs (IFSC regex, uppercased), branch, mobileNumber (+91 strip) });
```

Existing detail view (app/(main)/(manager)/users/[id]/page.tsx:27-31):
```typescript
const data = useQuery({ queryKey: [...], queryFn: async () => { const res = await apiFetch<AccountT>(`/api/admin/user/${id}`); return res.data; } });
// renders <Field label="Employee Type" value={account.employeeType} /> (line 131) and <Field label="Account Number" value={bank.accountNumber} /> (line 152 — UNMASKED today)
```

Existing user delete/restore (features/users/user-action.tsx): mutations DELETE `/api/admin/user/${id}` and PATCH `/api/admin/user/${id}/restore`, invalidate ["user","list"], AlertDialog confirms — VERIFY only, do not rebuild.

From components/role-access.tsx: `RoleAccess` props = `{ children, allow: (role) => boolean, fallback?, loading? }` — bank form gate: `allow={(r) => can(r, "write", "bank") || can(r, "update", "bank")}`; account gate: `allow={(r) => can(r, "update", "account")}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend services/admin.api.ts with account/bank endpoints + create hooks/use-admin.ts with tests</name>
  <files>services/admin.api.ts, hooks/use-admin.ts, tests/admin-bank-api.test.ts</files>
  <read_first>
    - services/admin.api.ts (created in plan 06-01 — extend, keep throw-on-failure style)
    - hooks/use-payroll.ts (invalidation-only hook pattern, D-29)
    - ../saher-backend/src/admin/account/{schema,controller}.ts and ../saher-backend/src/admin/bank/{schema,controller}.ts — THE CONTRACT SOURCE for the strict-partial account schema and bank body fields (research verified `accountBaseSchema.partial().strict()` and bank create/update/restore; confirm exact field names if these files moved under ../saher-backend/src/admin/**)
    - tests/payroll-api.test.ts (msw test style)
  </read_first>
  <action>
    Per RESEARCH §Code Examples + PATTERNS rows for `services/admin.api.ts` and `hooks/use-admin.ts`:
    1. In `services/admin.api.ts` add (all via `apiFetch`, throw-on-failure style, zod schemas where a body/response is parsed):
       - `getAdminUserDetail = async (id: string): Promise<AccountT>` — GET `/api/admin/user/${id}`, return `res.data`. Define/reuse a zod schema only if you parse the response; otherwise type the return as `AccountT` from "@/hooks/use-profile" (existing detail page already relies on this shape).
       - `updateAccount = async ({ id, data }: { id: string; data: Partial<AccountFields> })` — PUT `/api/admin/account/${id}` with the account fields ONLY (see step 2 for the exact allowlist). No user/bank keys — the backend `.strict()` rejects them.
       - `createBank = async (data: BankInput)` — POST `/api/admin/bank`; `updateBank = async ({ id, data })` — PUT `/api/admin/bank/${id}`; `restoreBank = async (id: string)` — PATCH `/api/admin/bank/restore/${id}`. Type `BankInput` from `bankDetailSchema` (features/register/register-schema.ts) — do NOT redefine the bank schema.
       - `maskAccount = (num: string): string` — export from `features/admin/bank-details.tsx` (per PATTERNS: inline tiny util, not a lib file) — so define it in Task 2/3; Task 1's tests import it from there.
    2. Define an exported `accountUpdateSchema` (zod) in admin.api.ts mirroring `accountBaseSchema.partial().strict()`: exactly `gender, dateOfBirth, dateOfJoining, phoneNumber, secondaryPhoneNumber, employeeId, department, designation, employeeType, employeeShift, salaryStructure, address, bank` all `.optional()`, `.strict()` (unknown keys rejected) — reuse the enum/refine semantics from `features/register/register-schema.ts` (`part-time ⇒ employeeShift` refine; the five-value employeeType). Export `type AccountUpdateInput = z.infer<...>`.
    3. Create `hooks/use-admin.ts`: queryKey `["user","list"]` for the directory query via `getAdminUsers` (reuse the 06-01 service — the directory page and user-action.tsx invalidations stay coherent); account detail query `["admin","account", id]` enabled on `!!id` via `getAdminUserDetail`; mutations `updateAccount`, `createBank`, `updateBank`, `restoreBank` each `onSuccess` invalidating BOTH `["admin"]` and `["user"]` prefixes (bank/account changes must refresh the directory detail surfaces). NO optimistic cache writes (D-29). Export a flat object bag per hook-convention (`useAdminUsers`, `useAdminAccount(id)`, `useBankMutations()`).
    4. Create `tests/admin-bank-api.test.ts` (msw): (a) `updateAccount` PUTs to `/api/admin/account/:id` with a body that contains NO `user` and NO `bank` top-level keys (assert `Object.keys(JSON.parse(body))` — pins the strict-partial 400 guard); (b) `createBank` POSTs to `/api/admin/bank`; (c) `restoreBank` PATCHes `/api/admin/bank/restore/:id`; (d) `accountUpdateSchema.safeParse({ user: {...}, bank: {...} })` fails (strict), and a valid account-only payload passes; (e) `maskAccount("1234567890")` returns `"•••• 7890"` and `maskAccount("12")` returns `"••••"` (import from features/admin/bank-details.tsx — Task 3 must export it before this test runs; if Task 3 ordering is an issue, put the util export in Task 1 as a minimal `features/admin/bank-details.tsx` stub and flesh the form in Task 3 — see plan task ordering note).
  </action>
  <acceptance_criteria>
    - Source assertion: services/admin.api.ts exports `getAdminUserDetail`, `updateAccount`, `createBank`, `updateBank`, `restoreBank`, `accountUpdateSchema`; updateAccount body path contains no `user:`/`bank:` keys
    - Source assertion: hooks/use-admin.ts exports `useAdminUsers`, `useAdminAccount`, `useBankMutations`; every mutation uses `invalidateQueries` (no `onMutate`/optimistic writes)
    - Source assertion: accountUpdateSchema is `.strict()` (rejects `user`/`bank` keys)
    - Test command: `pnpm test tests/admin-bank-api.test.ts` passes all 5+ tests
    - Gates: `pnpm lint` && `pnpm typecheck` pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/admin-bank-api.test.ts && pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>Account/bank service endpoints + strict-partial schema + invalidation-only hook exist and are msw-tested; maskAccount unit-tested (exported from features/admin/bank-details.tsx, form fleshed in Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Account edit drawer wired to the employee detail page (ADMN-03)</name>
  <files>features/admin/account-edit.tsx, app/(main)/(manager)/users/[id]/page.tsx</files>
  <read_first>
    - features/register/employee-details.tsx (the account-field form pattern: Controllers, Field/FieldLabel/FieldError, aria-invalid, Select for employeeType)
    - app/(main)/(manager)/users/[id]/page.tsx (the existing detail view — where the Edit Account button + drawer mount)
    - features/register/register-schema.ts (account field semantics + five-value employeeType + part-time refine)
    - components/ui/dialog.tsx or sheet.tsx (existing drawer primitive — pick the one already used for edit flows in this app)
    - lib/date.ts (IST display for dateOfBirth/dateOfJoining — `formatIstDate`/`isoToIstInput`; never raw Date rendering)
  </read_first>
  <action>
    Per RESEARCH ADMN-03 + UI-SPEC §Account & Bank Details:
    1. Create `features/admin/account-edit.tsx`: a client form/dialog component that receives `account: AccountT` and `open/onOpenChange` props. Build with react-hook-form + zodResolver over `accountUpdateSchema` (from admin.api.ts). Fields: gender (Select), dateOfBirth + dateOfJoining (date inputs prefilled via `isoToIstInput`), phoneNumber, secondaryPhoneNumber (optional), employeeId, department, designation, employeeType (Select with the FIVE values), employeeShift (Select, only shown/required when employeeType === "part-time" — honor the schema refine), salaryStructure, address. Submit calls `updateAccount.mutate({ id: account.id, data: values })` from `useBankMutations`-adjacent exposure; disable submit while `isPending` (D-26, show "Saving…"); on success close the dialog (invalidate refreshes the detail view). NO bank or user fields in this form (strict 400 guard).
    2. In `app/(main)/(manager)/users/[id]/page.tsx`: add an "Edit Account" Button (copy per UI-SPEC) in the account card header, wrapped in `<RoleAccess allow={(r) => can(r, "update", "account")}>` (admin+manager). Render the account-edit dialog when clicked. Keep the existing account/bank Field rendering for view mode.
    3. Dates: the existing detail view renders `dateOfBirth`/`dateOfJoining` — ensure they render through `formatIstDate` (lib/date.ts) if they currently render raw (fix if raw).
  </action>
  <acceptance_criteria>
    - Source assertion: features/admin/account-edit.tsx exists; submit payload shape = account fields only (no `user`, no `bank` keys); submit disabled while `isPending` with "Saving…" label
    - Source assertion: employeeType Select lists exactly free/intern/full-time/part-time/volunteer; employeeShift field is required only for part-time
    - Source assertion: users/[id]/page.tsx contains an "Edit Account" button gated by `can(r, "update", "account")` opening the component
    - Source assertion: no raw `new Date(...).toLocaleString`-style rendering of account dates — date display routes through lib/date.ts functions
    - Behavior assertion: `pnpm typecheck` passes (dialog prop types compile)
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>Account detail is viewable and editable per employee via strict-partial PUT; dialog gating and IST date rendering verified.</done>
</task>

<task type="auto">
  <name>Task 3: Bank details form with masking + manager gating; verify soft-delete/restore (ADMN-04, ADMN-05)</name>
  <files>features/admin/bank-details.tsx, features/users/column.tsx, app/(main)/(manager)/users/[id]/page.tsx (extension)</files>
  <read_first>
    - features/register/bank-details.tsx (the exact bank form: Controller + Field/FieldLabel/FieldError + aria-invalid pattern over bankDetailSchema)
    - features/admin/bank-details.tsx (created in Task 1 as the maskAccount stub — flesh it out here)
    - features/users/user-action.tsx (existing delete/restore — VERIFY, do not rebuild)
    - components/role-access.tsx + lib/permissions.ts
    - .planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md §Account & Bank Details and §User Soft-Delete & Restore
  </read_first>
  <action>
    Per RESEARCH Pitfall 2 + Open Q1 + UI-SPEC — bank writes are manager-only, NO delete control:
    1. Complete `features/admin/bank-details.tsx`: export `maskAccount` (already stubbed in Task 1 — keep its exact behavior and signature). Add the bank create/edit form reusing `bankDetailSchema` from "@/features/register/register-schema" — fields exactly as the register wizard renders them (accountHolderName, bankName, accountNumber, ifcs, branch, mobileNumber) with the same Controller/Field/FieldError pattern. For edit mode prefill the values (including the FULL accountNumber — the edit input is the one place the full number appears). For create mode send `createBank(data)`; edit mode send `updateBank({ id, data })`. Wrap the form + any restore affordance in `<RoleAccess allow={(r) => can(r, "write", "bank") || can(r, "update", "bank")}>` and show the muted note "Bank details can only be changed by managers." (UI-SPEC copy). "Add Bank Details" and "Save Bank Details" CTAs per UI-SPEC. Disable submit while pending (D-26). Do NOT render any delete/trash control for banks — the backend 403s for every role. **USER RULING (maker note, no code change):** ADMN-04's "delete bank details" is ACCEPTED as restore-only — create/view/update/restore + masking satisfy it; no role holds `delete,bank`. Record this accepted interpretation (do not build a delete control, do not surface a delete gap).
    2. Restore path: when the fetched bank response exposes a soft-deleted state (e.g. `isDeleted: true` / present-but-inactive per the live `admin/bank` response shape — verify field against `../saher-backend/src/admin/_services/bank.ts`), render a "Restore Bank" button (copy per UI-SPEC) calling `restoreBank(id)`. If the backend bank response has no deletable-state field at all, skip the restore affordance entirely (contract-honest; note in the SUMMARY which case was found).
    3. Masking (ADMN-04, applies to LISTS only): in `app/(main)/(manager)/users/[id]/page.tsx`, change the `<Field label="Account Number" value={bank.accountNumber} />` rendering (line ~152) to `value={maskAccount(bank.accountNumber)}` (import from features/admin/bank-details.tsx). In `features/users/column.tsx` — add an "Account" column only if the directory rows carry bank data (they do NOT today — the `/admin/users` list response has no bank; if the live column data still lacks it, skip the column and note why in the SUMMARY; masking in the employee detail view is then the enforcement point). Wherever an accountNumber renders OUTSIDE the bank edit input, it MUST pass through `maskAccount`. Grep-gate: `rg "accountNumber}" features/ app/` after the change — any JSX render of the raw field outside features/admin/bank-details.tsx fails the gate.
    4. ADMN-05 verification (no rebuild): confirm `features/users/user-action.tsx` delete/restore mutations + AlertDialog confirm flow exist on the directory (they do today); verify the delete confirm copy matches UI-SPEC ("This permanently removes the employee. This cannot be undone." for permanent delete) and the isActive/deleted badges render per column.tsx. If copy differs, adjust strings only. The 403-guard nuance (D-30: backend restore may 403 for staff role) needs no frontend change — note observed behavior in the SUMMARY.
  </action>
  <acceptance_criteria>
    - Source assertion: features/admin/bank-details.tsx exports `maskAccount` and renders the bank form gated by `can(r, "write", "bank") || can(r, "update", "bank")` with the manager-only note string exactly "Bank details can only be changed by managers."
    - Source assertion: NO file in features/ or app/ renders `bank.accountNumber` raw outside features/admin/bank-details.tsx (grep gate: `rg "accountNumber}" features/ app/` matches only bank-details.tsx)
    - Source assertion: no delete/trash control for banks exists (grep `bank.*delete|deleteBank` in features/app returns no matches)
    - Behavior assertion: `maskAccount("1234567890")` === "•••• 7890" (existing test still passes)
    - Source assertion: features/users/user-action.tsx delete + restore mutations remain present (ADMN-05 flow intact)
    - Gates: `pnpm lint` && `pnpm typecheck` pass; updating the users/[id] view to masked rendering compiles
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test && rg -n 'accountNumber}' features/ app/ | rg -v 'features/admin/bank-details.tsx' | wc -l</automated>
  </verify>
  <done>Bank create/edit/restore (manager-only) live; account numbers masked in every non-editing render; ADMN-04 restore-only scope recorded per user ruling; directory delete/restore verified intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (account/bank writes) | KYC + bank payload crosses here; server enforces `bank:write`/`update` role gates |
| client → API (user delete/restore) | Destructive user mutation crosses here; server blocks self-delete |
| render layer | Bank account numbers and KYC docs render on screen — masking boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | accountNumber rendering | mitigate | `maskAccount` on every non-editing render; raw value only inside the bank edit input; grep gate `rg "accountNumber}"` enforces zero raw renders outside features/admin/bank-details.tsx |
| T-06-02-02 | Elevation of Privilege | bank create/edit/restore | mitigate | forms gated `can(r,"write"\|"update","bank")` (manager-only) via RoleAccess; server `authorize("bank:write")` is the enforcement boundary; admins see no bank write affordances (Pitfall 2) |
| T-06-02-03 | Tampering | updateAccount payload | mitigate | `accountUpdateSchema` `.strict()` rejects `user`/`bank` keys client-side (msw test pins it); backend `.partial().strict()` double-enforces — prevents cross-resource write smuggling |
| T-06-02-04 | Denial of Service / Tampering | delete/restore user | mitigate | existing AlertDialog confirms (incl. permanent-delete copy); backend self-delete block + isActive guard; restore gated `update,user` server-side (D-30 403 cases noted, not hidden) |
| T-06-02-05 | Information Disclosure | logging/toasts | mitigate | no `console.log` (lint error); `apiFetch` toasts server messages which do not echo account numbers; `logError` context never includes bank numbers — do not add them |
| T-06-02-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — package legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm lint`, `pnpm typecheck`, `pnpm test` all pass (new tests/admin-bank-api.test.ts included)
- Grep gate: `rg -n 'accountNumber}' features/ app/` matches ONLY features/admin/bank-details.tsx
- Grep gate: no `deleteBank`/bank-trash affordance exists anywhere
- Contract spot-check: `../saher-backend/src/admin/bank/_services*` field for bank soft-delete state confirmed; if no such field, restore affordance omitted per plan
</verification>

<success_criteria>
- Employee detail shows account (IST dates) + masked bank number; Edit Account saves account-only fields and reflects after invalidation
- Manager adds/edits/restores bank details; admin sees read-only bank info with the manager-only note; masked display everywhere except the edit input
- No bank delete control shipped (user accepted restore-only per ADMN-04 ruling — every role 403s on delete,bank)
- Directory delete/restore of users unchanged and functional
</success_criteria>

<output>
Create `.planning/phases/06-admin-bank-accounts-events-depth/06-02-SUMMARY.md` when done
</output>