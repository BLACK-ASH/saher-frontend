---
phase: 06-admin-bank-accounts-events-depth
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - services/program.api.ts
  - hooks/use-programs.ts
  - features/program/program/all-programs.tsx
  - features/program/program/program-header.tsx
  - features/program/program/add-program.tsx
  - features/program/program/program-editor.tsx
  - app/(main)/program/page.tsx
  - app/(main)/program/[id]/page.tsx
  - tests/program-api.test.ts
autonomous: true
requirements: [EVNT-01, EVNT-02]

must_haves:
  truths:
    - "A manager/admin browses the programs list with active/trash tabs, creates/edits programs, and soft-deletes + restores them"
    - "A program detail page offers clear drill-down navigation toward the program's workshops and sessions"
    - "Attaching participants later works (the attach body is fixed to {participantIds})"
  artifacts:
    - path: "services/program.api.ts"
      provides: "restoreProgram + fixed addParticipantsInProgram body + isDeleted list param"
      contains: "participantIds"
    - path: "hooks/use-programs.ts"
      provides: "restore mutation invalidating ['programs']"
      exports: ["restore"]
    - path: "features/program/program/all-programs.tsx"
      provides: "active|trash tabs + create CTA + search"
      contains: "TrashTabPattern"
  key_links:
    - from: "features/program/program/all-programs.tsx"
      to: "services/program.api.ts"
      via: "getPrograms({ keyword, page, isDeleted }) + restoreProgram"
    - from: "app/(main)/program/[id]/page.tsx"
      to: "/program/workshops?keyword=<programId>"
      via: "drill-down links"
---

<objective>
The programs level reaches full CRUD + trash/restore depth with drill-down navigation, and the two contract bugs that break rosters and lists (wrong attach body, missing `isDeleted` filter) are fixed at the service layer.

Purpose: EVNT-01 (drill-down) and EVNT-02 (program CRUD with trash/restore) start the events depth. RESEARCH verified: `POST /api/events/programs/participants/:id` expects `{participantIds[]}` but the frontend sends a raw array (broken today — fixed here so EVNT-05 works); restore = `PATCH /api/events/programs/restore/:id`; list supports `keyword` (regex title/description + ObjectId) and `isDeleted` filtering. Delete requires `delete,event` (admin only); create/edit/restore = admin+manager. Programs use the "sloppy" `isDeleted` cast — frontend must send explicit `isDeleted=false|true` (Pitfall 10).
Output: contract-correct program service + restore hook + program list/detail UI with trash tabs and drill-down.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-RESEARCH.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-PATTERNS.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md
@services/program.api.ts
@hooks/use-programs.ts
@features/program/program/all-programs.tsx
@app/(main)/program/[id]/page.tsx
@components/shared/trash-tab-pattern.tsx

Contract checks performed during RESEARCH (confidence HIGH, verified against `../saher-backend/src/**`):
- `POST /api/events/programs/participants/:id` body = `{participantIds: string[]}` ($addToSet); current frontend sends `JSON.stringify(participants)` → broken.
- `PATCH /api/events/programs/restore/:id` — restore; delete = `DELETE /api/events/programs/:id` (admin-only `delete,event`); create/edit/restore admin+manager.
- `GET /api/events/programs?keyword=&page=&limit=&isDeleted=` — isDeleted param is cast `(query.isDeleted as boolean) || false` (sloppy — string "false" is truthy, rescued by Mongoose boolean cast → always send explicit `"false"`/`"true"` strings).
- `GET /api/events/programs/:id` returns PARTICIPANTS POPULATED (`SingleParticipantT`) — the roster source of truth (Pitfall 3).

<interfaces>
<!-- Key contracts the executor needs. Verified from current source. -->

From services/program.api.ts (current — Task 1 modifies):
```typescript
export type ProgramsT = { id: string; title: string; description: string; };
export type SingleParticipantT = ProgramsT & { participants: ParticipantT[] };
export type QueryProps = { keyword?: string; limit?: number; page?: number; };
export const getPrograms = async ({ keyword, page = 1, limit = 10 }: QueryProps) // → add isDeleted
export const getSingleProgram = async (id: string) // → SingleParticipantT (populated participants — KEEP as roster source)
// line 77-90 — THE BUG:
export const addParticipantsInProgram = async ({ id, participants }: { id: string; participants: string[] }) =>
  apiFetch("/api/events/programs/participants/" + id, { method: "POST", body: JSON.stringify(participants) });
// → body: JSON.stringify({ participantIds: participants })
```

From hooks/use-programs.ts (current exports): `{ programs: list, program: detail, add, update, del, participants, addParticipants, removeParticipant }` where `participants` query = getParticipantFromProgram (raw ObjectIds — roster UI must NOT use it; 06-05/06-06 handle remaining consumers).

From components/shared/trash-tab-pattern.tsx:
```typescript
type TrashTabPatternProps = { title: string; description: string; children?: React.ReactNode; };
export function TrashTabPattern({ title, description, children }: TrashTabPatternProps)
```

From lib/normalize-list.ts: `NormalizedList<T> = { items, page, limit, totalPages, totalRecords }` — the list hook's `list.data` is this shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix program service contract (attach body, restore, isDeleted) + hook restore mutation + tests</name>
  <files>services/program.api.ts, hooks/use-programs.ts, tests/program-api.test.ts</files>
  <read_first>
    - services/program.api.ts (the bugged addParticipantsInProgram at lines 77-90; deleteProgram at 69-75 as the restore-pattern analog)
    - hooks/use-programs.ts (base hook + addParticipants/removeParticipant mutation pattern at lines 48-60)
    - ../saher-backend/src/events/program/{program.controller,program.schema,program-participant.controller}.ts — THE CONTRACT SOURCE for the {participantIds} body and restore route (re-verify only if moved under ../saher-backend/src/events/**)
    - tests/payroll-api.test.ts (msw style with `server.use(...)` + envelope helper)
  </read_first>
  <action>
    Per RESEARCH §Code Examples (program section) + PATTERNS `services/program.api.ts` row:
    1. In `services/program.api.ts`: fix `addParticipantsInProgram` body from `JSON.stringify(participants)` to `JSON.stringify({ participantIds: participants })` (verified `addParticipantsToProgramSchema`). Keep signature `{ id, participants: string[] }`.
    2. Add `restoreProgram = async (id: string)` — PATCH `/api/events/programs/restore/${id}` via `apiFetch`, same error-handling style as the other fns in this file (keep the file's existing `if (!res.success) toast.error(...)` legacy style — do NOT churn it, per PATTERNS).
    3. Extend `getPrograms`: accept `isDeleted?: string` in the destructure and append `&isDeleted=${isDeleted}` to the URL (send explicitly; Pitfall 10 — never omit for list consistency).
    4. In `hooks/use-programs.ts`: add a `restore` mutation (mutationFn restoreProgram) with `onSuccess` invalidating `["programs"]` (mirror the `del` pattern). Return it in the hook bag.
    5. Create `tests/program-api.test.ts` (msw): (a) `addParticipantsInProgram({id, participants:["p1","p2"]})` POSTs body where `JSON.parse` has a top-level `participantIds` array equal to `["p1","p2"]` and NO other keys; (b) `restoreProgram("pg1")` hits `PATCH /api/events/programs/restore/pg1`; (c) `getPrograms({keyword:"x", isDeleted:"true"})` URL contains `isDeleted=true` (and `isDeleted=false` when passed false).
  </action>
  <acceptance_criteria>
    - Source assertion: services/program.api.ts addParticipantsInProgram body is `JSON.stringify({ participantIds: participants })`
    - Source assertion: services/program.api.ts exports `restoreProgram` calling PATCH `/api/events/programs/restore/${id}`
    - Source assertion: `getPrograms` URL template includes `isDeleted=${isDeleted}` when provided
    - Source assertion: hooks/use-programs.ts returns `restore` in the hook bag
    - Test command: `pnpm test tests/program-api.test.ts` passes (participantIds body assertion is mandatory)
    - Gates: `pnpm lint` && `pnpm typecheck` pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/program-api.test.ts && pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>Attach body matches the verified schema; restoreProgram + isDeleted list filtering exist and are msw-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Programs list — active/trash tabs, create CTA, search, restore (EVNT-02)</name>
  <files>features/program/program/all-programs.tsx, features/program/program/program-header.tsx, features/program/program/add-program.tsx, features/program/program/program-editor.tsx, app/(main)/program/page.tsx</files>
  <read_first>
    - features/program/program/all-programs.tsx (current list rendering — likely a plain keyword search + table/cards; reconcile, do not rewrite blindly)
    - features/program/program/add-program.tsx and program-editor.tsx (existing create/edit forms — verify fields title/description; description is HTML via tiptap — keep)
    - features/noticeboard or features/attendance-correction/corrections — whichever already uses TrashTabPattern + isDeleted tabs as the reference implementation (grep `TrashTabPattern` usage in features/)
    - components/shared/trash-tab-pattern.tsx + components/shared/pagination-footer.tsx
    - lib/normalize-list.ts (list.data shape `{items,...}`)
    - .planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md §Events Hierarchy Drill-Down (copy: "Create Program", "No programs yet", TrashTabPattern guidance)
  </read_first>
  <action>
    Per UI-SPEC §Events Hierarchy Drill-Down + PATTERNS #6 (CRUD + trash/restore tabs):
    1. Rework `all-programs.tsx`'s data path: the program list must request with explicit `isDeleted` (per tab) and render `list.data.items`. Add Active|Deleted tab state (follow the existing TrashTabPattern consumer reference found in read_first — active tab passes `isDeleted: "false"`, deleted tab `isDeleted: "true"`). Keep keyword search bound to the existing search box (pass `keyword` through `getPrograms`).
    2. Row actions: edit (opens existing program-editor), soft-delete (AlertDialog confirm per UI-SPEC destructive-only rule), restore (the new `restore` hook mutation) on deleted rows. Delete button only for roles with `can(r, "delete", "event")` (admin only per RESEARCH) — gate with RoleAccess; create/edit/restore visible to admin+manager (`can(r,"write"|"update","event")` — verify the exact permission strings in lib/permissions.ts and use them).
    3. Create CTA: "Create Program" (accent) opening the existing add-program form; keep the existing editor/upload flow in place (title + tiptap description per the existing components — do not redesign).
    4. Empty states per UI-SPEC: "No programs yet — the first step is to create one." (map to NoData component copy).
  </action>
  <acceptance_criteria>
    - Source assertion: all-programs.tsx requests `getPrograms` with an `isDeleted` value switched by Active|Deleted tabs and renders `items` from the normalized list
    - Source assertion: a "Create Program" CTA opens the existing add-program form; restore action calls the hook `restore` mutation; delete action is gated by `can(r, "delete", "event")`
    - Source assertion: restore/delete row actions exist per tab (delete on active rows, restore on deleted rows)
    - Behavior assertion: switching tabs changes the `isDeleted` query param (observable in the hook queryKey or the request URL)
    - Gates: `pnpm lint` && `pnpm typecheck` pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>Programs list: active/trash tabs, search, create/edit, gated delete, restore — all wired to the contract-correct service.</done>
</task>

<task type="auto">
  <name>Task 3: Program detail drill-down navigation (EVNT-01)</name>
  <files>app/(main)/program/[id]/page.tsx, features/program/program/program-header.tsx</files>
  <read_first>
    - app/(main)/program/[id]/page.tsx (current detail — what it renders today: description, images, any workshop/session links)
    - app/(main)/program/page.tsx (the list shell that links into [id])
    - .planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md §Events Hierarchy Drill-Down (breadcrumb Program › Workshop › Session; filter by parent via keyword=<parentId>)
  </read_first>
  <action>
    Per EVNT-01 + UI-SPEC §Events Hierarchy Drill-Down:
    1. On `app/(main)/program/[id]/page.tsx`: add/confirm a breadcrumb (Home › Programs › {program.title}) using the existing breadcrumb UI component if present (components/ui/breadcrumb exists per UI-SPEC registry). 
    2. Add two explicit drill-down sections/links: "Workshops" linking to `/program/workshops?keyword=<programId>` (or the workshop index route the app already uses — verify the actual workshops route from the app router tree; adjust the href pattern to the real route, keeping `keyword=<programId>` as the filter opener) and "Sessions" linking to `/program/sessions?keyword=<programId>`. Pass the program id as the keyword param so the target level lists only this program's children (RESEARCH EVNT-01: per-program workshops via `?keyword=<programId>` — the regex matches ObjectId).
    3. If the detail page already lists children inline, KEEP that rendering and ADD the drill-down links as navigation shortcuts — do not remove existing content. If it renders the roster from the raw-ObjectId source (`participants` query from use-programs) leave it alone for plan 06-05 to migrate — but if it renders roster from `program.data.participants` already it is fine.
  </action>
  <acceptance_criteria>
    - Source assertion: program/[id]/page.tsx contains breadcrumb + links whose hrefs embed `keyword=` + the program id, targeting the workshops and sessions routes
    - Source assertion: the hrefs point at existing app routes (verified against the router tree in read_first)
    - Behavior assertion: clicking "Workshops" on a program detail lands on a workshops list filtered to that program (manual spot-check against the backend, or URL assertion `?keyword=<id>` in the link)
    - Gates: `pnpm lint` && `pnpm typecheck` pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>Program detail exposes breadcrumb + workshop/session drill-down links filtered by program id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (program mutations) | Program CRUD crosses here; server gates delete to `delete,event` (admin) |
| client → API (attach participants) | participantIds array crosses here; $addToSet merge server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Elevation of Privilege | program delete | mitigate | delete affordance gated `can(r,"delete","event")` (admin-only per backend matrix) via RoleAccess; server `authorize()` is the enforcement boundary |
| T-06-03-02 | Information Disclosure | trashed programs in active lists | mitigate | explicit `isDeleted=false` sent on active-tab list calls (Pitfall 10); trashed rows only render under the Deleted tab |
| T-06-03-03 | Tampering | addParticipantsInProgram body | mitigate | body fixed to the verified `{participantIds[]}` schema and pinned by an msw test asserting the exact JSON shape (prevents malformed arrays silently merged server-side) |
| T-06-03-04 | Tampering | restore/delete against soft-deleted rows | mitigate | trash tabs keep deleted rows read-only for edits until restored (Pitfall 8 — backend `findByIdAndUpdate` lacks isDeleted filters on edit; UI prevents the footgun) |
| T-06-03-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — package legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm lint`, `pnpm typecheck`, `pnpm test` pass (new tests/program-api.test.ts included)
- Grep gate: `rg "participantIds" services/program.api.ts` present; `rg "JSON.stringify\(participants\)" services/program.api.ts` absent
- Contract spot-check: `../saher-backend/src/events/program/**` route shape unchanged since 2026-08-29
</verification>

<success_criteria>
- Attach-participants POST carries `{participantIds}` (EVNT-05 substrate ready)
- Programs list: active/trash tabs, keyword search, create/edit, admin-gated delete, restore
- Program detail breadcrumb + workshop/session drill-down links filtered by program id
</success_criteria>

<output>
Create `.planning/phases/06-admin-bank-accounts-events-depth/06-03-SUMMARY.md` when done
</output>