---
phase: 06-admin-bank-accounts-events-depth
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - services/workshop.api.ts
  - services/session.api.ts
  - hooks/use-workshops.ts
  - hooks/use-sessions.ts
  - features/program/workshop/all-workshops.tsx
  - features/program/workshop/add-workshop.tsx
  - features/program/workshop/workshop-editor.tsx
  - features/program/workshop/workshop-header.tsx
  - features/program/session/all-sessions.tsx
  - features/program/session/add-session.tsx
  - features/program/session/session-editor.tsx
  - features/program/session/session-header.tsx
  - app/(main)/program/workshops/page.tsx
  - app/(main)/program/workshops/[id]/page.tsx
  - app/(main)/program/sessions/page.tsx
  - app/(main)/program/sessions/[id]/page.tsx
  - tests/workshop-session-api.test.ts
autonomous: true
requirements: [EVNT-03, EVNT-04]

must_haves:
  truths:
    - "A manager/admin creates, edits, soft-deletes, and restores workshops within a program"
    - "A manager/admin creates, edits, soft-deletes, and restores sessions with IST-correct future-dated datetime pickers and an explicit workshop"
    - "Workshop lists filter by program; session lists filter by workshop (drill-down by keyword=<parentId>)"
  artifacts:
    - path: "services/workshop.api.ts"
      provides: "restoreWorkshop + isDeleted list param"
      exports: ["restoreWorkshop"]
    - path: "services/session.api.ts"
      provides: "restoreSession + isDeleted list param"
      exports: ["restoreSession"]
    - path: "features/program/session/session-editor.tsx"
      provides: "IST date/time contract: isoToIstInput prefill + combineDateAndTimeToIso submit + explicit workshop"
      contains: "combineDateAndTimeToIso"
  key_links:
    - from: "features/program/session/session-editor.tsx"
      to: "lib/date.ts"
      via: "combineDateAndTimeToIso + isoToIstInput (never raw Date)"
    - from: "features/program/workshop/all-workshops.tsx"
      to: "services/workshop.api.ts"
      via: "getWorkshops({ keyword: programId, isDeleted })"
---

<objective>
Workshops and sessions reach full CRUD + trash/restore depth with parent-filtered drill-down lists and the IST-correct, future-dated session datetime contract.

Purpose: EVNT-03 (workshop CRUD within a program) and EVNT-04 (session CRUD with IST pickers). RESEARCH verified: `POST /api/events/workshops/:programId` (requires existing program), `PATCH /api/events/workshops/restore/:id`, sessions `POST /api/events/sessions/:programId` with `date`/`startTime` FUTURE-only, `endTime > startTime`, `workshop` optional but AUTO-CREATES a shallow workshop when omitted (Pitfall 7 — the UI must always pass an explicit workshop), `speaker` = ≥1 user ObjectId via the user-search picker. `updatedSessionSchema` also future-dates. The existing session editor already uses `combineDateAndTimeToIso` — this plan verifies and completes the contract (prefill on edit, explicit workshop, inline past-date 400 surfacing). Session restore lands here; reminder + export service fns land in 06-07 (file ownership: session.api.ts edited by 06-04 first, then 06-07).
Output: workshop/session services + hooks + list/detail/editor UI fully reconciled.
Scope note (checker WARNING 2): this plan carries 17 files across 3 tasks, but it is intentionally NOT split into separate workshop/session plans because Task 1 (restore/isDeleted service + hook + msw-tests) is SHARED infrastructure for BOTH surfaces — a split would duplicate that service layer and force an artificial dependency chain. Instead the plan already separates concerns across tasks (Task 1 = shared service layer, Task 2 = workshop UI [6 files], Task 3 = session UI [6 files]); each individual task stays within the 4-7-file guidance. Splitting would not reduce context cost enough to justify the duplicated service scaffolding, so this is kept as-is.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-RESEARCH.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-PATTERNS.md
@.planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md
@services/workshop.api.ts
@services/session.api.ts
@hooks/use-workshops.ts
@hooks/use-sessions.ts
@features/program/session/session-editor.tsx
@app/(main)/program/sessions/[id]/page.tsx

Contract checks performed during RESEARCH (confidence HIGH, verified against `../saher-backend/src/**`):
- Workshops: `POST /api/events/workshops/:programId`, `PUT /api/events/workshops/:id`, `DELETE /api/events/workshops/:id`, `PATCH /api/events/workshops/restore/:id`; list `?keyword&page&limit&isDeleted` (keyword matches program title/id — drill-down filter).
- Sessions: `POST /api/events/sessions/:programId`, `PUT/DELETE /api/events/sessions/:id`, `PATCH /api/events/sessions/restore/:id`; `date` and `startTime` must be future (400 "Date must be present or future"); `endTime > startTime`; `speaker` = `z.array(objectId()).min(1)` against **Users** (user-search picker); omitted `workshop` auto-creates a workshop named after the session (Pitfall 7 — always pass explicit workshop).
- List `isDeleted` param: workshops/sessions compare `=== 'true'` (send explicit `"false"`/`"true"` strings, Pitfall 10).
- Create/update sends notifications + push to speakers — double-submit protection matters (D-26).

<interfaces>
<!-- Key contracts the executor needs. Verified from current source. -->

From services/session.api.ts (current — Task 1 extends):
```typescript
export type SessionT = { id; title; program: {id,title}; workshop: {id,title}; description; date: string; startTime: string; endTime: string; speaker: DefaultUserT[]; images: {id,src,alt}[]; review?: string; participants?: ParticipantT[]; };
export type SessionCreateT = { title; workshop?: string; program: string; description; date: string; startTime: Date; endTime: Date; speaker: string[]; };
export const addSession = async ({ programId, data }: { programId: string; data: SessionCreateT }) // POST /api/events/sessions/:programId
export const updateSession = async ({ id, data }: { id; data: Partial<Omit<SessionT,"images"> & {images: string[]}> }) // PUT
export const deleteSession = async (id: string) // DELETE
// attendance mutations (mark/update/delete) exist at lines 105-157 — untouched by this plan (06-06 consumes)
```

From lib/date.ts (IST contract — read signatures before any date work):
```typescript
export const formatIstDate = ...            // display
export const formatIstDateTime = ...        // display
export const isoToIstInput = (iso: string | Date): string   // prefill date/time inputs
export const combineDateAndTimeToIso = ...  // → ISO string with +05:30 for submission
```

From features/program/session/session-editor.tsx (existing — verify + complete):
```typescript
// values.date is a yyyy-mm-dd string; submit maps:
//   startTime: new Date(combineDateAndTimeToIso(values.date, values.startTime)),
//   endTime: new Date(combineDateAndTimeToIso(values.date, values.endTime)),
// speaker: values.speaker.map((e: MailUser) => e.id.toString())
// WORKS: endTime input has min={form.watch("startTime")}
// GAPS TO CHECK: (a) edit-mode prefill via isoToIstInput — does [id]/page.tsx feed values? (b) is `workshop` always set? (c) is the past-date 400 surfaced inline?
```

From components/user-search-picker.tsx: `{ value: MailUser[]; onChange; multiple (true for speakers); ... }` — speakers select Users, not participants (A6).

From hooks/use-workshops.ts + use-sessions.ts: built on `createResourceListHook` (baseKey ["workshops"] / ["sessions"]) — add restore mutations invalidating the base key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workshop + session service restore/isDeleted + hook restore mutations + tests</name>
  <files>services/workshop.api.ts, services/session.api.ts, hooks/use-workshops.ts, hooks/use-sessions.ts, tests/workshop-session-api.test.ts</files>
  <read_first>
    - services/workshop.api.ts (deleteWorkshops at lines 71-77 as the restore-pattern analog)
    - services/session.api.ts (deleteSession at lines 97-103 as the analog; getSessions at 44-57 for the isDeleted param pattern)
    - hooks/use-workshops.ts and hooks/use-sessions.ts (createResourceListHook wiring)
    - ../saher-backend/src/events/workshop/workshop.controller.ts and ../saher-backend/src/events/session/session.controller.ts — THE CONTRACT SOURCE for restore routes + isDeleted list handling (re-verify only if they moved)
    - tests/payroll-api.test.ts (msw style)
  </read_first>
  <action>
    Per RESEARCH §Code Examples + PATTERNS rows for workshop.api.ts and session.api.ts:
    1. `services/workshop.api.ts`: add `restoreWorkshop = async (id: string)` — PATCH `/api/events/workshops/restore/${id}` (same error-handling style as siblings — keep the file's legacy `if (!res.success)` style, do not churn). Extend `getWorkshops` to accept + append `isDeleted` (`&isDeleted=${isDeleted}`) when provided.
    2. `services/session.api.ts`: add `restoreSession = async (id: string)` — PATCH `/api/events/sessions/restore/${id}`. Extend `getSessions` with the same explicit `isDeleted` param. DO NOT add reminder/export functions in this plan — 06-07 owns them (file-ownership split is sequential).
    3. `hooks/use-workshops.ts`: add `restore` mutation (mutationFn restoreWorkshop) invalidating `["workshops"]`, returned in the bag. `hooks/use-sessions.ts`: add `restore` mutation (mutationFn restoreSession) invalidating `["sessions"]`, returned in the bag.
    4. Create `tests/workshop-session-api.test.ts` (msw): (a) `restoreWorkshop("w1")` hits `PATCH /api/events/workshops/restore/w1`; (b) `restoreSession("s1")` hits `PATCH /api/events/sessions/restore/s1`; (c) `getWorkshops({keyword:"let", isDeleted:"true"})` URL contains `isDeleted=true`; (d) `getSessions({isDeleted:"false"})` URL contains `isDeleted=false`; (e) `addSession({programId:"pg1", data:{...minimal payload...}})` POSTs to `/api/events/sessions/pg1` with body containing `speaker: string[]` and `workshop` present (pin the explicit-workshop rule — Pitfall 7).
  </action>
  <acceptance_criteria>
    - Source assertion: services/workshop.api.ts exports `restoreWorkshop`; services/session.api.ts exports `restoreSession`
    - Source assertion: both list fns append `isDeleted` when provided
    - Source assertion: hooks/use-workshops.ts + use-sessions.ts each return `restore` invalidating their base queryKey
    - Source assertion: session.api.ts contains NO `sendSessionReminder`/`requestSessionExport` yet (06-07 owns those)
    - Test command: `pnpm test tests/workshop-session-api.test.ts` passes (restore URLs + isDeleted param + explicit-workshop body)
    - Gates: `pnpm lint` && `pnpm typecheck` pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm test tests/workshop-session-api.test.ts && pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>Restore endpoints + isDeleted list params exist for workshops and sessions, msw-tested; hooks expose restore.</done>
</task>

<task type="auto">
  <name>Task 2: Workshop CRUD reconcile + trash/restore + program drill-down (EVNT-03)</name>
  <files>features/program/workshop/all-workshops.tsx, features/program/workshop/add-workshop.tsx, features/program/workshop/workshop-editor.tsx, features/program/workshop/workshop-header.tsx, app/(main)/program/workshops/page.tsx, app/(main)/program/workshops/[id]/page.tsx</files>
  <read_first>
    - features/program/workshop/all-workshops.tsx (current list — keyword search + cards/table; reconcile)
    - features/program/workshop/add-workshop.tsx + workshop-editor.tsx (existing forms — verify title/description fields; do not redesign)
    - features/program/program/all-programs.tsx (reference implementation of TrashTabPattern tabs + row actions — copy the tab wiring pattern, not the code)
    - components/shared/trash-tab-pattern.tsx (+ the TrashTabPattern consumer reference from other features)
    - .planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md §Events Hierarchy Drill-Down (copy: "Create Workshop", "No workshops in this program", breadcrumbs)
  </read_first>
  <action>
    Per UI-SPEC §Events Hierarchy Drill-Down + PATTERNS #6 (trash/restore tabs):
    1. `all-workshops.tsx`: support an incoming `keyword` (program id) filter — when reached from a program detail drill-down link the program filter is active (read the page's existing searchParams/params threading; if missing, add wiring so `keyword` from the URL query drives `getWorkshops`). Add Active|Deleted tabs passing explicit `isDeleted` (same wiring as 06-03 Task 2). Keep keyword search.
    2. Row actions: edit (existing workshop-editor), soft-delete (AlertDialog; destructive), restore (new hook `restore`) on deleted-tab rows. Delete visible to `delete,event` admins; create/edit/restore to admin+manager (permission strings verified in lib/permissions.ts).
    3. Create CTA "Create Workshop" — the POST is `:programId`-scoped: from drill-down context use the program id; on the standalone list require selecting a program (usePrograms list in a Select) before create. Do NOT ship a create path with no program (backend 400 "Program not found").
    4. `workshops/[id]/page.tsx`: breadcrumb (Programs › {program.title} › {workshop.title}) + drill-down link "Sessions" → `/program/sessions?keyword=<workshopId>`; keep existing detail content. `workshop-header.tsx` reconciled to show the program context link.
  </action>
  <acceptance_criteria>
    - Source assertion: all-workshops.tsx passes `keyword` (from URL/params) and per-tab `isDeleted` into `getWorkshops`
    - Source assertion: "Create Workshop" flow always resolves a programId before POST (Select or drill-down context); no programless create path
    - Source assertion: restore action on deleted rows calls the hook `restore`; delete gated by `can(r,"delete","event")`
    - Source assertion: workshops/[id]/page.tsx has breadcrumb + sessions drill-down link with `keyword=<workshopId>`
    - Gates: `pnpm lint` && `pnpm typecheck` pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>Workshops: program-scoped CRUD, trash/restore tabs, drill-down from program and into sessions — contract-correct.</done>
</task>

<task type="auto">
  <name>Task 3: Session CRUD reconcile + IST datetime contract + trash/restore (EVNT-04)</name>
  <files>features/program/session/all-sessions.tsx, features/program/session/add-session.tsx, features/program/session/session-editor.tsx, features/program/session/session-header.tsx, app/(main)/program/sessions/page.tsx, app/(main)/program/sessions/[id]/page.tsx</files>
  <read_first>
    - features/program/session/session-editor.tsx (full read — the IST contract + speaker picker + workshop wiring live here)
    - features/program/session/all-sessions.tsx, add-session.tsx, session-header.tsx (reconcile surfaces)
    - app/(main)/program/sessions/[id]/page.tsx (detail — edit prefill source + where restore action mounts)
    - services/session.api.ts (SessionT/SessionCreateT shapes)
    - lib/date.ts (isoToIstInput, combineDateAndTimeToIso, formatIstDateTime signatures)
    - .planning/phases/06-admin-bank-accounts-events-depth/06-UI-SPEC.md §Events Hierarchy Drill-Down (copy: "Create Session", "Save Session", "No sessions in this workshop", future-dated note)
  </read_first>
  <action>
    Per RESEARCH EVNT-04 + Pitfalls 7/9 + UI-SPEC §Events Hierarchy Drill-Down:
    1. `all-sessions.tsx`: parent-filter via `keyword` from URL (workshop id or program id per the drill-down level), Active|Deleted tabs with explicit `isDeleted` (same tab wiring as 06-03 Task 2), row actions: edit → session-editor, delete (AlertDialog, `delete,event` admins), restore (hook `restore`) on deleted rows, "Create Session" CTA (program + workshop context resolved — see step 2).
    2. `session-editor.tsx` (the contract core): (a) `workshop` MUST always resolve to an explicit workshop id before submit — when the editor opens from drill-down context pass the workshop/program ids in; on standalone use selects for program then workshop (workshops filtered `keyword=<programId>`). If the current editor can POST without `workshop`, fix it to require it (Pitfall 7 — prevents phantom auto-created workshops). (b) Verify submit converts via `combineDateAndTimeToIso` to ISO with +05:30 and `speaker` as user ObjectId strings (existing mapping — confirm, do not change if correct). (c) EDIT-mode prefill: when editing an existing session, prefill date/startTime/endTime from the session's ISO strings via `isoToIstInput` — check what `[id]/page.tsx` passes today and complete it (a session whose form opens empty on edit is a bug). (d) Surface the past-date 400 inline: on mutation error whose message contains "Date must be present or future", show a FieldError on the date field (mirror how other forms surface server field errors — or toast the message per existing conventions; the requirement is the user learns the session must be future-dated, inline preferred per UI-SPEC "surface the 400 message inline"). (e) `endTime` input keeps `min={startTime}` (already present). Speaker stays `UserSearchPicker` multiple, min 1 (schema enforces).
    3. `[id]/page.tsx` session detail: breadcrumb (Programs › {program} › {workshop} › {session}), session date/time rendered via `formatIstDateTime` (IST — replace raw rendering if present), "Edit Session" opens session-editor prefilled, restore affordance on deleted state if applicable (detail fetch of a deleted session: follow what the backend returns; restore lives on the trash tab primarily — detail restore only if trivially available from the fetched row's state).
    4. Copy per UI-SPEC: "Create Session", "Save Session", "No sessions in this workshop — the first step is to create one."
  </action>
  <acceptance_criteria>
    - Source assertion: session-editor.tsx cannot submit without an explicit `workshop` id in the payload (grep the submit builder — `workshop:` must be present on the POST path)
    - Source assertion: all date/time values in the editor render/prefill via lib/date.ts functions (`isoToIstInput` on edit prefill; `combineDateAndTimeToIso` on submit) — no raw `new Date(...)`-derived input values
    - Source assertion: speaker selection is multiple users (`UserSearchPicker` multiple) mapped to ObjectId strings
    - Behavior assertion: editing an existing session prefills date/startTime/endTime (source-level: editor receives session data and maps through isoToIstInput)
    - Source assertion: all-sessions.tsx has Active|Deleted tabs with explicit isDeleted + parent keyword filter + restore action
    - Gates: `pnpm lint` && `pnpm typecheck` pass; `pnpm test` full suite green
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>Sessions: workshop-scoped CRUD, IST contract enforced (prefill + submit + future-date surfacing), trash/restore, speaker picker — contract-correct and double-submit-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (workshop/session mutations) | Event payloads cross here; server enforces role gates + future-date validation |
| client → API (speaker selection) | User ids cross here; notification + push fire server-side on create/update |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Elevation of Privilege | workshop/session delete | mitigate | delete affordance gated `can(r,"delete","event")` (admin) via RoleAccess; server `authorize()` is enforcement |
| T-06-04-02 | Tampering | session date/time payload | mitigate | `combineDateAndTimeToIso` (+05:30) + `endTime min=startTime` client-side; server refine future-dates — inline 400 surfacing tells the user instead of silent retries |
| T-06-04-03 | Tampering | phantom workshop creation | mitigate | explicit `workshop` always present in submit (Pitfall 7) — pinned by the msw test asserting the body contains `workshop` |
| T-06-04-04 | Tampering | speaker notification spam | mitigate | submit disabled while pending (D-26) — create/update fire server notifications+push to speakers; double-submit would duplicate them |
| T-06-04-05 | Information Disclosure | trash rows editable | mitigate | trash tabs keep deleted rows read-only for edits until restored (Pitfall 8 — backend edit lacks isDeleted filter on workshop/participant) |
| T-06-04-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — package legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm lint`, `pnpm typecheck`, `pnpm test` pass (new tests/workshop-session-api.test.ts included)
- Grep gate: `rg "sendSessionReminder|requestSessionExport" services/session.api.ts` returns nothing (06-07 owns those)
- Contract spot-check: `../saher-backend/src/events/{workshop,session}/**` route shapes unchanged since 2026-08-29
</verification>

<success_criteria>
- Workshops: program-scoped create/edit/delete/restore + trash tabs + drill-down filter
- Sessions: workshop-scoped create/edit/delete/restore + trash tabs + IST future-dated pickers + explicit workshop + speaker picker
- Session detail renders IST datetime and edit prefills correctly
</success_criteria>

<output>
Create `.planning/phases/06-admin-bank-accounts-events-depth/06-04-SUMMARY.md` when done
</output>