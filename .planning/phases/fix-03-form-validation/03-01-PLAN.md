---
phase: fix-03-form-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VALIDATION-01, VALIDATION-02]
user_setup: []

must_haves:
  truths:
    - "Every major form shows field-level validation errors before submit"
    - "Invalid data (negative amounts, empty required fields, bad dates) is rejected by the frontend"
    - "Backend rejects invalid data even when frontend validation is bypassed"
    - "Backend validation errors are surfaced to the user via toast"
    - "No form silently accepts data that violates business rules"
  artifacts:
    - path: "services/reimbursement.api.ts"
      provides: "Bill create/update schemas with positive-amount constraint"
      contains: "positive"
    - path: "services/leave.api.ts"
      provides: "Leave apply schema with reason min/max and endDate >= startDate"
      contains: "refine"
    - path: "services/notice.api.ts"
      provides: "Notice create schema with title/description min(1)"
      contains: "min(1)"
    - path: "services/session.api.ts"
      provides: "Session create schema with required fields and time ordering"
    - path: "features/attendance/attendance-correction.tsx"
      provides: "Attendance correction create schema with message length bounds"
      contains: "max("
    - path: "features/register/register-schema.ts"
      provides: "Registration schema with email, phone, IFSC, employeeId constraints"
  key_links:
    - from: "services/reimbursement.api.ts"
      to: "../saher-backend/src/reimbursement/bill/schema.ts"
      via: "userBillCreateSchema must have amount > 0 on both sides"
      pattern: "positive"
    - from: "services/leave.api.ts"
      to: "../saher-backend/src/leave/leave.schema.ts"
      via: "applyLeaveSchema must match leaveApplicationSchemaBase constraints"
      pattern: "refine"
    - from: "features/register/register-schema.ts"
      to: "../saher-backend/src/admin/account/schema.ts"
      via: "Registration form fields must match backend accountBaseSchema"
      pattern: "z\\.object"
---

<objective>
Audit and fix frontend zod validation schemas for all major forms, and ensure backend schemas enforce the same business rules. Close validation gaps where frontend allows invalid data or backend lacks essential constraints.

Purpose: Forms are the primary data-entry surface. Without consistent validation on both sides, invalid data enters the database and users get no feedback on what they did wrong.

Output: Updated frontend zod schemas with corrected constraints; documented backend gaps flagged for future backend-phase fixes.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Backend schema reference (relative imports end .js):
@../saher-backend/src/reimbursement/bill/schema.ts
@../saher-backend/src/leave/leave.schema.ts
@../saher-backend/src/notice/notice.schema.ts
@../saher-backend/src/events/session/session.schema.ts
@../saher-backend/src/events/workshop/workshop.schema.ts
@../saher-backend/src/events/program/program.schema.ts
@../saher-backend/src/attendance/correction/correction.schema.ts
@../saher-backend/src/admin/account/schema.ts
@../saher-backend/src/admin/bank/schema.ts

Frontend schema reference:
@services/reimbursement.api.ts
@services/leave.api.ts
@services/notice.api.ts
@services/session.api.ts
@services/program.api.ts
@services/workshop.api.ts
@services/attendance.api.ts
@services/attendance-correction.api.ts
@features/register/register-schema.ts
@features/attendance/attendance-correction.tsx
@features/attendance-correction/attendance-correction-view.tsx
@features/program/session/session-editor.tsx
@features/noticeboard/notice-form.tsx
@features/leave/apply-leave-dialog.tsx
@features/reimbursement/create-bill-dialog.tsx
@features/reimbursement/edit-bill-dialog.tsx
@features/reimbursement/advance-bill-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all form schemas and produce a gap report</name>
  <files>.planning/phases/fix-03-form-validation/VALIDATION-AUDIT.md</files>
  <read_first>
    services/reimbursement.api.ts,
    services/leave.api.ts,
    services/notice.api.ts,
    services/session.api.ts,
    services/program.api.ts,
    services/workshop.api.ts,
    services/attendance.api.ts,
    services/attendance-correction.api.ts,
    features/register/register-schema.ts,
    features/attendance/attendance-correction.tsx,
    features/attendance-correction/attendance-correction-view.tsx,
    features/program/session/session-editor.tsx,
    features/noticeboard/notice-form.tsx,
    features/leave/apply-leave-dialog.tsx,
    features/reimbursement/create-bill-dialog.tsx,
    features/reimbursement/edit-bill-dialog.tsx,
    features/reimbursement/advance-bill-dialog.tsx
  </read_first>
  <action>
Read every file listed in read_first. For each form, extract the zod schema (or inline z.object) used with zodResolver and compare it to the backend equivalent. Produce a VALIDATION-AUDIT.md in the phase directory with this structure:

```
# Validation Audit — Phase 3

## Per-Form Audit

### Bill Create (userBillCreateSchema)
- Frontend: services/reimbursement.api.ts
- Backend: ../saher-backend/src/reimbursement/bill/schema.ts
- GAPS: [list]
- OK: [list]

### Bill Update (userBillUpdateSchema)
...

### Leave Apply (applyLeaveSchema)
...

### Leave Type (createLeaveTypeSchema)
...

### Notice Create (createNoticeSchema)
...

### Session Create (sessionCreateSchema)
...

### Workshop Create (addWorkshops)
- Frontend: services/workshop.api.ts (no zod schema — raw JSON.stringify)
- Backend: ../saher-backend/src/events/workshop/workshop.schema.ts
- GAPS: [list]
- OK: [list]

### Program Create (addProgram)
- Frontend: services/program.api.ts (no zod schema — raw JSON.stringify)
- Backend: ../saher-backend/src/events/program/program.schema.ts
- GAPS: [list]
- OK: [list]

### Attendance Correction Create (attendanceCorrectionCreateSchema)
...

### Attendance Correction Handle (attendanceCorrectionHandleSchema)
...

### User Registration (registerFormSchema)
...

### Admin Bank Details (bankDetailSchema)
...

### Admin Account Edit (accountUpdateInput)

## Summary of Gaps

| Form | Gap | Severity | Fix Location |
|------|-----|----------|-------------|
| Bill Create | amount allows 0 (frontend has .positive, backend has .coerce.number only) | HIGH | Backend schema needs .positive() |
| Leave Apply | type has no min(1) on frontend; backend has z.string().trim() with no min | MEDIUM | Frontend + Backend |
| ... | ... | ... | ... |
```

Be thorough — check every schema field against its backend equivalent. Flag: missing required constraints, type mismatches, missing enums, missing regex patterns, missing refinements, missing length bounds.
  </action>
  <verify>File .planning/phases/fix-03-form-validation/VALIDATION-AUDIT.md exists and contains at least 10 form audit sections with GAPS entries</verify>
  <acceptance_criteria>
    - [grep -c "GAPS:" .planning/phases/fix-03-form-validation/VALIDATION-AUDIT.md] returns >= 10
    - [grep -c "Severity.*HIGH\|Severity.*MEDIUM" .planning/phases/fix-03-form-validation/VALIDATION-AUDIT.md] returns >= 5
    - All 10+ major forms are audited (bill, leave, notice, session, workshop, program, attendance correction, registration, bank, profile)
  </acceptance_criteria>
  <done>Complete validation gap audit covering all major forms with severity ratings and fix locations identified</done>
</task>

<task type="auto">
  <name>Task 2: Fix frontend zod schemas — bill, leave, notice, attendance correction, session</name>
  <files>
    services/reimbursement.api.ts,
    services/leave.api.ts,
    services/notice.api.ts,
    features/attendance/attendance-correction.tsx,
    features/program/session/session-editor.tsx
  </files>
  <read_first>
    .planning/phases/fix-03-form-validation/VALIDATION-AUDIT.md,
    services/reimbursement.api.ts,
    services/leave.api.ts,
    services/notice.api.ts,
    features/attendance/attendance-correction.tsx,
    features/program/session/session-editor.tsx,
    ../saher-backend/src/reimbursement/bill/schema.ts,
    ../saher-backend/src/leave/leave.schema.ts,
    ../saher-backend/src/notice/notice.schema.ts,
    ../saher-backend/src/attendance/correction/correction.schema.ts,
    ../saher-backend/src/events/session/session.schema.ts
  </read_first>
  <action>
Apply validation fixes identified in Task 1's audit. Specific known gaps to address:

1. **Bill amount (VALIDATION-02):** `services/reimbursement.api.ts` — `userBillCreateSchema.amount` already has `.positive()`. Verify `userBillUpdateSchema.amount` also has `.positive()`. Verify `adminBillCreateSchema.advance` and `adminBillUpdateSchema.advance` have `.positive()`. These are already correct per current code — confirm and document.

2. **Leave type required (VALIDATION-01):** `services/leave.api.ts` — `applyLeaveSchema.type` already has `.min(1)`. Verify this matches backend. OK per audit.

3. **Leave proof field:** `services/leave.api.ts` — `applyLeaveSchema.proof` is `z.string().optional()`. This is the LEAVE-01 cross-cutting concern. Flag it in the audit but do NOT fix here (belongs to Phase 6 leave proof task). Leave as-is with a comment.

4. **Attendance correction message bounds:** `features/attendance/attendance-correction.tsx` — `attendanceCorrectionCreateSchema.message` has `.min(10).max(100)`. Backend has `.min(3).max(300)`. Align frontend max to 300 to match backend: change `.max(100, "Max Limit Is 100 Characters.")` to `.max(300, "Max Limit Is 300 Characters.")`. Update the corresponding error message.

5. **Session create time validation:** `features/program/session/session-editor.tsx` — `sessionCreateSchema` uses `z.string().min(1)` for date/startTime/endTime. Backend has `z.coerce.date().refine(date > new Date())` and a `.refine(endTime > startTime)`. Add a `.refine` on the frontend schema to validate endTime > startTime after combining date+time. This is a refine on the composed values, not the raw strings — the combine happens in onSubmit. Leave the string min(1) as-is since the combine step handles date validation; note this gap in audit as "session time ordering not validated client-side" — low severity since backend catches it.

6. **Notice form — no changes needed:** `createNoticeSchema` already has `title: z.string().min(1)` and `description: z.string().min(1)`. Backend has same. expiresAt is optional on both sides. OK.

7. **Workshop/Program — no zod schemas on frontend:** `services/workshop.api.ts` and `services/program.api.ts` use `JSON.stringify(data)` with no zod validation. These are simple CRUD (title + description). Flag in audit as "no client-side validation — relies entirely on backend". Low severity since backend has `z.string().min(1)` on both fields.

For each file modified:
- Use the Edit tool to make the specific change
- Preserve all existing code structure and comments
- Do NOT reformat unrelated code
- Run `pnpm lint` after changes to verify no lint errors
- Run `pnpm build` to verify type-check passes
  </action>
  <verify>pnpm lint passes and pnpm build succeeds after all schema changes</verify>
  <acceptance_criteria>
    - [grep -c "max(300" features/attendance/attendance-correction.tsx] returns 1 (message max aligned to backend)
    - [grep -c "positive" services/reimbursement.api.ts] returns >= 4 (amount, advance on create+update)
    - pnpm lint exits 0
    - pnpm build exits 0
  </acceptance_criteria>
  <done>All frontend zod schemas aligned with backend constraints; lint and build pass; audit gaps documented</done>
</task>

<task type="auto">
  <name>Task 3: Document backend validation gaps requiring backend-phase fixes</name>
  <files>.planning/phases/fix-03-form-validation/VALIDATION-BACKEND-GAPS.md</files>
  <read_first>
    .planning/phases/fix-03-form-validation/VALIDATION-AUDIT.md,
    ../saher-backend/src/reimbursement/bill/schema.ts,
    ../saher-backend/src/leave/leave.schema.ts,
    ../saher-backend/src/notice/notice.schema.ts,
    ../saher-backend/src/events/session/session.schema.ts,
    ../saher-backend/src/attendance/correction/correction.schema.ts,
    ../saher-backend/src/admin/account/schema.ts,
    ../saher-backend/src/admin/bank/schema.ts
  </read_first>
  <action>
Produce VALIDATION-BACKEND-GAPS.md documenting backend schemas that lack essential business-rule enforcement. These are fixes that belong to the backend repo (out of scope for this frontend phase) but must be tracked.

Known backend gaps to document:

1. **Bill amount allows negative (BILL-02 cross-cut):** `../saher-backend/src/reimbursement/bill/schema.ts` — `amount: z.coerce.number()` has NO `.positive()` or `.min(0)`. A direct API request with `amount: -100` will be accepted. Fix: add `.positive("Amount must be greater than zero")` to both `billSchema.amount` and `billSchema.advance`. This is the BILL-02 fix — flag here for cross-reference but implementation belongs to Phase 4.

2. **Bill advance allows negative:** Same file — `advance: z.coerce.number()` has no positive constraint. Fix: add `.positive()`.

3. **Leave type has no min length:** `../saher-backend/src/leave/leave.schema.ts` — `leaveApplicationSchemaBase.type` is `z.string().trim()` with no `.min(1)`. An empty string type would be accepted. Fix: add `.min(1, "Leave type is required")`.

4. **Notice expiry allows past dates only on create (update has no such check):** Backend `updateNoticeSchema` is `baseNoticeSchema.partial()` — the expiresAt refine (`date > new Date()`) only applies when the field is present. This is correct behavior (edit can keep old expiry). No fix needed — document as intentional.

5. **Session workshop is optional:** Backend `baseSchema.workshop` is `objectId().optional()`. The frontend `sessionCreateSchema` requires it with `.min(1)`. This is a frontend-only stricter constraint — acceptable but asymmetric. Document.

6. **Attendance correction handleSchema allows "pending" status on frontend but backend only allows ['reject', 'on-hold', 'approve']:** Frontend `attendanceCorrectionHandleSchema` has `status: z.enum(["reject", "pending", "on-hold", "approve"])` but backend only allows `["reject", "on-hold", "approve"]`. Fix: remove "pending" from the frontend enum to match backend. Apply this fix in the frontend.

Format the document as a numbered list with: gap description, file path, current code, suggested fix, which phase owns the fix.
  </action>
  <verify>File .planning/phases/fix-03-form-validation/VALIDATION-BACKEND-GAPS.md exists and contains at least 3 backend-specific gaps</verify>
  <acceptance_criteria>
    - [grep -c "BILL-02\|bill/schema" .planning/phases/fix-03-form-validation/VALIDATION-BACKEND-GAPS.md] returns >= 1
    - [grep -c "leave.schema" .planning/phases/fix-03-form-validation/VALIDATION-BACKEND-GAPS.md] returns >= 1
    - Document is clear about which gaps are backend-only vs frontend-fixable
  </acceptance_criteria>
  <done>Backend validation gaps documented with specific file paths, current code, and suggested fixes; cross-referenced to FIX_PHASE phases that own each fix</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form → API | User-submitted data crosses from browser to backend; frontend validation is UX-only, never security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | bill amount field | mitigate | Backend must enforce amount > 0; frontend .positive() is UX-only |
| T-03-02 | Tampering | attendance correction status enum | mitigate | Frontend enum must match backend allowed values; extra values silently rejected |
| T-03-03 | Information Disclosure | validation error messages | accept | Error messages reveal field constraints; acceptable for UX |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] pnpm lint passes
- [ ] pnpm build succeeds
- [ ] VALIDATION-AUDIT.md exists with 10+ form audits
- [ ] VALIDATION-BACKEND-GAPS.md exists with backend-specific gaps documented
- [ ] attendance correction handle schema status enum matches backend (no "pending")
- [ ] bill schemas all have .positive() on amount and advance fields
</verification>

<success_criteria>
- All major forms audited against backend schemas
- Frontend validation gaps closed (where frontend was less strict than backend)
- Backend gaps documented with specific fix instructions for backend phases
- No regressions (lint + build pass)
</success_criteria>

<output>
Create `.planning/phases/fix-03-form-validation/03-01-SUMMARY.md` when done
</output>
