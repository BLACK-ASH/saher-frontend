---
phase: 03-noticeboard-pilot
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - features/noticeboard/notice-form.tsx
  - features/noticeboard/notice-trash.tsx
  - app/(main)/(admin)/noticeboard/new/page.tsx
  - app/(main)/(admin)/noticeboard/[id]/edit/page.tsx
  - app/(main)/noticeboard/page.tsx
  - components/shared/pagination-footer.tsx
  - components/shared/trash-tab-pattern.tsx
  - components/pagination-footer.tsx
  - components/data-table.tsx
  - features/mail/data-table.tsx
  - features/attendance/attendance-correction-requests.tsx
  - features/attendance/attendance-table.tsx
  - features/attendance-correction/corrections/data-table.tsx
  - features/dashboard/attendance-grid/attendance-dashboard.tsx
  - features/dashboard/range-attendance-table.tsx
  - features/dashboard/today-attendance-table.tsx
  - features/users/data-table.tsx
  - tests/pagination-footer.test.tsx
  - features/noticeboard/notice-feed.tsx
  - .planning/codebase/SLICE-CONTRACT.md
autonomous: true
requirements: [NOTC-02, NOTC-03]

must_haves:
  truths:
    - "Admins create notices via /noticeboard/new with title, description, and 7-day default expiry"
    - "Admins edit notices via /noticeboard/[id]/edit with pre-filled form data from cache"
    - "After create/edit, redirect to /noticeboard feed showing the new/updated notice"
    - "Trash tab shows placeholder message explaining backend doesn't list deleted notices"
    - "Soft delete button on active cards removes notice from feed after cache invalidation"
    - "Permanent delete requires explicit confirmation dialog (shadcn Dialog)"
    - "PaginationFooter lives at components/shared/pagination-footer.tsx with all imports updated"
    - "TrashTabPattern component exists at components/shared/trash-tab-pattern.tsx as a reusable template"
    - "SLICE-CONTRACT.md documents the full recipe with noticeboard as living example per D-16/D-17"
  artifacts:
    - path: "features/noticeboard/notice-form.tsx"
      provides: "Shared NoticeForm component handling create and edit modes (per D-08)"
      exports: ["NoticeForm"]
    - path: "features/noticeboard/notice-trash.tsx"
      provides: "Trash tab content with placeholder and delete/restore action stubs"
      exports: ["NoticeTrash"]
    - path: "app/(main)/(admin)/noticeboard/new/page.tsx"
      provides: "Create notice route under admin group"
      exports: ["default"]
    - path: "app/(main)/(admin)/noticeboard/[id]/edit/page.tsx"
      provides: "Edit notice route under admin group, fetches notice from cache"
      exports: ["default"]
    - path: "app/(main)/noticeboard/page.tsx"
      provides: "Updated feed page with Tabs (Active/Trash per D-12) and soft delete button on cards"
    - path: "components/shared/pagination-footer.tsx"
      provides: "PaginationFooter promoted to shared components directory"
      exports: ["PaginationFooter"]
    - path: "components/shared/trash-tab-pattern.tsx"
      provides: "Reusable TrashTabPattern component for modules to extend"
      exports: ["TrashTabPattern"]
    - path: ".planning/codebase/SLICE-CONTRACT.md"
      provides: "Step-by-step recipe document covering all layers, referencing noticeboard as living example"
  key_links:
    - from: "features/noticeboard/notice-form.tsx"
      to: "services/notice.api.ts"
      via: "createNoticeSchema, CreateNoticeInput, useNotices"
      pattern: "from.*notice"
    - from: "features/noticeboard/notice-form.tsx"
      to: "lib/date.ts"
      via: "dateInputToIso for date conversion"
      pattern: "from.*lib/date"
    - from: "app/(main)/noticeboard/page.tsx"
      to: "features/noticeboard/notice-trash.tsx"
      via: "NoticeTrash tab content"
      pattern: "import.*NoticeTrash"
    - from: "app/(main)/noticeboard/page.tsx"
      to: "components/ui/tabs.tsx"
      via: "Tabs, TabsList, TabsTrigger, TabsContent"
      pattern: "from.*components/ui/tabs"
    - from: "components/shared/pagination-footer.tsx"
      to: "components/pagination-footer.tsx"
      via: "Re-export or moved component"
      pattern: "PaginationFooter"
    - from: ".planning/codebase/SLICE-CONTRACT.md"
      to: "services/notice.api.ts"
      via: "Living example reference"
      pattern: "notice\\.api"
---

<objective>
Admin CRUD for notices (create/edit forms, soft delete), trash tab pattern promotion to shared components, PaginationFooter promotion to `components/shared/`, and SLICE-CONTRACT.md documentation — completing NOTC-02, NOTC-03, and success criteria #4.

Purpose: Deliver the full admin lifecycle AND complete the pilot's meta-goal: promoting reusable patterns to shared components and writing the Slice Contract reference document. The trash tab proves the pattern for later modules. PaginationFooter moves to shared for cross-module reuse. The SLICE-CONTRACT.md becomes the recipe every later module copies.

Output: Working create/edit forms, trash tab placeholder, soft delete on active cards, shared components directory, and the documented Slice Contract.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-noticeboard-pilot/03-CONTEXT.md
@.planning/phases/03-noticeboard-pilot/03-RESEARCH.md

@.planning/phases/03-noticeboard-pilot/03-01-SUMMARY.md
@.planning/phases/03-noticeboard-pilot/03-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 1 and 2 that this plan consumes -->

From services/notice.api.ts (Plan 1):
```typescript
export type NoticeResponse = { _id: string; title: string; description: string; expiresAt: string; isDeleted?: boolean; createdAt?: string; updatedAt?: string };
export type CreateNoticeInput = { title: string; description: string; expiresAt?: string };
export const createNoticeSchema: ZodObject; // for zodResolver
export function createNotice(data: CreateNoticeInput): Promise<NoticeResponse>;
export function updateNotice(id: string, data: Partial<CreateNoticeInput>): Promise<NoticeResponse>;
export function deleteNotice(id: string): Promise<void>;
export function restoreNotice(id: string): Promise<void>;
export function permanentDeleteNotice(id: string): Promise<void>;
```

From hooks/use-notice.ts (Plan 1):
```typescript
export function useNotices(props?: { id?: string }): {
  notices: UseQueryResult<NoticeResponse[], Error>;
  notice: UseQueryResult<NoticeResponse | undefined, Error>;
  addNotice: UseMutationResult;
  editNotice: UseMutationResult;
  removeNotice: UseMutationResult;
  restore: UseMutationResult;
  permanentRemove: UseMutationResult;
}
```

From lib/date.ts:
```typescript
export function dateInputToIso(value: string): string; // "YYYY-MM-DD" → "YYYY-MM-DDT00:00:00+05:30"
export function formatIstDate(iso?: string | null | Date): string;
```

From components/ui/tabs.tsx:
```typescript
export function Tabs({ defaultValue, className, ...props }): JSX.Element;
export function TabsList({ ...props }): JSX.Element;
export function TabsTrigger({ value, ...props }): JSX.Element;
export function TabsContent({ value, ...props }): JSX.Element;
```

From components/ui/dialog.tsx:
```typescript
// Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter, DialogClose
```

From components/ui/field.tsx:
```typescript
export function Field({ ...props }): JSX.Element;
export function FieldLabel({ htmlFor, ...props }): JSX.Element;
export function FieldError({ errors }: { errors: FieldError[] }): JSX.Element;
```

From components/pagination-footer.tsx (current location — will be moved):
```typescript
export function PaginationFooter({ page, totalPages, onPageChange }: {
  page: number; totalPages: number; onPageChange: (page: number) => void;
}): JSX.Element;
```

Backend quirks:
- Backend addNotice adds +1 day to expiresAt — pre-fill form with 7 days so stored value is 7 days
- Backend editNotice returns OLD doc (new:false) — invalidate cache after edit, do NOT use mutation response
- No GET /notice/:id — edit page fetches from cached list
- No trash endpoint — trash tab is placeholder only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared NoticeForm component and admin create/edit routes</name>
  <files>features/noticeboard/notice-form.tsx, app/(main)/(admin)/noticeboard/new/page.tsx, app/(main)/(admin)/noticeboard/[id]/edit/page.tsx</files>
  <read_first>
    - services/notice.api.ts (createNoticeSchema shape, CreateNoticeInput type, updateNotice — returns OLD doc)
    - hooks/use-notice.ts (useNotices hook — addNotice and editNotice mutations, notices query for cache lookup)
    - features/leave/apply-leave-dailog.tsx (form pattern: useForm + zodResolver + Controller + Field/FieldLabel/FieldError)
    - features/login/components/login-form.tsx (form pattern reference: onSubmit handler, toast on success/error)
    - components/ui/input.tsx (Input component)
    - components/ui/textarea.tsx (Textarea component)
    - components/ui/field.tsx (Field, FieldLabel, FieldError)
    - components/ui/button.tsx (Button component)
    - lib/date.ts (dateInputToIso for converting date input to ISO with IST offset)
    - app/(main)/(admin)/layout.tsx (admin layout wraps in RoleGuard with can(r, "write", "user"))
    - app/(main)/attendance/page.tsx (thin page shell pattern)
  </read_first>
  <action>
Create the shared form component and both admin routes.

**features/noticeboard/notice-form.tsx** — Shared create/edit form (per D-08):
- Mark `"use client"` at top
- Export `NoticeForm({ mode, initialData }: Props)` — named export
- Props type: `{ mode: "create" | "edit"; initialData?: { id: string; title: string; description: string; expiresAt: string }; }`
- Import `useForm`, `Controller` from `react-hook-form`; `zodResolver` from `@hookform/resolvers/zod`
- Import `useRouter` from `next/navigation`; `toast` from `sonner`
- Import `Button`, `Input`, `Textarea`, `Field`, `FieldLabel`, `FieldError`
- Import `createNoticeSchema`, `CreateNoticeInput` from `@/services/notice.api`
- Import `useNotices` from `@/hooks/use-notice`
- Import `dateInputToIso` from `@/lib/date`
- Form defaults:
  - For create: `expiresAt` defaults to 7 days from today as `YYYY-MM-DD` string. Use: `const d = new Date(); d.setDate(d.getDate() + 7); d.toISOString().split("T")[0]` — per D-07
  - For edit: use `initialData.expiresAt` converted to `YYYY-MM-DD` input format. Use: `new Date(initialData.expiresAt).toISOString().split("T")[0]` — shows the stored date (backend +1 already applied)
  - `title`: empty string for create, `initialData.title` for edit
  - `description`: empty string for create, `initialData.description` for edit
- `useForm<CreateNoticeInput>({ resolver: zodResolver(createNoticeSchema), defaultValues: { ... } })`
- Submit handler `onNoticeSubmit(values: CreateNoticeInput)`:
  - Convert `expiresAt` via `dateInputToIso(values.expiresAt)` — produces ISO with IST offset
  - For create: call `addNotice.mutate(payload, { onSuccess: () => { toast.success("Notice created"); router.push("/noticeboard"); } })`
  - For edit: call `editNotice.mutate({ id: initialData!.id, data: payload }, { onSuccess: () => { toast.success("Notice updated"); router.push("/noticeboard"); } })`
  - IMPORTANT: Do NOT use the mutation response to update UI — backend returns OLD doc. Cache invalidation handles the update.
- Form layout (per D-05, clean layout):
  - `<form onSubmit={form.handleSubmit(onNoticeSubmit)} className="space-y-5 max-w-2xl">`
  - Title: Controller → Field → FieldLabel("Title") + Input + FieldError
  - Description: Controller → Field → FieldLabel("Description") + Textarea(rows={6}) + FieldError
  - Expiry Date: Controller → Field → FieldLabel("Expiry Date") + Input(type="date") + FieldError
  - Submit Button: `disabled={addNotice.isPending || editNotice.isPending}`, text "Create Notice" or "Update Notice" based on mode

**app/(main)/(admin)/noticeboard/new/page.tsx** — Create page:
- Thin page shell under admin route group (RoleGuard already applied by layout)
- `export default function NewNoticePage()` — no `"use client"` (Server Component)
- Render `<NoticeForm mode="create" />` wrapped in Suspense with DefaultLoader fallback
- Import `NoticeForm` from `@/features/noticeboard/notice-form`

**app/(main)/(admin)/noticeboard/[id]/edit/page.tsx** — Edit page:
- Thin page shell under admin route group
- `export default function EditNoticePage({ params }: { params: { id: string } })` — unwrap params for Next.js 16
- Fetch notice from cache: use `useNotices({ id })` to get the `notice` query; if `notice.data` exists, pass it as `initialData` to the form
- If notice not found (loading or undefined): show `<DefaultLoader />` or `<NoData />`
- If notice found: render `<NoticeForm mode="edit" initialData={{ id: notice.data._id, title: notice.data.title, description: notice.data.description, expiresAt: notice.data.expiresAt }} />`
- Mark this page `"use client"` since it uses hooks (useNotices)
- Alternative: if params is async in Next.js 16, use `React.use(params)` to unwrap
  </action>
  <verify>
    <automated>pnpm lint --quiet 2>&1 | grep -c "error" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - `features/noticeboard/notice-form.tsx` exists, exports `NoticeForm` named component
    - NoticeForm accepts `{ mode: "create" | "edit"; initialData?: {...} }` props
    - Create mode: `expiresAt` defaults to 7 days from today (per D-07), form has 3 fields (title, description, expiresAt)
    - Edit mode: form pre-fills from `initialData` (title, description, expiresAt converted to YYYY-MM-DD)
    - Form uses `useForm` + `zodResolver(createNoticeSchema)` + `Controller` for each field
    - Submit calls `addNotice.mutate` (create) or `editNotice.mutate` (edit)
    - On success: `toast.success("Notice created"/"Notice updated")` then `router.push("/noticeboard")`
    - `expiresAt` converted via `dateInputToIso()` before sending to backend
    - `app/(main)/(admin)/noticeboard/new/page.tsx` exists, renders `<NoticeForm mode="create" />`
    - `app/(main)/(admin)/noticeboard/[id]/edit/page.tsx` exists, fetches notice from cache and renders `<NoticeForm mode="edit" initialData={...} />`
    - Edit page handles loading/not-found states
    - Both admin pages are thin shells (≤20 lines each)
    - `pnpm lint` exits 0
    - No `console.log` statements in any new file
  </acceptance_criteria>
  <done>Admins create notices at /noticeboard/new and edit at /noticeboard/[id]/edit. Forms use react-hook-form + zod, pre-fill 7-day expiry on create, pre-fill existing data on edit, redirect to feed after save.</done>
</task>

<task type="auto">
  <name>Task 2: Add TrashTab pattern, soft delete, and permanent delete confirmation to noticeboard feed</name>
  <files>features/noticeboard/notice-trash.tsx, app/(main)/noticeboard/page.tsx</files>
  <read_first>
    - app/(main)/noticeboard/page.tsx (current feed page from Plan 2 — needs Tabs wrapper and delete button)
    - features/noticeboard/notice-card.tsx (card component — needs soft delete button added)
    - hooks/use-notice.ts (removeNotice mutation for soft delete)
    - components/ui/tabs.tsx (Tabs, TabsList, TabsTrigger, TabsContent)
    - components/ui/dialog.tsx (Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter)
    - components/ui/button.tsx (Button variants: default, outline, destructive)
    - lib/permissions.ts (can function, UserRole type)
    - hooks/use-me.ts (useMe for current user role)
    - components/no-data.tsx (NoData component)
    - components/loading.tsx (DefaultLoader)
  </read_first>
  <action>
Implement the trash tab placeholder pattern (per D-12/D-13/D-14/D-15) and add soft delete to active cards.

**features/noticeboard/notice-trash.tsx** — Trash tab content:
- Mark `"use client"` at top
- Export `NoticeTrash()` — named export, no props
- Since backend has NO endpoint to list deleted notices (per RESEARCH.md Pitfall 5), render a placeholder:
  - Card with a heading "Trash" and a message: "Deleted notices will appear here once the backend supports listing trashed items."
  - Use `<NoData />` component with a custom message, or a simple centered text block
  - Below the message, show a static example of what trash cards would look like (optional — helps prove the pattern for the Slice Contract document)
- Keep the component minimal — this is a structural placeholder, not a functional feature

**app/(main)/noticeboard/page.tsx** — Update feed page with Tabs and soft delete:
- Read the current file (created in Plan 2) before editing
- Add imports: `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` from `@/components/ui/tabs`
- Add import: `NoticeTrash` from `@/features/noticeboard/notice-trash`
- Add import: `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogFooter` from `@/components/ui/dialog`
- Add import: `removeNotice` from `useNotices()` hook (destructure from hook return)
- Wrap the existing feed content in Tabs:
  ```
  <Tabs defaultValue="active">
    <TabsList>
      <TabsTrigger value="active">Active</TabsTrigger>
      <TabsTrigger value="trash">Trash</TabsTrigger>
    </TabsList>
    <TabsContent value="active">
      {/* existing feed content: header, grid, pagination */}
    </TabsContent>
    <TabsContent value="trash">
      <NoticeTrash />
    </TabsContent>
  </Tabs>
  ```
- Add soft delete button to each notice card (in the card footer, next to expiry badge):
  - Add state: `const [deleteTarget, setDeleteTarget] = useState<NoticeResponse | null>(null);`
  - On each card, add a "Delete" Button (variant="ghost", size="icon") with Trash2 icon from lucide-react
  - Button onClick: `setDeleteTarget(notice)` — opens confirmation dialog
  - Confirmation dialog:
    - `<Dialog open={!!deleteTarget} onOpenChange={(open) => !open && setDeleteTarget(null)}>`
    - DialogContent with DialogHeader (DialogTitle: "Delete Notice"), body text "Are you sure you want to delete "{deleteTarget?.title}"?", DialogFooter with Cancel (variant="outline") and Delete (variant="destructive") buttons
    - Delete button onClick: `removeNotice.mutate(deleteTarget!._id, { onSuccess: () => { toast.success("Notice deleted"); setDeleteTarget(null); } })`
    - Cancel button: `setDeleteTarget(null)`
  - Gate delete button visibility: only show if `can(user?.role ?? "user", "delete", "notice")` — per the pre-existing permission matrix, user role has notice:delete
- Keep the "New Notice" button and all existing feed logic intact
- Ensure the Tabs do not interfere with the PaginationFooter state (page state stays in the component)
  </action>
  <verify>
    <automated>pnpm lint --quiet 2>&1 | grep -c "error" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - `features/noticeboard/notice-trash.tsx` exists, exports `NoticeTrash` component
    - Trash tab renders placeholder message about backend not supporting deleted notice listing
    - Feed page (`app/(main)/noticeboard/page.tsx`) renders Tabs with "Active" and "Trash" triggers (per D-12)
    - Active tab shows existing card grid with pagination (unchanged behavior)
    - Trash tab shows `<NoticeTrash />` placeholder content
    - Each active notice card has a "Delete" button (Trash2 icon) gated on `can(role, "delete", "notice")`
    - Clicking delete opens a confirmation Dialog (per D-15) showing notice title and "Are you sure?" message
    - Confirming calls `removeNotice.mutate(id)` with toast success feedback
    - Dialog has Cancel and Delete (destructive) buttons
    - `pnpm lint` exits 0
    - No `console.log` statements
    - `grep -c "Tabs" app/(main)/noticeboard/page.tsx` returns ≥3 (Tabs, TabsList, TabsTrigger, TabsContent usage)
    - `grep -c "Dialog" features/noticeboard/notice-trash.tsx` or the page file returns ≥1
  </acceptance_criteria>
  <done>Noticeboard feed has Active/Trash tabs (per D-12). Trash tab shows placeholder (backend gap documented). Active cards have soft delete with confirmation dialog (per D-15). Delete removes notice from feed via cache invalidation.</done>
</task>

<task type="auto">
  <name>Task 3: Promote PaginationFooter to components/shared/ and create TrashTabPattern</name>
  <files>components/shared/pagination-footer.tsx, components/shared/trash-tab-pattern.tsx, components/pagination-footer.tsx, components/data-table.tsx, features/mail/data-table.tsx, features/attendance/attendance-correction-requests.tsx, features/attendance/attendance-table.tsx, features/attendance-correction/corrections/data-table.tsx, features/dashboard/attendance-grid/attendance-dashboard.tsx, features/dashboard/range-attendance-table.tsx, features/dashboard/today-attendance-table.tsx, features/users/data-table.tsx, tests/pagination-footer.test.tsx, features/noticeboard/notice-feed.tsx</files>
  <read_first>
    - components/pagination-footer.tsx (current PaginationFooter — 63 lines, move to shared/)
    - tests/pagination-footer.test.tsx (test file — update import path)
    - features/noticeboard/notice-feed.tsx (noticeboard feed — update import path)
    - components/data-table.tsx (existing consumer — update import path)
    - features/users/data-table.tsx (existing consumer — update import path)
    - features/attendance/attendance-table.tsx (existing consumer — update import path)
    - features/attendance/attendance-correction-requests.tsx (existing consumer — update import path)
    - features/attendance-correction/corrections/data-table.tsx (existing consumer — update import path)
    - features/dashboard/attendance-grid/attendance-dashboard.tsx (existing consumer — update import path)
    - features/dashboard/range-attendance-table.tsx (existing consumer — update import path)
    - features/dashboard/today-attendance-table.tsx (existing consumer — update import path)
    - features/mail/data-table.tsx (existing consumer — update import path)
    - features/noticeboard/notice-trash.tsx (reference for trash pattern to extract)
    - components/ui/tabs.tsx (Tabs component used in trash pattern)
    - components/ui/dialog.tsx (Dialog component used in trash pattern)
    - components/no-data.tsx (NoData component used in trash pattern)
  </read_first>
  <action>
Promote PaginationFooter to `components/shared/` and create a reusable TrashTabPattern component. Update all imports across the codebase.

**Move PaginationFooter (per success criteria #4):**

1. Create `components/shared/pagination-footer.tsx` — copy the entire contents of `components/pagination-footer.tsx` (63 lines) into the new file. The file path changes but the component code is identical.

2. Update `components/pagination-footer.tsx` to become a re-export bridge (prevents breakage if any import was missed):
   ```typescript
   export { PaginationFooter } from "@/components/shared/pagination-footer";
   ```
   Replace the entire file content with this single re-export line.

3. Update the following 10 files to import from `@/components/shared/pagination-footer` instead of `@/components/pagination-footer`:
   - `components/data-table.tsx` (line 28)
   - `features/mail/data-table.tsx` (line 34)
   - `features/attendance/attendance-correction-requests.tsx` (line 33)
   - `features/attendance/attendance-table.tsx` (line 32)
   - `features/attendance-correction/corrections/data-table.tsx` (line 32)
   - `features/dashboard/attendance-grid/attendance-dashboard.tsx` (line 12)
   - `features/dashboard/range-attendance-table.tsx` (line 50)
   - `features/dashboard/today-attendance-table.tsx` (line 44)
   - `features/users/data-table.tsx` (line 34)
   - `features/noticeboard/notice-feed.tsx` (update import path)

4. Update `tests/pagination-footer.test.tsx` (line 3) to import from `@/components/shared/pagination-footer`.

**Create TrashTabPattern (per success criteria #4):**

5. Create `components/shared/trash-tab-pattern.tsx` — a reusable wrapper component that any module can use for its trash tab:
   - Mark `"use client"` at top
   - Export `TrashTabPattern({ title, description, children }: Props)` — named export
   - Props type: `{ title: string; description: string; children?: React.ReactNode; }`
   - Renders a Card with heading (`title`), description text, and optional `children` slot for module-specific trash content
   - When `children` is absent, render `<NoData message={description} />` as fallback
   - When `children` is provided, render them below the heading/description
   - Import `Card`, `CardHeader`, `CardContent`, `CardTitle` from `@/components/ui/card`
   - Import `NoData` from `@/components/no-data`
   - This component proves the pattern for D-12/D-13 — modules extend it with their own trash content when backend endpoints become available

6. Update `features/noticeboard/notice-trash.tsx` to import and use `TrashTabPattern`:
   - Import `TrashTabPattern` from `@/components/shared/trash-tab-pattern`
   - Wrap the placeholder content in `<TrashTabPattern title="Trash" description="Deleted notices will appear here once the backend supports listing trashed items." />`
   - Keep the existing placeholder content as the `children` prop or rely on the default NoData fallback

After all changes, run `pnpm lint` and `pnpm test -- --run` to verify nothing broke.
  </action>
  <verify>
    <automated>pnpm lint --quiet 2>&1 | grep -c "error" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - `components/shared/pagination-footer.tsx` exists with the PaginationFooter component (moved from original location)
    - `components/pagination-footer.tsx` is now a one-line re-export: `export { PaginationFooter } from "@/components/shared/pagination-footer";`
    - `components/shared/trash-tab-pattern.tsx` exists, exports `TrashTabPattern` component
    - All 10 consumer files import from `@/components/shared/pagination-footer` (grep confirms zero imports from old path except the re-export bridge)
    - `tests/pagination-footer.test.tsx` imports from `@/components/shared/pagination-footer`
    - `features/noticeboard/notice-trash.tsx` uses `TrashTabPattern` component
    - `pnpm lint` exits 0
    - `pnpm test -- --run` exits 0 (all tests pass including pagination-footer test)
    - `grep -r "from.*@/components/pagination-footer" --include="*.tsx" --include="*.ts" . | grep -v "components/pagination-footer.tsx" | wc -l` returns 0 (no stale imports)
  </acceptance_criteria>
  <done>PaginationFooter lives at components/shared/pagination-footer.tsx. TrashTabPattern exists as a reusable template. All 10+ consumer files updated. No broken imports. Success criteria #4 partially satisfied.</done>
</task>

<task type="auto">
  <name>Task 4: Write SLICE-CONTRACT.md with noticeboard as living example</name>
  <files>.planning/codebase/SLICE-CONTRACT.md</files>
  <read_first>
    - .planning/phases/03-noticeboard-pilot/03-CONTEXT.md (decisions D-16/D-17 — document covers all layers, references files as living examples)
    - services/notice.api.ts (service layer pattern — reference for schema + endpoint sections)
    - hooks/use-notice.ts (hook pattern — reference for query/mutation sections)
    - features/noticeboard/notice-form.tsx (form pattern — reference for form section)
    - features/noticeboard/notice-expiry-badge.tsx (badge pattern — reference for UI derivation section)
    - features/noticeboard/notice-feed.tsx (feed pattern — reference for pagination section)
    - features/noticeboard/notice-trash.tsx (trash pattern — reference for trash section)
    - components/shared/trash-tab-pattern.tsx (shared trash template — reference for reusable pattern section)
    - components/shared/pagination-footer.tsx (shared pagination — reference for pagination section)
    - lib/permissions.ts (can() helper — reference for RBAC section)
    - lib/date.ts (IST utilities — reference for date handling section)
    - lib/api-wrapper.ts (apiFetch — reference for HTTP section)
    - components/role-guard.tsx (RoleGuard — reference for route protection section)
  </read_first>
  <action>
Write `.planning/codebase/SLICE-CONTRACT.md` — the step-by-step recipe document for building new modules (per D-16/D-17). This document references noticeboard files as living examples rather than including inline code snippets.

**Document structure:**

```markdown
# Slice Contract — Module Recipe

> Every new module follows this recipe. Noticeboard is the reference implementation.
> All file references use noticeboard as the living example — update these paths when
> promoting the pattern to a new module.

## 1. Service Layer (`services/<module>.api.ts`)

**Pattern:** Define Zod schemas mirroring the backend, export typed async functions.

**See:** `services/notice.api.ts` — noticeSchema, createNoticeSchema, getNotices, createNotice, etc.

**Steps:**
1. Mirror backend entity shape as a Zod response schema (export both schema and inferred type)
2. Define input schemas for create/update operations
3. One async function per backend endpoint using `apiFetch<T>()`
4. GET endpoints returning arrays: return `res.data` directly (do NOT use `normalizeList` unless backend provides `meta`)
5. Mutations: return `res.data` for endpoints that return data; void for void endpoints
6. Export all schemas, types, and functions

**Backend contract notes:**
- Backend uses `new: false` on PUT — returns OLD document. Callers must invalidate cache, not trust response.
- Backend may add days to dates — document the quirk, compensate in frontend.

## 2. Data Hook (`hooks/use-<module>.ts`)

**Pattern:** TanStack Query wiring — one query per list/detail, one mutation per write operation.

**See:** `hooks/use-notice.ts` — useNotices with notices query + addNotice/editNotice/removeNotice/restore/permanentRemove mutations.

**Steps:**
1. Import all service functions
2. Export a single hook (e.g., `useNotices`)
3. Queries: `useQuery` with hierarchical key `["<resource>", "active"]` for list, `["<resource>", "detail", id]` for detail
4. Mutations: `useMutation` wrapping each write service function
5. All mutations: `onSuccess` → `queryClient.invalidateQueries({ queryKey: ["<resource>"] })`
6. Return flat object bag — no nested objects

## 3. Feature Components (`features/<domain>/`)

**Pattern:** One file per concern — feed, detail, form, card, badge, trash.

**See:** `features/noticeboard/` — notice-feed.tsx, notice-detail.tsx, notice-form.tsx, notice-card.tsx, notice-expiry-badge.tsx, notice-trash.tsx

**Key patterns:**
- `"use client"` on all components using hooks/state
- Import hooks (not services directly) from `hooks/use-<module>.ts`
- Import shared UI from `components/ui/*`
- Use `cn()` from `@/lib/utils` for conditional classes

## 4. Forms (`react-hook-form` + `zod`)

**Pattern:** Shared form component handling both create and edit modes.

**See:** `features/noticeboard/notice-form.tsx` — NoticeForm with mode: "create" | "edit"

**Steps:**
1. Define props: `{ mode: "create" | "edit"; initialData?: {...} }`
2. Use `useForm<InputType>({ resolver: zodResolver(schema) })`
3. Fields: `<Controller name="field" control={form.control} render={({ field, fieldState }) => ( <Field><FieldLabel>Label</FieldLabel><Input {...field} />{fieldState.error && <FieldError errors={[fieldState.error]} />}</Field> )} />`
4. Submit: call appropriate mutation, `onSuccess` → toast + redirect to list
5. Do NOT use mutation response to update UI — invalidate cache instead

## 5. Client-Side Pagination

**Pattern:** Component holds page state, slices full array, renders PaginationFooter.

**See:** `features/noticeboard/notice-feed.tsx` — PAGE_SIZE=10, useState for page, slice + PaginationFooter

**Steps:**
1. `const PAGE_SIZE = 10;`
2. `const [page, setPage] = useState(1);`
3. Compute: `const items = data ?? []; const totalPages = Math.ceil(items.length / PAGE_SIZE); const pageItems = items.slice((page - 1) * PAGE_SIZE, page * PAGE_SIZE);`
4. Clamp page when data shrinks: `if (page > totalPages && totalPages > 0) setPage(totalPages);`
5. Render `<PaginationFooter page={page} totalPages={totalPages} onPageChange={setPage} />`
6. PaginationFooter lives at `components/shared/pagination-footer.tsx`

## 6. Trash Tab Pattern

**Pattern:** Active/Trash tabs using shadcn Tabs. Trash content via TrashTabPattern wrapper.

**See:** `app/(main)/noticeboard/page.tsx` (Tabs), `features/noticeboard/notice-trash.tsx` (placeholder), `components/shared/trash-tab-pattern.tsx` (reusable wrapper)

**Steps:**
1. Wrap feed content in `<Tabs defaultValue="active">` with TabsList containing "Active" and "Trash" triggers
2. TabsContent for "active" contains the list view
3. TabsContent for "trash" renders `<TrashTabPattern>` (from `components/shared/trash-tab-pattern.tsx`)
4. For modules with working trash endpoints: pass custom children to TrashTabPattern
5. For modules without trash endpoints: use default NoData placeholder

## 7. RBAC Gating

**Pattern:** Frontend `can()` helper gates UI affordances. Backend may not enforce.

**See:** `lib/permissions.ts` (can function), noticeboard uses `can(role, "write", "notice")` for admin buttons

**Steps:**
1. Import `can` from `@/lib/permissions`
2. Get current user: `const { data: user } = useMe();`
3. Gate create/edit/delete buttons: `can(user?.role ?? "user", "action", "resource")`
4. Do NOT gate read access on `can(role, "read", resource)` — permission matrices may be incomplete
5. Document any backend authorization gaps (no `authorize()` middleware)

## 8. IST Date Handling

**Pattern:** All date display/parsing through `lib/date.ts` utilities.

**See:** `lib/date.ts` — formatIstDate, formatIstDateTime, dateInputToIso

**Steps:**
1. Display dates: `formatIstDate(isoString)` for date-only, `formatIstDateTime(isoString)` for date+time
2. Form input to ISO: `dateInputToIso("YYYY-MM-DD")` → `"YYYY-MM-DDT00:00:00+05:30"`
3. Never use `new Date().toLocaleDateString()` — always route through IST utilities
4. Badge/derivation logic can use raw Date math (e.g., expiry badge days calculation)

## 9. Route Structure

**Pattern:** Thin page shells composing feature components.

**See:** `app/(main)/noticeboard/page.tsx`, `app/(main)/noticeboard/[id]/page.tsx`, `app/(main)/(admin)/noticeboard/new/page.tsx`

**Steps:**
1. Staff-accessible routes: `app/(main)/<module>/page.tsx`
2. Detail routes: `app/(main)/<module>/[id]/page.tsx`
3. Admin-only routes: `app/(main)/(admin)/<module>/new/page.tsx`, `app/(main)/(admin)/<module>/[id]/edit/page.tsx`
4. Page files are thin shells (≤15 lines): export default function, render feature component
5. Add navigation entry to `components/sidebar/nav-list.tsx` in appropriate route group

## 10. Testing

**Pattern:** TDD for data layer (services, hooks, pure functions). Component tests for critical UI.

**See:** `tests/notice-api.test.ts`, `tests/notice-expiry.test.tsx`, `tests/notice-hook.test.tsx`

**Steps:**
1. Service tests: MSW handlers + assert service functions call correct endpoints
2. Hook tests: renderWithProviders + assert query/mutation shape
3. Pure function tests: fake timers for date-dependent logic
4. Form tests: submit validation + success path

---

*Document created: Phase 3 (Noticeboard Pilot)*
*Living examples: All noticeboard files in `services/notice.api.ts`, `hooks/use-notice.ts`, `features/noticeboard/`*
```

After writing the file, run `pnpm lint` to ensure no issues.
  </action>
  <verify>
    <automated>test -f .planning/codebase/SLICE-CONTRACT.md && echo "exists" || echo "missing"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/codebase/SLICE-CONTRACT.md` exists
    - Document has ≥10 sections covering: service layer, data hook, feature components, forms, pagination, trash tabs, RBAC gating, IST dates, route structure, testing
    - Each section references noticeboard files by path (e.g., "See services/notice.api.ts") — no inline code snippets per D-17
    - Document mentions `components/shared/pagination-footer.tsx` and `components/shared/trash-tab-pattern.tsx` as shared patterns
    - Document title or opening references "Slice Contract" and "noticeboard as living example" per D-16
    - No `console.log` statements (markdown file, but verify no accidental code)
  </acceptance_criteria>
  <done>SLICE-CONTRACT.md exists at .planning/codebase/SLICE-CONTRACT.md with complete recipe covering all layers. Noticeboard files are referenced as living examples. Document is ready for Phase 4+ modules to follow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form input → Backend | react-hook-form + zod validation before submission; backend re-validates |
| Delete action → Backend | Soft delete via DELETE /notice/:id; frontend hides button based on can() but backend has no authorize() — documented gap |
| Confirmation dialog → Permanent action | Dialog prevents accidental clicks; cancel closes without action |
| File move → Import consistency | PaginationFooter move to shared/ must update all 10+ import sites; re-export bridge prevents breakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | notice-form.tsx input fields | mitigate | zod validation (title min 1, description min 1) prevents empty submissions |
| T-03-06 | Elevation of Privilege | soft delete without backend RBAC | accept | Frontend gates on can(role, "delete", "notice"); backend has no authorize() — documented gap, no frontend fix possible |
| T-03-07 | Tampering | expiresAt date manipulation | mitigate | dateInputToIso converts date input to IST ISO string; backend validates |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | No new packages installed in this phase |
</threat_model>

<verification>
- `pnpm lint` — 0 errors
- `pnpm test -- --run` — all existing tests still pass (including pagination-footer test after import update)
- `grep -r "from.*@/components/pagination-footer" --include="*.tsx" --include="*.ts" . | grep -v "components/pagination-footer.tsx"` returns empty (all imports updated)
- Navigate to /noticeboard/new (as user role with notice:write) — create form renders with 7-day default
- Submit create — redirects to /noticeboard, notice appears in feed
- Navigate to /noticeboard/{id}/edit — form pre-fills with existing data
- Submit edit — redirects to /noticeboard, card shows updated values
- Click "Delete" on a card — confirmation dialog opens
- Confirm delete — notice removed from feed
- Click "Trash" tab — placeholder message visible via TrashTabPattern
- `.planning/codebase/SLICE-CONTRACT.md` exists and covers all 10 recipe sections
</verification>

<success_criteria>
- Create form at /noticeboard/new: title, description, expiresAt (7-day default), submit creates notice
- Edit form at /noticeboard/[id]/edit: pre-fills from cached data, submit updates notice
- After create/edit: redirect to /noticeboard, new/updated notice visible in feed
- Soft delete: click delete → confirm dialog → notice removed from active feed
- Trash tab: placeholder message via TrashTabPattern explaining backend doesn't list deleted notices
- Permanent delete dialog: shows notice title, "Are you sure?", Cancel/Confirm buttons (per D-15)
- All admin pages under app/(main)/(admin)/ with RoleGuard from layout
- All forms use react-hook-form + zod + Controller + Field/FieldError pattern
- No stale data shown after mutations (cache invalidated, refetch triggered)
- PaginationFooter lives at components/shared/pagination-footer.tsx (success criteria #4)
- TrashTabPattern exists at components/shared/trash-tab-pattern.tsx (success criteria #4)
- SLICE-CONTRACT.md documents full recipe with noticeboard as living example (success criteria #4, per D-16/D-17)
</success_criteria>

<output>
Create `.planning/phases/03-noticeboard-pilot/03-03-SUMMARY.md` when done
</output>
