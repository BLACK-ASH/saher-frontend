---
phase: 03-noticeboard-pilot
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - features/noticeboard/notice-feed.tsx
  - features/noticeboard/notice-card.tsx
  - features/noticeboard/notice-detail.tsx
  - app/(main)/noticeboard/page.tsx
  - app/(main)/noticeboard/[id]/page.tsx
  - components/sidebar/nav-list.tsx
autonomous: true
requirements: [NOTC-01]

must_haves:
  truths:
    - "Staff see active notices as a paginated card grid on /noticeboard"
    - "Each card shows title, description excerpt, created date (IST), and expiry badge"
    - "PaginationFooter navigates pages client-side (PAGE_SIZE=10)"
    - "Clicking a card navigates to /noticeboard/[id] showing full detail"
    - "Detail page shows title, description with line breaks, expiry date, created date in IST"
    - "Noticeboard link appears in sidebar under User group for all authenticated users"
  artifacts:
    - path: "features/noticeboard/notice-feed.tsx"
      provides: "Card grid with client-side pagination and PaginationFooter"
      exports: ["NoticeFeed"]
    - path: "features/noticeboard/notice-card.tsx"
      provides: "Single notice card with title, excerpt, dates, expiry badge, and click-to-detail"
      exports: ["NoticeCard"]
    - path: "features/noticeboard/notice-detail.tsx"
      provides: "Full notice detail view with line-break preservation and IST dates"
      exports: ["NoticeDetail"]
    - path: "app/(main)/noticeboard/page.tsx"
      provides: "Noticeboard feed route accessible to all authenticated staff"
      exports: ["default"]
    - path: "app/(main)/noticeboard/[id]/page.tsx"
      provides: "Notice detail route"
      exports: ["default"]
    - path: "components/sidebar/nav-list.tsx"
      provides: "Noticeboard entry in userRoutes sidebar"
  key_links:
    - from: "features/noticeboard/notice-feed.tsx"
      to: "hooks/use-notice.ts"
      via: "useNotices() hook"
      pattern: "from.*use-notice"
    - from: "features/noticeboard/notice-feed.tsx"
      to: "components/pagination-footer.tsx"
      via: "PaginationFooter component"
      pattern: "from.*pagination-footer"
    - from: "features/noticeboard/notice-card.tsx"
      to: "features/noticeboard/notice-expiry-badge.tsx"
      via: "NoticeExpiryBadge component"
      pattern: "from.*notice-expiry-badge"
    - from: "app/(main)/noticeboard/page.tsx"
      to: "features/noticeboard/notice-feed.tsx"
      via: "renders NoticeFeed"
      pattern: "import.*NoticeFeed"
---

<objective>
Staff-facing noticeboard: paginated card feed at /noticeboard and detail view at /noticeboard/[id], plus sidebar navigation entry.

Purpose: Deliver NOTC-01 — staff can browse active notices with expiry highlighted in IST. This is the primary read experience every staff member sees daily.

Output: Working noticeboard feed page with client-side pagination and detail page with line-break preservation.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-noticeboard-pilot/03-CONTEXT.md
@.planning/phases/03-noticeboard-pilot/03-RESEARCH.md

@.planning/phases/03-noticeboard-pilot/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 1 that this plan consumes -->

From services/notice.api.ts (created in Plan 1):
```typescript
export type NoticeResponse = { _id: string; title: string; description: string; expiresAt: string; isDeleted?: boolean; createdAt?: string; updatedAt?: string };
export function getNotices(): Promise<NoticeResponse[]> // returns raw array, NOT normalized
```

From hooks/use-notice.ts (created in Plan 1):
```typescript
export function useNotices(props?: { id?: string }): {
  notices: UseQueryResult<NoticeResponse[], Error>;
  notice: UseQueryResult<NoticeResponse | undefined, Error>;
  // ... mutations
}
```

From features/noticeboard/notice-expiry-badge.tsx (created in Plan 1):
```typescript
export function getExpiryStatus(expiresAt: string): "active" | "expiring" | "expired";
export function NoticeExpiryBadge({ expiresAt }: { expiresAt: string }): JSX.Element;
```

From components/pagination-footer.tsx:
```typescript
export function PaginationFooter({ page, totalPages, onPageChange }: {
  page: number; totalPages: number; onPageChange: (page: number) => void;
}): JSX.Element;
```

From lib/date.ts:
```typescript
export function formatIstDate(iso?: string | null | Date): string; // "26 Aug 2026"
export function formatIstDateTime(iso?: string | null | Date): string; // "26 Aug 2026, 10:30 AM"
```

From components/ui/card.tsx:
```typescript
// Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
```

From components/loading.tsx: `<DefaultLoader />`
From components/no-data.tsx: `<NoData />`

Backend quirks (from RESEARCH.md):
- GET /notice returns ONLY active non-expired notices (isDeleted:false, expiresAt>now)
- No GET /notice/:id endpoint — detail must find from full list
- createdAt/updatedAt may or may not exist (Mongoose timestamps uncertain)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build NoticeCard component and NoticeFeed with client-side pagination</name>
  <files>features/noticeboard/notice-card.tsx, features/noticeboard/notice-feed.tsx</files>
  <read_first>
    - services/notice.api.ts (NoticeResponse type shape)
    - hooks/use-notice.ts (useNotices hook return shape)
    - features/noticeboard/notice-expiry-badge.tsx (NoticeExpiryBadge component)
    - components/pagination-footer.tsx (PaginationFooter props: page, totalPages, onPageChange)
    - components/ui/card.tsx (Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter)
    - components/ui/badge.tsx (Badge variants available)
    - components/loading.tsx (DefaultLoader for loading state)
    - components/no-data.tsx (NoData for empty state)
    - lib/date.ts (formatIstDate for date display)
    - lib/permissions.ts (can function and UserRole type)
    - hooks/use-me.ts (useMe hook for current user role)
    - features/users/data-table.tsx (reference for card/grid layout patterns)
  </read_first>
  <action>
Create two feature components for the noticeboard staff feed.

**features/noticeboard/notice-card.tsx**:
- Mark `"use client"` at top
- Export `NoticeCard({ notice }: { notice: NoticeResponse })` — named export
- Render a shadcn Card with:
  - CardHeader: CardTitle (notice.title), CardDescription showing created date via `formatIstDate(notice.createdAt)` — fallback to "--" if createdAt missing
  - CardContent: first 150 chars of notice.description with "..." truncation if longer; `whitespace-pre-line` to preserve line breaks
  - CardFooter: flex row with `NoticeExpiryBadge` (per D-03) on the left, expiry date text "Expires: {formatIstDate(notice.expiresAt)}" on the right
- Wrap entire card in a clickable container that navigates to `/noticeboard/${notice._id}` using `useRouter().push()` — the entire card should be clickable (cursor-pointer, hover effect)
- Use `cn()` from `@/lib/utils` for conditional class merging
- Keep the component lean — no state, no hooks other than useRouter

**features/noticeboard/notice-feed.tsx**:
- Mark `"use client"` at top
- Export `NoticeFeed()` — named export, no props
- Import `useNotices` from `@/hooks/use-notice`
- Import `NoticeCard` from `./notice-card`
- Import `PaginationFooter` from `@/components/pagination-footer`
- Import `DefaultLoader` from `@/components/loading`
- Import `NoData` from `@/components/no-data`
- Import `Button` from `@/components/ui/button`
- Import `can` from `@/lib/permissions`, `useMe` from `@/hooks/use-me`
- Import `useRouter` from `next/navigation`
- Import `Bell` from `lucide-react`
- Constants: `const PAGE_SIZE = 10;`
- State: `const [page, setPage] = useState(1);`
- Get notices: `const { notices } = useNotices();`
- Get user for role check: `const { data: user } = useMe();`
- Compute: `const items = notices.data ?? []; const totalPages = Math.ceil(items.length / PAGE_SIZE); const pageItems = items.slice((page - 1) * PAGE_SIZE, page * PAGE_SIZE);`
- Reset page to 1 when data loads (handle edge case where page > totalPages after refetch)
- Loading state: if `notices.isLoading`, render `<DefaultLoader />`
- Empty state: if items.length === 0 and not loading, render `<NoData />` with message "No active notices"
- Feed layout:
  - Header row: `<h1 className="text-2xl font-bold">Noticeboard</h1>` on left, "New Notice" Button on right (only if `can(user?.role ?? "user", "write", "notice")` is true — per D-06 admin gating, note this is user role per the pre-existing matrix)
  - "New Notice" Button: variant="default", onClick navigates to `/noticeboard/new`, includes Bell icon from lucide-react
  - Card grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4` — responsive columns
  - Map `pageItems` to `<NoticeCard key={notice._id} notice={notice} />`
  - PaginationFooter at bottom, centered: `<div className="flex justify-center mt-6"><PaginationFooter page={page} totalPages={totalPages} onPageChange={setPage} /></div>`
- When page changes and items shrink (e.g., after delete), auto-clamp: `if (page > totalPages && totalPages > 0) setPage(totalPages);`
  </action>
  <verify>
    <automated>pnpm lint --quiet 2>&1 | grep -c "error" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - `features/noticeboard/notice-card.tsx` exists, exports `NoticeCard` named component
    - `features/noticeboard/notice-feed.tsx` exists, exports `NoticeFeed` named component
    - NoticeCard renders: card title from `notice.title`, description excerpt truncated to 150 chars, `NoticeExpiryBadge` with notice.expiresAt, `formatIstDate(notice.createdAt)` for created date
    - NoticeCard wraps in clickable element that calls `router.push("/noticeboard/${notice._id}")`
    - NoticeFeed renders: heading "Noticeboard", responsive grid (1/2/3 columns), PaginationFooter with PAGE_SIZE=10
    - NoticeFeed computes pagination: `items.slice((page - 1) * PAGE_SIZE, page * PAGE_SIZE)`
    - "New Notice" button visible only when `can(role, "write", "notice")` returns true
    - Loading state uses `<DefaultLoader />`, empty state uses `<NoData />`
    - `pnpm lint` exits 0
    - No `console.log` statements
  </acceptance_criteria>
  <done>Staff see paginated card grid of active notices. Cards show title, description excerpt, expiry badge, and dates. PaginationFooter navigates pages client-side.</done>
</task>

<task type="auto">
  <name>Task 2: Build NoticeDetail component, wire routes, and add sidebar entry</name>
  <files>features/noticeboard/notice-detail.tsx, app/(main)/noticeboard/page.tsx, app/(main)/noticeboard/[id]/page.tsx, components/sidebar/nav-list.tsx</files>
  <read_first>
    - features/noticeboard/notice-feed.tsx (created in Task 1 — reference for feed page wiring)
    - features/noticeboard/notice-expiry-badge.tsx (NoticeExpiryBadge for detail view)
    - hooks/use-notice.ts (useNotices hook — notices query for finding by ID)
    - services/notice.api.ts (NoticeResponse type)
    - lib/date.ts (formatIstDate, formatIstDateTime)
    - lib/permissions.ts (can function, UserRole type)
    - components/ui/card.tsx (Card, CardHeader, CardTitle, CardDescription, CardContent)
    - components/loading.tsx (DefaultLoader)
    - components/no-data.tsx (NoData)
    - components/sidebar/nav-list.tsx (current sidebar structure — userRoutes array, NavItem component)
    - app/(main)/attendance/page.tsx (reference for thin page shell pattern)
  </read_first>
  <action>
Create the detail view, wire both noticeboard routes, and add the sidebar navigation entry.

**features/noticeboard/notice-detail.tsx**:
- Mark `"use client"` at top
- Export `NoticeDetail({ noticeId }: { noticeId: string })` — named export
- Get notices list: `const { notices } = useNotices();`
- Find notice from list: `const notice = (notices.data ?? []).find((n) => n._id === noticeId);`
- Loading state: if `notices.isLoading`, render `<DefaultLoader />`
- Not found: if notices loaded but notice undefined, render `<NoData />` with "Notice not found"
- Render detail view in a Card:
  - CardHeader: CardTitle (full notice.title), subtext: `formatIstDateTime(notice.createdAt)` for created date — fallback to "--"
  - CardContent:
    - Description block: `<p className="whitespace-pre-line">{notice.description}</p>` — preserves line breaks (per D-04)
    - Info row: "Expires: {formatIstDate(notice.expiresAt)}" with `NoticeExpiryBadge` inline
  - Back button: `<Button variant="outline" onClick={() => router.back()}>← Back</Button>` at top or bottom
- Handle edge case: if single notice endpoint is added later, switch to fetching by ID (document this with a comment: `// TODO: use GET /notice/:id when backend adds single-notice endpoint`)

**app/(main)/noticeboard/page.tsx**:
- Thin page shell (per app convention): `export default function NoticeboardPage() { return <NoticeFeed />; }`
- Import `NoticeFeed` from `@/features/noticeboard/notice-feed`
- Add Suspense wrapper if needed: wrap in `<Suspense fallback={<DefaultLoader />}>` pattern like other pages
- No `"use client"` on page file (page is a Server Component by default, renders client feature component)

**app/(main)/noticeboard/[id]/page.tsx**:
- Thin page shell: `export default function NoticeDetailPage({ params }: { params: { id: string } }) { ... }`
- Unwrap params (Next.js 16 pattern): `const { id } = params;` (or `React.use(params)` if needed for async params)
- Render `<NoticeDetail noticeId={id} />` wrapped in Suspense
- Import `NoticeDetail` from `@/features/noticeboard/notice-detail`

**components/sidebar/nav-list.tsx** — Add noticeboard to userRoutes:
- Add import: `Bell` from `lucide-react` (add to existing lucide-react import block)
- Add entry to `userRoutes` array (after the existing entries, before the closing bracket):
  ```
  { label: "Noticeboard", url: "/noticeboard", icon: Bell }
  ```
- The noticeboard link appears for ALL authenticated users (staff can view active notices regardless of role per pitfall 4 in RESEARCH.md — do NOT gate on can(role, "read", "notice") since user role lacks that permission)
  </action>
  <verify>
    <automated>pnpm lint --quiet 2>&1 | grep -c "error" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - `features/noticeboard/notice-detail.tsx` exists, exports `NoticeDetail` component taking `noticeId` prop
    - Detail view finds notice from cached list via `(notices.data ?? []).find(n => n._id === noticeId)`
    - Detail view renders: full title, `whitespace-pre-line` description, `formatIstDate(notice.expiresAt)` with NoticeExpiryBadge, `formatIstDateTime(notice.createdAt)` for created date
    - `app/(main)/noticeboard/page.tsx` exists and renders `<NoticeFeed />`
    - `app/(main)/noticeboard/[id]/page.tsx` exists and renders `<NoticeDetail noticeId={id} />`
    - Both page files are thin shells (≤15 lines each), no inline UI logic
    - `components/sidebar/nav-list.tsx` has `{ label: "Noticeboard", url: "/noticeboard", icon: Bell }` in userRoutes
    - Bell icon imported from lucide-react in nav-list.tsx
    - `pnpm lint` exits 0
    - `grep -c "noticeboard" components/sidebar/nav-list.tsx` returns ≥1
  </acceptance_criteria>
  <done>Staff navigate to /noticeboard from sidebar, see paginated feed, click cards to view detail with expiry info. Both routes work as thin shells composing feature components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navigation → Detail page | noticeId comes from URL params; must be string-validated before use in query key |
| Sidebar rendering → role check | useMe() returns user role; can() gates "New Notice" button visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | notice-card.tsx description excerpt | accept | Description is public notice content; truncation at 150 chars is UX, not security |
| T-03-04 | Tampering | sidebar nav rendering | accept | Navigation is cosmetic; backend protectedRoute handles auth |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | No new packages installed in this phase |
</threat_model>

<verification>
- `pnpm lint` — 0 errors
- `pnpm test -- --run` — all existing tests still pass
- Navigate to /noticeboard in browser (if dev server running) — feed renders with card grid
- Click a card — navigates to /noticeboard/{id} — detail view renders
- Sidebar shows "Noticeboard" link with Bell icon under User group
</verification>

<success_criteria>
- /noticeboard shows paginated card grid with PAGE_SIZE=10
- Each card shows title, description excerpt (150 chars), created date (IST), expiry badge
- PaginationFooter navigates pages; page resets on data shrink
- /noticeboard/[id] shows full detail with line-break preservation and IST dates
- "New Notice" button visible only for users with notice:write permission
- Sidebar shows "Noticeboard" link for all authenticated staff
- All pages are thin shells composing feature components
</success_criteria>

<output>
Create `.planning/phases/03-noticeboard-pilot/03-02-SUMMARY.md` when done
</output>
