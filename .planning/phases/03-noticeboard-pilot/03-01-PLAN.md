---
phase: 03-noticeboard-pilot
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - services/notice.api.ts
  - hooks/use-notice.ts
  - features/noticeboard/notice-expiry-badge.tsx
  - tests/notice-api.test.ts
  - tests/notice-expiry.test.tsx
  - tests/notice-hook.test.tsx
autonomous: true
requirements: [NOTC-01]

must_haves:
  truths:
    - "Expiry badge shows green for notices >3 days from expiry, yellow for ≤3 days, red for expired"
    - "Service functions call the correct backend endpoints with correct HTTP methods"
    - "GET /notices returns the raw array from the backend response envelope"
    - "Hook exposes all six mutations (create, update, delete, restore, permanentDelete) with cache invalidation"
  artifacts:
    - path: "services/notice.api.ts"
      provides: "6 endpoint functions + zod schemas mirroring backend notice.schema.ts"
      exports: ["noticeSchema", "NoticeResponse", "createNoticeSchema", "CreateNoticeInput", "getNotices", "createNotice", "updateNotice", "deleteNotice", "restoreNotice", "permanentDeleteNotice"]
    - path: "hooks/use-notice.ts"
      provides: "TanStack Query hook with list query and 5 mutations"
      exports: ["useNotices"]
    - path: "features/noticeboard/notice-expiry-badge.tsx"
      provides: "Expiry status badge (active/expiring/expired) using D-03 colors"
      exports: ["NoticeExpiryBadge"]
    - path: "tests/notice-api.test.ts"
      provides: "MSW-backed tests for all 6 service functions"
    - path: "tests/notice-expiry.test.tsx"
      provides: "Unit tests for expiry badge color derivation"
    - path: "tests/notice-hook.test.tsx"
      provides: "Hook integration test verifying query and mutation shape"
  key_links:
    - from: "hooks/use-notice.ts"
      to: "services/notice.api.ts"
      via: "import service functions"
      pattern: "from.*services/notice"
    - from: "features/noticeboard/notice-expiry-badge.tsx"
      to: "components/ui/badge.tsx"
      via: "import Badge component"
      pattern: "from.*components/ui/badge"
    - from: "tests/notice-api.test.ts"
      to: "tests/test-server.ts"
      via: "import MSW server for handler registration"
      pattern: "import.*test-server"
---

<objective>
TDD: failing tests for the noticeboard service layer, data hook, and expiry badge — then implement all three to pass.

Purpose: Establish the data foundation every noticeboard feature builds on. Tests define the backend contract behavior before any UI exists. The expiry badge is a pure function ideal for TDD.

Output: Working service/hook/badge modules with passing tests, proving the backend contract is correctly consumed.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-noticeboard-pilot/03-CONTEXT.md
@.planning/phases/03-noticeboard-pilot/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/api-wrapper.ts:
```typescript
export function apiFetch<T>(url: string, init?: RequestInit): Promise<ApiResponse<T>>
// ApiResponse<T> = { success: boolean; message: string; data: T; meta?: MetaResponse }
```

From components/ui/badge.tsx:
```typescript
// Available variants: "default" | "secondary" | "success" | "verify" | "outline-success" | "warn" | "outline-warn" | "destructive" | "outline" | "ghost" | "link"
export function Badge({ variant, ...props }): JSX.Element
```

From tests/test-server.ts:
```typescript
import { setupServer } from "msw/node";
export const server = setupServer(); // handlers registered inline via server.use()
```

From tests/setup.ts:
```typescript
// beforeAll: server.listen({ onUnhandledRequest: "error" })
// afterEach: cleanup(); server.resetHandlers()
// afterAll: server.close()
```

From tests/render-with-providers.tsx:
```typescript
export function renderWithProviders(ui: ReactElement, options?: RenderOptions): RenderResult
// Wraps in QueryClientProvider with retry: false, refetchOnWindowFocus: false
```

Backend contract (from RESEARCH.md):
- GET /api/notice → returns raw array in data (NOT envelope with meta)
- POST /api/notice → returns new notice; backend adds +1 day to expiresAt
- PUT /api/notice/:id → returns OLD doc (new: false); must invalidate cache
- DELETE /api/notice/:id → soft delete (null response)
- PATCH /api/notice/:id/restore → restore (null response)
- DELETE /api/notice/:id/permanent → permanent delete (null response)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — Write failing tests for expiry badge, service functions, and hook</name>
  <files>tests/notice-expiry.test.tsx, tests/notice-api.test.ts, tests/notice-hook.test.tsx</files>
  <read_first>
    - tests/test-server.ts (MSW server setup — handlers registered via server.use())
    - tests/setup.ts (global setup — onUnhandledRequest: "error")
    - tests/render-with-providers.tsx (renderWithProviders helper for QueryClient-wrapped renders)
    - tests/permissions.test.ts (existing test style reference)
    - services/attendance.api.ts (service function pattern: apiFetch + return res.data)
    - hooks/use-attendance.ts (hook pattern: useQuery + useMutation + invalidateQueries)
    - lib/date.ts (IST date utilities — formatIstDate, dateInputToIso for expiry display)
    - components/ui/badge.tsx (available badge variants for expiry mapping)
  </read_first>
  <action>
Write three test files that define expected behavior before any implementation exists. All tests will fail (RED phase).

**tests/notice-expiry.test.tsx** — Pure unit tests for expiry badge logic:
- Test: expiry date 10 days from now → status "active" (green badge)
- Test: expiry date 2 days from now → status "expiring" (yellow badge)
- Test: expiry date yesterday → status "expired" (red badge)
- Test: expiry date exactly today (now <= expiry) → status "active"
- Test: expiry date exactly 3 days from now → status "expiring" (≤3 threshold)
- Import from `@/features/noticeboard/notice-expiry-badge` the `getExpiryStatus` helper (export it as a named export alongside the component)
- Use `describe("getExpiryStatus", ...)` grouping
- Mock `Date.now()` via `vi.useFakeTimers()` + `vi.setSystemTime()` to control "now" precisely
- Assert return values are string literals: "active", "expiring", "expired"

**tests/notice-api.test.ts** — MSW-backed service function tests:
- Import `server` from `@/tests/test-server` and `http`/`HttpResponse` from `msw`
- Use `beforeEach(() => server.use(...))` to register handlers per test
- GET test: server returns `HttpResponse.json({ success: true, message: "ok", data: [{_id: "1", title: "Test", description: "Desc", expiresAt: "2026-09-15T00:00:00Z", isDeleted: false}] })` → assert `getNotices()` returns the array (length 1, first item title matches)
- POST test: server returns `HttpResponse.json({ success: true, message: "created", data: {_id: "2", title: "New", description: "Body", expiresAt: "2026-09-10T00:00:00Z", isDeleted: false} })` → assert `createNotice({title:"New", description:"Body"})` returns object with id "2"
- PUT test: server returns old doc `HttpResponse.json({ success: true, message: "updated", data: {_id: "1", title: "Old Title", description: "Old Desc"} })` → assert `updateNotice("1", {title:"New Title"})` returns the old doc (testing that we return res.data, not the input)
- DELETE (soft) test: server returns `HttpResponse.json({ success: true, message: "deleted", data: null })` → assert `deleteNotice("1")` completes without error (returns void/null)
- PATCH restore test: server returns `HttpResponse.json({ success: true, message: "restored", data: null })` → assert `restoreNotice("1")` completes without error
- DELETE permanent test: server returns `HttpResponse.json({ success: true, message: "permanent", data: null })` → assert `permanentDeleteNotice("1")` completes without error
- Each test: verify the correct URL was called by checking the handler received the right request

**tests/notice-hook.test.tsx** — Hook integration test:
- Use `renderWithProviders` to wrap a test component that calls `useNotices()`
- Register an MSW handler for GET /api/notice returning 3 mock notices
- Assert: hook returns `notices` query with `data` array of length 3
- Assert: hook returns `addNotice` mutation (check `typeof addNotice.mutate === "function"`)
- Assert: hook returns `editNotice`, `removeNotice`, `restore`, `permanentRemove` mutations
- Assert: mutations have `isPending` boolean property
- Use `await waitFor(() => expect(notices.data).toHaveLength(3))` pattern

Run `pnpm test -- --run` after writing — all new tests MUST fail (RED).
  </action>
  <behavior>
    - getExpiryStatus("2026-09-15") with system time 2026-08-26 → "active"
    - getExpiryStatus("2026-08-28") with system time 2026-08-26 → "expiring"
    - getExpiryStatus("2026-08-25") with system time 2026-08-26 → "expired"
    - getNotices() with MSW returning array of 2 notices → returns array of length 2
    - createNotice({title, description}) → returns object with _id field
    - updateNotice("id", {title}) → returns old document (pre-update values)
    - deleteNotice("id") → completes successfully, returns null/undefined
    - restoreNotice("id") → completes successfully
    - permanentDeleteNotice("id") → completes successfully
    - useNotices() hook → returns object with notices, addNotice, editNotice, removeNotice, restore, permanentRemove
  </behavior>
  <verify>
    <automated>pnpm test -- --run 2>&1 | grep -E "(FAIL|PASS|notice)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/notice-expiry.test.tsx` exists with ≥5 test cases covering active/expiring/expired thresholds
    - `tests/notice-api.test.ts` exists with ≥6 test cases (one per endpoint)
    - `tests/notice-hook.test.tsx` exists with ≥1 test case verifying hook shape
    - All tests import from non-existent modules (`@/services/notice.api`, `@/hooks/use-notice`, `@/features/noticeboard/notice-expiry-badge`) and FAIL with "module not found" or similar
    - `pnpm test -- --run` shows new test files with FAIL status
    - No `console.log` statements in test files (lint rule)
  </acceptance_criteria>
  <done>All three test files written and failing. Tests define the exact behavior the implementation must satisfy.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — Implement service layer, data hook, and expiry badge to pass tests</name>
  <files>services/notice.api.ts, hooks/use-notice.ts, features/noticeboard/notice-expiry-badge.tsx</files>
  <read_first>
    - tests/notice-expiry.test.tsx (read the test assertions to know exact expected behavior)
    - tests/notice-api.test.ts (read the test assertions to know exact expected behavior)
    - tests/notice-hook.test.tsx (read the test assertions to know exact expected behavior)
    - services/attendance.api.ts (service pattern: import apiFetch, define schema, export typed async fns returning res.data)
    - hooks/use-attendance.ts (hook pattern: useQuery + useMutation + invalidateQueries, flat return bag)
    - lib/date.ts (IST utilities — formatIstDate for badge display context)
    - lib/api-wrapper.ts (apiFetch signature and envelope shape)
  </read_first>
  <action>
Implement three files to make all tests pass (GREEN phase).

**services/notice.api.ts** — Mirror backend notice.schema.ts shapes exactly (per D-20):
- Import `apiFetch` from `@/lib/api-wrapper`
- Import `z` from `zod`
- Define `noticeSchema = z.object({ _id: z.string(), title: z.string(), description: z.string(), expiresAt: z.string(), isDeleted: z.boolean().optional(), createdAt: z.string().optional(), updatedAt: z.string().optional() })` — matching backend entity shape from notice.model.ts
- Export `type NoticeResponse = z.infer<typeof noticeSchema>`
- Define `createNoticeSchema = z.object({ title: z.string().min(1), description: z.string().min(1), expiresAt: z.string().optional() })`
- Export `type CreateNoticeInput = z.infer<typeof createNoticeSchema>`
- `getNotices`: call `apiFetch<NoticeResponse[]>("/api/notice", { method: "GET" })`, return `res.data` (raw array — do NOT use normalizeList, backend has no meta)
- `createNotice(data: CreateNoticeInput)`: POST to `/api/notice` with `JSON.stringify(data)`, return `res.data`
- `updateNotice(id: string, data: Partial<CreateNoticeInput>)`: PUT to `/api/notice/${id}` with `JSON.stringify(data)`, return `res.data` — NOTE: backend returns OLD doc (new:false), callers must invalidate cache
- `deleteNotice(id: string)`: DELETE `/api/notice/${id}`, return void
- `restoreNotice(id: string)`: PATCH `/api/notice/${id}/restore`, return void
- `permanentDeleteNotice(id: string)`: DELETE `/api/notice/${id}/permanent`, return void

**hooks/use-notice.ts** — TanStack Query hook:
- Import `useQuery`, `useMutation`, `useQueryClient` from `@tanstack/react-query`
- Import all 6 service functions from `@/services/notice.api`
- Export `useNotices` (accepts optional `{ id?: string }` param)
- `notices` query: `queryKey: ["notices", "active"]`, `queryFn: getNotices`
- `notice` query: `queryKey: ["notices", "detail", id]`, `queryFn` fetches from cache or full list, `enabled: !!id`
- All mutations (`addNotice`, `editNotice`, `removeNotice`, `restore`, `permanentRemove`): `onSuccess` invalidates `["notices"]` key
- Return flat bag: `{ notices, notice, addNotice, editNotice, removeNotice, restore, permanentRemove }`

**features/noticeboard/notice-expiry-badge.tsx** — Export both the component AND the helper:
- Export `getExpiryStatus(expiresAt: string): "active" | "expiring" | "expired"` as a named export
- Logic: `const now = new Date(); const expiry = new Date(expiresAt); if (expiry <= now) return "expired"; const daysRemaining = Math.ceil((expiry.getTime() - now.getTime()) / (1000 * 60 * 60 * 24)); if (daysRemaining <= 3) return "expiring"; return "active";`
- Export `NoticeExpiryBadge({ expiresAt }: { expiresAt: string })` component
- Map status to badge variant: active → "outline-success", expiring → "outline-warn", expired → "destructive"
- Map status to label: active → "Active", expiring → "Expiring Soon", expired → "Expired"
- Render `<Badge variant={variant}>{label}</Badge>` from `@/components/ui/badge`
- Mark `"use client"` at top

Run `pnpm test -- --run` after implementing — all new tests MUST pass (GREEN).
  </action>
  <verify>
    <automated>pnpm test -- --run 2>&1 | grep -E "(FAIL|PASS|notice)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- --run` exits 0 (all tests pass, including new notice tests)
    - `services/notice.api.ts` exports `noticeSchema`, `NoticeResponse`, `createNoticeSchema`, `CreateNoticeInput`, `getNotices`, `createNotice`, `updateNotice`, `deleteNotice`, `restoreNotice`, `permanentDeleteNotice`
    - `hooks/use-notice.ts` exports `useNotices` function that returns object with `notices`, `addNotice`, `editNotice`, `removeNotice`, `restore`, `permanentRemove` properties
    - `features/noticeboard/notice-expiry-badge.tsx` exports both `getExpiryStatus` function and `NoticeExpiryBadge` component
    - `grep -c "normalizeList" services/notice.api.ts` returns 0 (must NOT use normalizeList for raw array endpoint)
    - `grep -c "invalidateQueries" hooks/use-notice.ts` returns ≥1 (mutations must invalidate cache)
    - `grep -c '"use client"' features/noticeboard/notice-expiry-badge.tsx` returns 1
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>All tests pass. Service layer mirrors backend contract, hook provides full query/mutation interface, badge correctly derives expiry status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend API | All /api/* calls go through apiFetch with cookie credentials; no input sanitization beyond zod schemas |
| User input → Form fields | react-hook-form + zod validation on create/edit forms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | notice.api.ts service functions | accept | Backend is canonical; frontend sends validated zod output; backend re-validates |
| T-03-02 | Information Disclosure | noticeSchema response shape | accept | No sensitive data in notice entity (title, description, dates only) |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | No new packages installed in this phase |
</threat_model>

<verification>
- `pnpm test -- --run` — all tests pass including new notice tests
- `pnpm lint` — 0 errors
- `pnpm build` — type-checks pass (build may fail on pre-existing register issue, unrelated)
</verification>

<success_criteria>
- Expiry badge correctly classifies notices into active/expiring/expired with correct threshold (3 days)
- All 6 service functions call correct endpoints with correct HTTP methods
- GET /notice service returns raw array (not normalized envelope)
- Hook exposes list query and all 5 mutations with cache invalidation
- Tests cover happy path for all service functions and expiry badge logic
- No new dependencies installed
</success_criteria>

<output>
Create `.planning/phases/03-noticeboard-pilot/03-01-SUMMARY.md` when done
</output>
