---
plan: 03-04
gap_closure: true
type: execution
wave: 4
autonomous: true
objective: Close verification gaps from 03-VERIFICATION.md — fix SLICE-CONTRACT.md §9 to the post-CR-01 route pattern, add permission-gated Edit affordance on NoticeDetail, record accepted backend-constraint override for restore/permanent-delete UI.
files_modified:
  - .planning/codebase/SLICE-CONTRACT.md
  - features/noticeboard/notice-detail.tsx
  - .planning/phases/03-noticeboard-pilot/03-VERIFICATION.md
---

<objective>
Close the 3 gaps found by phase 03 verification:
1. SLICE-CONTRACT.md §9 documents deleted `(admin)` noticeboard authoring routes — rewrite to the implemented pattern (authoring routes at `app/(main)/<module>/new` and `/[id]/edit` with dedicated "use client" layout.tsx gating on `can(r, action, resource)`).
2. No Edit affordance anywhere — add an Edit button on NoticeDetail gated on `can(role, "update", "notice")` linking to `/noticeboard/[id]/edit`.
3. Restore/permanent-delete have no UI path because the backend has no trash-list endpoint (externally constrained) — record accepted override instead of building dead UI.
</objective>

<tasks>
<task id="1">
  <name>Update SLICE-CONTRACT.md §9 route structure</name>
  <files>.planning/codebase/SLICE-CONTRACT.md</files>
  <details>Replace step 3 and the See line: authoring routes live at `app/(main)/noticeboard/new/page.tsx` and `app/(main)/noticeboard/[id]/edit/page.tsx` (NOT under `(admin)`), each module provides its own "use client" `layout.tsx` gating on `can(r, action, resource)` per-resource — do NOT rely on group-layout RoleGuard inheritance for write/update surfaces.</details>
</task>
<task id="2">
  <name>Add Edit affordance to NoticeDetail</name>
  <files>features/noticeboard/notice-detail.tsx</files>
  <details>Import useMe + can; when `can(user?.role ?? "user", "update", "notice")`, render an Edit button next to Back linking to `/noticeboard/${noticeId}/edit`.</details>
</task>
<task id="3">
  <name>Record accepted override for trash UI</name>
  <files>.planning/phases/03-noticeboard-pilot/03-VERIFICATION.md</files>
  <details>Mark gap 3 as accepted override (backend lacks trash-list endpoint); bump overrides_applied to 1. Backend dependency noted for Phase 7 audit.</details>
</task>
</tasks>
