---
phase: fix-06-notice-leave
plan: 01
type: execute
wave: 1
depends_on: [fix-02-image-preview]
files_modified:
  - saher-backend/src/notice/notice.schema.ts
  - saher-backend/openapi/components/schemas/notice-response.yaml
  - saher-frontend/services/notice.api.ts
  - saher-frontend/features/noticeboard/notice-card.tsx
  - saher-frontend/features/noticeboard/notice-feed.tsx
  - saher-frontend/features/noticeboard/notice-trash.tsx
  - saher-frontend/features/noticeboard/notice-detail.tsx
  - saher-frontend/features/noticeboard/notice-edit.tsx
  - saher-frontend/tests/notice-api.test.ts
  - saher-frontend/tests/notice-hook.test.tsx
  - saher-frontend/features/leave/apply-leave-dialog.tsx
  - saher-backend/src/leave/leave.schema.ts
  - saher-backend/src/leave/leave.controller.ts
  - saher-backend/src/libs/utils/leave-logs.ts
  - saher-backend/src/libs/utils/leave.ts
  - saher-backend/src/database/leave-balance.model.ts
  - saher-backend/openapi/components/schemas/leave-response.yaml
autonomous: true
requirements: [NOTICE-01, LEAVE-01, LEAVE-02]
user_setup: []

must_haves:
  truths:
    - "A user with notice:delete permission can soft-delete a notice and it disappears from the active list, appears in the trash tab, and remains restorable — the delete request reaches DELETE /api/notice/:id with a real ObjectId."
    - "A leave application submitted with an uploaded proof succeeds, stores the canonical Media ObjectId reference (not a URL string), and renders back on read without a 'Type String at path proof' validation error."
    - "The leave balance endpoint returns correct used/remaining per leave type so no impossible negative remaining or doubled 'Added' value appears (used never exceeds allocatedDays unless prior approved leave legitimately consumed part of it)."
  artifacts:
    - path: "saher-frontend/services/notice.api.ts"
      provides: "Notice contract uses id (not _id) matching the backend normalized shape"
      contains: "id: z.string()"
    - path: "saher-backend/src/notice/notice.schema.ts"
      provides: "getNoticeSchema emits id + createdAt so the list response carries display fields"
      contains: "createdAt"
    - path: "saher-frontend/features/leave/apply-leave-dialog.tsx"
      provides: "Proof upload emits the Media ObjectId (file.id) to the leave form"
      contains: "field.onChange(file.id)"
    - path: "saher-backend/src/leave/leave.controller.ts"
      provides: "getLeaveBalance computes used/remaining correctly"
      contains: "remaining"
  key_links:
    - from: "saher-frontend/features/noticeboard/notice-feed.tsx"
      to: "DELETE /api/notice/:id"
      via: "removeNotice.mutate(deleteTarget.id) with a real ObjectId"
      pattern: "deleteTarget\\.id"
    - from: "saher-frontend/features/leave/apply-leave-dialog.tsx"
      to: "POST /api/leave/application/apply"
      via: "proof carries the Media ObjectId string file.id"
      pattern: "file\\.id"
    - from: "saher-backend/src/libs/utils/leave-logs.ts recordLeaveUsage"
      to: "saher-backend/src/leave/leave.controller.ts getLeaveBalance"
      via: "used Map keyed by leave code is incremented once per approval and read back as remaining = allocatedDays - used"
      pattern: "\\.used\\.|<leaveCode>"
---

<objective>
Stabilize the Notice and Leave modules per FIX_PHASE.md Phase 6 (NOTICE-01, LEAVE-01, LEAVE-02): root-cause and fix notice deletion, the leave proof field type error, and the incorrect leave balance/allotment — without refactoring unrelated code or inventing business rules.

Purpose: These are the highest-severity remaining data-contract and money/approval-path regressions in the org's daily-driver modules. Fixing the root causes keeps notice soft-delete policy intact, aligns leave proof to the Phase 2 image/file reference convention, and makes leave balances trustworthy.

Output: Working notice soft-delete end-to-end, a leave proof field that stores the canonical Media ObjectId (never a URL string), and a leave balance computation that returns correct used/remaining per leave type.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/catlium/Code/saher/saher-frontend/FIX_PHASE.md
@.planning/phases/fix-02-image-preview/02-01-PLAN.md

# Root-cause investigation summary (trace results):

# ---- NOTICE-01 (delete does not work) ----
# Backend GET /api/notice (notice.controller.ts getNotices):
#   notices.map(n => normalizeDoc(n.toObject()))   # _id -> id
#   getNoticeSchema.array().parse(normalized)        # zod object() STRIPS unknown keys
# getNoticeSchema = baseNoticeSchema.extend({ id: objectId })
#   -> response objects are exactly { id, title, description, expiresAt }
#   (createdAt/updatedAt/isDeleted stripped; _id already replaced by id)
# Frontend (services/notice.api.ts noticeSchema + every noticeboard component)
#   declares/reads notice._id (required) AND runs removeNotice.mutate(deleteTarget!._id).
#   _id is undefined after the backend parse -> DELETE /api/notice/undefined
#   -> Notice.findById(undefined) casts/fails -> 'deletion does not work'.
#   Also notice.createdAt is stripped -> cards render 'Created: undefined'.
# ROOT CAUSE: frontend `_id` field name diverges from backend's canonical `id`
#   (all other modules read `id`). Fix on the source-of-truth side: backend
#   getNoticeSchema keeps `id` and ADDS createdAt; frontend reads `id`.
#   Soft-delete policy is already correct (deleteNotice sets isDeleted=true) —
#   do NOT introduce hard deletion.

# ---- LEAVE-01 (Type String at path proof) ----
# leave.model.ts proof: { type: ObjectId, ref: 'Media' }  (expects Media ObjectId)
# leave.schema.ts: proof: z.string().optional() in create + get schemas
# Frontend apply-leave-dialog uses ImageUpload -> onUploadSuccess(file)
#   currently field.onChange(file.url)  [URL string — WRONG, Phase 2 not applied]
#   After Phase 2 the canonical convention is { id, alt, src }; proof must store
#   the Media ObjectId string file.id, NOT the src/url.
# ROOT CAUSE: proof carries an incompatible representation (a URL string) that
#   violates the Phase 2 image/file reference convention and the model's
#   ObjectId ref, surfacing as the proof type error on read/validation.
#   Depends on fix-02-image-preview which makes ImageUpload emit file.id.
#   This task verifies that dependency is honored and reconciles the zod schema
#   so stored legacy URL strings AND ObjectId strings both read cleanly.

# ---- LEAVE-02 (Allotted=12, Added=24) ----
# leave-balance.model.ts: only stores `used` Map { user, year }.
# getLeaveBalance (leave.controller.ts): remaining = allocatedDays - used,
#   balance[name] = { used, remaining }. Frontend LeaveBalanceCard:
#   total = used + remaining  -> always equals allocatedDays.
# So the only way 'Added=24' shows when allotted=12 is used == 24 (or a
#   cross-year/duplicate leave-balance doc). Trace list (must confirm against DB):
#   * calculateLeaveDays (libs/utils/leave.ts) totalDays used by recordLeaveUsage
#     $inc { used.<code> }: verify the day count isn't inflated (e.g. 24 vs 12).
#   * recordLeaveUsage (libs/utils/leave-logs.ts) accumulated `used` across
#     approvals; check per-year isolation and idempotency.
#   * reviewLeaveApplicationController approval path: exactly one $inc per approval
#     (status guard already prevents re-approval — confirm no double-increment path).
#   * LeaveBalance has NO unique index on {user,year} — upsert races can create
#     duplicate docs; getLeaveBalance's findOne then misses/returns one.
# ROOT CAUSE to confirm at runtime: used over-accumulated (or duplicate docs).
#   Fix the arithmetic source, not the displayed number.
</context>

<tasks>

<task type="auto">
  <name>Task 1: NOTICE-01 — align notice contract to backend `id` (fix soft-delete, preserve createdAt)</name>
  <files>
    saher-backend/src/notice/notice.schema.ts,
    saher-backend/openapi/components/schemas/notice-response.yaml,
    saher-frontend/services/notice.api.ts,
    saher-frontend/features/noticeboard/notice-card.tsx,
    saher-frontend/features/noticeboard/notice-feed.tsx,
    saher-frontend/features/noticeboard/notice-trash.tsx,
    saher-frontend/features/noticeboard/notice-detail.tsx,
    saher-frontend/features/noticeboard/notice-edit.tsx,
    saher-frontend/tests/notice-api.test.ts,
    saher-frontend/tests/notice-hook.test.tsx
  </files>
  <read_first>
    saher-backend/src/notice/notice.schema.ts,
    saher-backend/src/notice/notice.controller.ts,
    saher-backend/src/libs/utils/normailize-doc.ts,
    saher-frontend/services/notice.api.ts,
    saher-frontend/features/noticeboard/notice-feed.tsx,
    saher-frontend/features/noticeboard/notice-card.tsx,
    saher-frontend/features/noticeboard/notice-trash.tsx,
    saher-frontend/features/noticeboard/notice-detail.tsx,
    saher-frontend/features/noticeboard/notice-edit.tsx,
    saher-frontend/tests/notice-api.test.ts,
    saher-frontend/tests/notice-hook.test.tsx
  </read_first>
  <action>Root-cause fix for NOTICE-01. The backend GET /api/notice serializes each notice as `{ id, title, description, expiresAt }` (normalizeDoc turns `_id`→`id`; the zod object schema strips unknown keys), but the frontend declares/reads `_id` everywhere. The delete call therefore sends `undefined` as the id, so `Notice.findById(undefined)` casts/fails and deletion appears broken. Keep soft delete (backend deleteNotice already sets `isDeleted=true` and returns 200) — do NOT hard delete.

Backend (source of truth, keep `id` canonical — consistent with every other module):
1. In `saher-backend/src/notice/notice.schema.ts`, extend `getNoticeSchema` to preserve the fields the UI renders: append `createdAt: z.coerce.date()` (and `updatedAt: z.coerce.date().optional()`) to the existing `baseNoticeSchema.extend({ id: objectId })`. Do NOT add `isDeleted` to the response (the list filter is server-side; the trash tab does not read it). Leave `createNoticeSchema`/`updateNoticeSchema` unchanged.
2. Update `saher-backend/openapi/components/schemas/notice-response.yaml`: add a `createdAt` property (type string, format date-time) alongside `id`/`title`/`description`/`expiresAt`. Endpoint response shape changed, so run `cd ../saher-backend && pnpm docs:lint` and then `graphify update .` in the backend repo root.

Frontend (align to `id`):
3. `saher-frontend/services/notice.api.ts`: in `noticeSchema`, change `_id: z.string()` to `id: z.string()` and add `createdAt: z.string().optional()` (keep `title`, `description`, `expiresAt`, `isDeleted?`, `updatedAt?`). The `NoticeResponse` type derives via z.infer — no manual edit needed beyond the schema. Leave the CRUD functions unchanged.
4. Across the noticeboard feature replace every `notice._id` (and `n._id`, `deleteTarget!._id`, `restoreTarget!._id`) with `notice.id` (resp. `n.id`, `deleteTarget!.id`, `restoreTarget!.id`):
   - `features/noticeboard/notice-card.tsx`: `router.push(`/noticeboard/${notice._id}`)` → `${notice.id}`.
   - `features/noticeboard/notice-feed.tsx`: `key={notice._id}` → `key={notice.id}`; `removeNotice.mutate(deleteTarget!._id, ...)` → `deleteTarget!.id`.
   - `features/noticeboard/notice-trash.tsx`: `key={notice._id}`, `restoreTarget!._id`, `deleteTarget!._id` → `.id`.
   - `features/noticeboard/notice-detail.tsx`: `.find((n) => n._id === noticeId)` → `.find((n) => n.id === noticeId)`.
   - `features/noticeboard/notice-edit.tsx`: `.find((x) => x._id === noticeId)` → `.find((x) => x.id === noticeId)`; `id: n._id` → `id: n.id`.
   The `createdAt` display in `notice-card.tsx` / `notice-detail.tsx` now resolves to a real value once the backend emits it — do nothing further there.
5. Update test fixtures to match the real contract: in `tests/notice-api.test.ts` and `tests/notice-hook.test.tsx`, replace the `_id` field on mock notice objects with `id` (keep same string values); in `notice-api.test.ts` change the create assertion that reads `created._id` to `created.id`. Keep all other assertions intact.</action>
  <verify>cd ../saher-backend && pnpm docs:lint 2>&1 | tail -3; cd ../saher-frontend && pnpm lint && pnpm build 2>&1 | tail -5 && grep -rn "\._id\|_id:" features/noticeboard/ services/notice.api.ts; echo "---id-check---"; grep -rn "notice\.id\|deleteTarget\.id" features/noticeboard/</verify>
  <acceptance_criteria>
    - "saher-backend/src/notice/notice.schema.ts contains 'createdAt' on getNoticeSchema"
    - "saher-backend pnpm docs:lint passes; graphify update . ran"
    - "saher-frontend/services/notice.api.ts noticeSchema contains 'id: z.string()' and no '_id'"
    - "grep '_id' in features/noticeboard/ and services/notice.api.ts returns NO matches"
    - "saher-frontend/features/noticeboard/notice-feed.tsx contains 'deleteTarget!.id'"
    - "saher-frontend/tests/notice-api.test.ts uses 'id:' (not '_id:') on mock notices and asserts 'created.id'"
    - "saher-frontend pnpm lint passes and pnpm build (type-check) passes"
  </acceptance_criteria>
  <done>The notice response contract uses `id`/`createdAt` on both sides; every `_id` read is replaced with `id`; DELETE sends a real ObjectId so soft-delete works; the trash tab restores/permanently-deletes with `id`; createdAt displays correctly; docs lint + graphify updated; frontend lint/type-check pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: LEAVE-01 — make leave proof store the canonical Media ObjectId and tolerate legacy strings on read</name>
  <files>
    saher-frontend/features/leave/apply-leave-dialog.tsx,
    saher-backend/src/leave/leave.schema.ts,
    saher-backend/openapi/components/schemas/leave-response.yaml
  </files>
  <read_first>
    saher-frontend/features/leave/apply-leave-dialog.tsx,
    saher-frontend/components/image-upload.tsx,
    saher-frontend/services/leave.api.ts,
    saher-backend/src/leave/leave.schema.ts,
    saher-backend/src/database/leave.model.ts,
    saher-backend/src/leave/leave.controller.ts,
    saher-backend/src/database/media-upload.model.ts
  </read_first>
  <action>Root-cause fix for LEAVE-01. `proof` on `leave.model.ts` is `{ type: ObjectId, ref: 'Media' }`, so it must store the Media ObjectId string (`file.id`), never a URL string. The current `apply-leave-dialog` calls `onUploadSuccess={(file) => field.onChange(file.url)}` (a URL), which violates the Phase 2 image/file reference convention and feeds an incompatible value into the ObjectId-typed field, surfacing as 'Type String at path proof'. This plan depends on fix-02-image-preview, which changes `ImageUpload` to emit `{ id, alt, src }` and sets `apply-leave-dialog` to `field.onChange(file.id)`.

1. Verify the Phase 2 dependency landed (`saher-frontend/components/image-upload.tsx` emits `src` and `apply-leave-dialog.tsx` uses `file.id`). If the `file.id` change is NOT present, apply it here: change `onUploadSuccess={(file) => field.onChange(file.url)}` to `onUploadSuccess={(file) => field.onChange(file.id)}`. Only store the 24-char ObjectId string — never the `src`/`url`. Do NOT introduce a separate file-representation type for Leave.

2. Reconcile the read schema so stored proofs parse cleanly regardless of value form. In `saher-backend/src/leave/leave.schema.ts`, keep `proof: z.string().optional()` in `createLeaveApplicationSchema` and `updateLeaveApplicationSchema` (the write side accepts the ObjectId string), and in `getLeaveApplicationSchema` keep `proof: z.string().optional()` so a normalized ObjectId string OR a legacy URL string both pass. Confirm via `normalizeDoc` that an ObjectId `proof` becomes a string on read. If a raw ObjectId (BSON) could ever reach the parse because the populate/lean path differs from normalizeDoc, wrap the parse defensively — but do NOT change the stored type or add a `url` field.

3. Update `saher-backend/openapi/components/schemas/leave-response.yaml` so the `data` properties include `proof` (type string) and `totalDays` (type number) alongside the existing fields, matching the actual response contract. Endpoint response shape documented is being aligned to code, so run `cd ../saher-backend && pnpm docs:lint` and `graphify update .` in the backend repo root if the YAML changes.

4. Do NOT modify `saher-backend/src/database/leave.model.ts` (proof stays `ObjectId ref 'Media'` by design) and do NOT run any destructive migration on existing records. Legacy records that already stored URL strings must remain readable (the read schema tolerates a string); new records store the ObjectId string.</action>
  <verify>cd ../saher-backend && pnpm docs:lint 2>&1 | tail -3; cd ../saher-frontend && pnpm lint && pnpm build 2>&1 | tail -5 && grep -rn "file\.id\|file\.url" features/leave/apply-leave-dialog.tsx</verify>
  <acceptance_criteria>
    - "saher-frontend/features/leave/apply-leave-dialog.tsx contains 'field.onChange(file.id)' and NOT 'field.onChange(file.url)'"
    - "saher-backend/src/leave/leave.schema.ts keeps 'proof: z.string().optional()' in all three schemas"
    - "saher-backend/src/database/leave.model.ts proof remains 'type: ObjectId, ref: Media' (unchanged)"
    - "saher-backend/openapi/components/schemas/leave-response.yaml documents proof (string) and totalDays (number) when present"
    - "saher-backend pnpm docs:lint passes; saher-frontend pnpm lint + pnpm build pass"
  </acceptance_criteria>
  <done>The leave proof field carries a Media ObjectId string (`file.id`), never a URL; the read schema tolerates both ObjectId strings and legacy URL strings so no record fails parse; the model keeps its ObjectId ref; docs are aligned and lint/typecheck pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: LEAVE-02 — trace and fix the leave balance/allotment calculation (remove the doubled 'Added' value)</name>
  <files>
    saher-backend/src/leave/leave.controller.ts,
    saher-backend/src/libs/utils/leave-logs.ts,
    saher-backend/src/libs/utils/leave.ts,
    saher-backend/src/database/leave-balance.model.ts,
    saher-backend/openapi/components/schemas/leave-response.yaml
  </files>
  <read_first>
    saher-backend/src/leave/leave.controller.ts,
    saher-backend/src/libs/utils/leave-logs.ts,
    saher-backend/src/libs/utils/leave.ts,
    saher-backend/src/database/leave-balance.model.ts,
    saher-backend/src/database/leave-log.model.ts
  </read_first>
  <action>Root-cause fix for LEAVE-02. Do NOT change the displayed number — trace the arithmetic that produces `Allotted=12, Added=24` and fix the source. `LeaveBalance` stores only a `used` Map keyed `{ user, year }`; `getLeaveBalance` computes `remaining = leaveType.allocatedDays - used` and returns `{ used, remaining }` per leave type, and the frontend `LeaveBalanceCard` shows `total = used + remaining`. Because `total` always equals allocatedDays, the only way the UI shows a doubled amount is `used` being over-accumulated (or a duplicate/cross-year LeaveBalance doc). Proceed step by step and confirm against the live database before and after:

1. Inspect the live data. Query `LeaveBalance` for the affected user and the current year, and `LeaveLog` for that user/year/leaveCode. Confirm whether `used.<code>` is genuinely accumulated (e.g. 24) and whether it reflects real approved leave or an error. Also check whether more than one `LeaveBalance` doc exists for the same `{ user, year }` (the model has NO unique index — duplicate docs make `getLeaveBalance`'s `findOne` return just one, hiding/absorbing the rest).

2. Audit `calculateLeaveDays` in `saher-backend/src/libs/utils/leave.ts`: verify `Math.floor((endDate.getTime() - startDate.getTime()) / (1000*60*60*24)) + 1` produces the correct day count for the recorded `startDate`/`endDate`. Confirm `z.coerce.date()` on the controller payload and the IST `+05:30` offset handling do not inflate the count (e.g. a 24 instead of 12). If the day count is inflated, fix `calculateLeaveDays` to be timezone-safe for the IST day range (use calendar-day difference, not raw ms off-by-one), then backfill any over-counted `used` in `LeaveBalance` (subtract the excess for the affected records) — do NOT invent business rules beyond correcting the arithmetic.

3. Audit `recordLeaveUsage` in `saher-backend/src/libs/utils/leave-logs.ts`: confirm `$inc { [used.<code>]: days }` fires exactly once per approved leave and that a `reviewLeaveApplicationController` re-submission cannot double-increment. The `leave.status !== 'pending'` guard in the controller blocks re-review, so the main risk is an upsert race creating duplicate `LeaveBalance` docs. Add a unique index `{ user: 1, year: 1 }` on `saher-backend/src/database/leave-balance.model.ts` to prevent duplicate docs, and make `recordLeaveUsage`/`getLeaveBalance` idempotent against such duplicates (e.g. use `findOneAndUpdate` with the unique index; dedupe any existing duplicates in a one-time data fix). This is a schema/index addition, not a destructive migration.

4. Verify year/date filtering: `getLeaveBalance` uses `String(new Date().getFullYear())` while `recordLeaveUsage` uses `leave.startDate.getFullYear().toString()`. Confirm they agree for in-year approvals; if a leave spans a year boundary or is approved in a different year than it starts, decide the intended year (the startDate year is the current convention — keep it) and make `getLeaveBalance` consistent so the tally lands in the correct year doc.

5. After the source fix, recompute: `remaining = allocatedDays - used` should be non-negative for any leave type unless historical approved leave legitimately exceeds the annual grant (which is itself a data problem to surface, not mask). Update `saher-backend/openapi/components/schemas/leave-response.yaml` only if the balance response shape changes (it currently is `{ id, user, year, balance }` — leave as-is unless a field changes).

6. Do NOT modify the frontend balance display to 'fake' a number. The frontend already derives `total = used + remaining`; once the backend `used` is correct, `Added` (used) and `Allotted` (allocatedDays) reconcile.</action>
  <verify>cd ../saher-backend && pnpm build 2>&1 | tail -5 && pnpm docs:lint 2>&1 | tail -3; node -e "console.log('verify balance calc via API or DB query per acceptance criteria')"</verify>
  <acceptance_criteria>
    - "saher-backend/src/database/leave-balance.model.ts declares a unique compound index on { user, year }"
    - "saher-backend/src/libs/utils/leave.ts calculateLeaveDays is calendar-day-correct and not raw-ms off-by-one for IST dates"
    - "saher-backend/src/libs/utils/leave-logs.ts recordLeaveUsage cannot double-increment the same approved leave into the same user/year doc"
    - "A DB/API check shows used.<code> <= allocatedDays for the affected user/type (Added no longer 24 when Allotted is 12 unless historical approved leave genuinely consumed it)"
    - "No frontend file changed to display a fabricated balance number"
  </acceptance_criteria>
  <done>The leave balance/allotment root cause is identified from live data and fixed at the arithmetic source: totalDays/business calc is correct, `used` accumulation is once-per-approval and per-year (no duplicate docs), and the endpoint returns correct used/remaining so the UI's Added/Allotted reconcile without masking.</done>
</task>

</tasks>

<verification>
Before declaring plan complete (run against BOTH repos):
- [ ] Backend `pnpm build` and `pnpm docs:lint` pass (endpoint/response changes documented); `graphify update .` ran in the backend repo root after any route/response change in this plan.
- [ ] Frontend `pnpm lint` and `pnpm build` (type-check) pass; `grep -rn "_id" features/noticeboard/ services/notice.api.ts` yields no match.
- [ ] NOTICE-01: `NoticeDetail`/`NoticeFeed` resolve `id` (not `_id`); a soft-delete request hits `DELETE /api/notice/:id` with a real ObjectId and 200s; the notice moves to the trash tab and restores.
- [ ] LEAVE-01: the leave apply dialog writes `file.id` (Media ObjectId string) to `proof`; a submitted leave with proof reads back and previews without 'Type String at path proof'; legacy URL-string proofs still parse on read.
- [ ] LEAVE-02: live `LeaveBalance`/`LeaveLog` inspection confirms the root cause; `used` is once-per-approved-leave and per-year; the balance endpoint returns non-negative remaining consistent with real approvals; no displayed number is faked.
- [ ] No destructive data migration, no hard delete introduced, no unrelated code refactored (FIX_PHASE.md rules 2.2 / 2.3 / 2.7).
</verification>

<success_criteria>

- All three tasks completed.
- Notice soft-delete works end-to-end (soft, appears in trash, restorable) — NOTICE-01.
- Leave proof respects the Phase 2 Media ObjectId convention and reads back without the type error; legacy records stay compatible — LEAVE-01.
- Leave balance/allotment computation is corrected at the source; Added/Allotted reconcile with real data — LEAVE-02.
- Both repos type-check and lint; backend docs lint passes and graphify is updated; no architected behavior is weakened or faked.
</success_criteria>

<output>
After completion, create `.planning/phases/fix-06-notice-leave/06-01-SUMMARY.md`
</output>
