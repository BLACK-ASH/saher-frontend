---
phase: 01-quality-gates-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - pnpm-workspace.yaml
  - vitest.config.ts
autonomous: false
requirements: [FNDT-01, FNDT-07]

must_haves:
  truths:
    - "Fresh checkout runs `pnpm lint` and `pnpm typecheck` green"
    - "`pnpm test` exists and exits 0 (config valid; zero tests is acceptable at this point)"
    - "react-hook-form resolves as a production dependency of the built app"
    - "jwt-decode is absent from dependencies and lockfile; styles/ directory is gone"
  artifacts:
    - path: "package.json"
      provides: "typecheck + test scripts, dependency moves, hygiene removals"
      contains: "\"typecheck\": \"tsc --noEmit\""
    - path: "vitest.config.ts"
      provides: "jsdom environment, setup file hook, @ alias mirror, v8 coverage (report-only)"
      contains: "environment:"
    - path: "pnpm-lock.yaml"
      provides: "synced lockfile reflecting moves/removals/new dev deps"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json paths"
      via: "resolve.alias '@' -> repo root"
      pattern: "alias.*@"
    - from: "package.json scripts.test"
      to: "vitest binary"
      via: "\"test\": \"vitest run\""
      pattern: "vitest run"
    - from: "pnpm-workspace.yaml"
      to: "msw install"
      via: "build-scripts allowlist still honored after install"
      pattern: "msw"
---

<objective>
Put the repo on the `dev` branch (D-01), clear package hygiene debt (FNDT-07), add the `typecheck`/`test` scripts (D-04), and stand up the vitest foundation — deps installed behind a legitimacy gate, config mirroring the `@/*` alias, msw build allowlist verified.

Purpose: Every later task in this milestone assumes green lint/typecheck gates and an installable test toolchain; this plan creates that ground truth.
Output: Cleaned `package.json`, synced lockfile, working `vitest.config.ts`, all work on local `dev`.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-quality-gates-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-quality-gates-test-infrastructure/01-PATTERNS.md
@package.json
@pnpm-workspace.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync dev branch with local main and continue on dev (per D-01)</name>
  <files>.git (branch operations only — no source files)</files>
  <read_first>.planning/phases/01-quality-gates-test-infrastructure/01-CONTEXT.md (decisions section)</read_first>
  <action>
Run `git fetch origin`. Checkout a local `dev` tracking `origin/dev` (`git switch dev` if it exists locally, otherwise `git switch -c dev origin/dev`). Merge local `main` into `dev` (`git merge main`) so the unpushed planning-doc commits ride the dev line; resolve conflicts if any surface (expected: none — docs vs feature code). Do NOT push anything in this task, and NEVER push to `main` (D-01 hard law; pushing `dev` happens only at the final checkpoint of plan 01-03). Stay on `dev` for all subsequent tasks in this phase.
  </action>
  <verify>
    <automated>test "$(git rev-parse --abbrev-ref HEAD)" = "dev" && git merge-base --is-ancestor main dev && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` outputs `dev`
    - `git merge-base --is-ancestor main dev` exits 0 (local main's doc commits are on dev)
    - `git log origin/main..main --oneline` still lists the local-only commits (nothing was pushed to main)
  </acceptance_criteria>
  <done>Local `dev` = `origin/dev` + local main's planning docs; working branch is dev; nothing pushed.</done>
</task>

<task type="auto">
  <name>Task 2: Package hygiene + gate scripts (FNDT-07, D-04)</name>
  <files>package.json, pnpm-lock.yaml, styles/ (deleted)</files>
  <read_first>package.json, .planning/phases/01-quality-gates-test-infrastructure/01-PATTERNS.md (section "Hygiene targets"), .planning/phases/01-quality-gates-test-infrastructure/01-CONTEXT.md</read_first>
  <action>
Edit `package.json`: (1) In `scripts`, add exactly two keys — `"typecheck": "tsc --noEmit"` and `"test": "vitest run"` — leaving `dev`/`build`/`start`/`lint` untouched (D-04). (2) Move `"react-hook-form": "^7.71.1"` out of `devDependencies` into `dependencies` (runtime dep — imported by `features/login/components/login-form.tsx` and `features/register/user-register.tsx`). (3) Delete the `"jwt-decode": "^4.0.0"` entry (zero imports anywhere in source). Before deleting anything else: re-run the importer check for `styles/` — search all `.ts/.tsx/.css/.mjs/.json` for `_variables`, `_keyframe-animations`, and any import resolving into `styles/`; PATTERNS.md pre-confirmed zero importers, this is the belt-and-suspenders re-check CONTEXT.md requires. Then delete the `styles/` directory (`_variables.scss`, `_keyframe-animations.scss`). Run `pnpm install` to regenerate `pnpm-lock.yaml` — never hand-edit the lockfile. Do NOT add test dependencies yet; that is Task 3 behind the legitimacy gate.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const ok=p.scripts.typecheck==='tsc --noEmit' && p.scripts.test==='vitest run' && !!p.dependencies['react-hook-form'] && !p.dependencies['jwt-decode'] && !p.devDependencies?.['react-hook-form']; process.exit(ok?0:1)" && test ! -d styles && pnpm install --frozen-lockfile >/dev/null 2>&1 && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` scripts contain exactly `"typecheck": "tsc --noEmit"` and `"test": "vitest run"`
    - `react-hook-form` appears under `dependencies` and not under `devDependencies`
    - `jwt-decode` appears nowhere in `package.json` or `pnpm-lock.yaml`
    - `styles/` directory does not exist
    - `pnpm install` succeeds; `pnpm lint` exits 0; `pnpm typecheck` exits 0 (if pre-existing baseline errors surface, fix them minimally — FNDT-07 demands a green baseline, not refactors)
  </acceptance_criteria>
  <done>Scripts added, dependency hygiene cleared, dead SCSS deleted, lint + typecheck baseline green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3a: Package legitimacy gate for new dev dependencies</name>
  <files>package.json, pnpm-lock.yaml (targets of the gated install)</files>
  <action>
Present the eight-package install set to the user and pause until they confirm legitimacy on npmjs.com — this gate is never auto-approvable. On approval, proceed immediately to Task 3b with exactly the approved set; if the user names a swap or drop, adjust the install command accordingly.
  </action>
  <verify>
    <automated>echo "resume signal received: $(git rev-parse --abbrev-ref HEAD) branch active"</automated>
  </verify>
  <acceptance_criteria>
    - Explicit "approved" (or equivalent) recorded from the user before any `pnpm add` runs
    - Any package the user rejected is excluded from Task 3b's install list
  </acceptance_criteria>
  <done>User confirmed all eight packages legitimate; unblocked to install.</done>
  <what-built>Researched install set for the test harness: vitest (^4 stable line — v5 is RC, skip it), @testing-library/react (^16.3 — React 19 compatible), @testing-library/dom (^10 peer of RTL 16), @testing-library/jest-dom, jsdom, msw (^2.x stable line), @vitejs/plugin-react, @vitest/coverage-v8 (matching vitest major — required for report-only coverage per D-09).</what-built>
  <how-to-verify>
    No Package Legitimacy Audit table exists in planning artifacts (no RESEARCH.md for this phase), so per policy all eight are treated [ASSUMED] and require human confirmation before install. For each, open the npm page and confirm: package name exact-match, publisher/maintainer is the expected org, weekly downloads in the millions where applicable, published within the last ~6 months, no install-scripts red flags beyond known builders:
    1. https://www.npmjs.com/package/vitest (vitest-dev) — expect v4.x stable
    2. https://www.npmjs.com/package/@testing-library/react (testing-library org) — expect 16.x, peer-supports React 19
    3. https://www.npmjs.com/package/@testing-library/dom (testing-library org)
    4. https://www.npmjs.com/package/@testing-library/jest-dom (testing-library org)
    5. https://www.npmjs.com/package/jsdom (jsdom org)
    6. https://www.npmjs.com/package/msw (mswjs) — expect 2.x line
    7. https://www.npmjs.com/package/@vitejs/plugin-react (vitejs org)
    8. https://www.npmjs.com/package/@vitest/coverage-v8 (vitest-dev) — version must match installed vitest major
  </how-to-verify>
  <resume-signal>Type "approved" once all eight check out, or name any package to swap/drop</resume-signal>
</task>

<task type="auto">
  <name>Task 3b: Install test toolchain + vitest config (FNDT-01, D-05, D-09)</name>
  <files>package.json, pnpm-lock.yaml, pnpm-workspace.yaml, vitest.config.ts</files>
  <read_first>tsconfig.json (paths block), eslint.config.mjs (flat-config precedent), pnpm-workspace.yaml, Dockerfile (Corepack precedent), .planning/phases/01-quality-gates-test-infrastructure/01-PATTERNS.md (sections "Required config surface" and "pnpm-workspace.yaml")</read_first>
  <action>
Install as devDependencies (latest stable within the lines approved in Task 3a): `pnpm add -D vitest @testing-library/react @testing-library/dom @testing-library/jest-dom jsdom msw @vitejs/plugin-react @vitest/coverage-v8` (ensure @vitest/coverage-v8 major matches vitest's). Create root-level `vitest.config.ts` following the flat-root-config precedent of `eslint.config.mjs`: use `defineConfig` from `vitest/config`; set `test.environment` to `"jsdom"`; `test.setupFiles` to `["./tests/setup.ts"]` (file itself arrives in plan 01-02 — config referencing it is fine); do NOT enable `globals`; configure `test.coverage` with `provider: "v8"` and default reporters, explicitly NO thresholds (D-09 — thresholds arrive in Phase 7). Mirror the tsconfig alias exactly: `resolve.alias` mapping `"@"` to the repo root directory — compute it with `import.meta.dirname` (Node 24 target; fall back to `process.cwd()` only if TS rejects `import.meta` under the project's module setting). Add the `/// <reference types="vitest/config" />` header only if the editor/TS complains — do not fork a second tsconfig. After install, verify `pnpm-workspace.yaml` still expresses the msw build allowance in whatever key the installed pnpm version expects (PATTERNS notes the key name semantics may differ across pnpm versions — keep the allowlist intent, adjust only the key name if pnpm warns during install).
  </action>
  <verify>
    <automated>pnpm exec vitest run --passWithNoTests && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run --passWithNoTests` exits 0 (config parses, jsdom env loads, alias resolves, zero tests tolerated)
    - A scratch check proves alias fidelity: temporarily importing `@/lib/api-wrapper` from any throwaway spec resolves to the real file (remove the scratch file after)
    - `pnpm build` exits 0 — production bundle unaffected by dev-only tooling
    - `vitest.config.ts` contains `jsdom`, `tests/setup.ts`, `v8` coverage provider, and no `thresholds` key
    - New devDependencies present in `package.json` and pinned in `pnpm-lock.yaml`
  </acceptance_criteria>
  <done>Toolchain installed behind the legitimacy gate; vitest config mirrors tsconfig alias; build stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | Eight new dev dependencies enter the dependency graph |
| local git → remotes | Branch operations could accidentally publish to `main`, whose push trigger still fires today's ungated deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | vitest, @testing-library/*, jsdom, msw, @vitejs/plugin-react, @vitest/coverage-v8 installs | mitigate | Blocking-human legitimacy checkpoint (Task 3a, never auto-approvable); versions locked in pnpm-lock.yaml; later CI installs use `--frozen-lockfile` |
| T-01-BR | Tampering/Elevation | git push targets | mitigate | Task 1 forbids any push; `main` remains frozen; the only push in this phase happens post-approval in plan 01-03's checkpoint, targeting `dev` exclusively |
| T-01-HY | Information Disclosure | styles/ deletion | accept | Zero importers confirmed twice (PATTERNS.md grep + Task 2 re-check); deletion cannot change runtime behavior |

</threat_model>

<verification>
- `pnpm lint && pnpm typecheck` green on the dev branch (success criterion 1, partial — `test` lands next plan)
- `pnpm exec vitest run --passWithNoTests` exits 0
- `pnpm build` still exits 0
- `node -e` dependency assertions from Task 2 hold
</verification>

<success_criteria>
- Work lives on local `dev`; `main` untouched and unpushed
- FNDT-07 hygiene items all verifiably applied
- Test toolchain installed with legitimate packages, config alias-faithful, coverage report-only
</success_criteria>

<output>
Create `.planning/phases/01-quality-gates-test-infrastructure/01-01-SUMMARY.md` when done
</output>
