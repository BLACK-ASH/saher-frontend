---
phase: 01-quality-gates-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/setup.ts
  - tests/render-with-providers.tsx
  - tests/test-server.ts
  - features/login/components/login-form.test.tsx
  - lib/api-wrapper.test.ts
autonomous: true
requirements: [FNDT-01]

must_haves:
  truths:
    - "A real feature component (LoginForm) renders under a QueryClientProvider test wrapper in jsdom and its visible output can be asserted"
    - "An msw handler intercepts fetch so the REAL `apiFetch` parses a full `{ success, message, data }` envelope end-to-end"
    - "`pnpm lint && pnpm typecheck && pnpm test` is fully green with zero skipped tests"
  artifacts:
    - path: "tests/setup.ts"
      provides: "jest-dom matchers, RTL cleanup, msw server lifecycle (listen/resetHandlers/close)"
      contains: "server.listen"
    - path: "tests/render-with-providers.tsx"
      provides: "renderWithProviders helper wrapping render in QueryClientProvider"
      contains: "QueryClientProvider"
    - path: "tests/test-server.ts"
      provides: "shared setupServer instance for handler registration"
      contains: "setupServer"
    - path: "features/login/components/login-form.test.tsx"
      provides: "reference component-render test pattern for later phases"
    - path: "lib/api-wrapper.test.ts"
      provides: "reference msw-at-boundary integration test pattern for later phases"
  key_links:
    - from: "tests/render-with-providers.tsx"
      to: "app/provider.tsx defaults"
      via: "fresh QueryClient per call with retry:false, refetchOnWindowFocus:false"
      pattern: "retry: false"
    - from: "lib/api-wrapper.test.ts"
      to: "lib/api-wrapper.ts apiFetch"
      via: "real apiFetch driven through msw-intercepted global fetch — no mocks of wrapper or query internals"
      pattern: "apiFetch"
    - from: "tests/setup.ts"
      to: "tests/test-server.ts server"
      via: "beforeAll listen / afterEach reset / afterAll close"
      pattern: "resetHandlers"
---

<objective>
Prove the harness with the two durable reference tests D-08 demands: one real component rendered through testing-library in jsdom, and one msw-intercepted call flowing through the actual `lib/api-wrapper.ts` envelope parsing.

Purpose: Success criteria 2 and 3 of the phase; these two files are the patterns every later module phase copies.
Output: Four small files under `tests/` plus two co-located test suites, all green.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-quality-gates-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-quality-gates-test-infrastructure/01-PATTERNS.md
@.planning/phases/01-quality-gates-test-infrastructure/01-01-SUMMARY.md

<interfaces>
<!-- Contracts extracted from the codebase — use directly, no exploration needed -->

From lib/api-wrapper.ts:
```typescript
export type MetaResponse = { page: number; limit: number; count: number; total: number }; // fields per PATTERNS.md :3-8
export type ApiResponse<T> = { success: boolean; message: string; data: T; meta?: MetaResponse };
export async function apiFetch<T>(url: string, options?: RequestInit): Promise<ApiResponse<T>>;
// Failure contract: malformed JSON or json.success !== true -> sonner toast + throw Error(json.message || `HTTP ${res.status}`)
```

From app/provider.tsx (defaults the helper must replicate):
```typescript
new QueryClient({ defaultOptions: { queries: { retry: false, refetchOnWindowFocus: false } } })
```

From features/login/components/login-form.tsx:
```typescript
export function LoginForm({ className, ...props }: React.ComponentProps<"div">);
// Renders labels "Email" and "Password"; submit button text "Login" (disabled while isPending)
// Uses useRouter() from next/navigation AND useLogin() -> needs QueryClientProvider + router mock
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provider-aware render slice — setup file, helper, LoginForm reference test (D-05, D-07, D-08)</name>
  <files>tests/setup.ts, tests/render-with-providers.tsx, features/login/components/login-form.test.tsx</files>
  <read_first>app/provider.tsx, features/login/components/login-form.tsx, hooks/use-login.ts, .planning/phases/01-quality-gates-test-infrastructure/01-PATTERNS.md (sections "Test setup file", "Shared test render helper", "Sample component render test"), vitest.config.ts</read_first>
  <action>
Create `tests/setup.ts`: import `@testing-library/jest-dom/vitest` for DOM matchers and register `afterEach(() => cleanup())` from @testing-library/react (msw lifecycle joins this file in Task 2). Create `tests/render-with-providers.tsx`: export `renderWithProviders(ui, options?)` that builds a fresh `new QueryClient({ defaultOptions: { queries: { retry: false, refetchOnWindowFocus: false } } })` per call — fresh instance per test prevents cache bleed, do NOT add gcTime unless isolation issues actually appear — and renders via RTL `render` with a `wrapper` of `QueryClientProvider client={client}`; return the render result. Do not pull in ThemeProvider/TooltipProvider (login-form uses neither directly). Create `features/login/components/login-form.test.tsx`: at module top `vi.mock("next/navigation")` stubbing `useRouter` to return `{ push: vi.fn(), replace: vi.fn(), refresh: vi.fn(), back: vi.fn() }` — plain jsdom has no Next router context and login-form calls useRouter unconditionally; then a single test rendering `<LoginForm />` through `renderWithProviders` asserting `getByLabelText(/email/i)`, `getByLabelText(/password/i)`, and `getByRole("button", { name: /login/i })` are all `toBeInTheDocument()`. Keep it render-level per D-08 — this is the durable reference pattern, not a form-behavior suite. Obey the no-console lint rule inside test files (warn/error only).
  </action>
  <verify>
    <automated>pnpm exec vitest run features/login && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run features/login` exits 0 with the LoginForm test passing (not skipped/todo)
    - The test imports LoginForm via `@/features/login/components/login-form` (alias resolution proven through the real import graph)
    - No mock of TanStack Query internals or services — only next/navigation is mocked
    - `pnpm lint` exits 0 (no-console respected in all new files)
    - `renderWithProviders` creates its QueryClient inside the function body (fresh per call), replicating app/provider.tsx defaults exactly
  </acceptance_criteria>
  <done>A real feature component renders under providers in jsdom with visible-output assertions passing — success criterion 2 met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: msw-at-the-boundary slice — shared server + apiFetch envelope integration test (D-06, D-08)</name>
  <files>tests/test-server.ts, tests/setup.ts, lib/api-wrapper.test.ts</files>
  <behavior>
    - Scenario 1: msw handler for GET /api/attendance/me returns HttpResponse.json({ success: true, message: "ok", data: fixture }); apiFetch("/api/attendance/me") resolves toMatchObject({ success: true, message: "ok", data: fixture })
    - Scenario 2: msw handler for GET /api/attendance/user/me returns envelope plus meta { page: 1, limit: 10, count: 1, total: 1 }; apiFetch result's meta deep-equals that object (MetaResponse passthrough proven)
    - Edge: a handler omitting success:true must make apiFetch throw even on HTTP 200 — proves handlers must serve full envelopes (boundary contract, not just happy path)
  </behavior>
  <read_first>lib/api-wrapper.ts, services/attendance.api.ts (fixture shapes for attendanceSchema), tests/setup.ts (current state from Task 1), .planning/phases/01-quality-gates-test-infrastructure/01-PATTERNS.md (sections "lib/api-wrapper.test.ts" and "Shared Patterns")</read_first>
  <action>
Create `tests/test-server.ts` exporting `const server = setupServer()` with NO default handlers — individual tests register handlers inline via `server.use(...)`, keeping fixtures beside the assertions. Extend `tests/setup.ts` to import that server and add the standard msw 2.x lifecycle: `beforeAll(() => server.listen({ onUnhandledRequest: "error" }))`, extend the existing afterEach with `server.resetHandlers()` (keep the Task-1 cleanup call), `afterAll(() => server.close())`. `onUnhandledRequest: "error"` turns stray fetches into loud failures — boundary hygiene, keep it. Create `lib/api-wrapper.test.ts` driving the REAL `apiFetch` — never mock the wrapper, TanStack Query, or services; only the socket is faked. Write three tests matching the `<behavior>` block: single-resource scenario with an attendance-shaped data fixture copied structurally from `services/attendance.api.ts`'s schema usage; list scenario proving meta passthrough; and the missing-success edge case asserting `rejects.toThrow()` with a handler returning `{ message: "nope" }` without `success`. Use `http.get` + `HttpResponse.json` from msw. Do NOT add refresh/retry scenarios — explicitly deferred to Phase 7 (D-08).
  </action>
  <verify>
    <automated>pnpm test && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 with both suites green (login-form render test still passes alongside)
    - `lib/api-wrapper.test.ts` asserts on the resolved ApiResponse including `meta` passthrough, and includes the missing-success rejection case
    - Handlers construct responses exclusively as full `{ success, message, data, meta? }` envelopes via HttpResponse.json
    - `server.listen({ onUnhandledRequest: "error" })`, `resetHandlers`, and `close` appear in tests/setup.ts
    - `pnpm lint && pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>An msw handler serves mock data through the real apiFetch wrapper and the parsed envelope asserts correctly — success criterion 3 met; full gate trio green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production bundle | Co-located `.test.ts(x)` files sit inside `lib/` and `features/`; if app code ever imported them, dev-only code ships to prod |
| msw interceptors → network | Tests fake sockets; misconfigured interception could mask real-API drift if reused carelessly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-TB | Tampering | test files leaking into production bundles | mitigate | Test files are never imported by app code (co-location convention only); `pnpm build` gate from plan 01-01 re-runs in CI proving exclusion; vitest globals stay off so test imports are explicit and tree-shakeable |
| T-01-SW | Information Disclosure | msw browser service worker artifact | mitigate | Node/jsdom interceptor path only (`setupServer`) — never run msw's browser init; if `public/mockServiceWorker.js` ever appears, delete it (nothing here serves mocked APIs to browsers) |
| T-01-FX | Information Disclosure | fixture data resembling real user PII | accept | Fixtures are synthetic minimal objects (boolean flags, numbers); no names/emails/tokens |
| T-01-ST | Denial of Service (test env) | sonner toasts firing during apiFetch error paths | accept | Harmless in jsdom; optionally spy via vi.spyOn rather than silencing globally |

</threat_model>

<verification>
- `pnpm lint && pnpm typecheck && pnpm test` all green (success criterion 1 now complete)
- Both reference tests pass against REAL app code paths (providers defaults, apiFetch envelope parsing)
- `pnpm build` still green (run once at plan end as bundle-contamination check)
</verification>

<success_criteria>
- Success criteria 2 and 3 satisfied by durable, copyable reference tests
- Zero skipped/todo tests; no mocks above or below the apiFetch boundary
- Patterns documented implicitly by the files themselves for Phases 2–7 reuse
</success_criteria>

<output>
Create `.planning/phases/01-quality-gates-test-infrastructure/01-02-SUMMARY.md` when done
</output>
