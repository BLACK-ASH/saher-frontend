---
phase: 08-bug-fixes-calendar-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - features/register/register-schema.ts
  - features/register/employee-details.tsx
  - features/profile/profile-info.tsx
autonomous: true
requirements: [BUGF-01, BUGF-02]

must_haves:
  truths:
    - "Registration form shows correct per-field error messages, not copy-pasted 'Date Of Birth Is Required' on 5 unrelated fields"
    - "Shift-2 label shows '2:00 PM' not '2:00 AM'"
    - "Profile change-email/password/profile handlers never show both error and success toast on the same failure"
  artifacts:
    - path: "features/register/register-schema.ts"
      provides: "Five corrected zod error messages for employeeId, department, designation, salaryStructure, address"
      contains: "Employee ID is required"
    - path: "features/register/employee-details.tsx"
      provides: "Corrected shift-2 time label"
      contains: "2:00 PM"
    - path: "features/profile/profile-info.tsx"
      provides: "Early return after toast.error in handleChangeEmail, handleChangePassword, handleChangeProfile"
      contains: "return;"
  key_links:
    - from: "features/register/register-schema.ts"
      to: "features/register/user-register.tsx"
      via: "registerFormSchema zodResolver"
      pattern: "zodResolver\\(registerFormSchema\\)"
    - from: "features/profile/profile-info.tsx"
      to: "lib/api-wrapper.ts"
      via: "apiFetch"
      pattern: "apiFetch"
---

<objective>
Fix the two simplest bugs: registration copy-paste error messages and profile double-toast. These are pure edits with zero dependencies — they ship first.

Purpose: Registration error messages are embarrassingly wrong (5 fields all say "Date Of Birth Is Required"), and the profile page shows both error and success toast on every failure. Both are trivial to fix and remove user-visible pain immediately.

Output: Corrected zod messages in register-schema.ts, corrected shift label in employee-details.tsx, early-return pattern in profile-info.tsx.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-fixes-calendar-overhaul/08-CONTEXT.md
@.planning/phases/08-bug-fixes-calendar-overhaul/08-RESEARCH.md
@.planning/phases/08-bug-fixes-calendar-overhaul/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix registration error messages (D-02) and shift-2 time label (D-02)</name>
  <files>features/register/register-schema.ts, features/register/employee-details.tsx</files>
  <read_first>
    - features/register/register-schema.ts (lines 67-76: the 5 copy-paste bug lines, and surrounding fields for message style)
    - features/register/employee-details.tsx (line 38: the shift-2 label)
  </read_first>
  <action>
    Per D-02 and RESEARCH F11:

    1. In `features/register/register-schema.ts`, replace lines 67-76 — the 5 fields that all incorrectly use `"Date Of Birth Is Required."` — with the correct per-field messages:
       - `employeeId: z.string().min(2, "Employee ID is required"),`
       - `department: z.string().min(2, "Department is required"),`
       - `designation: z.string().min(2, "Designation is required"),`
       - `salaryStructure: z.string().min(2, "Salary structure is required"),`
       - `address: z.string().min(2, "Address is required"),`

    2. In `features/register/employee-details.tsx`, line 38, change the shift-2 label from `"2:00 AM - 6:00 PM"` to `"2:00 PM - 6:00 PM"`.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>Five register-schema error messages are unique and field-correct; shift-2 label shows PM not AM; lint/typecheck green.</done>
  <acceptance_criteria>
    - Source assertion: `grep -c "Date Of Birth Is Required" features/register/register-schema.ts` returns 1 (only the original dateOfBirth field retains it)
    - Source assertion: `grep -c "Employee ID is required" features/register/register-schema.ts` returns 1
    - Source assertion: `grep -c "Department is required" features/register/register-schema.ts` returns 1
    - Source assertion: `grep -c "Designation is required" features/register/register-schema.ts` returns 1
    - Source assertion: `grep -c "Salary structure is required" features/register/register-schema.ts` returns 1
    - Source assertion: `grep -c "Address is required" features/register/register-schema.ts` returns 1
    - Source assertion: `grep "2:00 PM" features/register/employee-details.tsx` returns a match
    - Source assertion: `grep -c "2:00 AM" features/register/employee-details.tsx` returns 0
    - `pnpm lint` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix profile double-toast pattern (D-03)</name>
  <files>features/profile/profile-info.tsx</files>
  <read_first>
    - features/profile/profile-info.tsx (lines 37-75: the three handlers handleChangeEmail, handleChangePassword, handleChangeProfile)
  </read_first>
  <action>
    Per D-03 and RESEARCH F12/Pattern D:

    In `features/profile/profile-info.tsx`, add `return;` after every `toast.error(res.message)` call in the three handlers. The pattern to apply to each handler:

    For `handleChangeEmail` (around line 37-47):
    - After the line `toast.error(res.message);` inside the `if (!res.success)` block, add `return;` on the next line.

    For `handleChangePassword` (around line 48-57):
    - Same: add `return;` after `toast.error(res.message);` inside the `if (!res.success)` block.

    For `handleChangeProfile` (around line 59-75):
    - Same: add `return;` after `toast.error(res.message);` inside the `if (!res.success)` block.

    Note: Since `apiFetch` throws on non-success (per `lib/api-wrapper.ts`), the `!res.success` branch is likely unreachable in practice. But the code structure must be correct — the early return prevents `toast.success()` from running if the branch is ever reached.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>All three profile handlers have early return after toast.error; no path runs toast.error followed by toast.success; lint/typecheck green.</done>
  <acceptance_criteria>
    - Source assertion: `grep -c "return;" features/profile/profile-info.tsx` returns at least 3 (one per handler)
    - Source assertion: in each handler, the line `return;` appears within 2 lines after `toast.error(res.message);`
    - Source assertion: no handler contains `toast.success` without a `return;` before it in the error path
    - `pnpm lint` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client form validation → API | Registration zod messages are cosmetic only; server validates independently |
| API response → toast | Profile handlers show server messages; early return prevents false success toast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Spoofing | Registration error messages | accept | Messages are cosmetic client-side validation; server rejects invalid payloads regardless |
| T-08-01-02 | Information Disclosure | Profile error toast showing server message | accept | apiFetch already surfaces server error messages via sonner; early return just prevents false success |
</threat_model>

<verification>
- `pnpm lint` — 0 new errors
- `pnpm typecheck` — compiles clean
- Registration: each field shows its own error message on validation failure
- Profile: only one toast (error OR success) fires per operation
</verification>

<success_criteria>
- Registration form shows 5 distinct, field-correct error messages
- Shift-2 label reads "2:00 PM - 6:00 PM"
- Profile handlers never show both error and success toast
</success_criteria>

<output>
Create `.planning/phases/08-bug-fixes-calendar-overhaul/08-01-SUMMARY.md` when done
</output>
