---
phase: 08-bug-fixes-calendar-overhaul
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - ../saher-backend/src/notice/notice.controller.ts
  - ../saher-backend/src/notice/notice.routes.ts
  - services/notice.api.ts
  - hooks/use-notice.ts
  - features/noticeboard/notice-trash.tsx
autonomous: true
requirements: [BUGF-03, BUGF-06]

must_haves:
  truths:
    - "Admins can see deleted notices in a trash tab with restore and permanent-delete buttons"
    - "Backend getNotices accepts an isDeleted query parameter (default false) so trash listing works"
    - "Restore moves a notice back to active; permanent-delete irrecoverably removes it"
    - "Both actions use AlertDialog confirmation per D-20"
  artifacts:
    - path: "../saher-backend/src/notice/notice.controller.ts"
      provides: "getNotices reads isDeleted query param from req.query"
      contains: "isDeleted"
    - path: "../saher-backend/src/notice/notice.routes.ts"
      provides: "Route unchanged (GET / passes query to controller)"
      contains: "getNotices"
    - path: "services/notice.api.ts"
      provides: "getNotices accepts isDeleted parameter; export getTrashedNotices alias"
      contains: "isDeleted"
    - path: "hooks/use-notice.ts"
      provides: "trashedNotices query using getTrashedNotices; restore/permanentRemove mutations exported"
      contains: "trashedNotices"
    - path: "features/noticeboard/notice-trash.tsx"
      provides: "Real trash list with restore/permanent-delete buttons and AlertDialog confirmations"
      contains: "TrashTabPattern"
  key_links:
    - from: "features/noticeboard/notice-trash.tsx"
      to: "hooks/use-notice.ts"
      via: "trashedNotices query + restore/permanentRemove mutations"
      pattern: "useNotice|trashedNotices"
    - from: "hooks/use-notice.ts"
      to: "services/notice.api.ts"
      via: "getTrashedNotices queryFn"
      pattern: "getTrashedNotices"
    - from: "services/notice.api.ts"
      to: "lib/api-wrapper.ts"
      via: "apiFetch GET /api/notice?isDeleted=true"
      pattern: "apiFetch.*notice.*isDeleted"
---

<objective>
Wire the notice trash tab end-to-end: backend gets isDeleted query param support, frontend service/hook/trash component all connect to list, restore, and permanently delete trashed notices.

Purpose: The notice trash tab is a placeholder with dead code. Backend supports soft-delete/restore/permanent-delete but never lists deleted items. This plan bridges the gap across both repos.

Output: Backend getNotices controller accepts isDeleted param; frontend trash tab shows real trashed notices with restore + permanent-delete (AlertDialog-confirmed).
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bug-fixes-calendar-overhaul/08-CONTEXT.md
@.planning/phases/08-bug-fixes-calendar-overhaul/08-RESEARCH.md
@.planning/phases/08-bug-fixes-calendar-overhaul/08-PATTERNS.md

<interfaces>
<!-- Backend notice contract from RESEARCH F10 -->

From ../saher-backend/src/notice/notice.controller.ts (current getNotices, lines 111-117):
```typescript
export const getNotices = async (req: Request, res: Response) => {
  const notices = await Notice.find({
    isDeleted: false,        // hard-filter
    expiresAt: { $gt: new Date() },
  }).sort({ createdAt: -1 });
  res.status(200).json({ success: true, message: 'Notices retrieved', data: notices });
};
```

From ../saher-backend/src/notice/notice.routes.ts:
```typescript
noticeRouter.get('/', getNotices);
noticeRouter.post('/', authorize('write', 'notice'), validate(createNoticeSchema), createNotice);
// ...restore, permanent-delete already exist
```

From ../saher-backend/src/database/notice.model.ts:
```typescript
// Notice has TTL index on expiresAt — deleted-but-expired notices get physically purged
```

From hooks/use-notice.ts (existing dead code, lines 41-49):
```typescript
const restore = useMutation({ mutationFn: restoreNotice, onSuccess: invalidateNotices });
const permanentRemove = useMutation({ mutationFn: permanentDeleteNotice, onSuccess: invalidateNotices });
```

From services/notice.api.ts (existing service functions, lines 62-68):
```typescript
export const restoreNotice = async (id: string) => { await apiFetch(`/api/notice/${id}/restore`, { method: "PATCH" }); };
export const permanentDeleteNotice = async (id: string) => { await apiFetch(`/api/notice/${id}/permanent`, { method: "DELETE" }); };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — add isDeleted query param to getNotices (D-04, cross-repo per D-21)</name>
  <files>../saher-backend/src/notice/notice.controller.ts</files>
  <read_first>
    - ../saher-backend/src/notice/notice.controller.ts (full file — read getNotices at lines 111-117 and surrounding context)
    - ../saher-backend/src/notice/notice.routes.ts (route definition for GET /)
    - ../saher-backend/src/database/notice.model.ts (entity shape, TTL index)
  </read_first>
  <action>
    Per D-04 + RESEARCH F10/Pitfall 4:

    In `../saher-backend/src/notice/notice.controller.ts`, modify the `getNotices` function:

    1. Read `isDeleted` from `req.query.isDeleted` — default to `"false"`.
    2. Parse it: `const showDeleted = req.query.isDeleted === 'true';`
    3. Build the filter dynamically:
       - When `showDeleted` is false: `{ isDeleted: false, expiresAt: { $gt: new Date() } }` (existing behavior)
       - When `showDeleted` is true: `{ isDeleted: true }` — drop the expiresAt filter so recently-deleted items appear even if expired (TTL index may purge them later per Pitfall 4; this is best-effort)
    4. Keep the sort `{ createdAt: -1 }` in both cases.

    Note: the `noticeRouter.get('/', getNotices)` route definition does NOT need to change — the controller reads `req.query` which is already available.
  </action>
  <verify>
    <automated>grep -n "isDeleted" ../saher-backend/src/notice/notice.controller.ts</automated>
  </verify>
  <done>getNotices reads isDeleted query param; GET /api/notice returns active notices by default; GET /api/notice?isDeleted=true returns deleted notices; existing restore/permanent-delete routes untouched.</done>
  <acceptance_criteria>
    - Source assertion: `../saher-backend/src/notice/notice.controller.ts` contains `req.query.isDeleted` or `req.query["isDeleted"]`
    - Source assertion: the function has a conditional filter that includes `{ isDeleted: true }` when the param is `"true"`
    - Source assertion: default behavior (no param) still filters `isDeleted: false`
    - Source assertion: the `expiresAt` filter is only applied when `isDeleted` is not `"true"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Frontend — wire notice trash service + hook + trash tab component</name>
  <files>services/notice.api.ts, hooks/use-notice.ts, features/noticeboard/notice-trash.tsx</files>
  <read_first>
    - services/notice.api.ts (full file — existing getNotices, restoreNotice, permanentDeleteNotice)
    - hooks/use-notice.ts (full file — existing queries, mutations including dead restore/permanentRemove)
    - features/noticeboard/notice-trash.tsx (current placeholder)
    - features/reimbursement/recycle-bin.tsx (analog: the Phase 3 trash pattern with TrashTabPattern + table + restore/delete buttons)
    - features/reimbursement/recycle-bin.tsx lines 310-325 (AlertDialog delete-confirm pattern from management/page.tsx)
    - components/shared/trash-tab-pattern.tsx (TrashTabPattern component signature)
    - components/ui/alert-dialog.tsx (AlertDialog API)
    - components/no-data.tsx (NoData component for empty states)
    - components/loading.tsx (DefaultLoader for loading states)
    - lib/date.ts (formatIstDate for displaying dates)
  </read_first>
  <action>
    Per D-04, D-20, RESEARCH F10, PATTERNS §notice-trash.tsx:

    **services/notice.api.ts:**
    1. Modify the existing `getNotices` function to accept an optional `isDeleted` parameter (default `false`):
       ```typescript
       export const getNotices = async (isDeleted = false): Promise<NoticeResponse[]> => {
         const res = await apiFetch<NoticeResponse[]>(`/api/notice?isDeleted=${isDeleted}`, { method: "GET" });
         return res.data;
       };
       ```
    2. The existing `restoreNotice` and `permanentDeleteNotice` functions at lines 62-68 are already correct — no changes needed.

    **hooks/use-notice.ts:**
    1. Add a new query for trashed notices alongside the existing `notices` query:
       ```typescript
       const trashedNotices = useQuery({
         queryKey: ["notices", "trash"],
         queryFn: () => getNotices(true),
       });
       ```
    2. The existing `restore` and `permanentRemove` mutations (lines 41-49) are already wired with `onSuccess: invalidateNotices` — no changes needed.
    3. Add `trashedNotices` to the hook's return bag alongside `notices`, `restore`, `permanentRemove`.

    **features/noticeboard/notice-trash.tsx:**
    Replace the entire placeholder with a real trash list following the `recycle-bin.tsx` pattern:
    1. Import `useNotice` (or whatever the hook exports) to get `trashedNotices`, `restore`, `permanentRemove`.
    2. Import `TrashTabPattern`, `Table`/`TableHeader`/`TableBody`/`TableRow`/`TableCell`, `Button`, `AlertDialog` components, `formatIstDate` from `lib/date`, `NoData`, `RotateCcw`/`Trash2` icons from lucide-react.
    3. Implement the component:
       - State: `deleteTarget` (notice being permanently deleted), `restoreTarget` (notice being restored)
       - Render TrashTabPattern with title="Trash" and description="Deleted notices can be restored here."
       - If `trashedNotices.isLoading`: show DefaultLoader
       - If `trashedNotices.data` is empty: show NoData with title="No deleted notices" description="Deleted notices will appear here."
       - Otherwise: render a Table with columns: Title, Expiry, Actions
       - Each row: notice title, `formatIstDate(notice.expiresAt)` for expiry, two buttons — "Restore" (variant ghost) and "Delete Permanently" (variant ghost, destructive)
       - Restore button onClick: `setRestoreTarget(notice)` (opens restore AlertDialog)
       - Delete button onClick: `setDeleteTarget(notice)` (opens permanent-delete AlertDialog)
    4. Add two AlertDialogs following the management/page.tsx pattern (D-20):
       - **Restore confirm:** title="Restore notice?", confirm button "Restore", disabled while `restore.isPending`, onClick calls `restore.mutate(target.id, { onSuccess: () => { toast.success("Notice restored"); setRestoreTarget(null); } })`
       - **Permanent delete confirm:** title="Permanently delete notice?", description="This notice will be permanently removed. This action cannot be undone.", confirm button "Delete permanently" (destructive), disabled while `permanentRemove.isPending`, onClick calls `permanentRemove.mutate(target.id, { onSuccess: () => { toast.success("Notice permanently deleted"); setDeleteTarget(null); } })`
    5. Both AlertDialogs: `onOpenChange={(open) => !open && setDeleteTarget(null)}` (or restoreTarget).
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>Notice trash tab shows real trashed notices from backend; restore and permanent-delete work with AlertDialog confirmations; backend getNotices accepts isDeleted param; lint/typecheck green.</done>
  <acceptance_criteria>
    - Source assertion: `services/notice.api.ts` getNotices function accepts `isDeleted` parameter and passes it in the URL query string
    - Source assertion: `hooks/use-notice.ts` exports a `trashedNotices` query with queryKey `["notices", "trash"]`
    - Source assertion: `hooks/use-notice.ts` exports `restore` and `permanentRemove` mutations
    - Source assertion: `features/noticeboard/notice-trash.tsx` imports and uses `TrashTabPattern`
    - Source assertion: `features/noticeboard/notice-trash.tsx` contains two `AlertDialog` components (restore + permanent delete)
    - Source assertion: `features/noticeboard/notice-trash.tsx` contains `RotateCcw` and `Trash2` (or `Trash`) icon imports from lucide-react
    - Source assertion: `features/noticeboard/notice-trash.tsx` renders `formatIstDate` for notice expiry dates
    - `pnpm lint` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (notice trash listing) | isDeleted param crosses here; backend must not expose non-owned notices |
| API → trash UI | Deleted notice data renders in trash tab; restore/permanent-delete require authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Information Disclosure | isDeleted=true listing exposes all deleted notices | accept | Backend getNotices has no per-user filter (all notices are org-wide); existing authorize('delete','notice') guard on permanent-delete ensures only authorized roles can destroy; restore uses authorize('update','notice') |
| T-08-02-02 | Tampering | Notice permanent-delete abuse | mitigate | Existing authorize('delete','notice') guard retained on the permanent-delete endpoint; AlertDialog confirmation adds UX friction (D-20) |
| T-08-02-03 | Denial of Service | TTL index purges expired trashed items silently | accept | RESEARCH Pitfall 4: trash is best-effort window; long-deleted notices may be auto-purged; documented in the plan |
</threat_model>

<verification>
- Backend: `GET /api/notice` returns active notices (default); `GET /api/notice?isDeleted=true` returns deleted notices
- Frontend: Trash tab renders trashed notices with title, expiry, restore/delete buttons
- Restore: click Restore → AlertDialog → confirm → notice disappears from trash, appears in active
- Permanent delete: click Delete → AlertDialog → confirm → notice disappears permanently
- `pnpm lint && pnpm typecheck` pass
</verification>

<success_criteria>
- Admin can see deleted notices in the trash tab
- Restore moves notice back to active; permanent delete irrecoverably removes it
- Both actions use AlertDialog confirmation (D-20)
- Backend getNotices accepts isDeleted query param
- No regressions in existing notice active listing
</success_criteria>

<output>
Create `.planning/phases/08-bug-fixes-calendar-overhaul/08-02-SUMMARY.md` when done
</output>
