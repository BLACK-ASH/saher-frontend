---
phase: fix-09-full-regression
plan: 01
type: execute
wave: 1
depends_on: [all prior fix phases]
files_modified: []
autonomous: true
requirements: [REG-01, REG-02, REG-03, REG-04, REG-05, REG-06, REG-07, REG-08, REG-09, REG-10]
user_setup: []

must_haves:
  truths:
    - "All backend tests pass for every domain area (auth, program, session, workshop, users, bills, leave, attendance, notice, notifications)"
    - "Frontend passes lint, typecheck, and build with zero errors"
    - "Every frontend feature area has complete service → hook → component → page wiring"
    - "No console errors are introduced in any frontend feature area"
    - "Protected routes remain protected, roles still work, revoked users cannot bypass authorization"
    - "No unintended hard deletes, existing documents remain readable, no duplicate records introduced"
  artifacts: []
  key_links: []
---

<objective>
Run the full regression test suite across all 10 areas (REG-01 through REG-10) plus the cross-cutting checklist, verifying that every previous phase's fixes hold and no regressions were introduced.

Purpose: This is the final stabilization gate. Every prior phase (0–8) built or fixed a feature. This phase proves the entire system works together — not just that individual tasks compiled, but that the complete data flow from frontend to backend is correct.

Output: A written regression report documenting pass/fail for every checklist item, per the FIX_PHASE.md Section 7 reporting format. Any regressions found become new fix tasks.
</objective>

<execution_context>
@/home/catlium/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/catlium/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run backend regression suite and build/lint/typecheck gates</name>
  <files></files>
  <read_first>
    /home/catlium/Code/saher/saher-backend/AGENTS.md
    /home/catlium/Code/saher/saher-backend/tests/setup.ts
    /home/catlium/Code/saher/saher-backend/tests/auth/auth.test.ts
  </read_first>
  <action>
Run the full backend quality gates and test suite from the saher-backend directory. Record every result.

**Step 1 — Lint:**
```bash
cd /home/catlium/Code/saher/saher-backend && pnpm lint
```
Record exit code. If lint fails, document every error.

**Step 2 — Typecheck:**
```bash
cd /home/catlium/Code/saher/saher-backend && pnpm typecheck
```
Record exit code. If typecheck fails, document every error.

**Step 3 — Full test suite:**
```bash
cd /home/catlium/Code/saher/saher-backend && pnpm test
```
Record exit code. If any test fails, document which test, which area (REG-01 through REG-10), and the failure message.

**Step 4 — Build:**
```bash
cd /home/catlium/Code/saher/saher-backend && pnpm build
```
Record exit code. If build fails, document the error.

**Step 5 — Per-area regression mapping.**
Map each existing test file to its regression area:

| Area | Test Files | What It Proves |
|------|-----------|----------------|
| REG-01 (Auth) | tests/auth/auth.test.ts | Login, logout, tokens, email verification, revocation |
| REG-02 (Program) | tests/admin/admin.test.ts (program CRUD if present) | Program create/view/update/list/soft-delete |
| REG-03 (Session) | tests/events/events.test.ts (session endpoints) | Session create/view/update/list/soft-delete |
| REG-04 (Workshop) | tests/events/events.test.ts (workshop endpoints) | Workshop create/view/update/list/soft-delete |
| REG-05 (Users) | tests/user/user.test.ts, tests/admin/admin.test.ts | User create/edit/bank details/roles/revoke |
| REG-06 (Bills) | tests/payroll/payroll.test.ts, tests/reimbursement/*.test.ts | Bill list/create/view/withdraw/negative amount rejection |
| REG-07 (Leave) | tests/leave/leave.test.ts | Leave apply/balance/allocation/approval |
| REG-08 (Attendance) | tests/attendance/attendance.test.ts, tests/attendance/export.test.ts, tests/attendance/excel.service.test.ts | Check-in/check-out/correction/listing |
| REG-09 (Notice) | tests/notice/notice.test.ts | Notice create/view/update/delete |
| REG-10 (Notifications) | tests/notification/notification.test.ts | Notification fetch/display/unread |

For each area, list: which test files exist, how many test cases, which cases cover the regression checklist items, and whether all passed.

For areas where no test file exists or where test coverage is thin (e.g., workshop-specific CRUD not covered by events tests), flag as "MANUAL_VERIFICATION_NEEDED" and document what must be verified manually.

Do NOT fix any test failures in this task — only document them. Fixing belongs in prior phases.
</action>
<verify>
cd /home/catlium/Code/saher/saher-backend && pnpm lint && pnpm typecheck && pnpm test && pnpm build
All four commands exit 0. If any fail, document the failure — do not suppress.
</verify>
<acceptance_criteria>
  - [ ] pnpm lint exits 0 (no lint errors)
  - [ ] pnpm typecheck exits 0 (no type errors)
  - [ ] pnpm test exits 0 (all tests pass) — if any test fails, that is a regression and must be documented
  - [ ] pnpm build exits 0 (build succeeds)
  - [ ] Per-area regression mapping table is produced with pass/fail for each of REG-01 through REG-10
  - [ ] Any area with insufficient test coverage is flagged with MANUAL_VERIFICATION_NEEDED
  - [ ] Results are written in the FIX_PHASE.md Section 7 report format
</acceptance_criteria>
<done>Backend regression suite executed; every area mapped to pass/fail; failures documented with root cause; results in Section 7 format.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend regression — quality gates + feature-area wiring verification</name>
  <files></files>
  <read_first>
    /home/catlium/Code/saher/saher-frontend/AGENTS.md
    /home/catlium/Code/saher/saher-frontend/tests/setup.ts
    /home/catlium/Code/saher/saher-frontend/lib/api-wrapper.ts
    /home/catlium/Code/saher/saher-frontend/proxy.ts
  </read_first>
  <action>
Run frontend quality gates, then systematically verify each feature area's complete data flow: service → hook → component → page. This catches the class of bugs FIX_PHASE.md warns about: "a passing build does not mean a passing feature."

**Step 1 — Lint:**
```bash
cd /home/catlium/Code/saher/saher-frontend && pnpm lint
```
Record exit code.

**Step 2 — Typecheck:**
```bash
cd /home/catlium/Code/saher/saher-frontend && pnpm typecheck
```
Record exit code.

**Step 3 — Frontend tests:**
```bash
cd /home/catlium/Code/saher/saher-frontend && pnpm test
```
Record exit code and results.

**Step 4 — Build:**
```bash
cd /home/catlium/Code/saher/saher-frontend && pnpm build
```
Record exit code.

**Step 5 — Per-area wiring verification.**
For each REG area, verify the complete frontend data flow exists. For each area below, grep/check that these artifacts exist and are correctly wired:

REG-01 — Authentication:
- `features/login/` — login form with React Hook Form + zod
- `hooks/use-login.ts` or equivalent — login mutation via apiFetch
- `proxy.ts` — auth route guards using `saher_access_token` / `saher_refresh_token` cookies
- `lib/api-wrapper.ts` — 401 handling + token refresh singleton
- `app/(auth)/` — login, forgot-password, change-password, verify-email pages exist
- Verify: `grep -r "saher_access_token\|saher_refresh_token" proxy.ts` returns matches
- Verify: `grep -r "apiFetch\|api-wrapper" lib/api-wrapper.ts` exists and exports apiFetch

REG-02 — Program:
- `services/program.api.ts` — program CRUD service functions
- `hooks/use-program.ts` or equivalent — TanStack Query hooks for programs
- `features/program/` — program UI components (create, list, view, edit)
- `app/(main)/(admin)/program/` — program page
- Verify: grep for program-related queryKey patterns
- Verify: program CRUD hooks call the service functions (not bare fetch)

REG-03 — Session:
- `services/session.api.ts` — session CRUD service functions
- `hooks/use-session.ts` or equivalent — session query hooks
- `features/` or `app/` — session list/create/edit views
- Verify: session service functions use apiFetch, not bare fetch

REG-04 — Workshop:
- `services/workshop.api.ts` or session-related service — workshop CRUD
- Workshop-related hooks and UI components
- Verify: workshop create/edit/delete flows exist in code

REG-05 — Users:
- `services/user.api.ts` — user CRUD, profile, bank details
- `hooks/use-users.ts`, `hooks/use-profile.ts`, `hooks/use-me.ts`
- `features/users/` — user list, user detail, register form
- `app/(main)/users/` — user pages
- Verify: user register schema (`features/register/register-schema.ts`) has bankName as string (not number)
- Verify: profile page renders and loads data

REG-06 — Bills:
- `services/bill.api.ts` or reimbursement service
- `hooks/use-bill.ts` or use-reimbursement hooks
- `features/bill-management/` or reimbursement features
- Verify: negative amount validation exists in zod schema (not just frontend, but schema present)
- Verify: withdraw action mutation exists and calls the correct API endpoint

REG-07 — Leave:
- `services/leave.api.ts` — leave CRUD + balance
- `hooks/use-leave.ts` — leave hooks
- `features/leave/` — leave apply, balance, approval views
- Verify: leave balance/allocation hooks exist
- Verify: proof field accepts the correct type (not string vs ObjectId mismatch)

REG-08 — Attendance:
- `services/attendance.api.ts` — check-in, check-out, list, correction
- `hooks/use-attendance.ts` — attendance hooks with AttendanceStatus enum
- `features/attendance/` — attendance list, chart, correction views
- `app/(main)/attendance/` — attendance page
- Verify: check-in and check-out mutations exist and call the correct endpoints
- Verify: correction request submission flow exists

REG-09 — Notice:
- `services/notice.api.ts` — notice CRUD
- `hooks/use-notice.ts` — notice hooks
- `features/noticeboard/` — notice create, view, edit, delete
- `app/(main)/noticeboard/` — noticeboard page
- Verify: delete mutation exists and calls the correct endpoint

REG-10 — Notifications:
- `services/notification.api.ts` — notification fetch
- `hooks/use-notification.ts` — notification hooks
- Notification bell UI, unread count, list, read/unread state
- Verify: notification UI exists and is wired to the hook
- Verify: empty state and loading state components exist

**Step 6 — Cross-cutting frontend checklist (Section 6):**
- Protected routes: `grep -r "RoleGuard" app/` — confirm RoleGuard is used in admin routes
- React Query cache invalidation: check that mutations in each area call `queryClient.invalidateQueries` with the correct key
- No console.log: `grep -rn "console.log" --include="*.ts" --include="*.tsx" app/ features/ hooks/ services/ lib/` — should return zero matches (eslint enforces this, but verify)
- Forms: spot-check that major forms (register, leave apply, bill create, attendance correction) use `zodResolver` with their schema

**Step 7 — Write regression results.**
For each area, record: pass/fail, what was verified, any issues found. If an area has missing wiring (e.g., a service function exists but no hook calls it), flag it as a REGRESSION.

Do NOT fix any issues found — only document them.
</action>
<verify>
cd /home/catlium/Code/saher/saher-frontend && pnpm lint && pnpm typecheck && pnpm test && pnpm build
All four commands exit 0. If any fail, document the failure.
</verify>
<acceptance_criteria>
  - [ ] pnpm lint exits 0
  - [ ] pnpm typecheck exits 0
  - [ ] pnpm test exits 0 — existing tests must all pass; failures are regressions
  - [ ] pnpm build exits 0
  - [ ] Every REG area (01–10) has its complete service → hook → component → page chain verified
  - [ ] Cross-cutting Section 6 frontend checklist items verified: protected routes, cache invalidation, no console.log, form validation
  - [ ] Any missing wiring or regression is documented with file paths and line numbers
  - [ ] Results written in FIX_PHASE.md Section 7 report format
</acceptance_criteria>
<done>Frontend quality gates all pass; every feature area's complete data flow verified end-to-end; cross-cutting checklist items confirmed; regressions documented.</done>
</task>

<task type="auto">
  <name>Task 3: Compile final regression report and identify any remaining gaps</name>
  <files>.planning/phases/fix-09-full-regression/REGRESSION-REPORT.md</files>
  <read_first>
    /home/catlium/Code/saher/saher-frontend/FIX_PHASE.md
    .planning/phases/fix-09-full-regression/09-01-PLAN.md
  </read_first>
  <action>
Combine the results from Task 1 (backend) and Task 2 (frontend) into a single REGRESSION-REPORT.md file at `.planning/phases/fix-09-full-regression/REGRESSION-REPORT.md`.

Structure the report using the FIX_PHASE.md Section 7 format for both repos, then add a summary section.

**Report structure:**

```markdown
# Phase 9 Regression Report

## Backend Results

### Quality Gates
- Lint: PASS/FAIL
- Typecheck: PASS/FAIL
- Tests: PASS/FAIL (X/Y tests passing)
- Build: PASS/FAIL

### Per-Area Results

| Area | Description | Status | Test Coverage | Notes |
|------|------------|--------|---------------|-------|
| REG-01 | Authentication | PASS/FAIL | Complete/Partial/None | ... |
| REG-02 | Program | PASS/FAIL | ... | ... |
... (all 10 areas)

## Frontend Results

### Quality Gates
- Lint: PASS/FAIL
- Typecheck: PASS/FAIL
- Tests: PASS/FAIL (X/Y tests passing)
- Build: PASS/FAIL

### Per-Area Wiring Verification

| Area | Description | Service | Hook | Component | Page | Status | Notes |
|------|------------|---------|------|-----------|------|--------|-------|
| REG-01 | Authentication | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗ | PASS/FAIL | ... |
... (all 10 areas)

## Cross-Cutting Checklist

### Authentication
- [ ] Protected routes remain protected — PASS/FAIL
- [ ] Roles still work — PASS/FAIL
- [ ] Revoked users cannot bypass authorization — PASS/FAIL
- [ ] Refresh-token flow still works — PASS/FAIL
- [ ] Redis sessions behave correctly — PASS/FAIL

### API
- [ ] Existing API routes remain compatible — PASS/FAIL
- [ ] Error responses remain consistent — PASS/FAIL
- [ ] Validation errors are meaningful — PASS/FAIL
- [ ] No endpoint accidentally exposes private data — PASS/FAIL

### Database
- [ ] No unintended hard deletes — PASS/FAIL
- [ ] Existing documents remain readable — PASS/FAIL
- [ ] ObjectId relationships remain intact — PASS/FAIL
- [ ] No duplicate records introduced — PASS/FAIL
- [ ] No accidental balance duplication — PASS/FAIL

### Frontend
- [ ] Existing navigation works — PASS/FAIL
- [ ] Existing layouts work — PASS/FAIL
- [ ] Loading states work — PASS/FAIL
- [ ] Empty states work — PASS/FAIL
- [ ] Error states work — PASS/FAIL
- [ ] React Query cache invalidation works — PASS/FAIL
- [ ] No broken forms — PASS/FAIL
- [ ] No console errors introduced — PASS/FAIL

## Regressions Found

(list any regressions discovered, with root cause and suggested fix)

## Gaps Identified

(list any areas where verification was incomplete — e.g., missing test coverage, manual-only verification)

## Recommended Next Steps

(if regressions found: specific fix tasks; if all pass: stabilization declared complete)
```

This report IS the deliverable of Phase 9. It proves the system is stable or identifies what remains to be fixed.
</action>
<verify>
File .planning/phases/fix-09-full-regression/REGRESSION-REPORT.md exists and contains all 10 REG areas with explicit pass/fail status, plus the full cross-cutting checklist from Section 6.
</verify>
<acceptance_criteria>
  - [ ] REGRESSION-REPORT.md exists at .planning/phases/fix-09-full-regression/REGRESSION-REPORT.md
  - [ ] All 10 REG areas (REG-01 through REG-10) have explicit pass/fail status
  - [ ] Backend quality gates (lint/typecheck/test/build) results documented
  - [ ] Frontend quality gates (lint/typecheck/test/build) results documented
  - [ ] Cross-cutting checklist from Section 6 fully filled out (all items checked)
  - [ ] Any regressions found are listed with root cause and suggested fix
  - [ ] Any gaps (insufficient test coverage, manual-only areas) are listed
  - [ ] Report uses FIX_PHASE.md Section 7 format
</acceptance_criteria>
<done>Complete regression report written; all areas verified; any remaining work identified with concrete next steps.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend: pnpm lint exits 0 (saher-backend)
- [ ] Backend: pnpm typecheck exits 0 (saher-backend)
- [ ] Backend: pnpm test exits 0 (saher-backend)
- [ ] Backend: pnpm build exits 0 (saher-backend)
- [ ] Frontend: pnpm lint exits 0 (saher-frontend)
- [ ] Frontend: pnpm typecheck exits 0 (saher-frontend)
- [ ] Frontend: pnpm test exits 0 (saher-frontend)
- [ ] Frontend: pnpm build exits 0 (saher-frontend)
- [ ] REGRESSION-REPORT.md exists with all 10 areas verified
- [ ] Cross-cutting Section 6 checklist fully filled out
</verification>

<success_criteria>
- All quality gates pass in both repos (lint, typecheck, test, build)
- Every REG area (01–10) has been verified end-to-end — not just compilation
- Cross-cutting checklist from Section 6 is complete
- Any regressions are documented with root cause
- Any gaps are documented with recommended next steps
- The REGRESSION-REPORT.md serves as the stabilization sign-off
</success_criteria>

<output>
Create `.planning/phases/fix-09-full-regression/09-01-SUMMARY.md` when done.
</output>
